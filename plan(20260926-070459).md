# harness-agent 下一轮执行计划（面向 Agent 的逐项提示词）

审查基线：ki11a-Conton/harness-agent 主分支提交 3ff894638f51081f30417327638a88efc54769c5，2026-09-26。相对上一轮 7157653ff0303a583bd0b971fb459a05b6b30391 前进 7 个提交。后续执行本文件前，先核对新的 HEAD；若已变化，重新定位行号与反例，不能把本次行号当作不变事实。

同一 HEAD 的 Actions run 36217188308 共 7/7 jobs 成功。Windows/Ubuntu 的 closed-loop job 都打印 prereg-production-e2e: PASS、n5-prereg-closed-loop: PASS — 123/123、27/27 mutation(s) CAUGHT。这是对应 HEAD 的离线 CI 证据；没有运行真实付费模型，也没有验证模型质量提升。本轮审查使用仓库源码和 Actions 日志，没有在此环境对该 HEAD 重新执行测试，也没有下载 artifact 逐字节复核。

## 1. 先界定已完成的部分和缺口

已有实质性进展：prereg build 从冻结选择与真实 case 文件导出 31 个非 holdout 样本；validate/run 使用独立的执行身份观察；CLI 参数与授权 JSON 有严格入口；双平台 CI 覆盖发行版拒绝矩阵，离线正向执行记录 124 次 arm run、316 次 fake provider 进入、124/124 份原始 evidence 复验。阶段报告把 96 次失败、28 次通过和判定 REJECT 如实记下；paidExperimentRun、championPromotion 均为 NOT_RUN。因此不要重做旧 A0–A7 的已有功能，也不要把“离线 PASS”理解为全部生产约束已被独立证明。

本次源码审查的剩余问题按风险排序如下。它们是下一轮 Agent 的调查与修复起点；每项都应先写能复现的失败断言，再依测试结果修正结论：

| 编号 | 代码证据 | 影响 / 当前边界 | 对应任务 |
| --- | --- | --- | --- |
| G1 | prereg-arm-executor.ts#L162-L216：计算两份 checkout 的摘要后调用本进程 runOneCase，区别由 candidate 参数注入；文件开头也明确尚未通过 arm 自身构建执行。prereg-production-e2e.mjs#L381-L387 用 export {} 填充合成的“构建”。 | “两个不同摘要”不等于两份冻结构建确实分别运行；当前 124 次只证明当前进程 harness 在两个机制参数下能工作。 | B3、B5 |
| G2 | formal-run.ts#L719-L743 首次调用预留 token/USD/时长，toolCalls:0；#L795-L825 收到 retry 只增加 call ledger，不预留第二次的 token/USD/时长。openai.ts#L289-L324 的 retry 事件发生于下一次 fetch 前。 | 物理重试可绕过其他维度的请求前预算；工具调用没有进入该成本账本。 | B2 |
| G3 | formal-run.ts#L584-L609 将实测值直接加到 charged，未校验不大于持有的 reservation / 总 cap，找不到 reservation ID 时以零额度结算；#L400-L436 也承认时间预约只是调度份额。 | 报告中的“硬约束所有维度”仍需 per-call 截止、实测超界处置和不可恢复的冲正语义；不能只靠请求返回后再检查。 | B2 |
| G4 | prereg-execution-identity.ts#L139-L169 对所有 real provider/model 统一引用 PREFLIGHT_ESTIMATE.costPerCallUsd = 0.0005；原注释称其为估算。 | 估算值不能自动证明给定模型、token 配比、代理端点和重试的最高账单；付费路径不能在这个推断上宣称美元上限可靠。 | B2 |
| G5 | prereg-production-runner.ts#L163-L228 只重算 case 的内容；formal-run.ts#L303-L332 不复核 selectionRule/eligibilityDigest/selectionProvenanceDigest；case-selection.ts#L292-L313 使用会跟随 symlink 的文件遍历。 | build 的实际选择有进展，但 validate/run 尚缺独立的资格重算，fixture 后代软链接有越界读取/环路风险。须用测试判断具体可达性。 | B1 |
| G6 | prereg-run-evidence.ts#L132-L161：manifest.json 为合法 JSON null 时只比较其哈希，身份校验分支不报错；paired-campaign.ts#L220-L228 删除旧记录后再 rename。 | 证据验证有形状漏洞，记录写入有崩溃空窗；摘要相符不证明与可信预算/实际构建一致。 | B4 |
| G7 | prereg-production-e2e.mjs#L390-L489 的完整正向 run 是 in-process + 假 provider；真实 release 子进程只跑负向和 build/validate。#L517-L540 构建 aggregate 时传入 fake 计数及初始预算。 | 报告对两种试验有说明，但 productionOfflineReady=PASS 的标题仍超出“发行版子进程完整正向执行并核真实账本”的证据。 | B5、B6 |

## 通用执行合同（每个 Agent 开始时先读）

读取仓库 AGENTS.md、本计划所指源码与 docs/evidence/tool-call-efficiency-p1-p7-report.md；记录起始完整 SHA 和 git status --porcelain。保留 Runtime 既有 ToolOrchestrator、PermissionEngine、SandboxManager、verifier 路径；修改只针对可复现的缺陷。

B0–B6 都是离线工作。不得对真实外部付费端点发请求，不得读取/打印 key，也不得自动生成可用于付费的 approval。所有模拟 provider、loopback server、授权都须显著标记 TEST_ONLY；本地环境如有真实 key，先使测试拒绝执行。

作者电脑只有 Windows：给出 PowerShell 可执行命令；Ubuntu、冷启动、跨平台故障注入由当前 HEAD 的 GitHub Actions 执行。不要求作者安装本地 Linux/WSL/Docker。源码改动前跑最小 RED；失败必须来自目标断言，不能靠语法错误、超时、skip 或删除旧测试制造 RED。

一项不达标时标记 PARTIAL/BLOCKED，保留安全拒绝与已通过回归；不要通过降低预算、编造零调用、硬写 evidenceVerified=true 或移除 CI 门禁让其显示 PASS。要变更冻结实验定义、模型价格、授权金额或 arm 工件，先产生新 digest 并描述旧证据为何失效。

每项交付：起止 SHA、改动路径、RED→GREEN 的测试名和命令、退出码、skip 数、物理 HTTP/factory/重试/账本读取的实测来源；不可观察写 NOT_OBSERVED；报告失败原因码、Windows 运行结果和同 HEAD 的 Ubuntu Actions URL。无完整日志时不填“已验证”。

## B0 — 固定基线、补针对剩余缺口的 RED 与状态矩阵

给 Agent 的提示词：

做什么

以最新提交为基线，将 G1–G7 变成能失败的、离线的、单项可运行的反例；记录上一轮 A0–A7 的已实现范围，明确全量本地/CI 的当前结果。这个任务只校准证据和测试，不先修复生产实现。

怎么做

读最新 AGENTS.md、plan.md、PR/commit 增量、阶段报告与 CI 产物列表。建立 docs/evidence/prereg-next-gap-matrix.md：每行含旧承诺、HEAD 源码行、当前可观测行为、预期拒绝码或 hard gate、对应 B 任务、复现命令、实际状态。
在 packages/evaluation/src/\*formal\*test.ts、apps/cli/src/prereg-\*test.ts 或新的小测试文件分别构造：retry 第 2 次在美元/token 余额不够时物理传输为零；toolCalls 超上限；manifest=null 但哈希自洽；arm checkout 里嵌入不同可观察标记且两臂运行标记应不同；正向 release CLI 真实子进程；valid-root 但选择资格不符/软链接越界。测试用临时目录和 fake transport，真实端点请求计数必须为零。
对每项先记录旧代码的失败输出；如果源码分析与测试不一致，改写 G 行并保留测试证据，不为了迎合本计划强行让测试失败。保持 123 个原有离线测试和 27 条 mutation 仍能被 CI 跑到。
怎么验收

交付一个可审查的矩阵和每条反例单独命令、旧代码实际结果；目标缺口的 RED 由断言产生，其余回归保持绿。至少覆盖 G1、G2、G5、G6、G7；G3/G4 分别给可计算的越界/价格反例。
报告精确区分 offlineFixtureReady、当前 productionOfflineReady 的实测范围、paidExperimentRun=NOT_RUN、championPromotion=NOT_RUN；引用本 HEAD Actions URL，绝不将之前 7157653 的 CI 冒充新证据。
完成后把矩阵编号交给 B1–B6；若某反例无法复现，附测试源码和替代结论。

## B1 — 在 validate/run 独立证明所执行样本的资格与文件边界

给 Agent 的提示词：

做什么

让正式校验时重新从可信冻结证据与真实 case 派生 case ID 集合、选择规则、资格摘要、holdout 状态和 case 输入摘要。修复 fixture 子文件/目录软链接越界或环路。不能仅验证 prereg 自带 root digest 或 build 时产生的自报 catalog。

怎么做

复用 selectionFromFrozenEvidence 的冻结 taxonomy、选择规则与 catalog 生成逻辑，在 observeExecutionIdentity 或专门 read-only observer 中派生独立的 selectionProvenanceDigest、eligibilityDigest、规范 ID/顺序、suite 和内容；将它们与 artifact 逐项比较。不要通过回显 artifact 的 rule/digest 实现“匹配”。配置允许的 selectionPath/taxonomyPath 必须有固定受信来源或源 SHA 约束；自制文件不可替换正式冻结证据。校验必须在 provider factory 之前。
resolveBenchmarkCaseDir 的父目录 realpath 校验扩展到 request/expected/fixture 的每个后代。使用 lstat/realpath 和 visited set，显式决定是否一律拒绝 symlink；保证 Windows junction/reparse point 路径也 fail closed，拒绝坏 UTF-8/目录环、不规范相对路径与超出仓库范围的读取。loadBenchmarkCase 与 digest 路径使用同一边界规则，防止校验安全但执行跟随链接。
编写至少五种差异：选择中添加不合格但真实的样本；替换 eligibility/selection provenance 并重新计算 self-consistent root；更换 taxonomy；fixture 后代 symlink 指向仓库外（Linux Actions 验证，Windows 无法创建时可用跳过条件但必须有对应 Ubuntu 强制测试）；symlink 环路。每种均在 providerFactoryCalls=0 时拒绝，文档说明是否涉及 Windows junction。
怎么验收

官方冻结的 31 案例 build→validate 仍通过；含伪造资格、不同选择、holdout 或路径越界的合法形状且重新计算 root 的 artifact 均被拒绝，原因码可稳定断言。
输入目录外放置 SENTINEL 文件，拒绝测试验证没有读取/泄漏其字节；不得无限递归；Windows 与 Ubuntu 使用各自适用的路径样例。结果记录 0 provider factory、0 physical HTTP。
targeted tests、pnpm typecheck、pnpm build 通过；如冻结 31 个样本实际发生变化，先解释并重新生成 digest，不默默改样本迎合测试。

## B2 — 让每个物理请求按模型定价和全部预算维度事前可控

给 Agent 的提示词：

做什么

修复 G2–G4：每次物理发送（包括内部 retry）都要有可追踪的调用、input/output/total tokens、美元与时间额度；工具调用要按真实执行入账并在到达 cap 前阻断。价格、单次上界或可中断性无法证明时，付费路径明确 PRICING_UNKNOWN / BOUND_UNKNOWN 拒绝。

怎么做

查 OpenAICompatibleProvider、ModelEvent retry、runOneCase 的 tool/usage/timeout、judge 的 provider 调用与 R97 budget channel。画出 request 发出前 / retry 发出前 / stream 完成 / 取消 / 崩溃的责任表，并标明哪一个锁拥有 reservation ID。新增一个有本地可数 HTTP 端点的测试：首次响应 429，第二次仅当所有维度的新 reservation 成功才允许 fetch；余额仅够一次时第二次必须为 0 次网络请求。
统一一个可恢复的物理 attempt 记账协议：同一 campaign lock 下按维度预约，在发送前记录 durable reserved，已发送未知按保守上界结算；resume 时扫描未决 reservation，不因找不到 ID 或坏 JSON 自动归零。结算时验证实际 token/美元/时长不是负数、NaN 或超过可信上界；真实超界时冻结后续请求且 INVALID/BUDGET_EXHAUSTED，不能输出成功判定。重复 settle 不得再加一次或隐式允许不存在 ID。
tool call 在真实 dispatch 点接入相同上限；时间采用实际硬 timeout/abort、与剩余 campaign 时间的最小值。已发出请求的耗时可能因网络延迟超时，不能宣称“事后测时等于事前保证”；对无法限制的外部服务提供明确 fail-closed 的付费策略。judge/verifier 若能独立构造 provider，必须纳入相同 budget channel 或在 paid 正式路径禁用旁路。
摒弃把 PREFLIGHT_ESTIMATE 当定价上界：按 provider、model、端点与计费类别建立版本化、可追溯的实际最高费率/请求上界与失效条件；从定价版本、token 上界和最大物理尝试数计算 USD reservation。未知代理计费和任意换模型一律拒绝付费承诺。不要在代码里写“一个固定估算值对所有模型都保守”。
针对输入/输出/总 token、工具、时间、USD、内部 retry、并发、取消、进程崩溃后的 reopen 逐项加恰好到上限/超 1 的测试；分开记录物理 fetch 数、ledger reserved/charged/unknown 与错误码。
怎么验收

429→retry 的 loopback 用例证明余额不足时第二次物理 fetch 未发生且各维成本不减少；余额足时两次都有独立 reservation 和 settle。工具超过 cap 的下一次 dispatch 不执行，duration 结束后 charged 可复核。
对每个实际可界定维度：所有已发请求均有 pre-send reservation，重启后未知不会退款，charged/reserved 同一 ID 不可丢失/重复；任何越界或价格不明的 paid 路径不产生 ACCEPT。如果某 provider 不能提供这样的控制，用稳定 reason code 关闭该路径，并把 PARTIAL/BLOCKED 写入报告。
Windows PowerShell 可运行单测与本地 HTTP stub，Linux 崩溃/并发测试由 Actions 运行；pnpm typecheck、pnpm test、pnpm build 无回归，测试完全没有外部付费请求。

## B3 — 将 baseline/candidate 真正交给各自冻结的可执行构建

给 Agent 的提示词：

做什么

把 G1 的“计算两份不同目录摘要后仍运行当前驱动构建”改为从 baseline/candidate 两份冻结构建各自执行真实 harness，且复用 B2 的唯一预算渠道。不能再用 export {}; // arm:\* 合成 checkout 证明双构建已运行。

怎么做

对照 packages/evaluation/src/r97-\* 的 worker/driver、R97_ARM_BUILD_ENTRIES 与 apps/cli/src/benchmark-command.ts；列出 arm 的文件闭包、版本、真实可启动入口与 build digest 的关系。选一个 Windows/Ubuntu 均可运行的隔离 worker 协议（stdio/IPC），把 armId、case digest、prereg digest、plan digest、只读授权上下文、budget channel 作为输入，不在 worker 内重新读环境密钥或创建第二个未经预算的 provider。
driver 在任何请求前 verify 两份 checkout 的 git SHA、清洁状态、build closure、入口文件哈希和实际 child process 使用的路径一致；schema 约束 isolationBackendId/isolationStrength，不可用时拒绝。worker 从自身 checkout 加载对应 runOneCase、mechanism、sandbox/verifier，返回观察到的 build digest 与真实 activation/trace；driver 独立比对，不靠手填 candidate 标志让两臂看起来不同。
写具可观察差异的两个真实构建的 offline fixture：基线与候选各有一个版本化机制探针，故意交换路径、同构建两个目录、篡改 dist 一个字节、删除 worker 入口、子进程尝试绕过预算、子进程中断。所有拒绝都在外部请求前；正常用例必须从两个不同 worker 的实际输出重建执行身份。
怎么验收

同一个测试中可从实际被启动的两个 child process 测到不同 build SHA/manifest 与机制输出，和 prereg 两臂 digest 一一对应；只改驱动进程的 candidate 参数无法伪造该证据。
每次可能出网的 worker 请求都经 B2 的可计数同一预算渠道；无新 provider/env 旁路；换错构建、缺构建与同构建两臂均稳定拒绝，factory/fetch 次数按实际测量。
Windows 本地 targeted tests + Build；Ubuntu Actions 有对应测试和 artifact；若 Windows worker 技术限制无法在本轮解决，报告 BLOCKED 并继续保持不发费请求，不用运行当前进程来声称本项完成。

## B4 — 绑定真实执行、证据、结果记录和成本账本的恢复语义

给 Agent 的提示词：

做什么

修复 G6，让证据验证成为可拒绝的严格模式，再把每个 run record 与真实 worker、verifier、activation/security 及 B2 的物理调用 journal 连成可恢复链；不可把一个自哈希 JSON 文件当作“已证明真实执行”。

怎么做

先为 manifest.json=null、[]、数字、缺字段、重复 JSON key、互换两个 arm 目录、伪造 verifier verifiedCompletion=true、activation 内容不对应当前请求分别写 RED。验证器对非 plain object（含 null）立即报错；校验 schema、精确字段集、身份、verifier status 与 run outcome、security/activation 的一致性；所有被用于决策的原始字节都必须被哈希并引用。
在 manifest 中加入本任务真正使用的 worker 构建摘要、原始执行摘要与预算 attempt ID 列表/摘要；在可信 driver 侧交叉验证 B2 journal、请求数/usage/token 与 run outcome，并从已验证记录导出决策输入。明确同一用户能重写磁盘全部文件的威胁边界：hash 能检测意外损坏和单文件置换，若需对抗有写权限的恶意 Agent，需独立签名/受保护的 journal，不能宣称仅 hash 足以证明诚实执行。
writeRecordAtomic 改为先写 temp、sync，再覆盖式原子 rename；不先删 target。和 evidence 原子写入约定提交顺序、完成标记；恢复时扫描 runs/、evidence/ 与 budget journal，区分“已发请求未记结果（不可免费重试）”、“证据已写记录未提交”、“记录存在但证据缺失”、“额外/临时文件”。失败可由固定状态码诊断，不得当空白新 run。
同一计划两臂、重复 ID、顺序、budgetRemaining、unknown 以及 baseline 污染都须在聚合前重新计算。不要用 tokensUsed ?? 0 作为被验证的真实成本，也不拿注入 runner 的自报布尔值签发 ACCEPT。
怎么验收

manifest=null 且 sha256 正确仍得到 verified=false；伪造 verifier+manifest 却没有匹配可信 journal/worker 的组合不能得到 ACCEPT；任何缺失/替换/重复 artifact 都是 INVALID 或明确拒绝。
注入三处进程中断/写入错误后恢复，旧 record 不会出现先 rm 后 rename 的无文件窗口；已可能计费的请求仍记 unknown，不能重复运行同一个 arm 来换回满额度。Windows 与 Ubuntu 各提交可复核的恢复日志。
合法双构建离线跑出的 verified records 可重新从字节 + journal 计算 aggregate；pnpm test/Build 通过，不修改冻结 verdict 阈值来换取 ACCEPT。

## B5 — 发行版 CLI 子进程正向走完双构建离线 E2E

给 Agent 的提示词：

做什么

扩展 scripts/e4/prereg-production-e2e.mjs：不仅在 node apps/cli/dist/main.js 中证明负向拒绝和 build/validate，也要从该发布入口完整运行 prereg run 的 31 案例 × 2 次重复 × 2 臂。以 B3 真实构建、B2 唯一预算与本地计数 HTTP 传输证明执行；保持 0 外部请求/0 实际费用。

怎么做

将目前 writeArmCheckout 的假 .js 闭包从生产就绪判据移走，建立两份可执行的冻结测试构建；在 CI 中用具名 SHA 与 build digest 标注。保留 in-process 正向作为单元回归，另增 release CLI child process 的完整正向测试。把测试专用密钥仅设为无效哨兵字符串，唯一允许的 endpoint 为 127.0.0.1 的 counting OpenAI-compatible stub；对任意外网 socket 强制 fail closed，绝不把普通真实 key 当测试信号。
CLI 实际执行 build→validate→run --authorization TEST_ONLY --mode first-run→中断→resume（单独可控的测试场景），从子进程输出目录读取 实际 ledger view、124 个 per-arm records、worker manifest、verifier 和 aggregate，交叉检查 physicalFetches == ledger physical attempts（计入 retry/unknown 的规则明确）、budgetRemaining == cap - billed/unknown。不向 aggregate 注入 fake.entered() 或初始 campaignWorstCaseModelCalls 当真实账本数。
负向矩阵新增 B1–B4 的证据/身份/账本/构建漂移；每一例断言拒绝码与被拒绝前、后的本地 stub 物理请求差值。实验质量输出只看假 provider 的结果，不能用硬编码通过数声明 candidate 胜出。
怎么验收

两平台 Actions 都展示：同一 HEAD 的 release CLI 子进程正向完成 124 次 arm run；两个真构建被启动、124 份记录和证据核对，HTTP 实测次数与 durable ledger 规则一致，外网请求为 0、费用为 0；负向拒绝仍在 provider factory/transport 前生效。
Artifact 保存 SHA、OS、run ID、attempt、选择与 arm build digests、实测 factory/client/HTTP/retry/usage/unknown、通过/失败数、恢复状态和 decision。缺一项则写 NOT_OBSERVED，不能编数。
pnpm typecheck、pnpm test、pnpm build 与 existing closed-loop/mutation 不降级。Windows 用户用 PowerShell 复现；Ubuntu 冷启动只看 GitHub Actions。

## B6 — 校正 readiness 报告、预算解锁条件和实验判定材料

给 Agent 的提示词：

做什么

将 docs/evidence/tool-call-efficiency-p1-p7-report.md 与下轮报告改成与 B0–B5 证据范围一一相符；生产离线 ready 以真实发布入口和双构建为准。为未来独立付费审批准备可审查的实验方案与质量/成本判定模板，但本任务不运行付费实验。

怎么做

把当前 productionOfflineReady=PASS 明确拆成：release CLI 负向与 build/validate 已证明；当前进程 adapter 124-arm fake 正向已证明；release CLI 正向双 worker 待 B3/B5 结果。B3/B5 尚未通过时 readiness 应为 PARTIAL/NOT_READY，不要让一个绿色脚本汇总覆盖真实执行限制。已有 316 次 fake 调用、96/28 结果和 REJECT 要连同来源保留。
给出正式比较的观测规范：冻结 31 个样本为何由 R85 agent_limit|tool_limit + toolFailures 选出；重复次数与 AB/BA 顺序；每个案例 baseline/candidate 的 verifier、activation、security、tool 数、physical calls、token 与 USD；unknown、infra/judge error、超时和 missing evidence 如何让判定 INVALID/INCONCLUSIVE，而非被当作失败或默认 0。确认这些输入在 B4 是从证据推导。
建立可提交但不能生效的 approval 模板：exact prereg digest、两构建 SHA/digest、model/endpoint digest、逐维最坏上限、失效时间、审批人和已知计费来源；留 UNAUTHORIZED 状态。未来 real endpoint 必须验证外部当期定价来源与代理差异；不把代码里的 planning estimate 当客观账单。
怎么验收

文档中四个 readiness 状态分别报告；没有把 PASS 写在缺少双构建发行版正向/不可核价预算的前提上。所有数字可追溯到本 HEAD 的 CI logs/artifacts 或标 NOT_OBSERVED。
在无 key、无真实审批的运行中，paidExperimentRun=NOT_RUN、championPromotion=NOT_RUN；不存在能默认启动付费请求的命令/工作流。
报告附 Windows 本地命令、Ubuntu Actions 链接与重新审查触发条件（新 HEAD、模型/定价/构建/选择/策略变动）。

## B7 — 条件任务：仅在新的明确付费授权之后运行真实实验

给 Agent 的提示词（默认状态：BLOCKED，先做审查材料）：

做什么

当前只能产出可审核的实验命令、精确预算、审批 JSON 模板、冷启动与断电恢复步骤，以及批准前的只读验证。不得因为本计划提到 B7 就执行付费调用。 用户必须另行明确批准实际模型、外部服务、最大美元/调用/token/时间上限与执行窗口；若没有这项批准，到此停止，维持 PAID_NOT_RUN。

怎么做

在 B1–B6 全部 PASS 的同一冻结 HEAD 下重新构建 artifact，验证两 arm 真实构建、31 案例及预算/价格；从只读 prereg validate 和 dry-run 输出完整身份/来源/上限和即将执行的 exact 命令。准备且仅准备 auth 模板，不自行签署 paid:true 的可执行审批。
待用户后续单独提供明确授权与独立 approval artifact 后，先核时间/预算、空目录、同 digest、唯一出网路径；以小额封顶、可恢复 journal 开始，超额/unknown/证据缺失立即停机。执行记录必须包含真实物理请求、账单可核对凭据和每臂 verifier；失败时不自动重启成第二个新 allowance。
汇总 ACCEPT/REJECT/INCONCLUSIVE/INVALID 及理由。任何 ACCEPT 也不得自动 promotion；独立呈报用户决定。
怎么验收

现在的验收：没有明确新增授权时，0 实际外部模型请求、0 费用、无有效 paid approval 文件，状态 BLOCKED: PAID_NOT_AUTHORIZED。
将来批准后需实际账单、预算 journal、双 arm 证据、CI SHA、决策输入和单独 promotion 审批链全齐；不齐则不宣称实验完成。

## 推荐顺序与统一最终交付

B0 → B1 → B2 → B3 → B4 → B5 → B6；B2 与 B3 的设计可协同，但任何付费解锁都依赖它们全部验收。B7 目前只产出待审材料。单项修复若改动前置工件，应重跑相关依赖的测试和同一新 HEAD 的 Windows/Ubuntu CI。

每个 Agent 的最终汇报固定包含：任务号 PASS/PARTIAL/BLOCKED、起止 SHA/工作树、代码和文档变更、RED 与 GREEN 测试证据、provider factory/实际 HTTP/retry/预算/工具计数来源、构建/样本/实验/证据摘要链、Windows 本地命令与 GitHub Actions Ubuntu URL，以及未解决风险。不得将不在日志里的数值写成“实测 0”。