# harness-agent 下一步计划：关闭当前执行链路的剩余缺口

> 审查日期：2026-09-22  
> 仓库：ki11a-Conton/harness-agent  
> 本次固定审查提交：`735c22a83e8718a33248167b0086822ca7577461`  
> 对比基线：`0075f723cfb6f6e38cf55829974b9d7634cf612b`，其后新增 27 个 commit。  
> 最近一次代码/测试改动：`64d95cbbd681bba78a16577e396d823c77ebfb45`；之后这段提交只改报告。  
> 开发者环境：Windows；Ubuntu 验收由 GitHub Actions 执行，不要求安装 Linux、WSL 或 Docker。  
> 使用方式：先发送“通用 Agent 提示词”，然后逐项发送 A1–A7 的完整任务。本文中的任务尚待实施，不是完成报告。

## 0. 本次审查的结论

**有新提交，而且上一轮很多工作已经实际落地。下一步应修补已复现的边界问题，不要重新实现 R98–R101。**

当前提交的 [CI 35714298502](https://github.com/ki11a-Conton/harness-agent/actions/runs/35714298502) 已完成，7 个 job 均为 success。其中包括 Windows/Ubuntu 两个专用 closed-loop job、两个常规测试 job、Ubuntu coverage、Ubuntu offline cold-start、release attestation。这里说的是 job 结论；不表示工作流内所有条件步骤都执行过。

但是，CI 绿灯还没有覆盖本文复现的异常路径。当前实现还不能据此认定“批准身份、调用预算、取消与恢复已全部满足原计划”。本轮仍以零外部模型请求完成修复与验收，付费实验保持 `PAID_NOT_RUN`。

### 0.1 旧计划的哪些部分已经落地

| 旧任务 | 本次确认的实际进展 | 本轮处理 |
| --- | --- | --- |
| T1 / R98-A 调用预算 | 新增 `createLedgerBudgetedProvider`，正常多轮调用逐次 reserve；失败写盘有反例；driver/worker 使用 campaign lifecycle | 保留；A1 补流提前关闭/异常退款，A2 补整目录丢失后的重授额 |
| T2 / R98-B 执行状态 | running 所有权、attempt 标识、恢复与 retry 状态已有实现；读改写使用 campaign lock；相关测试纳入闭环 | 不重新设计状态机；仅按 A1/A2/A3/A6 的具体需要补跨模块连接 |
| T3 / R99-A 证据和汇总 | 删除了找不到案例时使用第一行报告的 fallback；保存 evidence；校验 hash 和 detail；汇总包含历史结果 | 保留；A5 防止错误判定先发生、再被合法哈希永久保存 |
| T4 / R100-A 正式入口与身份 | 有 `--arm-worker`；模型与 endpoint 显式传入；case staging 不再从 driver repo 回退；增加构建摘要 | 部分完成；A3 修真实观测和时钟，A4 补实际执行依赖，A5 修拒绝结果被覆盖 |
| T5 / R99-B 停止 | `boundedStop` 已有 grace、强制结束进程树、字节上限及 output_limit；有专门测试 | 工具层已实现；A6 把能力接到实际 arm 执行与正在运行的 unit |
| T6 / R101-A 双平台闭环 | 两个历史 arm 的离线闭环、acceptance matrix、mutation gate 已进入 Windows/Ubuntu CI，当前 head 全绿 | 保留现有 job；A7 增补本轮反例并完成一次收尾验收 |

仓库报告记录的 `471/471`、16 units、42 logical calls、6 weak passes，是仓库已有实验报告中的数字。本次独立核实了当前 CI 的 run/job/step 状态，**没有下载该 run 的完整 artifact 重新计算这些数字，也没有在当前工作区重跑整套 pnpm test**。不能把这些数字写成这次审查的本地实测。

### 0.2 本次独立复现的缺陷

审查下载了固定 SHA 的相关源码，使用 Node v24.19.0，在临时目录进行零网络诊断。ledger/state/channel 使用当前源码经 Node 去除 TypeScript 类型后的模块；driver/worker 使用当前 `.mjs`。所有临时目录最终清理，没有修改 GitHub 上的代码。

worker 探针使用合成 arm 的导出模块，测试执行边界与错误处理，不代表真实 benchmark 得分。driver 参数捕获探针替换了授权 gate/观测检查，目的仅是检查下游参数，**不构成真实授权绕过的端到端证明**。

| 编号 | 证据类别 | 实测或代码事实 | 影响 | 任务 |
| --- | --- | --- | --- | --- |
| F1 | 独立复现 | inner generator 已进入 1 次；消费者读一个事件后 break，CLI 再抛错；worker consumed=0，reservation=`abandoned`，grant=1 时另一次 reserve 仍成功 | 已开始的调用在异常路径被当成未发送而退款 | A1 |
| F2 | 独立复现 | grant=1，已 committed=1；保留 claim anchor、删除 campaign 根目录；同一 planDigest 在新目录得到 `first-run`，第二次 reserve 成功 | 普通输出清理可以重新赋予同一批准完整额度 | A2 |
| F3 | 静态确认 + 参数捕获 | 正式 main 仍读取 `plan.observation`，优先使用其中旧 `now`；传给 worker 的 `inputsDigest` 为空；staging 只检查 case.json 能解析且 suite 相符 | 保存的旧观测可能被当成当前事实；实际 case 内容未被完整绑定 | A3 |
| F4 | 独立复现 + 静态确认 | 修改 `packages/core/dist/runtime/runtime.js`，`armBuildIdentity.buildDigest` 不变；另一个执行后计算的 execution digest 已变化。driver manifest 仍只列 dist/index.js 和部分 src | 已批准的 digest 没有覆盖实际执行依赖；记录变化不等于执行前拒绝变化 | A4 |
| F5 | 独立复现 | executionIdentity.drift 明确记录 unapproved-model，但 worker 最终 `status=completed`、`failureCategory=null`、`verifierPassed=true` | 身份不符仍可进入 PASS 和证据链 | A5 |
| F6 | 独立复现 + 静态确认 | 40ms 时触发取消，合成 worker 约 412ms 后仍 PASS，consumed=1；unit timeout=80ms，约 421ms 才返回 infrastructure。driver 也没有转发 signal 或 campaign 剩余时间 | 当前执行无法由 campaign 取消立即停止；deadline 不是硬上限，原因分类可能丢失 | A6 |

以上时间是一次诊断的观测值，不是性能 SLA。F6 的合成 CLI 在一次 await 中延迟 350ms，用来显示等待期间没有可强制停止的边界；并未宣称真实案例固定延迟相同。

P0 在本计划中指“在考虑付费执行前必须关闭的预算/批准边界缺口”，不表示已经发生真实超支或线上攻击。

### 0.3 关键代码导航

行号对应固定审查 SHA，仅供定位；Agent 实施时以函数名为准。

| 文件 / 函数 | 当前问题所在 |
| --- | --- |
| `packages/evaluation/src/r97-budget-channel.ts` / `createLedgerBudgetedProvider`，约 205–241 行 | 结算在 yield 循环之后；消费者 return/break 不走 throw 的 catch，也不继续执行循环后的结算 |
| `scripts/e4/r97-arm-worker.mjs` / `runArmUnit`，约 1802、1895 行 | 正常返回才获取 budgetStats；`budgetStats === null` 被用作 abandon 的依据 |
| `packages/evaluation/src/r97-campaign-lifecycle.ts` / `openR97Campaign`，约 280–331 行 | 缺失的旧 campaign header 被视为 stale claim，可走 first-run |
| `scripts/e4/r97-campaign-driver.mjs` / `main`，约 1680–1689 行 | `const observation = plan.observation`；`now: observation.now ?? now` |
| 同文件 / `runDriver`，约 874–905 行 | 转发模型和 endpoint，但没有 inputsDigest、signal、campaign 剩余期限 |
| `scripts/e4/r97-arm-worker.mjs` / `stageCase`、`inputDigestFor` | staging 验证 suite，没有与批准的内容指纹比较；缺 inputsDigest 时使用 unknown |
| 同文件 / `BUILD_ARTIFACT_PATHS`、`armBuildIdentity` | 只覆盖几个入口文件，不包含其导入的 runtime 实现等 |
| `packages/evaluation/src/r97-plan.ts` / `R97_DRIVER_ARTIFACTS` | dist/index.js 是转导出入口；其摘要不是所有执行模块字节的摘要 |
| `scripts/e4/r97-arm-worker.mjs` / `runArmUnit`，约 1822–1859 行 | 身份漂移设置 harness verdict 后，后续独立的 if/else 又用报告覆盖 verdict |
| `scripts/e4/r97-arm-exec.mjs` / `runArmCaseInProcess` | 两次直接 await arm 的 runBenchmarkCommand；signal 只传到 provider，不能强制终止不响应 signal 的 await |
| `scripts/e4/r97-campaign-driver.mjs` / `makeCampaignStop` | 只在 unit 之间检查停止；不是正在运行 unit 的取消通道 |

## 1. 通用 Agent 提示词

以下整段作为所有任务的共同前缀：

```text
你在 ki11a-Conton/harness-agent 中实施本计划指定的一项任务。

目标：关闭该任务给出的实际缺陷，保留已经工作的实现，不另建 R102+ 通用框架。

开始前：
1. 记录 HEAD 和 git status；阅读根 AGENTS.md、适用的 tasks 文件、package.json、当前任务列出的源码与测试。
2. 审查基线是 735c22a83e8718a33248167b0086822ca7577461。HEAD 更晚时先复验，不机械覆盖后续修复。确有实现和有效回归测试则报告 ALREADY_FIXED，并给证据。
3. 不删除、stash 或重置用户未提交内容。若测试要求 clean tree，使用隔离 checkout。测试输出放忽略目录或 checkout 外。

实现原则：
4. 遵守 Runtime Freeze。本轮依据是确定性预算、身份、执行与证据错误；优先改 scripts/evaluation/adapter，不重写 core Runtime。
5. 工具副作用继续通过 ToolOrchestrator、PermissionEngine、SandboxManager 和 Verification。
6. 先建立一个能在当前代码上失败的最小行为测试，再修复同一问题。断言实际调用、持久状态、退出码和进程是否结束，不能只匹配源码中的函数名或注释。
7. 默认零外部模型请求。可以用隔离 scripted provider 或本地 fixture；不要读取真实 API key，不以真实付费试跑证明基础正确性。
8. providerOverride 在历史 arm 中代表 offline-test。不能注入真实付费 provider 后继续声称 offline-test。
9. Windows 是开发者本机环境。Node 脚本、PowerShell 兼容命令优先；Linux 专属验收交 GitHub Actions，不能要求用户先装 WSL、Docker 或 Linux。
10. 不改冻结 benchmark 的计数、题目、原始历史结果或已签发批准内容来绕过失败。适配/身份合同改变后需要新 planDigest，旧批准不得自动转为新批准。

验证与交付：
11. 本地先构建需要执行的 dist，再跑针对性测试。pnpm test 默认排除 r97-driver-closed-loop.test.ts；涉及 driver 必须运行该专用测试或包含它的 closed-loop suite。
12. 局部实现阶段跑相关测试即可；全套 typecheck/test 和跨平台收尾统一在 A7 做一次。若根 AGENTS 或具体风险要求额外安全测试，补相应测试并说明原因。
13. 交付代码差异、最小 RED/GREEN 证据、真实运行的命令/退出码、剩余限制。不要把计划中的命令写成已执行。
14. 本轮结束依据是验收条件，不是新增测试数、报告页数或 commit 数。
15. 不自动 push、merge、发布，也不执行付费实验；按当前会话已有授权处理这些外部动作。审查/修复/本地验证本身不需要反复请求确认。
```

### 顺序与依赖

| 顺序 | 任务 | 优先级 | 前置 | 完成标志 |
| --- | --- | --- | --- | --- |
| 1 | A1 流关闭和异常后的预算结算 | P0 | 无 | 调用已进入后不会因 break/throw 被退款 |
| 2 | A2 campaign 整体丢失后的预算连续性 | P0 | 无；建议接 A1 顺序实施 | 同一批准不能靠删除目录重新授额 |
| 3 | A3 正式入口的当前观测、时钟和输入 | P0 | 保持 A1/A2 合同 | 旧快照不能代替当前执行事实 |
| 4 | A4 执行依赖的完整构建身份 | P0 | A3 的观测边界 | 实际执行字节变化会在调用前被拒绝 |
| 5 | A5 身份拒绝与结果分类的优先级 | P0 | 与 A3/A4 衔接 | 身份不符不能被 PASS 覆盖 |
| 6 | A6 正在执行的 unit 可被有界终止 | P1 | A1；同时兼容 A3–A5 | 取消、deadline、超量输出可终止真实执行 |
| 7 | A7 双平台回归、证据和本轮收尾 | P1 | A1–A6 | 同一实际入口在两平台验收，报告结论与证据一致 |

按此顺序执行即可，不要求多个 Agent 并行改相同文件。A3、A4、A5 分开是为了分别验收“观测是否新鲜”“字节是否完整绑定”“拒绝是否真正生效”。

## A1 — 修复流提前关闭和异常路径退回已使用预算

### 可直接发送给 Agent 的提示词

你负责 F1。当前正常多轮调用已接入 ledger；缺陷在生成器关闭和异常收尾。请修复现有 budget channel 与 worker 的交接，不复制一份预算系统。

**阅读/修改入口：**

- `packages/evaluation/src/r97-budget-channel.ts`
- `packages/evaluation/src/r97-budget-ledger.ts`
- `scripts/e4/r97-arm-exec.mjs`
- `scripts/e4/r97-arm-worker.mjs`
- `r97-budget-channel.test.ts`、`r97-budget-write-fault.test.ts`、`r97-arm-worker-contract.test.ts`

### 做什么

1. 消费者执行 break、iterator.return、abort 或异常退出时，已进入的逻辑调用仍占用额度。
2. 只有可证明从未进入 inner provider 的预留额度可以 abandon。
3. 正常返回与异常返回都能获取同一份真实调用状态；不能用“没有返回 stats”推断“没有发送”。

### 怎么做

1. 先写 RED：campaign grant=1；真实 channel 包装计数 provider；消费一个非终态事件后 break；模拟 arm CLI 随后抛错。通过真实 runArmUnit 收尾，证明当前 reservation 被 abandoned 且第二次 reserve 成功。
2. 同时写较小的 channel 单测：直接调用 iterator.return，不依赖整个 worker。这样能区分生成器结算缺失与 worker 误退款两个原因。
3. 在 yield 所在的 try/finally 中保证恰好一次结算；显式跟踪未进入、已进入、已观察终态、已结算。completed、error、cancelled 的含义按真实 ModelEvent 合同处理。
4. 消费者在非终态后 return，标记 unknown 并保留额度；在已观察到 completed 后 return，按合同结算，不能留下无限期 reserved。不要吞掉结算错误并宣称完成。
5. 将调用状态建立在 dispatch 前，使异常时 worker 也能读到。可以复用共享 stats/回调或持久的 attempt 关联，选择当前架构中最小的实现；不要只在成功 return 后赋值 budgetStats。
6. 删除 `budgetStats === null` 等价于未发送的推断。abandon 必须建立在明确的 dispatch 状态上；状态不确定时保守保留额度并报告 unknown。
7. 修复未调用 provider 的正常退出：dry-run 拒绝/过期/解析失败仍应释放真正未使用的预留，不能为了防超支把所有失败都永久收费。
8. 保持 `granted = committed + outstanding + unknown + remaining`。transport retries 单独记录；不把报告里的 model_calls 当成发送计数的唯一依据。

### 怎么验收

- [ ] grant=1，break→CLI throw 后 innerCalls=1；第一条不是 abandoned；remaining=0；第二次 reserve 被拒绝。
- [ ] iterator.return、消费者 throw、provider throw、无终态流和取消各有覆盖；同一 reservation 最多一次终态写入。
- [ ] completed 事件刚发出就停止消费时，预算不会悬空，也不会重复结算。
- [ ] 从未进入 inner provider 的失败可以返回原预留；外部调用计数为 0。
- [ ] 正常两轮调用产生两次预留，与 worker 和 ledger 统计一致；另一个 client 不能重复采用 firstReservationId。
- [ ] reserve 写盘失败时 innerCalls=0；结算写盘失败时拒绝宣称成功，保留可恢复状态。
- [ ] 恢复 unknown 不自动重发，也不增加同一批准的剩余额度。

针对性命令（需要构建 dist）：

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-budget-channel.test.ts packages/evaluation/src/r97-budget-write-fault.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts
```

交付预算状态转移的简短说明，以及 RED/GREEN 的 innerCalls、reservation status、remaining、第二次 reserve 结果。不要提交真实 key 或外部请求日志。

## A2 — 删除 campaign 输出后，同一批准不能重新获得额度

### 可直接发送给 Agent 的提示词

你负责 F2。当前 lifecycle 能发现“header 在、ledger 丢失”，也能发现两个仍存在的根目录；但清理旧根目录后，它把原 claim 当成过期目录记录，允许同一批准重新 first-run。请修复批准的一次性预算连续性。

**阅读/修改入口：**

- `packages/evaluation/src/r97-campaign-lifecycle.ts`
- `packages/evaluation/src/r97-budget-ledger.ts` 中 claim anchor、claim 锁和 first-run 检查
- `scripts/e4/r97-campaign-driver.mjs` 的打开/恢复接口
- `r97-campaign-lifecycle.test.ts`、`r97-budget-ledger.test.ts`、`r97-driver-closed-loop.test.ts`

### 做什么

1. 在同一持久授权记录范围内，同一 planDigest 的已领取额度不能因输出目录消失而刷新。
2. 区分“第一次申请”“恢复”“已有领取记录但数据丢失”，给第三种情况稳定的拒绝码。
3. 保留清理临时测试目录的便利，方法是隔离测试授权/claim 目录，而不是让正式批准自动重新有效。

### 怎么做

1. 建立独立临时 claims 目录和 campaign A。grant=1，完成一次 reserve/commit；删除 A，但保留 claims；同计划打开 campaign B。当前会成功，测试必须先 RED。
2. 先检查现有 claim 的保存和加锁合同。把“此批准已建立过 campaign”作为持久事实；路径不再存在不等于从未消费过。缺失旧 header/ledger 时报告状态丢失，不自动清空已领取历史。
3. 让 first-run 对授权的领取在现有 claim 锁内完成。并发两个根目录领取同一批准时，最多一个获得额度；另一个拒绝或恢复到同一权威状态。
4. 正式入口明确 start/resume 的意图；若保留 auto，也必须受持久领取记录约束，不能靠“当前目录为空”判断可以重新授额。
5. 需要移动目录时，采用能验证原消费记录的显式迁移；本任务无需新增迁移平台。暂不支持迁移也可以，但必须拒绝并说明如何恢复原记录，不能自动复制成新预算。
6. 更新现有 stale-claim 测试。已消费但目录删除的 claim 不属于可安全忽略的垃圾；普通测试应使用独立的 claim namespace/独立批准 ID。
7. 写清边界：这是同一受管理持久存储内的防误重授，不能声称能抵御机器所有者删除全部本地状态或跨无共享状态的多主机重复运行。若无法证明原授权未领取，正式执行应拒绝，而不是猜测零消费。
8. 只有新的、独立获批的授权才能带来新额度。不得把“随便改一个 planDigest 字段”当成获得批准。

### 怎么验收

- [ ] A 已消费完→删除 A→同批准打开 B：拒绝；provider 构造数和调用数均为 0。
- [ ] 同路径删除后重建、换 `--out`、换 `--ledger` 都不能刷新额度。
- [ ] 只删除 header、ledger 或 execution-state 中任意一项，分别返回稳定的丢失/损坏错误，不新建替代状态。
- [ ] 两个真实进程以 barrier 同时抢同一批准的两个根目录，至多一方获得预算；不靠长 sleep 猜测竞争时机。
- [ ] 正常首次创建可运行；正常 resume 保留累计消费；全完成后 resume 新增调用为 0。
- [ ] 新的独立测试授权可以建立新 campaign，不被旧授权的失败状态误伤。
- [ ] claims 文件损坏/不可读/写入失败均拒绝执行，且错误可诊断。

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-campaign-lifecycle.test.ts packages/evaluation/src/r97-budget-ledger.test.ts packages/evaluation/src/r97-driver-closed-loop.test.ts
```

交付原批准、原消费、清理动作、第二次打开结果的证据。修复不依赖开发者保留某个偶然存在的临时输出目录。

## A3 — 正式 CLI 使用当前观测、真实时钟和实际 staged 输入

### 可直接发送给 Agent 的提示词

你负责 F3。`checkExecutionObservationV1` 已存在，但传给它的事实仍可能来自 plan 文件。请把新观测接到真实 main→runDriver→worker 路径，不能只增加纯函数测试。

**阅读/修改入口：**

- `scripts/e4/r97-campaign-driver.mjs` 的 main、observeArms、runDriver、fingerprintCaseInCheckout
- `scripts/e4/r97-observe-arms.mjs`
- `scripts/e4/r97-arm-worker.mjs` 的 stageCase、inputDigestFor、runArmUnit
- `packages/evaluation/src/r97-plan.ts`、`r92-authorization.ts`
- `r97-driver-closed-loop.test.ts`、`r97-plan.test.ts`、`r97-execution-identity.test.ts`

### 做什么

1. 正式执行的 now 来自执行器时钟；plan 中的 observation 仅是审阅快照。
2. 开始和恢复执行前，重新读取两臂的当前 HEAD、构建身份、实际案例字节和执行参数。
3. worker 接到明确批准的案例指纹，并校验自己复制后真正要运行的内容。

### 怎么做

1. 写真实 CLI 的 RED：生成合法离线测试批准，保留计划时 observation；让批准窗口相对当前执行时间已经过期。通过实际 main 调用真实 gate，要求任何 worker/provider 开始前拒绝。不要把 gate 换成恒 true。
2. 正式 main 无条件使用可信执行时间；不能 `observation.now ?? now`。测试时钟只通过明确隔离的测试入口注入，不能由正式计划内容控制。
3. 实际调用现有 observeArms/指纹能力，基于传入的两个 checkout 执行观测；不要把复制快照再命名为 executionObservation 当作修复。
4. 统一源案例定位、规划输入和 staged 输入的语义。按现有 canonical fingerprint 合同检查 case.json、request.md、expected.md、fixture 及其规定的其他输入，不能只检查 suite。
5. 将 approved case fingerprint / inputsDigest 显式传给 worker；缺少必需摘要直接拒绝。消除 `repo:unknown` 可以用于正式执行的路径。
6. staging 完成后重新计算实际字节的指纹，与批准值比较；在首个 generate 前完成。针对复制期间源文件变化的测试使用同步 barrier。
7. resume 先验证批准身份、当前输入和已有结果证据，再决定 skip/执行剩余单位。不能因单位已 completed 就忽略其输入漂移，也不能在发现旧证据损坏前先花预算执行其他单位。
8. 模型、provider、endpoint 从同一批准材料解析；实际参数不同则拒绝，环境默认值不能静默重定向。检查当前 `approvedIdentity.endpointBaseUrl` 和拒绝 reason：公开结果中保留 endpoint identity/脱敏信息，不原样输出可能含 token 的 URL。
9. 把 CLI 接受的选项逐一核对：值缺失、NaN、负数、拼错 flag 给配置错误；批准已决定的 model/provider flag 若保留，必须校验一致，不能接受后忽略。

### 怎么验收

- [ ] 实际 CLI 在当前时间已过期、plan.observation.now 仍有效时拒绝；不依赖传 `--now`，调用计数为 0。
- [ ] 保持 plan/observation 完全不变，分别改 request、fixture、case.json 的实际内容：首次执行和 resume 均在新调用前失败。
- [ ] 只改 case 的内容且 suite 不变，仍失败；测试不能只覆盖非法 JSON。
- [ ] worker 收到批准摘要；复制后字节不匹配时不进入 inner provider。
- [ ] 无改动的正常两臂执行通过；全完成后恢复新增调用为 0，累计结论保持一致。
- [ ] 污染环境 endpoint、模型参数不一致、缺必需参数分别有正式入口反例。
- [ ] 用虚构 sentinel token 验证 stdout、driver-result、evidence、错误 reason 不包含完整敏感 URL；不要使用真实凭证。
- [ ] 两个不同 checkout 的观测来自各自目录，缺输入时不会回退到 driver repo。

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-driver-closed-loop.test.ts packages/evaluation/src/r97-plan.test.ts packages/evaluation/src/r97-execution-identity.test.ts packages/evaluation/src/r97-redaction.test.ts
```

交付一张数据来源表：批准快照、当前观测、worker staged 指纹分别在哪产生、在哪比较。对“真实 gate + 正式 main”的用例明确标注，不能以测试替身的结果冒充授权验收。

## A4 — 将真实执行依赖纳入批准的构建身份

### 可直接发送给 Agent 的提示词

你负责 F4。不要只往固定数组再加一个 runtime 文件。当前问题是“入口文件 hash”和“实际执行依赖 hash”分离，且广一些的 execution digest 在执行后记录，未参与批准拒绝。

**阅读/修改入口：**

- `scripts/e4/r97-arm-worker.mjs` 的 BUILD_ARTIFACT_PATHS、armBuildIdentity
- `scripts/e4/r97-arm-exec.mjs` 的 executionManifest、armExecutionDigest、loadArmModules
- `packages/evaluation/src/r97-plan.ts` 的 R97_DRIVER_ARTIFACTS、computeDriverBuildDigestV1 和授权构造
- `r97-execution-identity.test.ts`、`r97-plan.test.ts`、`r97-driver-closed-loop.test.ts`

### 做什么

1. 批准材料包含实际执行字节的稳定 manifest/digest；正式执行在加载/调用前重新计算并比较。
2. arm 与 driver 各自覆盖实际可执行依赖；不能以转导出的 index.js 代替其下层模块。
3. 明确旧摘要合同的处理；新合同需要重新生成批准材料。

### 怎么做

1. 先保留入口文件不变，只改 `packages/core/dist/runtime/runtime.js` 字节，复现当前 arm buildDigest 不变。再只改 driver 实际加载的 evaluation dist 子模块、保持 src/index.js 不变，建立另一反例。
2. 用现有 executionManifest 思路定义明确且完整的执行集合。覆盖 CLI benchmark 入口、真实 runtime/provider/verifier/tool/security 依赖以及本轮 adapter；覆盖 driver 的 gate、plan、ledger、lifecycle、channel、state、evidence 实际 dist 模块。
3. 根据真实 import/构建依赖确认范围，不依赖文件名猜测。无需递归摘要整个仓库和全部 node_modules；第三方依赖如由锁文件/构建出处绑定，应说明可验证关系。
4. 统一规范化相对路径、排序、字节 hash 和 manifest schema。缺少必需文件、文件不可读、依赖解析到意外目录时拒绝，不能 silently skip 后生成一个更小集合。
5. 让计划生成、执行前复核、worker record 和 evidence 使用同一身份合同。若保留不同用途的 digest，名称和关联必须明确，不能一个用于批准、另一个仅在事后展示。
6. 执行前检测同长度、同 mtime 的内容改变；mtime/size 不作为身份替代。注意模块缓存：不得用已缓存的旧模块执行、却把新磁盘文件 digest 记为实际执行身份。
7. 原两份历史 arm 的 SHA 保持不变；需要的兼容 adapter 属于 driver 执行材料并有自己的摘要。不要直接修改历史基线后仍沿用原 SHA/旧批准。
8. 更新 schema/版本和拒绝信息；旧材料缺新身份字段应明确拒绝重新生成，不能静默补字段后继续使用旧授权。

### 怎么验收

- [ ] 改 core runtime 的被导入模块、入口不变：digest 变化；旧计划在第一调用前被拒绝。
- [ ] 分别改 model/provider、verifier、安全/工具执行依赖、budget-channel/lifecycle/gate 的实际 dist：均被当前合同覆盖。
- [ ] 只改 src、未构建的情况不会被误报为“执行了新源码”；报告能区分源码与执行产物。
- [ ] 只改执行 dist、保留 src、大小和 mtime：旧批准仍失效。
- [ ] 删除一个必需依赖或解析到另一 checkout：拒绝，不能摘要剩余集合后继续。
- [ ] 相同内容的 clean build 在声明支持的路径/平台规范下可稳定复核；若构建确有平台差异，明确绑定各平台产物，不硬宣称跨平台相同 hash。
- [ ] 正常双历史 arm 的身份仍能建立，执行证据准确记录它们及 adapter 的来源。

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-execution-identity.test.ts packages/evaluation/src/r97-plan.test.ts packages/evaluation/src/r97-driver-closed-loop.test.ts
```

交付实际 manifest 示例、摘要 schema，以及三项 RED/GREEN：传递依赖修改、仅 dist 修改、缺文件。说明哪些身份变化要求重新批准。

## A5 — 身份不符必须拒绝，不能被后续 PASS 覆盖

### 可直接发送给 Agent 的提示词

你负责 F5。`runArmUnit` 已发现 executionIdentity.drift，但后续报告分类会覆盖它。请修复结果优先级，并把能提前发现的身份不符移到调用之前。

**阅读/修改入口：**

- `scripts/e4/r97-arm-exec.mjs` 的 dry-run 身份解析、capturing.createClient/generate
- `scripts/e4/r97-arm-worker.mjs` 的 runArmUnit、classifyReport、terminalDetailFor
- `packages/evaluation/src/r97-campaign-evidence.ts`
- `r97-execution-identity.test.ts`、`r97-arm-worker-contract.test.ts`、`r97-arm-report-evidence.test.ts`

### 做什么

1. 身份、预算、执行停止等拒绝不能被成功报告覆盖。
2. 在传给 inner provider 前验证实际 model ref；离线 scripted 替身与批准 provider 的允许关系显式建模。
3. worker、终态记录、evidence、driver 汇总对同一单位给出一致结论。

### 怎么做

1. 建立最小协议 fixture：批准 approved-model；dry-run 声明正确模型；实际 createClient 使用 unapproved-model；最终写一个 verification_passed=true 的合成报告。当前 worker 会 drift 非空但 PASS，先锁定这个 RED。
2. 将 verdict 决策写成明确、互斥的状态转换。保留第一个阻止有效评分的事实及其他诊断，不能后续无条件赋值覆盖。不要只在最后把 verifierPassed 置 false 而仍写 completed/passed detail。
3. dry-run 未成功、必要身份缺失或不匹配时，在实际 dispatch 前结束；不能将 null 身份解释为“没有漂移”。
4. createClient/generate 边界核对真实 modelRef；不匹配时不进入 inner generate。离线替身 provider 与被批准目标 provider 的差异可按既有 offline-test 合同允许，但应单独标识，不能泛化为所有 provider 差异都忽略。
5. 即使某项漂移只能执行后观察到，也必须拒绝该结果参与评分，并保留已经发生的预算消费，不能为方便重试而退款。
6. 复用已有 evidence schema 和分类流程；必要时增加结构化拒绝码，不另建第二套 verdict 来源。确保保存的 report 是诊断证据，不是覆盖身份失败的权威。
7. 将通过、合法 case_failed、provider/harness/infrastructure 失败保持区分；本任务不能把所有低分案例都改成基础设施失败。

### 怎么验收

- [ ] 漂移+成功报告：worker 非 PASS；verifierPassed=false；终态/evidence/driver 结论一致。
- [ ] 实际 modelRef 与批准不同：inner generate 次数为 0。
- [ ] dry-run exit 非零、无法解析身份、缺必要字段、endpoint 不符：不进入实际案例调用。
- [ ] 完全匹配的 offline provider 替身仍可得到真实 verifier PASS；合法负例仍记 case_failed。
- [ ] 首次失败后 resume 新增调用为 0，历史失败不会变成 COMPLETE 的成功 campaign。
- [ ] 同一错误结果通过独立 validator 校验的是“证据完整的失败”，不会因此被解释成任务通过；明确区分 artifact valid 与 campaign successful。
- [ ] 身份失败与 timeout 同时出现时结论稳定，不受 if 语句顺序偶然影响。

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-execution-identity.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts packages/evaluation/src/r97-arm-report-evidence.test.ts packages/evaluation/src/r97-campaign-validator-cli.test.ts
```

交付一张小型 verdict 优先级表和漂移反例。测试中的合成 PASS 必须清楚标为协议 fixture，不能写进真实 benchmark 成绩。

## A6 — 让取消、deadline、output_limit 终止当前实际执行

### 可直接发送给 Agent 的提示词

你负责 F6。现有 boundedStop 工具已经能杀进程，但实际 worker 改成进程内 await runBenchmarkCommand。请连接实际执行路径，不再仅补 boundedStop 自身的测试。

**阅读/修改入口：**

- `scripts/e4/r97-campaign-driver.mjs` 的 main、makeCampaignStop、runDriver
- `scripts/e4/r97-arm-worker.mjs` 的 runArmUnit、DeadlineBudget、boundedStop、ByteCap
- `scripts/e4/r97-arm-exec.mjs` 的 runArmCaseInProcess、mergeSignals
- A1 的 budget channel 收尾与当前 execution-state 恢复合同
- `r97-bounded-stop.test.ts`、`r97-driver-closed-loop.test.ts`、`r97-arm-worker-contract.test.ts`

### 做什么

1. 用户取消和 campaign deadline 能终止正在运行的 unit，不只是阻止下一 unit。
2. 总期限覆盖 staging、dry-run、dispatch、工具和收尾；不能每阶段重新给完整时间。
3. 不响应 AbortSignal 的执行也有可验证的强制终止边界；已发送调用保留预算。

### 怎么做

1. 建立两类 RED：正式 driver 运行中触发取消；真实 worker 执行一个会卡住 dry-run/dispatch 的协议 fixture。记录当前 unit 仍完成或超过期限的事实。
2. driver 将 cancellation signal 和 campaign 剩余期限传到 worker。effective unit deadline 取 unit 上限与 campaign 剩余时间的较小值，并保留触发原因。
3. 普通 provider 继续传播组合 signal，释放监听器；但不要认为传入 signal 就能强制结束任意 await。
4. 对实际 arm 执行建立可终止的隔离边界，优先复用现有 child runner/boundedStop。独立子进程加载指定 arm 的导出入口并复用同一 ledger，可沿用已有历史 arm ABI；不需要重写 Runtime。
5. 如果选择其他隔离方案，必须证明它能结束忽略 signal 的 provider、挂起 verifier/tool 及其子进程。单独 Promise.race 返回后放任后台执行继续，不算修复。
6. 复用 polite stop→grace→强制进程树停止→await close→一次 settle 的既有实现；output_limit 也接同一路径。输出限制作用于真正执行的进程，按字节计算。
7. 子进程/协议必须绑定 arm SHA、批准输入和 adapter digest，继续经过 A3–A5；不能为隔离而退回“每 unit 固定收费 1”或绕过预算。
8. 中途停止后，从可持久验证的预算与 attempt 状态恢复统计。证明未 dispatch 才退款；已 dispatch 未知则 unknown，resume 不自动重发。不要只因为 worker 抛错就把消耗填 0。
9. 统一 timeout/cancelled/output_limit 的原因与证据。异常发生在 deadline 之后也不能统统落入 infrastructure catch；保存 cause，区分业务负例。
10. 处理 SIGINT/SIGTERM 监听器清理和重复信号；Windows 用已有跨平台进程树方案。实现与测试不引入 Bash/WSL 依赖。

### 怎么验收

- [ ] 当前 unit 运行中发取消：unit 在声明的 grace+调度容差内结束，不 PASS，不再启动下一个 unit。
- [ ] campaign 剩余时间小于 unit timeout：当前 unit 按 campaign 剩余时间停止。
- [ ] dry-run hang、provider 忽略 signal、工具/verifier hang、持续输出四类 fixture 都能通过实际 worker 路径被终止。
- [ ] 终止后父进程、子进程和孙进程均不继续写 sentinel 文件；仅返回 Promise 不算通过。
- [ ] 判定原因分别为 timeout/cancelled/output_limit；exit/error/record/evidence 对得上。
- [ ] 启动失败或明确未 dispatch：零调用，释放未使用预留；已进入后被强制终止：预算不恢复。
- [ ] 停止与正常 close 竞争不会重复写终态、重复 commit 或遗留 listener。
- [ ] 测试 watchdog 仅用于兜底清理；若 watchdog 实际介入，该用例必须失败，不能把清理成功计为实现通过。
- [ ] Windows/Ubuntu 两平台均运行上述关键场景，不因平台/历史构建缺失而静默 skip。

```text
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-bounded-stop.test.ts packages/evaluation/src/r97-driver-closed-loop.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts
```

交付各停止场景的触发点、耗时上界、退出原因、进程存活检测和预算状态。计时断言允许合理 CI 调度余量，但不能以数分钟超时掩盖一个未生效的短 deadline。

## A7 — 把反例接入双平台闭环，更新结论后结束本轮

### 可直接发送给 Agent 的提示词

你负责集成 A1–A6 的验收。当前已经有可用 closed-loop/mutation/CI，不新建验收框架，也不为了增加数量继续发散任务。

**阅读/修改入口：**

- `.github/workflows/ci.yml` 的 `r97-r98-closed-loop` job
- `scripts/e4/r97-closed-loop.mjs` 的 SUITE_FILES
- `scripts/e4/r97-acceptance-matrix.mjs`、`r97-mutation-check.mjs`
- `scripts/e4/r97-offline-acceptance.mjs`、`r97-validate-campaign.mjs`
- `docs/E4-R99-R101-report.md` 和本计划的验收清单

### 做什么

1. A1–A6 新反例进入真实 Windows/Ubuntu 必跑路径，不能只存在于未执行的测试文件。
2. 保留两个历史 arm、真实工具和 verifier 的正常离线链路；增加异常路径验收。
3. 报告只保留能追溯到具体源码与证据的当前结论，完成后停止本轮基础设施修改。

### 怎么做

1. 复核 SUITE_FILES 与常规 pnpm test 的排除项。新测试若放现有文件，确认那个 describe/test 确实执行；若新增文件，显式接入专用 suite。
2. acceptance matrix 建立下面的行为映射。用具体测试名/证据关联各行，不允许一个无关测试让多行自动通过。
3. mutation gate 保留现有有效覆盖，并补能破坏本轮关键不变量的最小变异，例如跳过 generator finally 结算、允许丢失根目录重新领取、把正式时钟改回快照、恢复 verdict 覆盖、断开当前 unit 的取消。
4. mutation 在隔离副本执行，结束校验工作树恢复；断言对应测试真正 RED，不接受语法错误、构建失败或 unrelated timeout 冒充捕获了缺陷。
5. 正常闭环仍运行两个冻结 arm：各自 build、真实 request、工具、verifier、落盘 evidence、独立 validator、零新增调用的 resume。不能全部替换为合成报告。
6. 使用全新 `--out`；现有脚本会拒绝复用结果目录。不要为方便验收移除这一 guard。Windows 命令使用 Node/pnpm，不增加 Bash 前提。
7. 在干净的实施状态运行一次全套 typecheck/test 和专用闭环。若测试要求 clean tree，使用隔离、包含本轮修改的 checkout；报告具体状态，不把所有失败笼统归为环境问题。
8. 查看包含最后代码/测试/workflow 改动的真实 CI run 与两个平台 job；保留 run ID、head SHA、attempt、artifact 名称与步骤状态。无法取得某 artifact 时明确未复核其内部数字。
9. 更新现有报告的“当前状态”索引/小结，将已过期断言标注为历史。无需删除历史，也不要再逐个 docs commit 追加整段收尾声明。
10. 文档 commit 不需要在正文里引用它自己的 SHA。区分“实现提交已验收”“文档又更新了”“当前 head 是否另有 CI”；不要使用“只改文档绝不可能改变 CI”这样的绝对推断。

### 必须覆盖的行为矩阵

| 行 | 场景 | 最低证据 |
| --- | --- | --- |
| M1 | 流提前 break/return 后 CLI 异常 | innerCalls=1；预算不退；第二次 reserve 拒绝 |
| M2 | 已消费 campaign 整目录删除后重新打开 | 同批准拒绝；没有新调用 |
| M3 | 旧 observation.now + 当前时间已过期 | 真实 main/gate 拒绝；没有进入 worker |
| M4 | 计划后修改实际 case/staged 字节 | 首次/恢复均拒绝，调用 0 |
| M5 | 只改被导入 dist 子模块 | 实际批准 digest 不匹配，调用前拒绝 |
| M6 | 身份漂移 + 成功报告 | 非 PASS；落盘与汇总一致 |
| M7 | 当前 unit 中取消/总 deadline/output_limit | 当前执行及进程树停止；原因准确；预算保守结算 |
| M8 | 正常双 arm 工具/verifier 路径 | 正常通过与真实负例仍成立；不是协议 fixture 伪造通过 |
| M9 | 正常/失败/unknown 的 resume | 新增调用与累计结果符合合同；损坏证据先拒绝 |

### 怎么验收

- [ ] Windows/Ubuntu 专用 job 都通过，M1–M9 中适用场景没有 skip；缺历史 arm 或依赖应失败并诊断。
- [ ] 新反例实际进入必跑 suite；mutation 命中对应断言，恢复后 GREEN。
- [ ] typecheck 与全套测试通过；driver 专用测试没有因 package.json 排除而漏跑。
- [ ] validator 独立检查本次结果；首次/恢复的证据、预算、失败与 PASS 数可以对应。
- [ ] 上传 artifact 含批准摘要、当前观测、构建 manifest、预算/状态、证据和验收结果；不含 key 或带凭证 URL。
- [ ] 区分 weak/strong pass、run completeness、artifact validity 和 model-quality claim；离线 scripted 通过不支持模型能力提升。
- [ ] 报告引用实际 CI head；本地结果、CI job 状态、读取 artifact 后得到的数字分别说明来源。
- [ ] 本轮最终状态为 `OFFLINE_ACCEPTED / PAID_NOT_RUN`，或列出仍失败的明确条件；没有证据就不写 DONE。

从仓库根目录执行的收尾命令：

```text
pnpm typecheck
pnpm test
pnpm docs:verify
node scripts/e4/r97-closed-loop.mjs --all --out .ci/r97-review-closeout --arms-root .ci/r97-review-arms
node scripts/e4/r97-validate-campaign.mjs --campaign .ci/r97-review-closeout/acceptance/ledger
node scripts/e4/r97-mutation-check.mjs --out .ci/r97-review-closeout/mutation-report.json
```

上述 `--out` 必须此前不存在；若已经使用，换一个新的目录名。测试结果与修复合入前的旧目录不能混用。A6 如改变了 artifact 布局，应更新命令及脚本合同，不能留下不能执行的示例。

如无远端推送/运行权限，先完成本地可审查差异和离线证据，把 CI 状态标为 `NOT_RUN` 并说明缺的是哪一个动作；不得把本地推测写成两平台成功。开发者无需为此配置本地 Linux。

## 2. 每个任务必须使用的交付格式

```text
任务：A<n> / 标题
状态：DONE | PARTIAL | ALREADY_FIXED | BLOCKED
审查基线：735c22a83e8718a33248167b0086822ca7577461
实施 HEAD / 差异范围：实际值

问题与影响：
- 用一两句话描述真实执行路径中的错误。

修改：
- 哪些文件/函数发生变化，为什么能关闭这个错误。
- schema / 批准摘要 / 恢复兼容性是否变化。

反例：
- 输入、故障注入点、实际调用计数、持久状态。
- 修复前 RED 的具体断言；修复后同一断言 GREEN。

验证：
- 实际运行的命令、退出码、关键观测值。
- 外部模型请求数；本轮应为 0。
- 哪些使用协议 fixture，哪些运行真实历史 arm / gate / 工具 / verifier。
- CI run URL、head、attempt、两个平台 job 状态；没有运行写 NOT_RUN。

剩余问题：
- 未满足的验收项，或明确“本任务所列项已满足”。
- 不新增与当前反例无关的大型重构计划。
```

## 3. 本轮结束条件与之后的方向

只有以下条件同时成立，才关闭本轮：

1. 任意 stream 关闭/异常/取消路径都不把已进入的调用当成免费调用。
2. 同一批准在现有持久授权记录范围内不会因输出目录丢失而重新授额。
3. 正式 CLI 使用当前时间和当前输入；实际 staged 内容与批准一致；恢复也验证这些事实。
4. 实际执行模块的变化会使旧构建身份失效，而且在下一次调用之前被发现。
5. 身份不符不会被成功报告覆盖；结果、状态、证据和汇总结论一致。
6. 取消、期限和输出上限能结束当前实际执行及其进程树，并留下可恢复的保守预算状态。
7. 以上反例和正常双 arm 路径通过 Windows/Ubuntu 必跑 CI，证据与实施提交对应。

**达到这些条件后停止这一轮基础设施修改。** 不因用户再次询问进度，就重新安排已完成的锁、ledger、evidence、matrix 或双平台 job。

下一阶段的选择应基于项目目标与已有实验数据：如要研究 Agent 效果，回到仓库规定的 `benchmark → failure cluster → hypothesis → challenger → paired eval`；如暂时没有覆盖最终计划的付费授权，就保留 `PAID_NOT_RUN`，不能以扩大离线脚本规模冒充模型效果提升。

本计划不要求执行付费实验，也不把获得付费授权作为完成 A1–A7 的前置条件。当前 `runArmCaseInProcess` 只构造 scripted provider；这证明的是离线执行链路。真实付费 transport 的接入/身份若今后要实现，应在单独明确的任务中验证，不能把一个未接通的布尔选项当成已具备付费执行能力。

## 4. 审查依据

- [固定审查提交 735c22a](https://github.com/ki11a-Conton/harness-agent/commit/735c22a83e8718a33248167b0086822ca7577461)
- [与 0075f72 的提交比较](https://github.com/ki11a-Conton/harness-agent/compare/0075f723cfb6f6e38cf55829974b9d7634cf612b...735c22a83e8718a33248167b0086822ca7577461)
- [当前提交 CI 35714298502](https://github.com/ki11a-Conton/harness-agent/actions/runs/35714298502)
- [budget channel](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/packages/evaluation/src/r97-budget-channel.ts)
- [campaign lifecycle](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/packages/evaluation/src/r97-campaign-lifecycle.ts)
- [campaign driver](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/scripts/e4/r97-campaign-driver.mjs)
- [arm worker](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/scripts/e4/r97-arm-worker.mjs)
- [arm executor](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/scripts/e4/r97-arm-exec.mjs)
- [计划与构建身份合同](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/packages/evaluation/src/r97-plan.ts)
- [现有阶段报告](https://github.com/ki11a-Conton/harness-agent/blob/735c22a83e8718a33248167b0086822ca7577461/docs/E4-R99-R101-report.md)

注意证据层次：上面的源码与 CI 链接是仓库事实；0.2 的数值是本次隔离诊断；各任务中的勾选框与命令是未来验收要求。三者不能互相替代。
