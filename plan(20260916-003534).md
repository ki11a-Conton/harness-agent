# harness-agent 下一阶段执行计划（R84–R87）

> 审查基线：`6693a6a52fb488bbe3273625eca8c1adb0981957`  
> 审查日期：2026-09-16  
> 适用仓库：`ki11a-Conton/harness-agent`  
> 执行环境约束：开发者本机只有 Windows；**不得把本地 Linux、WSL 或 Docker 作为验收前提**。Linux 验收只允许由 GitHub Actions 的 `ubuntu-latest` 完成。

## 0. 本轮代码审查结论

### 已确认完成

1. R79 的命令验证器已经改为结构化 `file + args`，正常路径使用 `shell: false`，Windows 兼容路径有测试覆盖。
2. R80 已处理 turn error 与 cleanup error 的优先级，避免清理失败掩盖主失败。
3. R81/R83 已把 provider、model、endpoint 身份绑定到执行计划，并修复“digest 使用显式 flags，但真实请求仍走默认 endpoint/model”的严重偏差。
4. R82 已增加 GitHub Actions Ubuntu 冷启动验证；最新提交对应的 CI run `34986884100` 全部成功，包括 Windows、Ubuntu、coverage、Ubuntu offline cold-start 和 release attestation。
5. R83 已完成 86 个 case 的串行实跑并在报告中给出汇总：21/86 通过、0 个进程级 run failure、1,327 次 model call。

### 下一阶段必须解决的问题

1. **R83 证据不可独立复核。** 报告引用的 `.ci/run-grok-campaign.ps1`、逐 case report、`run.log`、`manifest.jsonl` 和 campaign summary 位于被 `.gitignore` 排除的 `.ci/`，当前仓库只有人工汇总报告。
2. **R83 报告存在可证伪的配置错误。** 报告 §6 写 `maxIterationsPerTurn = 20`，但 `5f2af6e` 与 `dd80676` 的 `apps/cli/src/benchmark-command.ts` 均明确传入 `30`。默认值 20 不等于该 benchmark 路径的有效值。
3. **本次汇总跨两个 source SHA。** 8 个 case 为 `5f2af6e`，78 个 case 为 `dd80676`。二者代码差异只有报告文档，但仍应在证据清单中逐项保留真实 SHA，不得把整场 campaign 伪装成单一不可变计划。
4. **低通过率尚不能直接归因。** 46 个 `tool_limit`、8 个 `agent_limit` 可能来自模型行为、工具协议、错误反馈、验证器、预算或真实 harness 缺陷；目前没有证据支持直接提高上限。
5. **holdout 已被正式使用。** 下一轮实现和调参不得查看或利用 holdout 的逐 case prompt、输出或失败细节；否则会把 holdout 变成训练集。

## 1. 全局执行规则（每个 Agent 都必须遵守）

- 一次只执行一个任务，按 R84 → R85 → R86 → R87 顺序推进。后续任务的前置条件未满足时必须返回 `BLOCKED`，不得自行绕过。
- 开始任务前记录 `git status --short` 和 `git rev-parse HEAD`；不得覆盖用户已有改动，不得使用 `git reset --hard` 或 `git checkout --` 清理工作区。
- 除 R87 在获得**新的明确授权**后可能调用真实模型外，R84–R86 必须保持 **0 次 provider/model 调用、0 付费**。
- 不得把 API key、Authorization header、完整 prompt、完整模型输出、用户路径、带 query/userinfo 的 endpoint 写入 git、测试快照或报告。
- 不得用“86/86 已运行”代替“21/86 通过”。进程成功、case 通过、验证通过必须分别统计。
- 不得因为 `tool_limit` 多就直接提高 `maxToolCalls`、`maxIterationsPerTurn`、timeout 或重试次数。只有可复现证据证明预算设置本身是根因时，才允许单独立项。
- Windows 本地允许执行 PowerShell 与 Node/pnpm 验证；POSIX/Linux 专属验证必须提交到 GitHub Actions，并以 `ubuntu-latest` 的干净 checkout 结果验收。
- 每个任务单独提交；提交信息建议使用 `E4-R84: ...`、`E4-R85: ...`。不得把后续任务偷带进当前提交。
- 每个任务完成后新增 `docs/E4-Rxx-report.md`，记录起始 SHA、结束 SHA、文件清单、命令、exit code、测试数量、CI URL/run id、0-call/paid-call 数量以及未完成项。

---

## R84 — 固化 R83 campaign 的可核验证据链，并纠正报告事实错误

### 给 Agent 的完整提示词

你正在 `ki11a-Conton/harness-agent` 仓库执行 E4-R84。目标是把 R83 已经发生的 86-case campaign 转换成**可重复离线验证、可检测篡改、不会泄密**的证据链；本任务不得重跑任何 case，不得调用 provider。

#### 开始前检查

1. 确认当前代码至少包含提交 `6693a6a52fb488bbe3273625eca8c1adb0981957`。
2. 在原来运行 campaign 的 Windows 电脑检查下列本地材料是否仍存在：
   - `.ci/run-grok-campaign.ps1`
   - `.ci/bench-grok/manifest.jsonl`
   - `.ci/bench-grok/results/<suite>/<caseId>/...`
   - `.ci/bench-grok/campaign-summary.json`（如果存在）
3. 如果原始材料不存在，立即停止并报告 `BLOCKED: R83_RAW_ARTIFACTS_MISSING`。不得根据 `docs/E4-R83-report.md` 反向生成“原始结果”，也不得付费重跑来掩盖证据丢失。
4. 在读取内容前先复制一份只读备份到仓库外的、明确的 Windows 路径；记录备份目录和 SHA-256 总清单，但报告中不得暴露用户名等私有路径。

#### 要做什么

1. 将经过脱敏和审查的 campaign runner 移出被忽略的 `.ci/`，纳入 `scripts/benchmark/run-campaign.ps1`。
2. 新增离线命令 `agent benchmark campaign validate <campaign-root> [--json]`，对整场 campaign 而不是单个 artifact 做 fail-closed 校验。
3. 生成并提交一个脱敏、机器可读的 R83 证据清单，例如 `docs/evidence/e4-r83-campaign-manifest.json`。
4. 修正 `docs/E4-R83-report.md` 中 `maxIterationsPerTurn = 20` 的错误，改为真实有效值 30，并准确解释 20 只是 runtime 默认值、不是 benchmark 路径的传入值。
5. 明确写出 8 个 case 属于 `5f2af6e`、78 个属于 `dd80676`；不得将它们改写为一个 source SHA。可以说明 `git diff 5f2af6e dd80676` 是 docs-only，但证据必须保留逐 case 真实值。

#### 怎么做

1. 先对 runner 和原始数据做 secret scan：至少检测 OpenAI 风格 key、Bearer header、URL userinfo、query token、常见 provider key 格式。发现秘密时，只在工作副本中脱敏；只读备份保持原样并留在仓库外。
2. runner 必须满足：
   - key 只从环境变量读取；
   - 不把 key 放入 argv、日志、manifest 或异常文本；
   - 每个 case 原子落盘，支持 resume，已完成 case 不再计费；
   - 默认串行；任何未来并发都必须显式 opt-in，本任务不得新增默认并发；
   - 计划生成与真实执行之间继续要求 digest；
   - 显式记录 provider/model/endpoint identity digest、source SHA、case id、suite 和 limits。
3. `campaign validate` 应在现有 `benchmark validate` 基础上复用 V3/artifact 校验能力，但增加 campaign 级检查：
   - 从版本化 benchmark case 源得到期望集合，而不是把 manifest 自身当真值；
   - 恰好 86 个唯一 `(suite, caseId)`，数量为 adversarial 13、stress 11、regression 30、holdout 32；
   - 不允许缺失、重复、未知 case 或一个 case 多份最终结果；
   - 每个 JSON 均可解析、schema 合法，summary 可从 case 结果重新计算；
   - 重新计算通过数、suite 分布、termination 分布、model calls、input/output tokens，并与提交的脱敏 summary 比较；
   - 校验 manifest 的 resume 记录与最终结果一一对应；
   - 校验每个 artifact 的 SHA-256，并计算稳定的 root digest。排序必须固定，Windows 与 Ubuntu 结果一致；
   - 保留 source SHA 分布，验证只能出现报告声明的 SHA；
   - 将“process execution success”与“case success/verified completion”分开输出；
   - 空目录、0-case、仅 summary 无原始 case、只有部分 suite 时必须非零退出。
4. 脱敏证据清单只能包含复核所需字段：case id、suite、source SHA、identity/digest、终止原因、pass/fail、调用与 token 数、duration、artifact hash。不要提交完整 prompt、模型输出、绝对路径或 key。
5. 对原始数据缺字段的情况不得补造；用明确的 `unknown`/`not_recorded` 和 reason code，并让 promotion-grade 校验失败。
6. 给新校验器写 table-driven 测试，至少覆盖：有效 campaign、缺 case、重复 case、未知 case、hash 篡改、summary 篡改、source SHA 漂移、manifest 截断、空目录、secret 命中、Windows 路径分隔符、CRLF/LF 稳定 digest。

#### 怎么验收

Windows 本地必须完成：

```powershell
pnpm install --frozen-lockfile
pnpm build
pnpm test
pnpm test:coverage
pnpm docs:verify
node apps/cli/dist/bin.js benchmark campaign validate .ci/bench-grok --json
git diff --check
```

另外必须用临时副本执行负向验证：删除一个 case、复制一个 case、修改一个 token 数、修改一个文件字节，四种情况都必须返回非零且给出稳定 reason code。不要修改唯一的原始目录。

GitHub Actions 必须在 `windows-latest` 和 `ubuntu-latest` 使用仓库内的**脱敏合成 fixture**运行同一个 validator；Ubuntu cold-start 必须继续成功。不得把用户的私有原始 campaign 上传为公开 CI artifact。

R84 只有在以下条件全部满足时才可标记 `DONE`：

- validator 从原始数据重新算出 86 stored、21 passed、1,327 model calls、4,051,523 input tokens、440,594 output tokens，并匹配各 suite/termination 汇总；
- 原始文件任一字节被改动时 hash/root digest 校验失败；
- runner 已版本化且通过 secret review；
- R83 的 20/30 错误已纠正；
- Windows 与 Ubuntu CI 均通过；
- 本任务 provider call 计数为 0。

---

## R85 — 对失败结果做零调用归因，建立可行动的 failure taxonomy

### 给 Agent 的完整提示词

你正在执行 E4-R85。前置条件是 R84 已完成且 `benchmark campaign validate` 对原始 R83 campaign 返回 VALID。目标是从已存在的 trace/event/report 中区分“模型没有完成任务”和“harness 自身存在缺陷”。本任务只能离线分析，provider call 必须为 0。

#### 要做什么

1. 新增离线分析命令，例如 `agent benchmark campaign triage <campaign-root> --out <dir>`。
2. 对 adversarial、stress、regression 三个开发 suite 做逐 case 证据归因。
3. 对 holdout 只保留聚合数字，不读取、不输出、不引用逐 case prompt、模型输出或失败过程，以防继续污染 holdout。
4. 产出 `docs/E4-R85-report.md` 和一个脱敏机器可读 taxonomy summary。
5. 只选择最多两个“有重复证据、属于 harness、可用离线测试复现”的候选缺陷进入 R86。

#### 怎么做

1. 分类必须使用互斥的主原因，并允许附加次级标签。建议主类：
   - `MODEL_BEHAVIOR`：模型持续选择无效/重复动作，但工具和反馈正确；
   - `TOOL_PROTOCOL`：schema、结果表达、错误反馈或工具选择造成可复现误导；
   - `VERIFIER_OR_ORACLE`：任务已满足但验证器错误拒绝，或 oracle 定义不一致；
   - `HARNESS_CONTROL_FLOW`：状态机、终止、恢复、重复调用处理等有缺陷；
   - `BUDGET_EXHAUSTION_UNATTRIBUTED`：只知道触顶，尚无证据说明是谁导致；
   - `PROVIDER_OR_TRANSPORT`：真实 429/5xx/断连/invalid response；
   - `SECURITY_POLICY_DENIAL`：安全策略按设计阻止操作；
   - `INSUFFICIENT_EVIDENCE`：trace 不足，禁止猜测。
2. 对 46 个 `tool_limit` 至少提取：工具序列、连续相同调用的规范化签名、成功/失败/拒绝数、相同错误重复次数、最后一次状态变化、是否存在“结果未变仍重复调用”。
3. 对 8 个 `agent_limit` 分辨它们是否真的触发 benchmark 有效值 30，并检查 model-call、iteration、tool-call 的计数边界是否一致。
4. 对 `verification_failed` 检查 verifier 输入、命令、exit code、stdout/stderr 和 artifact 状态，但报告中只保留脱敏片段或合成复现。
5. 对 `cancelled`、`time_limit`、`model_error` 检查事件顺序，区分 timeout、AbortSignal、provider error 与 cleanup error，不得把它们合并为普通 case failure。
6. 实现确定性 fingerprint：相同语义的工具名+规范化参数+相关结果状态产生稳定 hash；敏感字段必须先移除再 hash。
7. 为每个建议修复提供证据包：受影响 case 数、至少两个非 holdout 样本、共同事件模式、反例、最小合成复现、预计修复层、不可用的替代解释。
8. 如果没有任何 harness 缺陷达到上述门槛，结论必须是 `NO_CONFIRMED_HARNESS_DEFECT`，R86 不应为了“必须改代码”而继续。

#### 怎么验收

- 同一份 campaign 连续运行两次，taxonomy JSON 字节级一致；排序、时间字段和绝对路径不得造成漂移。
- 聚合总数必须与 R84 validator 一致；任何无法分类的 case 进入 `INSUFFICIENT_EVIDENCE`，总数不得丢失。
- 合成 fixtures 覆盖至少：重复成功调用、重复相同错误、schema rejection、verifier false negative、真实 provider error、timeout/cancel、正常长任务但无重复。
- 测试证明 endpoint、key、Authorization、完整输出不会进入 triage artifact。
- `pnpm build`、`pnpm test`、`pnpm test:coverage`、`pnpm docs:verify`、`git diff --check` 全通过。
- GitHub Actions 的 Windows 与 Ubuntu 运行同一离线 fixture 并产出相同 digest。
- provider/model 调用为 0；不得创建新的 paid plan 或使用 R83 的旧授权。

---

## R86 — 只修复 R85 已证实的高影响 harness 缺陷

### 给 Agent 的完整提示词

你正在执行 E4-R86。只有当 R85 给出至少一个 `CONFIRMED_HARNESS_DEFECT` 时才开始；否则返回 `NOT_NEEDED: NO_CONFIRMED_HARNESS_DEFECT`。本轮最多修两个缺陷，不得顺手做架构重写，也不得查看 holdout 逐 case 数据。

#### 要做什么

1. 把 R85 的最小合成复现先转为失败测试，记录 RED 证据。
2. 在缺陷的最窄责任层修复，例如 tool-call controller、错误反馈、verification transition 或 termination mapping。
3. 保持现有安全边界、预算边界、恢复语义和事件可审计性。
4. 增加结构化事件/指标，使未来能够判断修复路径是否真实触发，而不是只看最终 pass rate。

#### 怎么做

1. 每个缺陷独立提交：测试先失败，再最小实现，再回归测试。报告应记录 RED 命令和失败断言，而不只是写“已 TDD”。
2. 如果 R85 证实存在无进展重复调用，可实现有状态但受限的 loop detection/recovery：
   - fingerprint 必须基于规范化工具名、脱敏参数和结果状态；
   - 只有“重复且世界状态/结果未变化”才算 stall；
   - 首次触发给模型结构化反馈，超过明确阈值才终止；
   - 不能把合法轮询、分页、重试或逐步编辑误判为循环；
   - 阈值必须进入有效配置和 execution-plan digest。
3. 如果证实是工具错误反馈不足，应改为稳定、可行动、无敏感信息的错误合同；不得把 raw exception 或 secret 送入模型。
4. 如果证实是 verifier/transition 缺陷，应以真实 artifact/world state 为准，保证 success、verification failure、cleanup failure 的优先级明确且 exactly-once。
5. 不允许把单纯提高 `maxToolCalls=100`、worker 30、`maxIterationsPerTurn=30` 或 600 秒 timeout 当作修复。预算调整若必要，必须另立证据和兼容性计划。
6. 用 R85 的非 holdout trace 做离线 replay，证明新代码改变的是目标机制。不得承诺真实模型 pass rate 已提升，因为本任务没有付费调用。

#### 怎么验收

- 每个修复都有 RED → GREEN 的最小测试、至少一个反例测试和一个安全回归测试。
- 离线 replay 中，目标 failure fingerprint 数下降或被明确转换成新的结构化终止；非目标 trace 结果不变。
- 现有 termination reason、resume、budget、permission、secret-redaction 测试全部通过。
- `pnpm build`、`pnpm test`、`pnpm test:coverage`、`pnpm docs:verify`、`git diff --check` 全通过。
- GitHub Actions 必须通过 Windows 与 Ubuntu；Ubuntu cold-start 在全新 checkout 中通过。
- 本任务 provider call 为 0；报告不得声称“真实 channel 已改善”。

---

## R87 — 经重新授权后进行小规模、串行、非 holdout 的基线/候选 A/B

### 给 Agent 的完整提示词

你正在执行 E4-R87。目标不是立即重跑 86 个 case，而是用最小付费实验验证 R86 的修复是否改善了已证实的失败机制。R83 的授权**不自动延续**到本任务。

#### 强制授权门

在产生任何真实请求前，向操作者展示并要求一次新的明确确认，至少包含：

- provider、model、endpoint identity digest；
- baseline SHA 与 candidate SHA；
- 精确 case 清单，只能来自 regression/adversarial/stress，禁止 holdout；
- 是否重复、串行度固定为 1；
- 每 case 与全局 model-call/token/time/cost-estimate 上限；
- 价格未知时明确写 `actual cost unknown`，不得把启发式 estimated cost 当成账单价格；
- execution-plan digest 和过期条件。

没有新授权时输出 `NOT_RUN: PAID_AUTHORIZATION_REQUIRED`，任务仍可正确结束；不得沿用 R83 key、digest 或口头授权。

#### 要做什么

1. 从 R85 的开发集证据中冻结 6–10 个代表性 case；至少包括目标 failure fingerprint 和不会触发该问题的反例。
2. 在代码修改前的 baseline SHA 与 R86 candidate SHA 上使用相同 identity、limits、case 顺序和 runner。
3. 严格串行，并在每个 case 后原子持久化；支持 resume，禁止重复计费已完成的 arm。
4. 首要指标是 R85 定义的机制指标；verified completion/pass rate 只是次要指标。
5. 生成脱敏 A/B manifest、逐 arm hash、汇总和诚实结论。

#### 怎么做

1. 优先先跑零调用 replay A/B。只有 replay 证明 candidate 命中了目标路径，才进入真实请求授权门。
2. case 选择规则必须在执行前写入 plan 并绑定 digest；不得看到 candidate 结果后替换 case。
3. 为减少顺序偏差，可用固定、预先提交的交错顺序运行 baseline/candidate，但总并发始终为 1。
4. 对每个 arm 记录 source SHA、provider/model/endpoint identity、limits、model calls、tokens、duration、termination、pass/fail 和目标 fingerprint 计数。
5. 达到任一全局上限、identity 漂移、digest 不匹配、secret scan 命中或连续 provider error 时立即 fail-closed；不要自动扩大预算。
6. 不得读取 holdout 逐 case 数据来选样本、修代码或解释结果。R83 的 holdout 只允许以总计 9/32 出现在历史背景中。
7. 小样本只用于机制确认，不得宣称总体基准显著提升。若结果混合或噪声大，结论写 `INCONCLUSIVE`。

#### 怎么验收

- 未授权路径：网络/provider 0 调用，明确输出 `NOT_RUN: PAID_AUTHORIZATION_REQUIRED`。
- 授权路径：所有请求严格匹配已确认 digest，串行度为 1，case 数与预算均不超限；中断后 resume 不重复已完成 arm。
- baseline 与 candidate 除 source SHA/预声明修复外的身份和 limits 完全相同；validator 可重算所有汇总。
- candidate 必须在目标机制指标上改善，且反例、安全违规数和 verified completion 不恶化，才可标记 `MECHANISM_VALIDATED`。
- 若未改善、出现新安全问题或只能靠提高预算改善，标记 `REJECTED`；若样本不足或 provider 噪声过大，标记 `INCONCLUSIVE`。
- 完成 Windows 本地 gates 和 GitHub Actions Windows/Ubuntu gates；Linux 验收只使用 CI。
- **即使小型 A/B 通过，也不得自动开始完整 86-case 重跑。** 全量 campaign 必须有新的独立计划、重新选择未污染的 holdout，并再次取得付费授权。

---

## 2. 阶段退出标准

本计划完成不等于“分数已经提升”，而是满足以下可审计状态：

1. R83 的原始 campaign 有可检测篡改的脱敏证据链，仓库中的数字可由工具重新计算。
2. R83 报告的 20/30 配置错误已纠正，两个 source SHA 被诚实保留。
3. 失败归因不会把模型失败、预算触顶和 harness 缺陷混为一谈。
4. 只有已证实的 harness 缺陷被修复，并能用离线 replay 验证目标机制。
5. 所有 Linux 验收均由 GitHub Actions 完成，不要求开发者本机安装 Linux、WSL 或 Docker。
6. 任何新增真实模型调用都经过新的、范围明确的付费授权；没有授权时系统安全地停在 `NOT_RUN`。

## 3. 明确不在本计划内

- 不立即重跑完整 86-case campaign。
- 不使用 R83 holdout 的逐 case 信息做调参。
- 不通过提高工具、迭代、时间或重试上限掩盖根因。
- 不公开上传原始模型 transcript、API key 或私有 campaign 目录。
- 不把 0 个进程失败写成 86 个 case 通过。
- 不要求本机 Linux 冷启动；CI 的 `ubuntu-latest` 是唯一 Linux 验收环境。
