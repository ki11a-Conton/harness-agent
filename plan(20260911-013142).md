# harness-agent：bcf34b7 增量审查与下一轮 Agent 执行计划

审查日期：2026-09-11。本文每个任务均为可独立复制给代码 Agent 的提示词，包含做什么、怎么做和怎么验收。

## 1. 固定审查基线与结论

- 仓库：https://github.com/ki11a-Conton/harness-agent
- reviewedSourceSha：`bcf34b7179152ac2fc24931f268fada8a67f82d9`。
- 上轮比较基线：`36c9c267aff5f32a0ccd9364da3306e28114921c`。
- 本轮比较范围：20 个新提交，46 个文件，4547 行增加、1541 行删除。
- 最终提交自己的 CI：https://github.com/ki11a-Conton/harness-agent/actions/runs/34548502173 。GitHub 返回 completed / success；Linux、Windows、coverage、release attestation 四个 job 全部 success。
- 仓库 R20 报告引用的 run 34546408621 对应前一提交 9fd0f33；本次额外核查了 bcf34b7 自己的 run，不能将这两个 SHA 混写。
- 本地实际执行：`pnpm typecheck` exit 0；`pnpm docs:verify` ALL CHECKS PASS；`pnpm test` 311 个测试文件、5627 个测试全部通过。
- `build` 与 `typecheck` 当前均为 `tsc -b`；本次未把同一脚本计作两份独立验证。
- 本次没有调用真实模型，没有进行付费 benchmark，没有发布，也没有修改业务代码。

本轮结论：上一轮修复确实已经提交，旧问题的直接复现大部分已被阻断。现有 CI 绿色是真实事实，但不能据此认定证据消费链已经没有缺陷。本计划聚焦剩余的生产入口一致性与证据绑定，不开启新一轮无依据的 Runtime 重构。

### 1.1 旧计划复核摘要

| 旧问题范围 | 本轮观测 | 结论边界 |
|---|---|---|
| 缺网格、缺 security、未完成 run | 原复现由 ACCEPT 变为 INVALID，promotion 拒绝 | 基础缺字段情况修复；空对象计划仍有新绕过 |
| 重复次数充当 case 数 | 一个 case 三次重复统计 cases=1、activationEligibleCases=1 | 原计数问题关闭 |
| 相对 promotion bundle | 不再因 cwd 相对 decision 读取失败 | 原额外读取分支已移除 |
| 策略文本漂移 | benchmark 与 startup guidance 完全相同 | 共享文本修复成立 |
| 无效 observation | 原坏 digest、null SHA 记录不再 observed | run 身份与最终测试结果绑定仍有缺口 |
| gate 运行中 HEAD 变化 | 原 A→B 复现 passed=false | 该基础场景关闭 |
| gate 输出修改 | 通用 loader 拒绝修改后的输出 | 生产 release verifier 未接入同一文件复核 |
| terminal ACK 写失败 | action=1，队列保留，prompt 仍 promoted | 单独 RECOVERED 写失败场景修复；复合存储故障需专项验证 |
| Windows CRLF | 最新 Windows CI 成功，LF 契约已提交 | 新 checkout 通过有证据；不把成功重跑本身当作唯一因果实验 |

旧复现中的所有 INVALID 不代表其每一条子条件都独立获得证明。例如缺 plan 已经使样本 INVALID，会遮蔽 security 独立断言；新回归必须用先通过的完整 fixture，一次只改变一项。

## 2. 本轮发现与证据

### F01 / 高：生产 release verifier 丢失输出复核保证

涉及：`apps/cli/src/release-command.ts` 的 `readGateEvidence`，`apps/cli/src/release-verify.ts` 的 `parseRawEvidence`，`packages/evaluation/src/gate-evidence-v2.ts` 的 `loadGateEvidenceV2`。

生产路径为 readFile → parseRawEvidence → validateGateEvidenceInstance → aggregate。parseRawEvidence 只调用结构检查并映射成简化字段，丢弃 logRef、artifactRefs、environmentClass、providerCalls 等事实。R19 新增的磁盘重新计算摘要只存在于通用 loader。

最小复现：为全部必需 gate/platform 写入结构完整的 V2 fixture，引用同一份原始日志并记录正确摘要；随后修改日志。结果：

```json
{"id":"RELEASE_REF_REVERIFY","genericLoaderPassed":false,"productionReady":true}
```

这证明消费链不一致；fixture 不代表真实 CI 造假，也不否认最新 CI 的实际成功。

### F02 / 高：executionPlan 只验证为 object，未验证完整协议及交叉绑定

涉及：`packages/evaluation/src/champion-eval-v3.ts` 的 deriveV3Decision，`packages/evaluation/src/promotion-envelope.ts` 的 eligibility 检查，`apps/cli/src/benchmark-command.ts` 的 BenchmarkExecutionPlan。

评估器和 promotion loader 对 executionPlan 只检查 object/non-null/non-array。未重新计算该计划与 manifest.planDigest 的对应关系，也未将计划中的网格、policy、source、isolation 与实际两臂严格绑定。

最小复现：三个 case、两次重复，baseline 不通过、candidate 通过，完整 security refs 和 activation refs，填入 `executionPlan: {}`、`runComplete: true`、正确样本键；用现有 writer 重算摘要，并由真实 evaluator 生成 decision，再生成 envelope。结果：

```json
{"id":"EMPTY_EXECUTION_PLAN","decision":"ACCEPT","promotion":{"accepted":true,"codes":[],"details":[]}}
```

本问题不是要求哈希抵御拥有全部写权限的攻击者，而是要求系统能拒绝自身协议上不完整或互相矛盾的产物。

### F03 / 中：观察记录未绑定被请求的 runId

涉及：`apps/cli/src/observation-evidence.ts` 的 loadObservationEvidence；`apps/cli/src/usage-audit.ts`；`apps/cli/src/commands.ts` 的 usage-audit 分支。

loader 以 runId 找文件，但不要求行内 runId 等于参数。将 old.jsonl 原样复制为 new.jsonl 后：

```json
{"id":"OBSERVATION_RUN_REPLAY","requestedRun":"new","loadedRun":"old","observed":true}
```

另外，真实 `usage-audit --strict` 分支没有传 runId，会扫描所有历史 run。内部函数支持指定 run，不等于 CLI 已落实严格单次运行语义。

### F04 / 中：observation commit 仍早于测试最终结果

源码确认：`apps/cli/src/e4-09-production-e2e.test.ts` 在 observationRun.commit() 后仍执行 audit 断言，并在 finally 中 await harness.close()。这些后续操作失败时，已写入的 passed 记录不会撤回。这里的 commit 是测试函数内调用，不是测试框架确认最终结果后的 hook。

本项为明确控制流缺口；本轮没有向现有测试故意注入失败。执行任务必须补充真正的测试进程级复现。

### F05 / 中：源码指纹绑定的是状态文本，不是修改内容

源码确认：`probeSourceSnapshot` 用 SHA256(git status --porcelain) 作为 dirty tree fingerprint。同一个已修改文件先写内容 A 再写内容 B，status 字符串可以完全相同，指纹也相同。它不能支持“任意源码修改都会改变确认身份”的声明。

另需核对：confirmed effectiveModelParams 当前主要保存 budgetTokens；temperature 在 executeBenchmark 中从环境读取。必须区分真正进入请求的参数与仅写入 manifest 的参数，先证明实际差异再扩展身份协议。

### V01 / 待验证：恢复 ACK 失败后的第二次持久化失败与唤醒

`recoverHead` 在 RECOVERED 写失败后，仍用吞错返回输入的 persistRecoveryRecord 保存 needsReconcile。若整个存储同时不可写，该标记可能没有落盘。另有 acquireLease / intent 写失败分支直接返回 wait-lease，未安排有限重试。

本轮只确认了源码路径，没有宣称已证明重复外部副作用。必须用复合故障和确定性 scheduler 验证最终队列、turn、prompt、recovery record 的一致性，再决定最小修复。

## 3. 所有任务共同执行规则

以下规则与每个任务提示词一起生效。

1. 开始时阅读 AGENTS.md、当前 plan.md 指向的计划、相关 tasks 文件；记录 git rev-parse HEAD 和 git status --short。若分支比 bcf34b7 更新，先检查目标缺陷是否已修复。
2. 不覆盖用户未提交的改动。必要时使用独立 worktree。审查基线与修复后的被测 SHA 分开记录。
3. Runtime architecture FROZEN。只有确定性正确性、安全、release integrity、已证明基础设施 benchmark 缺陷或实测性能回归允许改 Runtime。F01–F05 不需要重写 Runtime。
4. Core 不依赖 UI/provider/business integration；工具副作用继续经过 ToolOrchestrator、PermissionEngine、SandboxManager 和 Verification。
5. 先复现，再最小修复，再回归。每条负例都从已通过的正例派生；一次只改变目标字段。不能靠另一个无关错误让负例变红。
6. 不使用跳过测试、降低阈值、伪造 providerCalls、手改 ACCEPT、重写历史 benchmark 证据作为修复。
7. 默认只做离线验证。真实 provider、付费实验和正式发布不属于本计划的自动执行范围。
8. 每个任务输出 docs/E4-Rxx-report.md：问题、改动、复现前后、命令退出码、被测 SHA、残余限制。未运行写 NOT_RUN，不能写 PASS。
9. 测试命令使用仓库现有 pnpm/vitest。精准测试结束后运行 typecheck；全部任务收口时再跑全仓测试与所需门禁。避免每次文档改动重复运行所有测试。
10. 状态仅用 TODO / IN_PROGRESS / PASS / PARTIAL / BLOCKED / NOT_NEEDED。PASS 需要逐条验收证据；NOT_NEEDED 需要证明当前代码已满足条件。

## 4. 任务顺序

| 任务 | 优先级 | 前置依赖 | 结果 |
|---|---|---|---|
| E4-R21 发布证据消费链统一 | P1 | 无 | 篡改产物不能 READY |
| E4-R22 执行计划严格解析与交叉绑定 | P1 | 无 | 空计划/矛盾计划不能 ACCEPT/promote |
| E4-R23 内容级源码与执行参数身份 | P2 | R22 协议设计 | 确认与执行绑定真实输入 |
| E4-R24 观察记录运行身份与最终结果 | P1 | 无 | 旧记录和失败测试不能 observed |
| E4-R25 恢复复合故障专项验证 | P2 | 无 | 明确 ACK/存储故障收敛语义 |
| E4-R26 总验收与文档收口 | P1 | R21–R25 | 一份准确的关闭矩阵 |

建议按表顺序执行。每个任务完成后停止扩大范围，更新报告再进入下一项。

## 5. E4-R21：统一生产 release 的结构、语义和文件复核

### 给 Agent 的提示词

你负责 E4-R21。请修复 F01，使生产 release verify 真正使用包含文件复核的完整 V2 证据验证链。先阅读共同规则，以及 release-command.ts、release-verify.ts、gate-evidence-v2.ts、相关测试和 .github/workflows/ci.yml。

### 做什么

- 消除通用 loader 拒绝、release verify 却 READY 的矛盾。
- 保留每条平台实例的完整证据，先验证再聚合。
- 定义可迁移的 evidence bundle 根目录与相对路径协议。
- 明确区分“证据结构有效”与“gate 成功”。

### 怎么做

1. 先建全部 gate/platform 的有效 fixture，确认真实 resolveReleaseVerdict 返回 ready=true。随后只修改一条日志，重现 F01。
2. 提取或扩展统一验证接口，使结构验证、HEAD/argv/环境语义、artifact/log 字节摘要检查都在生产入口被调用。
3. 不要仅在 parseRawEvidence 加布尔值；它当前的有损映射是问题的一部分。异步文件复核应有清晰结果类型，携带 gate、platform、sourcePath 和失败原因。
4. 相对路径只能相对明确的 trusted bundle root 解析。禁止“先在 evidence 文件旁找，不存在再到 cwd 找”的隐式双根行为。检查当前 CI download-artifact 的目录布局，并调整生成端和消费端保持一致。
5. Windows artifact 被下载到 Linux 后应能复核。不要把 Windows 工作区绝对路径当作跨平台可携带引用。
6. 对 artifactRefs 非数组、null 元素、缺 digest、logRef:null 等不合法输入返回结构错误，不能使 verifier 崩溃。对缺失日志的策略写清：声明为必须证据的日志缺失应阻断；允许诊断日志缺失时不得宣称它已验证。
7. 生产入口应执行 offline providerCalls 语义检查；不能在转换时丢掉后让违规记录通过。
8. 检查输出 digest 的实际可重算范围，明确摘要证明什么。避免声称任意人无法重算哈希；这里实现的是内容完整性和受控证据来源边界。

### 怎么验收

- [ ] 有效完整 bundle：通用 loader 和真实 release verify 均成功。
- [ ] 修改/删除任意必要 artifact 或 log：release ready=false，CLI 非零，并指出具体 gate/platform/path。
- [ ] 同路径在 cwd 放另一份文件不能改变 bundle 判定。
- [ ] bundle 移动到另一目录、改变进程 cwd 后仍正确验证。
- [ ] Linux/Windows/coverage 所需平台覆盖仍严格；缺 Windows 不能被重复 Linux 抵消。
- [ ] offline providerCalls 非零、坏引用结构均被拒绝且无未捕获异常。
- [ ] 使用现有 release-command/release-verify/gate-evidence 测试集验证；报告给出真实生产入口的新增负例名称。
- [ ] CI 消费下载后的 bundle，包含必要引用文件，不能通过删除引用规避复核。

交付 docs/E4-R21-report.md，解释原路径为什么遗漏复核，以及现在从读取到 READY 的调用链。

## 6. E4-R22：严格验证 executionPlan 与完整实验身份

### 给 Agent 的提示词

你负责 E4-R22。请修复 F02：executionPlan:{} 目前能够通过真实 evaluator 和 promotion loader。阅读共同规则、BenchmarkExecutionPlan、paired execution identity、V3 schema/loader/writer、champion-eval-v3、promotion-envelope 和 R13–R15 测试。

### 做什么

- 建立共享、版本化、可运行时解析的执行计划协议。
- 在 promotion-grade artifact 的写入和读取边界交叉验证完整计划。
- 将 expected grid、决策 policy、source、candidate、模型身份和隔离事实绑定到同一计划。
- 保留旧产物的诊断读取能力，但不能以字段缺失绕过晋升要求。

### 怎么做

1. 用三个 case、两次重复、完整 security 和 activation 的有效 pair 建正例；调用真实 runV3ChampionEval 和 loadPromotionEnvelope。
2. 将 executionPlan 改成 {}，其余摘要按 writer 正常重算，证明目前仍 ACCEPT。测试应穿过真实文件 reader，而不是只测辅助 predicate。
3. 把协议类型与 parser 放在合适的共享包。evaluation 不能反向依赖 apps/cli；CLI 应依赖共享协议。
4. parser 校验 schemaVersion、必需字段、数字边界、唯一 case IDs、fingerprint 映射、repeat、policy、thresholdDigest、isolation、source 等，拒绝未知协议版本。
5. 用同一 canonical serializer 重算 planDigest；baseline/candidate manifest.planDigest、decision.planDigest 必须一致并对应实际计划。
6. 从解析后的计划推导期望样本键。不能继续只信任独立填写的 expectedSampleKeys；保留该字段时要求完全相等。
7. 对齐实际 outcome 的 suite/case/repetition/arm，以及计划 candidate、provider/model、judge、source、配置身份。列出哪些两臂字段应相同、哪些是允许的受控差异。
8. promotionEligible 由已验证事实决定：完成性、隔离模式及必要 provenance 一致；不能只检查 boolean=true。对具体 self-test/provenance 引用先梳理现有生产字段，不虚构证据。
9. applied policy 必须等于计划 policy。缺 thresholdDigest/decisionPolicy、更改其中一份、重算 decision 自身摘要都不能绕过计划绑定。
10. 评估入口与 promotion replay 复用同一校验。避免 evaluator ACCEPT、loader 才发现实验缺失关键信息。

### 怎么验收

- [ ] executionPlan 缺失、{}、数组、未知版本、错类型：全部不可晋升。
- [ ] plan 字段改变但保留旧 planDigest：拒绝。
- [ ] 两臂一起删去计划中的某个 case/repetition，并重算自身内容摘要：仍拒绝。
- [ ] manifest 网格和计划网格冲突：拒绝。
- [ ] decision policy 与计划 policy 不同，即便重算 decision 摘要：拒绝。
- [ ] candidate/source/model/judge/isolation 任一交叉绑定不一致：拒绝并给出稳定原因。
- [ ] 真实离线 production E2E 仍可走到 ACCEPT→verified envelope；测试使用模拟隔离时明确标记它仅证明接线。
- [ ] 相对路径正常 bundle 仍通过，单次重复统计不能冒充多 case。
- [ ] 旧历史产物可诊断读取，但 promotion-grade 必需字段缺失时明确 ineligible。

交付 docs/E4-R22-report.md，附协议字段交叉绑定表和一次一字段的负例矩阵。

## 7. E4-R23：内容级源码指纹和有效执行参数

### 给 Agent 的提示词

你负责 E4-R23。请修复 F05，并验证确认计划和实际请求参数是否一致。依赖 R22 的共享协议。阅读 probeSourceSnapshot、runBenchmarkCommand、executeBenchmark、runPairedPromotion、runtimeConfigForHash 和 provider 参数解析。

### 做什么

- 使 dirty tree fingerprint 对真实输入内容改变敏感。
- 确认计划使用解析后的有效参数；实际执行消费同一结果。
- 将无法获取源码状态视为 unknown，不能误认为 clean。

### 怎么做

1. 在临时 Git 仓库中提交文件，再写 A，记录指纹；同一文件改为 B，保持 status 输出相同，证明当前指纹不变。
2. 定义内容摘要范围：tracked 工作树内容、staged 内容/状态、相关 untracked 输入、删除和文件模式。按确定顺序编码路径和内容，说明 ignored 产物不参与的理由。
3. 结合当前 journal 身份用途选择适当实现；无需每次请求扫描整个磁盘。不要用 mtime、status 文本或固定字符串代替内容。
4. source probe 区分 git 无仓库、status 命令失败、有效 clean 和有效 dirty；晋升路径要求明确可验证的干净源码。
5. 追踪 temperature、模型 ID、base URL/endpoint、budget 等从环境到真实 provider 请求的路径。仅将真正影响请求的值加入 effective identity；敏感 key 不入摘要输入或日志。
6. 同一 resolved 配置对象供 dry-run、确认、journal、执行使用，减少执行时再读环境变量。需要 endpoint 身份时保存脱敏规范值，明确代理凭证排除规则。
7. 若某参数目前只写 manifest、未进入请求，应先修正文档/字段含义或接线，并用请求捕获证明，不能凭名称认定生效。
8. 对确认后发生的源码或参数漂移，在首次 provider call 前拒绝；长运行中的源码变更至少使最终产物不可晋升。

### 怎么验收

- [ ] 同文件 A→B、status 不变：fingerprint 改变。
- [ ] 内容完全相同：fingerprint 稳定；只改无关输出目录不产生无意义 journal 身份变化。
- [ ] staged/unstaged、删除、相关 untracked 变化按协议被记录。
- [ ] source probe 错误不会被标 clean 并获得 promotion eligibility。
- [ ] 有效模型参数变更使确认身份变化；旧 plan digest 执行被拒绝，provider 调用数为 0。
- [ ] request capture 中的真实参数与计划一致；没有真实网络调用。
- [ ] 报告说明 fingerprint 的覆盖范围、性能代价和不能证明的内容。

交付 docs/E4-R23-report.md。不得借此重写 Runtime 或模型质量策略。

## 8. E4-R24：观察证据必须来自指定运行的最终通过结果

### 给 Agent 的提示词

你负责 E4-R24，修复 F03/F04。阅读 observation-evidence.ts、usage-audit.ts、commands.ts 的 usage-audit、e4-09-production-e2e.test.ts、Vitest 配置及 CI。目标是让严格审计只能接受指定运行中最终通过的测试证据。

### 做什么

- 行内 runId 与请求 runId 严格相等。
- 严格 CLI 必须选择唯一 run，不能默认扫描全局历史目录。
- 在测试框架最终结果确定后发布 committed observation。
- 明确内容 digest 仅证明完整性，不能单独证明测试实际执行。

### 怎么做

1. 先复现 old.jsonl→new.jsonl 原样复制仍 observed 的问题。loader 校验行内 runId，错误记录需要明确诊断。
2. 设计唯一 suite run ID 与 test identity；区分一次整体运行、某测试、retry attempt。路径参数禁止目录穿越；不允许 arbitrary runId 覆盖其他文件。
3. 给 usage-audit CLI 增加显式 run 选择，并严格解析未知参数。--strict 缺少运行身份时非零；诊断扫描可以保留，但输出不能冒充严格验收。
4. observation collector 仅记录候选事件。把最终提交移到可靠的 reporter/runner 完成阶段，依据测试框架最终状态，覆盖 assertions、afterEach、afterAll、资源关闭等约定范围。
5. 不要用简单地把 commit 移到测试末行替代最终结果绑定。也不要把测试内“我 passed”的布尔参数当独立证明。
6. 如果 audit 要读取 committed 证据，应在测试运行完成后作为独立阶段运行，避免同一测试必须先写成功再断言自身成功的循环。
7. 写入采用临时文件和原子发布；失败运行不能留下可消费的成功文件。多个 worker 不覆盖彼此记录。
8. 绑定真实测试 ID、文件、结果摘要、run 来源；testFile 存在本身不足以证明 testName 执行过。无执行结果映射的行仅诊断。
9. CI 为当前 run 生成并传入显式 ID，将报告和 observation 一起保存。测试重跑时旧成功不可掩盖当前失败。

### 怎么验收

- [ ] old 行复制到 new 文件：请求 new 时 observed=false。
- [ ] 缺 runId 的 --strict 明确失败；明确 diagnostic 模式不表示 release-grade 通过。
- [ ] 先 observe 后 assertion fail：没有 committed passed 证据。
- [ ] 测试 body 通过、cleanup/hook 失败：最终严格审计不通过。
- [ ] 同 SHA 旧运行成功、新运行失败：新运行不能借用旧证据。
- [ ] 不存在的 testName，即使文件存在且 row digest 正确，也不能证明 observed。
- [ ] 多 worker、retry、重复提交均有确定结果，无丢记录或跨 run 污染。
- [ ] 真实生产 E2E 成功运行结束后，独立 usage-audit --strict 对该 run 的七项能力全部 observed。
- [ ] 用真实离线 Vitest 子进程测试最终结果协议，而非只测试 collector.commit()。

交付 docs/E4-R24-report.md，并展示 test runner→结果→observation→strict audit 的实际调用链。

## 9. E4-R25：恢复复合存储故障和有限唤醒验证

### 给 Agent 的提示词

你负责 E4-R25。本任务首先是 V01 专项验证，尚不预设必须修改 Runtime。阅读 AGENTS 的 Runtime Freeze、session-actor.ts、recovery-state-machine.ts、DurableRecoveryStore、R17 报告和 recovery-durable 测试。只有形成确定性复现后才做最小修复。

### 做什么

- 验证 RECOVERED 与 needsReconcile 连续写失败时的真实收敛结果。
- 验证 lease/intent 写失败后的自愈是否依赖外部消息。
- 区分已完成 action、未知 action 结果、durable turn 终态和 recovery ACK。

### 怎么做

1. 先保留 R17 正例：仅 RECOVERED 写失败、marker 成功落盘时 action 一次、队列不前进、store 恢复后只补 ACK。
2. 新故障 store 在 action 完成后拒绝所有写入，而非只拒绝 RECOVERED。记录真实 durable record、内存 marker、Turn.status、prompt.status、队列长度、调用次数。
3. 对同一 actor 恢复、关闭后新 actor 恢复分别测试；不能把一个 MemoryStore 实例上的重建直接称为进程持久性证明。至少补一个真实 DurableRecoveryStore 场景。
4. 用确定性 scheduler 触发回调来验证恢复，而不是手动 drainFollowupsForTest 然后声称已证明自动唤醒。
5. 测试 lease CAS 冲突、暂时写失败、intent 写失败恢复后的有限重检；断言故障期不调用 action，恢复后不用新用户消息即可收敛，且无热循环。
6. 若 pending marker 无法落盘，系统不能假定已持久化。优先依据现有 durable turn 终态和其他可信记录协调；无法证明执行结果时进入明确的 reconcile/blocked 状态。
7. 对非幂等外部副作用，不能宣称仅靠一个事后布尔 marker 实现任意崩溃窗口的 exactly-once。解释事务边界及不确定结果处理。
8. 若复现证明现有 turn 终态协议已能安全收敛，保留代码，补真实证明和修正文档即可。禁止为满足任务数量强行改 Runtime。

### 怎么验收

- [ ] 每种故障都有完整状态时间线和 action 调用次数。
- [ ] ACK 与 marker 连续失败后不会假 consume/假 shift。
- [ ] 新 actor 不会因丢失内存 marker 静默重放已知完成的 action。
- [ ] lease/intent 暂时错误恢复后有有限唤醒，测试真实触发 scheduler callback。
- [ ] action 未知时的行为有明确合同，不伪称 exactly-once。
- [ ] 若需要修改 Runtime，报告明确对应的复现与 Freeze 例外类别。
- [ ] 相关 recovery、durability、race/security 回归通过；没有降低断言或扩大 retry budget 掩盖故障。

交付 docs/E4-R25-report.md，允许 NOT_NEEDED，但必须提供上述场景的实际证据。

## 10. E4-R26：总验收、计划入口与文档纠偏

### 给 Agent 的提示词

你负责 E4-R26。请在 R21–R25 完成后，按原验收条件逐条关闭，而不是仅收集任务报告的 PASS 标题。阅读共同规则、每个任务报告、当前源码、README/HANDOVER、CI 和 docs verifier。

### 做什么

- 产出准确关闭矩阵与最终固定源码验证结果。
- 更新唯一当前计划入口，避免旧计划、旧报告和最新证据互相冲突。
- 保留工程门禁、证据完整性和模型质量三种结论的边界。

### 怎么做

1. 为 F01–F05/V01 建表：实现文件/符号、负例名称、正例名称、实际测试命令、被测 SHA、最终状态。
2. 手工检查至少一条真实生产调用链，保证新 helper 确实被 CLI/CI 消费，不只在单测里出现。
3. 在干净固定 commit 跑 typecheck、全仓 test、docs:verify，以及改变模块所需安全/集成门禁。build 与 typecheck 相同也如实说明。
4. 对上传后的 CI 用精确 SHA 查 run/jobs/artifacts。未授权 push 时只完成本地可审查改动并标 CI NOT_RUN，不声称远端已验证。
5. R21 修改了 bundle 路径协议，必须检查真实 Windows→Linux 下载后的 release attestation 消费；平台本地成功不能代替跨平台消费成功。
6. 报告注明运行前后的 clean 状态、provider 调用类别。离线测试使用 fake/scripted provider，应写真实模型调用 0，不能把 fake 调用也写 0。
7. README 中过时测试数量改为指向 CI 或去掉易变数量；真实模型 benchmark 示例应符合当前 dry-run/预算/plan-digest 协议。先用 CLI 解析验证示例，不执行付费请求。
8. 本详细计划建议归档为 plan(20260911-review).md，由根 plan.md 作为唯一入口引用。先检查 docs verifier 的实际要求，不盲目用整份长文覆盖索引结构。
9. 历史报告保留原时间与源码语境；新增 superseded 指引，不把旧证据改写为新 SHA 的结果。
10. 最终文档更新 commit 若触发新 CI，报告区分 testedSourceSha 和 documentationCommitSha，避免写当前 SHA 引发自引用循环。

### 怎么验收

- [ ] 三个本轮真实复现 F01/F02/F03 全部变为拒绝，正常正例仍通过。
- [ ] F04/F05 有独立负例，V01 有验证结果或最小修复证明。
- [ ] 每项 PASS 都有逐条验收记录；缺环境项仍为 PARTIAL/BLOCKED/NOT_RUN。
- [ ] 全仓测试和相关门禁结果准确，没有将历史 CI 挪作当前 SHA 的证据。
- [ ] 更新后的 docs verifier 通过；根 plan.md 唯一入口与实际文件一致。
- [ ] 最终报告明确 runtimeReleaseReady、promotion evidence integrity、champion quality 的各自结论。
- [ ] 没有新增未经请求的付费运行、正式 release 或架构扩张。

交付 docs/E4-R26-report.md 和一页状态摘要。该任务通过后停止这轮工程加固；下一阶段如需提升模型质量，回到 benchmark→failure cluster→hypothesis→challenger→paired eval，用实际失败证据决定策略任务。

## 11. 可直接复制的总控提示词

```text
请在 harness-agent 仓库执行本计划 E4-R21 至 E4-R26。
先读取 AGENTS.md 和当前计划入口，确认工作树与 HEAD；本计划审查基线为
bcf34b7179152ac2fc24931f268fada8a67f82d9。
按依赖顺序执行。每项任务先建立有效正例，再用单因素变更复现缺陷，
之后做最小修复并验证真实生产入口。不要把辅助函数的单测通过等同于
CLI/CI 的消费链已经接入。
每项任务严格完成“做什么、怎么做、怎么验收”，并写对应报告。
遇到更新后的代码已经修复，先复核再标 NOT_NEEDED；不要重复造工程。
Runtime 处于冻结状态，V01 必须先证明确定性缺陷才改 Runtime。
本计划默认离线，不执行付费模型调用和正式发布。
最终提交逐项关闭矩阵、测试结果、固定被测 SHA、残余限制。
只有完成所有验收条目才能写 PASS，报告标题不构成验收证据。
```
