# harness-agent 增量审查与 Agent 修订计划（2026-09-10）

> 审查对象：36c9c267aff5f32a0ccd9364da3306e28114921c，main。
> 比较基线：016473870c15f123edbbd6c624550b09ee69fe75。
> 新增 27 个提交；62 个文件变化，+5,279 / -1,921 行。
> 本文只针对这份代码快照，不代表之后的 HEAD。
> 本轮进行了源码审查、离线构建/测试、生产函数负例复现和真实 GitHub Actions 日志核对，没有修改业务代码、推送、晋升真实 champion 或调用真实模型 API。
> 交付方式：以下 E4-R12～R20 每一节均是可交给 coding agent 的完整任务提示词；分发时同时附上第 3 节约束。
> 这是旧 E4-R01～R11 的剩余工作，不是要求重做已完成部分，也不是开启新架构阶段。

## 1. 审查结论与已完成部分

这次有实质修复，不能再沿用上一轮“13 个 Windows 测试失败”的结论。最新代码的 Windows 测试步骤已通过，CI 红点已经移到 docs gate。

但“全部任务完美完成”还不能成立：一些修复只覆盖了显式坏值，未覆盖字段省略、空集合、真实调用链的旧分支、失败后的持久化边界。文档中的部分“已关闭”也把较大问题缩成了较小子项。

### 1.1 本次验证记录

| 检查 | 实际结果 | 说明 |
|---|---|---|
| 本地 pnpm typecheck | PASS | tsc -b，当前 SHA |
| 本地 pnpm test | PASS：308 files / 5,569 tests | 默认测试集；约 33.67 秒 |
| 本地 pnpm docs:verify | PASS | 不能替代 Windows 同命令结果 |
| 当前 SHA Linux CI | SUCCESS | job 102772446785 |
| 当前 SHA coverage CI | SUCCESS：308 files / 5,569 tests | statements 89.83%、branches 80.97%、functions 92.73%、lines 91.61% |
| 当前 SHA Windows 测试步骤 | PASS：5,568 passed / 1 skipped | 旧 13 个失败已消失，308 个测试文件通过 |
| 当前 SHA Windows gate 生成步骤 | FAILURE | docs gate exitCode=1，不能产出完整 release green |
| 当前 SHA release attestation | SKIPPED | 不可宣称 runtimeReleaseReady |
| 本次诊断脚本 | 已运行，见附录 | 输出是缺陷观测，不以脚本 exit 0 代表代码正确 |
| 真实网络/付费模型调用 | 0 | fake provider 调用另计 |

本轮没有单独重复本地 coverage 全量运行；coverage 数字来自准确匹配当前 SHA 的 GitHub job。也没有把旧提交的 build/release:verify 结果冒充本轮结果。

### 1.2 来源

- [审查 commit](https://github.com/ki11a-Conton/harness-agent/commit/36c9c267aff5f32a0ccd9364da3306e28114921c)
- [27 个提交 diff](https://github.com/ki11a-Conton/harness-agent/compare/016473870c15f123edbbd6c624550b09ee69fe75...36c9c267aff5f32a0ccd9364da3306e28114921c)
- [当前 CI run 34446539494](https://github.com/ki11a-Conton/harness-agent/actions/runs/34446539494)
- [Windows job 102772446679](https://github.com/ki11a-Conton/harness-agent/actions/runs/34446539494/job/102772446679)
- [Coverage job 102772446527](https://github.com/ki11a-Conton/harness-agent/actions/runs/34446539494/job/102772446527)
- [当前 R10 报告](https://github.com/ki11a-Conton/harness-agent/blob/36c9c267aff5f32a0ccd9364da3306e28114921c/docs/E4-R10-report.md)

完整 Windows job 日志本轮已可读取，不应继续把“需要 admin 才能查看日志”当成已证实的阻塞。实际问题是 release runner 没有把内部 docs 命令的 stdout/stderr 保留下来并交给外层；外层只能看到 gate docs: exitCode=1 FAIL。

### 1.3 确认可以保留的修复

| 旧任务 / 子问题 | 本次确认 | 后续处理 |
|---|---|---|
| R07 canonical root | Windows 测试通过，Linux 全量无回归 | 保留，不再重写 cwd containment |
| R02 strict V3 evaluator read | 修改 contentDigest 后，evaluator 现在抛 CONTENT_DIGEST_MISMATCH | 关闭该子问题，保留 regression |
| R02 repetition 安全记录覆盖 | ESCAPE 与下一次 NO_ATTACK_ATTEMPT 保留为两个 ID | 关闭该子问题；仍需补其他事实投影 |
| R03 not_observed / classifier_error | 现在会 REJECT，不再算 clean | 保留；空记录另有缺口 |
| R03 minRecoveryRate 未执行 | 非空门槛且无样本现在会 REJECT | 关闭“完全没用门槛”；分母仍需改为计划定义 |
| R04 改 digest、candidateId、版本 | 独立负例全部拒绝，错误码与改动对应 | 保留，不再重造 envelope 体系 |
| R05 budget guidance 安装 | completionGuidance 已进入 main agent prompt，新增真实 turn 测试通过 | 保留；剩余是评测与上线策略不一致、target 绑定 |
| R05 CAS loser | 现在返回 baseline，不再返回旧 champion | 关闭该子问题 |
| R06 quarantine 二次读取 | 现在再次抛 CORRUPT_RECORD，不再变成 undefined | 关闭该子问题 |
| R06 锁 owner/liveness | 已增加 token 与活进程检查、一个真实子进程测试 | 保留；不能据此关闭 actor 终态 ACK 问题 |
| R08 注释即 observed | 注释负例现在 observed=false | 关闭该子问题；JSONL 验证仍宽松 |
| R09 V2 接线 | CLI、CI、audit 已进入 V2 路径 | 关闭“生产仍全用 V1”；证据语义仍需验证 |
| R11 实跑报告归因 | 已承认原始响应缺失、usage unknown、有限模型结论 | 保留，不要求重新付费造 ACCEPT |

特别说明：上一份 runtime 诊断脚本只查旧的 guidance 标题和 completionPolicy 字段。新代码改用 completionGuidance，不能因旧脚本输出 false/null 就说机制完全未安装。本轮检查的是新安装字段和真实测试，发现的是“安装内容与 benchmark 不同”，不是重复宣称“没有安装”。

## 2. 剩余缺陷清单：证据、范围与优先级

下表“复现”使用临时 fixture 调用当前生产函数。恶意 fixture 仅用于检查拒绝边界，不代表现实攻击已经发生。

| ID | 证据等级 | 当前行为 | 任务 |
|---|---|---|---|
| N01 | CI＋源码 | Windows docs exit 1；runner 捕获但丢弃详细 stdout/stderr，外层 grep 找不到根因 | R12 |
| N02 | 离线模拟，根因假设 | 同一 ledger 正常读取 PASS；将引用文本转换 CRLF 后出现 11 个 DIGEST_MISMATCH | R12 |
| N03 | 源码 | preflight plan 仍不含模型/源码/策略完整身份；identity 在执行阶段另建，两个 digest 未统一授权 | R13 |
| N04 | 源码 | identity 的 baselineConfigHash='baseline'、candidateConfigHash=runtime hash、dirty fingerprint='dirty'、self-test 未传实际身份 | R13 |
| N05 | 源码 | expectedSampleKeys 对每个已含 repetition 的 pair 再展开 repeat，形成重复键；writer 未保留完整计划与 policy | R13 |
| N06 | 复现 | 省略/置空 expectedSampleKeys 后，两臂共同缺样本仍 ACCEPT | R14 |
| N07 | 复现 | 1 个独立 case 重复 3 次，被统计为 cases=3、activationEligibleCases=3，得到 ACCEPT | R14 |
| N08 | 复现 | securityOutcomes=[] 得到 ACCEPT；合法外层 bundle 同样可被 loader 接受 | R14 |
| N09 | 复现 | runComplete=false、expectedSampleKeys=[]、promotionEligible=true 的自洽 fixture 仍 ACCEPT，promotion loader accepted=true | R14、R15 |
| N10 | 源码 | verification 使用 some(passed)，recovery budgetExhausted 固定 false，activation 丢 validation/eligibility，安全归约发生在事件裁剪之后 | R14 |
| N11 | 复现＋源码 | 相对 decisionArtifactPath 仍从 cwd 读取，返回 DECISION_ARTIFACT_MISSING；同文件仍存在二次读取分支 | R15 |
| N12 | 复现＋源码 | benchmark 与 startup 的 budget guidance 内容不同，却宣称 SAME；pending 缺 evidence 仍可 applied | R16 |
| N13 | 复现 | terminal ACK 抛错后 storedState=RECOVERY_IN_PROGRESS，但 queueLength=0、promptStatus=consumed、lease=null | R17 |
| N14 | 源码 | wait-lease 分支未设置到期唤醒；报告说调度已接线不足以证明这条分支 | R17 |
| N15 | 复现 | observation：null SHA、错误 symbol、虚构 test、全 0 digest 仍 observed=true | R18 |
| N16 | 源码 | observeCapability 在测试后续断言前写 passed，且使用全局追加 JSONL；没有最终测试结论关联 | R18 |
| N17 | 复现 | gate 运行中 HEAD 从 A 变 B，前后都 clean，generator 仍 passed=true 且记录 A | R19 |
| N18 | 复现 | gate artifact 内容后来改变，loader 仍 loadedPassed=true，verify 仍 ok=true | R19 |
| N19 | 复现＋源码 | 旧缺字段对象仍可通过公开 verifyGateEvidenceV2；loader/parser 虽更严，验证路径未统一 | R19 |
| N20 | 源码 | release gate 用 child exitCode 打印 PASS，与 evidence 的 failed/invalid 状态可矛盾 | R19 |
| N21 | 报告对照 | R06 声称终态落盘才出队，但 N13 反证；R10 旧缺陷 ID 语义缩窄，遗漏原任务验收条件 | R20 |

N02 尚不能直接证明 Windows 当前 runner 的失败就由换行造成。必须拿到 R12 改进后的具体失败项再判定；不要直接关闭 digest 检查。

## 3. Agent 执行规则

1. 先读 AGENTS.md、相关 tasks/、对应旧 R 报告、本任务源码和测试。Runtime 继续冻结；只为以上有证据的 correctness/security/release-integrity 缺陷修改。
2. 不绕过 ToolOrchestrator、PermissionEngine、SandboxManager、Verification；Core 不依赖 CLI 或具体 candidate。
3. 每个问题先写会在当前版本失败的 regression，再修实现。正常链路的证据必须由上一生产阶段生成；负例可用合成坏 fixture。
4. 不改用户真实 champion state、数据目录、provider 凭据；不发真实模型请求。fake 调用次数与真实网络调用数分别记录。
5. 不为了全绿降低门槛、取消 Windows、skip 新失败、扩大 allowlist 或将 unknown 当作安全。
6. 子任务执行 targeted tests＋typecheck；最终固定源码提交上跑 required gates。不要每改一段文档重复整仓 coverage。
7. 测试可使用临时目录；测试前后检查新增意外文件。不要把任务自身的预期源码改动算成测试污染。
8. 正式 evidence 用明确 testedSourceSha 和干净检出，保存于外部目录/CI artifact。提交里的文档写 reviewedSourceSha，不要求自引用包含自身的 SHA。
9. 首先区分 PASS、FAIL、INVALID、NOT_RUN、BLOCKED。坏 evidence 不是没运行；缺 evidence 不是所有命令都失败。
10. 如果执行时 HEAD 已变化，先重跑相关负例。已修子项直接复用有效证据，不重复实现。
11. 报告里的“完成”必须逐条对应原验收条件，不能将一个大缺陷换成较小标题后宣称全部关闭。
12. 未获另外授权，不自动推送、合并、发布、付费复跑或 promote。当前只需交付可审阅的修复和证据。

### 推荐顺序与依赖

R12 → R13 → R14 → R15 → R16 → R17 → R18 → R19 → R20。

R12 先让 CI 红点可诊断；R13–R16 修评测到应用的一致性；R17 修实际数据丢失；R18/R19 修观察与发布证明；R20 最后收口。

| 任务 | 优先级 | 最小交付 |
|---|---|---|
| R12 Windows docs 与失败日志 | P1，立即处理 | 可见根因、针对性跨平台修复 |
| R13 授权计划和真实执行身份 | P0 | 一份不可变计划贯穿 preflight/journal/V3 |
| R14 V3 与 evaluator 的完整性 | P0 | 缺字段、缺事实、伪样本不能 ACCEPT |
| R15 promotion 残留读取分支 | P0 | 所有引用守卫在先，统一读取和失败拒绝 |
| R16 策略一致与 pending 可信性 | P0 | 评估的策略就是安装的策略 |
| R17 recovery ACK 与唤醒 | P0 | 不吞终态失败、不提前消费、不永久等租约 |
| R18 observation 可信验证 | P1 | 绑定最终测试结果、SHA、真实观察 |
| R19 gate evidence 真正复验 | P0 | 源码不变、输出可重算、状态与 CLI 一致 |
| R20 总验收与文档纠偏 | P1 | 逐验收条件关闭，不虚构 READY |

---

## E4-R12：找出 Windows docs 红点，并保留真实失败原因

你是负责跨平台 docs/release 诊断的 coding agent。修复 N01，验证 N02。不要再修改已经通过的 exec root containment。

### 做什么

先修“无法看见失败原因”的运行器，再根据真实输出修 docs。当前不能简单归因 runner 或声称需要 admin。

### 输入与范围

- apps/cli/src/release-command.ts：runGate、releaseGateCmd。
- packages/evaluation/src/gate-evidence-v2.ts：runGateV2。
- apps/cli/src/docs-verify.ts。
- packages/evaluation/src/evolution-ledger.ts。
- .github/workflows/ci.yml、docs/evolution/evolution-ledger.json。
- 当前 Windows job 102772446679，以及其 docs gate 原始 evidence。

### 怎么做

1. 添加一个子命令打印明确失败原因并 exit 1 的测试，验证 production release gate 的输出和 evidence 能定位该原因。
2. 当前 runGate 捕获 stdout/stderr，runGateV2 只哈希前 4,000 字，随后都不保存；不要继续让 CI grep 外层 gate 摘要找内部错误。
3. 保存完整或受控流式 stdout/stderr 日志，提供日志相对引用和 digest；控制台打印 gate、退出码、有限错误摘要。日志脱敏，不能带 key。
4. 区分进程启动异常、signal、timeout、maxBuffer/ENOBUFS 和真实 child exit。不要把 stderr 为空时的异常 message 丢掉。
5. 在 Windows runner 直接运行 docs:verify，并通过同一 release wrapper 运行，比较 cwd、实际 argv、文件编码、换行与结果。
6. 检查 ledger 引用文件实际 bytes 与 git blob 的差别。本轮 CRLF 模拟产生 11 条 digest mismatch，优先验证 autocrlf 和 git attributes，而不是先改业务逻辑。
7. 若确认是 checkout 换行：为受 hash 约束的证据文件定义稳定的存储/checkout 契约，优先最小范围属性规则；历史 raw-byte digest 不得静默改成“随便归一化”。
8. 如需要版本化 canonical-text digest，显式迁移算法与旧记录，保留历史来源；不要逐台机器重算历史摘要让它永远通过。
9. 若实际失败另有原因，按真实检查项修复，明确否定/保留 CRLF 假设。
10. 保持 Windows matrix、docs gate 和 ledger digest 校验；不靠 continue-on-error 绕过发布要求。

### 怎么验收

- [ ] 人为 docs 失败，完整原因能从日志引用读取，CI 摘要指向真实失败项。
- [ ] empty stderr、spawn error、timeout、长输出均有有限且准确诊断。
- [ ] Windows fresh checkout 与 Linux 使用同一证据字节/版本化摘要契约。
- [ ] 实际 Windows docs gate 通过，并保留原始结果；仅 Linux 通过不能关闭。
- [ ] 修改一个受保护 artifact 的实质内容后仍 DIGEST_MISMATCH。
- [ ] 不再使用“无 FAIL 行所以是 runner 问题”的推断。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run apps/cli/src/release-command.test.ts apps/cli/src/docs-verify.test.ts
pnpm docs:verify
~~~

补充 ledger 的现有测试文件命令；最终验收必须附新 SHA 的 Windows job URL。

### 交付

最小修复、跨平台负例、docs/E4-R12-report.md。报告写清根因是否已确认；如果尚缺 runner 证据就 PARTIAL，不伪称关闭。

---

## E4-R13：让确认计划真正绑定运行身份，不再只增加一个 digest

你是负责 paired execution identity 的 coding agent。修复 N03–N05。保留已有 journal identity/header 验证和累计预算，不重写 executor。

### 做什么

当前 resume identity 比上一轮更强，但 preflight 的用户确认仍是旧的较小计划；执行阶段另建 identity，且若干字段只是占位值。应让实际授权覆盖真实输入、策略与配置。

### 输入与范围

- apps/cli/src/benchmark-command.ts：buildBenchmarkExecutionPlan、preflight、runPairedPromotion。
- packages/evaluation/src/paired-execution-identity.ts、paired-plan.ts、paired-executor.ts。
- packages/evaluation/src/paired-v3-builder.ts。
- ArmFactory、有效 runtime/provider 配置解析与 case loader。

### 怎么做

1. 写 production preflight→execute 测试，不只测试人工 buildExecutionIdentityV1：相同 caseId、repeat、seed，更换 model/provider、候选实现或 case 内容时，旧确认必须失效。
2. 在确认前构造一份规范实验计划，执行器消费其严格副本；scheduleDigest 只表达调度，executionPlanDigest 表达真正的授权身份。
3. 从已解析的真实对象计算 baselineConfigHash 与 candidateConfigHash；'baseline' 不是配置 hash，runtime hash 不能冒充 candidate 策略 hash。
4. dirty 工作树不能只写 treeFingerprint='dirty'；正式可晋升运行可直接拒绝 dirty，开发 resume 则需绑定实际源码内容。
5. 绑定实际模型有效参数、case 输入、suite/judge 版本、策略片段、预算、隔离 backend/self-test、policy 及 thresholdDigest；秘密本身不得进入计划。
6. 不能让 constructor 默认值替代安全相关身份。未知关键值应诊断/不可晋升，而非“惰性字段无安全含义”。
7. expectedSampleKeys 从每个实际 planned pair 生成一次；当前 plan.pairs 已含 repetition，再展开 repeat 会得到 C×R² 条目。要求 C×R 条唯一 pair keys。
8. 保存完整计划或可读取引用，并将 digest、policy、expected grid、runComplete 纳入 V3。只有 hash 没有可复验原文不够。
9. 计划匹配失败在任何 generate 前拒绝，保留旧 journal。禁止把旧记录复制到新身份下省钱。
10. 保持已有按已保存 arm 累计 calls 的修复；增加中断、恢复、identity 不匹配回归，不把 attempts/transport retry 算成独立样本。
11. 明确人工追加预算形成新的授权，不把新目录当作自动获批。

### 怎么验收

- [ ] 同一确认计划从 dry-run 到 executor/journal/V3 身份一致。
- [ ] 改模型、策略、case 内容、源码、policy、隔离中的任意一项，旧确认在调用前拒绝。
- [ ] candidateConfigHash 来自实际 candidate，而非 runtimeConfigHash 的复制。
- [ ] 3 cases × repeat 2 的 expected grid 长度=6、唯一键数=6；逻辑 arm 数=12。
- [ ] 无关键占位符，没有缺 policy 的可晋升 V3。
- [ ] 不变计划 resume 不重复已完成 arm，累计预算不归零。
- [ ] 无完整计划的旧产物仍可诊断读取，但明确不可晋升。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run apps/cli/src/benchmark-command.test.ts packages/evaluation/src/paired-execution-identity.test.ts packages/evaluation/src/paired-executor.test.ts packages/evaluation/src/paired-v3-identity.test.ts
~~~

### 交付

源码、真实入口 regression、docs/E4-R13-report.md。给出“授权字段→执行字段→持久证据”的逐项对应；不要只给三个不同 digest 的截图。

---

## E4-R14：关闭可选字段绕过，并让统计来自完整事实

你是负责 V3/evaluator 正确性的 coding agent。依赖 R13。修复 N06–N10，保留 strict bytes loader 和每次 repetition 独立安全 ID。

### 做什么

当前有字段时做检查，但删除字段/置空就跳过；同时一些摘要仍由有损事件或猜测产生。本任务分两次小提交：先关闭计划/样本绕过，再修事实投影。

### 输入与范围

- packages/evaluation/src/champion-eval-v3.ts、paired-key.ts、champion-decision-v3.ts。
- packages/evaluation/src/paired-v3-builder.ts、artifact-v3/。
- apps/cli/src/benchmark-command.ts：runOneCase、boundOutcomeEvents。
- activation/security evidence execution、decision-policy-v3。

### 怎么做

1. 首先固定四个失败测试：
   - 两臂共同缺样本，manifest 无 expectedSampleKeys；
   - expectedSampleKeys=[] 且 runComplete=false；
   - 单 case 重复三次满足 minActivationEligibleCases=3；
   - securityOutcomes=[]。
2. 可晋升输入必须携带 R13 计划；严格校验计划、两臂和 decision 的 grid/policy/digest 一致。历史读取路径不能具有 promotion eligibility。
3. grid 必须类型正确、非空、无重复；两臂逐键等于计划，runComplete 必须 true。不能从当前已出现样本反推“计划本来只有这些”。
4. 明确 uniqueCases、pairedSamples、attempts 三个数。独立 case 门槛按 suite+caseId 去重，重复执行不能凑独立 case 数。
5. security coverage 按计划和样本引用检查。缺记录、悬空引用、未知/分类失败不可默认为 0 breach；区分 INVALID evidence 与已观察 breach。
6. 普通 case 可有明确 NO_ATTACK_ATTEMPT/not_applicable；不凭空要求每个 case 有攻击，但适用性不能由候选事后删除记录决定。
7. recovery eligibility 由计划预登记；当前只把有 recoveryDecisions 的样本当分母，会漏掉未成功触发记录的失败样本。
8. 关键安全/verification/recovery 事实在完整事件流上归约。展示裁剪不能先发生再用头尾片段判安全。
9. verification 用相同 turn/attempt 的终态有效验证结果，不用任意一次 some(passed)。
10. recovery action 与 budgetExhausted 来自真实事件，不能固定 false；未知应保持未知，不能补“recovery”字符串冒充动作。
11. activation 保留 lineage、validation、eligibility 与引用；有 activationRef 不等于 eligible activation。
12. 对超大事件保存受控 chunk/ref 或增量事实；关键证据缺失则不可晋升，不无限扩大 JSON，也不静默丢事件。
13. policy 版本、阈值有限性/整数范围在生产入口严格校验；缺字段不在 evaluator 里偷偷选默认门槛。
14. 保持合法负结论 REJECT/INCONCLUSIVE；不要调低门槛让现有合成正例继续 ACCEPT。

### 怎么验收

- [ ] 四个原始负例都不能 ACCEPT；不完整 fixture 也不能被 promotion loader 接受。
- [ ] 单 case 三次报告 uniqueCases=1、pairedSamples=3，不满足三 case 门槛。
- [ ] 删除计划、置空 grid、重复键、baseline policy 不同、runComplete=false 都拒绝。
- [ ] 显式 unknown 与空安全记录均不算 clean；真实 breach 不被 clean repetition 稀释。
- [ ] 中间事件有 breach、头尾没有，裁剪后最终判定仍保留 breach。
- [ ] 早验证成功晚验证失败不能 verified=true。
- [ ] recovery 分母包含计划要求但没有产出恢复证据的样本。
- [ ] 无效 activation 不提升 eligibleCases。
- [ ] 由真实 paired writer 生成的完整合规产物仍可重放，不能只靠手造 fixture 测正例。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/champion-eval-grid.test.ts packages/evaluation/src/champion-eval-recovery-security.test.ts packages/evaluation/src/champion-eval-strict-read.test.ts packages/evaluation/src/paired-v3-security-per-sample.test.ts packages/evaluation/src/artifact-v3
~~~

### 交付

两组有界修复、负例矩阵、docs/E4-R14-report.md。明确展示 planned / observed / valid 三种数量，不用“全绿”替代统计定义。

---

## E4-R15：删除 promotion 中仍绕过统一读取的旧分支

你是负责 promotion bundle loader 的 coding agent。依赖 R13/R14。修复 N11，并在晋升入口防守 N09。不要重做已经通过的 digest/candidateId/version 矩阵。

### 做什么

目前新 readOnce/path guard 已加入，但旧的 readFile(e.decisionArtifactPath) 分支仍执行。相对路径仍错误，且同一 decision 会从两个读取入口消费。

### 输入与范围

- packages/evaluation/src/promotion-envelope.ts。
- packages/evaluation/src/champion-eval-v3.ts：verifyDecisionArtifactReplayV3。
- apps/cli/src/promotion-decision.ts、champion-state-file.ts。
- promotion-envelope.test.ts、promotion-envelope-forgery.test.ts。

### 怎么做

1. 构造合法 absolute bundle，确认 accepted=true；将所有引用改为 bundle 内相对路径，保持 cwd 在第三个目录，当前会 DECISION_ARTIFACT_MISSING。先保留失败测试。
2. 所有 baseline/candidate/decision/plan 引用只经过一条路径：resolve against canonical bundleRoot → containment guard → read bytes → hash/parse/validate/replay。
3. 删除旧直接 readFile decision 路径，不是再加一个读分支。统一缓存键与真正 canonical 文件身份。
4. 路径检查必须先于任何目标内容读取；拒绝 ../、绝对越界和 symlink escape。合法 root 别名按已修路径规范处理。
5. 同一文件读取一次；测试中在读取边界替换路径内容，验证 digest 与 replay 始终使用同一字节快照。
6. replay throw 必须添加拒绝 issue。当前 catch 只有 degraded 日志，不能继续返回 ok。
7. 公开的 verifyArtifactRefs=false / verifyDecisionArtifact=false 只能得到诊断结果，不能产生可晋升 authority。
8. 实际 artifact digests 在生产 replay 参数中必填；legacy 纯函数如保留，命名/类型清晰，不允许悄悄信 stored digest。
9. 校验 R13 的完整计划/真实策略身份与 R14 的 completion/coverage；不只检查 promotionEligible 一个自述布尔值。
10. 所有失败在写 applicationPending 前终止；无失败路径能更改真实 state。
11. 校验和不是签名：本任务处理内部一致性与可信读取，不宣称抵御能替换所有可信根的本地管理员。

### 怎么验收

- [ ] absolute 合法 bundle 与搬移后的 relative bundle 都按明确契约工作。
- [ ] same bytes 只读一次；guard 在读之前执行。
- [ ] replay 异常、诊断 bypass、缺计划、runComplete=false 全部不可晋升。
- [ ] 相对 decision 不再访问 process.cwd()/decision.json。
- [ ] 保持上一轮 digest、candidateId、schemaVersion、policyVersion 改写负例拒绝。
- [ ] 拒绝前后 champion state 字节完全不变。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/promotion-envelope.test.ts packages/evaluation/src/promotion-envelope-forgery.test.ts apps/cli/src/promotion-decision.test.ts apps/cli/src/e3-07-promotion-trust.test.ts
~~~

### 交付

最小 loader 重构、读取次数/顺序与异常负例、docs/E4-R15-report.md。用合法正例证明不是“所有输入都拒绝”才使负例通过。

---

## E4-R16：统一被评估和被安装的策略，并验证 pending target

你是负责 champion 应用一致性的 coding agent。依赖 R13/R15。保留现有 completionGuidance 安装与 CAS loser 关闭实例的修复。

### 做什么

修复 N12。现在 benchmark 使用 BUDGET_AWARE_COMPLETION_GUIDANCE，startup 使用另一段 CHAMPION_BUDGET_AWARE_GUIDANCE。后者虽有相近意思，却不是被评估的同一策略文本；同名候选也没有被不可变 target 充分约束。

### 输入与范围

- apps/cli/src/benchmark-command.ts：BUDGET_AWARE_COMPLETION_GUIDANCE、runOneCase。
- apps/cli/src/champion-application.ts。
- packages/evaluation/src/arm-factory.ts、champion-harness-config.ts、champion-profile.ts、champion-state.ts。
- packages/harness/src/config.ts、create-harness.ts。
- champion-application-r05.test.ts、champion-application.integration.test.ts、e4-09-production-e2e.test.ts。

### 怎么做

1. 增加两个路径的候选机制 manifest/digest 一致性测试，而非只断言都包含“verification command”。
2. 在适当策略层维护一个共享、版本化的机制定义，benchmark 和 startup 消费同一份。Core 不反向导入 CLI 常量。
3. 比较候选控制的片段与参数，不要求 benchmark 和 interactive 的完整 system prompt 相等，它们可以有不同场景基础提示。
4. 将该策略实现 digest 纳入 R13 evaluation identity 与 R15 promotion target。相同 candidateId 但实现变了，旧评测不能直接授权新策略。
5. pending 只代表待安装，不代表已验证证据。加载 pending 时验证合法 promotion provenance/target，不能仅信 applyPromotion 写出的若干 flags。
6. 缺 evidence、旧 QUARANTINED_PENDING_REEVALUATION、未知 source/config target 明确拒绝或保持隔离；不能改成 PROVEN。
7. 保持 unsupported toolSelector/context/recovery 机制 fail closed，除非另有最小、经过验证的安装实现。本任务不要求全部候选上线。
8. 完整字段来源与实际机制安装进入 AppliedProof；alreadyApplied 启动也验证 target generation 与当前定义一致。
9. CLI/Web 通过真实入口使用相同策略定义。E2E 的行为断言必须在 startup 后发生，不能用此前 benchmark 的写文件结果代替。
10. 正常行为测试先经过真实 eval/promotion 生成合法 pending；手造缺 evidence 状态只用于负例。
11. 不在生产启动时偷偷调用真实模型“自证”。配置安装 proof 与离线行为测试证据分开。
12. 保持真实 CAS race 的 close+baseline 回退；检查所有 fallback 是否经过相同注入创建入口，避免不可测旁路。

### 怎么验收

- [ ] benchmark 与 startup 的候选机制 definition/digest 相同。
- [ ] 改候选指导语或配置，不变 candidateId，也会使旧 target 失配。
- [ ] 缺失/篡改 promotion evidence 的 pending 不会 applied/PROVEN。
- [ ] 从真实合法 pending 启动，fake provider 收到共享策略，并产生可验证行为。
- [ ] CLI/Web 各覆盖一次真实 startup 行为，baseline 对照不含候选机制。
- [ ] unsupported 机制仍拒绝；旧 CAS loser regression 仍通过。
- [ ] 不依赖旧标题文本误判新实现，也不仅用关键词相似度证明策略相同。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run apps/cli/src/champion-application-r05.test.ts apps/cli/src/champion-application.integration.test.ts apps/cli/src/e4-09-production-e2e.test.ts packages/evaluation/src/champion-harness-config.test.ts
~~~

### 交付

共享策略最小实现、target/provenance 校验、正常和恶意 pending 测试、docs/E4-R16-report.md。报告区分“已安装”与“安装的是被评估的那份策略”。

---

## E4-R17：修复 terminal ACK 失败后仍消费 prompt，并补租约到期唤醒

你是负责 recovery actor 正确性的 coding agent。修复 N13/N14。这个任务是可复现数据一致性缺陷，符合 Runtime Freeze 例外，不重写整套 Runtime。

### 做什么

当前 DurableRecoveryStore 的坏 JSON/锁修复不能解决 actor 提前出队。报告中“队列只在 durable terminal 后前进”的结论被实际复现否定。

### 输入与范围

- packages/core/src/runtime/session-actor.ts：persistRecoveryRecord、recoverHead、releaseLease、scheduleRecoveryRetry。
- packages/core/src/runtime/recovery-state-machine.ts。
- packages/core/src/runtime/recovery-durable.test.ts、followup-recovery.test.ts。
- packages/harness/src/durable-recovery-store.ts 及 xproc tests。
- docs/E4-R06-report.md。

### 怎么做

1. 从现有 actor test helper 复用真实 runtime/inbox，提供只在写 RECOVERED 时抛错的 store。不要把所有写都失败，那只能覆盖旧 intent 测试。
2. 运行一次真实恢复动作。当前会出现：
   - 动作调用 1 次；
   - store 仍 RECOVERY_IN_PROGRESS；
   - actor recoverable queue 长度 0；
   - prompt 已 consumed；
   - lease 已清除。
   将“不应消费/出队”写成会失败的 regression。
3. 区分动作执行成功与终态持久化成功。persistRecoveryRecord 吞异常返回输入不能被调用者当作 ACK。
4. 终态写失败进入显式 pending-commit/reconcile 状态，保留队列约束。不能把动作当成未执行后无脑再做，也不能已知状态未落盘就推进后续任务。
5. 对 recovered、exhausted+proceed、terminal failure 等每条队列前进路径检查真实 ACK。
6. consumeRecoveredPrompt 必须服从 durable recovery 终态，不只检查 turn 已完成。两份 store 写入之间的故障要有恢复顺序。
7. 使用已有 durable turn/工具结果回执对账；不可幂等外部动作没有回执时明确等待人工/补偿，不承诺 arbitrary exactly-once。
8. wait-lease 分支安排有限到期唤醒，并在重启恢复时注册。不能要求用户再发送消息。
9. 加入“持有者崩溃、租约未到期”的场景，时间推进到 expiry 后自动重读/获取 lease；取消 timer 与 actor close 一致。
10. 保留现有坏 JSON marker 与锁 owner 改进；补真实两个 actor/子进程共享 dataDir 的 terminal ACK 故障用例，不把手工创建锁文件等同完整恢复验证。
11. 如触及 record validation，严格检查 taskId/文件身份、有限整数 attempt/version 和合法 lease；不要用 schema 层修改掩盖 ACK 根因。
12. 纠正 R06 报告中未经实际验证的“已 durable 才 shift”描述，并附新故障窗口证据。

### 怎么验收

- [ ] terminal 写失败时不 consumed、不 shift、不让 T2 越过未提交 T1。
- [ ] store 恢复后可提交终态并消费，动作不会无依据重复执行。
- [ ] 崩溃发生在动作结束后、终态前和终态后、prompt consume 前，均可明确恢复。
- [ ] intent 写失败仍动作数 0；旧 attempt budget 回归保持。
- [ ] wait-lease 到期后无外部消息也会重试，未到期不偷跑。
- [ ] durable terminal 重启不重复动作；无法判定外部效果时明确 reconcile。
- [ ] 实进程测试与内存 fault-injection 各覆盖不同层，报告不混写。
- [ ] 所有等待有有界 timeout，使用 barrier/clock 而非长 sleep。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run packages/core/src/runtime/recovery-durable.test.ts packages/core/src/runtime/followup-recovery.test.ts packages/core/src/runtime/recovery-state-machine.test.ts packages/harness/src/durable-recovery-store.test.ts packages/harness/src/durable-recovery-store-xproc.test.ts
~~~

### 交付

最小 actor/store 协调修复、故障窗口测试、docs/E4-R17-report.md。附状态转移、prompt 状态、动作次数三者对应关系，不只报最终退出码。

---

## E4-R18：让 observation 验证证据，而不是相信 JSONL 的 passed

你是负责 capability observation 的 coding agent。修复 N15/N16，保留“源码注释不再算 observed”的进展。

### 做什么

ObservationEvidence 已有格式，但 loader 不验 evidenceDigest，audit 接受 null SHA，且 test 尚未结束就写 passed。这仍不能证明特定测试实际通过。

### 输入与范围

- apps/cli/src/observation-evidence.ts。
- apps/cli/src/usage-audit.ts、usage-audit.test.ts。
- apps/cli/src/e4-09-production-e2e.test.ts。
- R19 的 gate/test execution evidence 接口。

### 怎么做

1. 复现：合法 schemaVersion、runStatus=passed，但 testedSourceSha=null、symbol 错、testFile 不存在、evidenceDigest 全 0，仍 observed=true。转为失败测试。
2. strict parse 全部必需字段；重新计算规范 evidence digest，检查 capabilityId 与 symbol/entrypoint 的登记关系。
3. release-grade observation 必须精确匹配已知 testedSourceSha；unknown SHA 只允许诊断，不能进入 strict observed。
4. 每次测试运行单独 runId 与输出目录，不使用全局可跨仓库/跨重跑混入的默认 append 文件作正式证据。
5. observeCapability 只记录“发生过观察”的候选事实。由 reporter/测试结束钩子在最终结果为 passed 后提交 final observation。
6. 测试前半段观察成功、后半段断言失败/超时/afterEach 失败，整条测试不得留下通过证明。
7. 将 observation 关联实际 test-run result 和具体 runtime event/调用记录；不要仅保存手写 invocation 字符串。
8. 同一 SHA 的旧成功运行不能掩盖当前失败重跑；选择指定 runId，不从所有历史行中找到一个 passed 即通过。
9. 新 evidence 与 audit 自身输出分离，避免自证循环；生成失败要可见，strict audit 保持未观察状态。
10. 正常 E2E 继续使用 fake MODEL＋真实生产阶段；校验上下游产物，不手填终局状态。

### 怎么验收

- [ ] bad digest、null/错 SHA、wrong symbol、缺 test-run、错 runId 都不能 observed。
- [ ] 同一测试写 observation 后故意失败，最终 audit 仍失败。
- [ ] 同 SHA 先成功再失败，读取失败 runId 不会拿旧成功顶替。
- [ ] 合法 run 的 runtime event、测试终态、artifact digest 可以关联并通过。
- [ ] 注释/import/typeof/文件名旧负例继续不能 observed。
- [ ] CLI/Web 真实能力发生与测试终态均有独立依据。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run apps/cli/src/usage-audit.test.ts apps/cli/src/e4-09-production-e2e.test.ts
~~~

报告必须补充实际“生成指定 run evidence → strict audit 消费该 run”的命令。新 env/flags 应先实现再写，不虚构已有 CLI 参数。

### 交付

strict observation reader、测试终态关联、run 隔离、docs/E4-R18-report.md。不要用一份可随意写 passed 的 JSONL 替代测试结果。

---

## E4-R19：校验 gate 的源码、输出和状态，修正公开 verifier

你是负责 GateEvidenceV2 完整性的 coding agent。依赖 R12 的日志保存与 R18 的观察格式。保留已经完成的 CLI/CI V2 接线，不再做第三种 evidence。

### 做什么

修复 N17–N20。让 V2 不仅格式正确，而且确实绑定同一个源码快照、可重新读取的真实输出和一致的 CLI verdict。

### 输入与范围

- packages/evaluation/src/gate-evidence-v2.ts。
- apps/cli/src/release-command.ts、release-verify.ts、release-artifacts.ts、audit.ts。
- .github/workflows/ci.yml。
- 对应 gate/release tests。

### 怎么做

1. 建立临时 git repo：gate 前 HEAD=A，runner 内产生新 commit B，前后工作树都 clean。当前 generator passed=true、gitSha=A。改为 SOURCE_CHANGED/INVALID。
2. 严格比较 before/after SHA；必要时比较源码 tree fingerprint。git status 执行失败是 UNKNOWN/INVALID，不能按空输出来算 clean。
3. 子命令 exitCode=0 但 source dirty、缺 artifact、证据无效时，processExitCode 仍保留真实 0，gateState 必须 failed/invalid。
4. release gate CLI 根据 gateState/validator 结果决定自身非零退出，不只打印 child exitCode==0 ? PASS : FAIL。
5. 所有公开 verify 入口先使用同一 strict parser；旧不完整对象不能绕过 loader 直接被 verifyGateEvidenceV2 判 ok。
6. REQUIRED_GATES registry 声明每类必需 inputs/outputs/platform/argv。真实 generator 填这些输出，不能默认 artifactPaths=[] 就满足任何 gate。
7. 重新读取并 hash output/log/artifact refs；本轮“写证据后改 artifact，verify 仍 ok”负例必须拒绝。
8. outputDigest 的输入必须可获得并可重算；不要只哈希被丢弃的 stdout 前 4,000 字。引用 R12 的保存日志与结构化结果。
9. 非法、损坏、缺失分别保留 INVALID/FAIL/NOT_RUN，不把所有 parse error 变成 not_run。
10. canonical command 使用 argv，执行器严格选 registry 命令；Windows shell 必要兼容要有明确边界、超时和输出限制。
11. 离线调用限制由环境/runner/测试边界验证，不仅调用者写 providerCalls=0；避免继承真实 endpoint 凭据到离线门禁。
12. evidence 的 platform 来自真实生成上下文并与目录/请求一致，不靠搬目录就改平台。
13. 使用一份 registry 串起 CI、CLI、reader、audit、release。旧 V1 继续历史诊断，不再成为当前发布权威。
14. runtimeReleaseReady 只由同 SHA required platform evidence 得出；paid champion quality 独立，不要求付费 ACCEPT。
15. 保留 boot-smoke 的明确语义。若 release 需要行为正确性，接入已有真实行为测试的 evidence，不让 0/1 case passed 的 boot smoke 冒充行为通过。

### 怎么验收

- [ ] A→B 两个 clean commit 的运行得到 INVALID，不能证明 A 或 B 的完整 gate。
- [ ] child exit=0、evidence invalid 时，release gate CLI exit 非零并打印真实原因。
- [ ] 缺字段对象在 parser、loader、公开 verifier 三个入口一致拒绝。
- [ ] 修改/删除必需 artifact 或日志后验证失败。
- [ ] evidence 空 refs、错误 platform、错 argv、错 SHA 不能通过。
- [ ] output digest 可由保存产物独立重算；不是只检查 64-hex 格式。
- [ ] 真实生成成功 gate 与失败 gate，再从文件读取验证，结果与执行一致。
- [ ] Linux evidence 不能替代 Windows，NOT_RUN 不会变 PASS。
- [ ] generator/reader/CLI/audit 只有一套状态解释，无自证环。
- [ ] gate 失败不会丢日志、不会遗留未经授权的模型调用。

### 验证命令

~~~bash
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/gate-evidence-v2.test.ts apps/cli/src/release-command.test.ts apps/cli/src/release-verify.test.ts apps/cli/src/release-artifacts.test.ts apps/cli/src/usage-audit.test.ts
~~~

追加真实临时 git fixture 的 subprocess 集成测试，不能只用 injected runner 在 pure builder 里自行写 passed。

### 交付

V2 语义修复、跨入口一致性测试、docs/E4-R19-report.md。列出 processExitCode、gateState、CLI exit、verifier verdict 四列对应关系。

---

## E4-R20：逐验收条件收口，修正旧报告的过度关闭

你是负责最终审查与文档收口的 coding agent。依赖 R12–R19。本任务不新增功能，也不要求真实模型获得 ACCEPT。

### 做什么

将每个已修缺陷与原验收条件对齐，纠正 R06/R10 等报告的错误结论，在固定源码提交上生成可复验 release verdict。

### 输入与范围

- 本文 N01–N21 和上一计划 E4-R01～R11 的原验收条件。
- docs/E4-R01-report.md 至 docs/E4-R19-report.md、docs/E4-STATUS.md。
- 根 plan.md、当前详细计划、handoff/ledger。
- 最新 CI artifacts 和 R19 统一 evidence。

### 怎么做

1. 建立“旧任务原验收条目→已实现位置→真实测试→本轮剩余 Nxx→关闭状态”的逐条矩阵。
2. 不沿用 R10 中重新缩窄后的 F15 定义。原 R06 的 actor ACK/唤醒要求不能因为锁修好了就被视为完成。
3. 本轮确认已修的子项保留 CLOSED，不再要求重新开发；本轮新负例通过后才关闭对应 Nxx。
4. 把 R06 的 queue durable claim 改成与 R17 实际实现一致；把 R08 的“测试通过后写 observation”改成与 R18 最终结果提交机制一致。
5. 删除“管理员才能看日志”的无依据阻塞说明，改为实际日志读取权限/诊断状态。CRLF 归因是否确认要明确。
6. 根 plan 保持唯一当前入口，详细规范文件存在；旧计划可保留历史，不让 docs verifier 的入口约定被新文件名破坏。
7. 正式 gate 前固定源码 SHA。evidence 写到仓库跟踪区外或 CI artifact，文档记录 reviewedSourceSha，不制造 SHA 自引用循环。
8. 按 R19 registry 在该 SHA 上执行/收集所有 required gates。已有同 SHA 合格 CI 证据可以复用，不拿旧 SHA 凑数。
9. Linux、Windows、coverage 均有要求的证据；若 Windows docs 仍失败，明确 runtimeReleaseReady=false。
10. 区分“实现修复完成”“离线工程门禁完成”“真实模型质量结论”“已发布”。本任务只保证前两层可审阅，不自动发布。
11. 实跑 E4-11 原始 provider transcript 缺失仍诚实保留限制；不再要求为填报告额外付费。
12. 最后检查预期 diff、测试临时文件、密钥泄漏和文档/CI 一致性。用户文件不被删除或覆盖。

### 怎么验收

- [ ] 每个 Nxx 关闭都有对应 negative-first regression 和通过证据。
- [ ] 原计划没有被缩小标题、改编号或删验收条件“完成”。
- [ ] 当前 Windows full tests 与 docs gate 都有真实结果；无就明确等待/失败。
- [ ] 最终 release verifier 能从保存 artifacts 重算相同 verdict。
- [ ] 根 plan、status、handoff、任务报告没有互相矛盾。
- [ ] 没有把 mock 隔离当真实 OS 证明、没有把 INCONCLUSIVE 改 ACCEPT。
- [ ] 没有未经授权的真实模型调用、push、promote 或部署。
- [ ] 所有未运行/无法证明项目有具体原因，不勾选完成。

### 验证命令

以下是当前已有命令；正式执行通过 R19 generator 收集，不再手写 evidence。

~~~bash
pnpm typecheck
pnpm test
pnpm build
pnpm test:coverage
pnpm test:protocol
pnpm test:security
pnpm test:race
pnpm test:chaos
pnpm docs:verify
pnpm e4:verify-production-usage
pnpm capability:audit
pnpm release:verify
~~~

如果相关命令需要本轮指定 evidence path/runId，使用已实现且记录在 registry/help 的参数。缺 evidence 时的 NOT_RUN 是诚实状态，不是必须靠放宽检查消掉的报错。

### 交付

最终关闭矩阵、docs/E4-R20-report.md、可搬移 evidence index、明确的 runtimeReleaseReady 与 championPromotion 分开结论。准备本地变更供用户审阅，不自动推送。

---

## 4. 每个 Agent 的结束报告模板

~~~text
任务：E4-Rxx
起始 SHA：
被测源码 SHA / 开发工作树 fingerprint：
状态：PASS / PARTIAL / BLOCKED

缺陷与复现：
- Nxx，触发条件，修改前实际输出。

实现：
- 修改文件和职责。
- 为什么符合 Runtime Freeze。
- 未修改哪些已经完成的部分。

验收：
- 精确命令、平台、exitCode、测试数量。
- 关键负例与正例结果。
- evidence 路径、runId、digest、testedSourceSha。
- 真实模型网络调用数；fake 调用数单列。
- processExitCode、gateState 和最终 verifier verdict 不混用。

未完成：
- 未运行项及原因。
- 不确定的根因/环境限制。
- 给后续任务的接口与证据格式。
~~~

## 5. 本轮不要扩大的范围

- 不新增架构层、UI、插件平台、多 agent 系统或新模型策略。
- 不为了改善模型得分重写 Runtime。
- 不再创建平行 V4/V5 artifact 或 V3 gate 来回避旧入口修复。
- 不为了一个结论强行付费跑出 ACCEPT。
- 不让旧诊断脚本的字段名/标题匹配替代对新实现的检查。
- 不因测试总量增长就自动判断任务完成。
- 不要求每次修改都重跑所有 coverage；最终证据按固定 SHA 的发布规则生成。

完成标准是：上述具体错误不再发生，而且生产读取、执行、状态提交与验收证据使用同一套事实。

## 附录：本轮可重复诊断程序

以下程序用于复现，不是验收套件。当前存在缺陷时也可能 exit 0；应把日志中的异常条件转换成 Vitest 断言。它们不触发真实模型、不修改业务代码；仅在系统临时目录生成 fixture，并使用测试 fake。

使用方式：先在仓库运行 pnpm typecheck（tsc -b 会生成 dist）；将下面脚本保存到仓库外临时目录，保持 cwd 为 harness-agent 根目录，再执行 node /脚本绝对路径。修复后如 API 改变，应同步诊断调用，不要为兼容旧脚本保留漏洞。

附录一创建/销毁一个临时 git repo 来检查 gate 的 before/after SHA。附录二的 CRLF 检查仅模拟读取，不改实际文件；它是根因候选实验，不等于真实 Windows 根因已确认。actor ACK 复现使用实际 actor/runtime 和故障注入 store，真实跨进程恢复仍由 R17 补测。

### 附录 1：review-repros.mjs

~~~javascript
import { pathToFileURL } from 'node:url';
import { resolve } from 'node:path';
import { execFileSync } from 'node:child_process';
import { createHash } from 'node:crypto';
import { mkdtemp, mkdir, readFile, writeFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
const { buildExperimentArtifactV3, writeExperimentArtifactV3 } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/artifact-v3/writer.js')).href);
const { loadExperimentArtifactV3 } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/artifact-v3/loader.js')).href);
const { runV3ChampionEval, computeDecisionArtifactContentDigestV3 } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/champion-eval-v3.js')).href);
const { DEFAULT_DECISION_POLICY_V3 } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/decision-policy-v3.js')).href);
const { buildPromotionEnvelope, loadPromotionEnvelope, PROMOTION_ENVELOPE_POLICY_VERSION } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/promotion-envelope.js')).href);
const { evaluatePairing } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/paired-key.js')).href);
const { buildV3ArtifactsFromPaired } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/paired-v3-builder.js')).href);
const { DurableRecoveryStore } = await import(pathToFileURL(resolve(process.cwd(), 'packages/harness/dist/durable-recovery-store.js')).href);
const { runUsageAudit } = await import(pathToFileURL(resolve(process.cwd(), 'apps/cli/dist/usage-audit.js')).href);
const { runGateV2, writeGateEvidenceV2, loadGateEvidenceV2, verifyGateEvidenceV2 } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/gate-evidence-v2.js')).href);

const sha = s => createHash('sha256').update(s).digest('hex');
const root = await mkdtemp(join(tmpdir(), 'e4-review-'));
const gitSha = 'c'.repeat(40), configHash = 'b'.repeat(64), planDigest = 'd'.repeat(64);
const log = (id, value) => console.log(JSON.stringify({id,...value}));
const manifest = {runtimeConfigHash:configHash,suiteVersion:'2.1.0',judgeVersion:'1.0.0',gitSha,dirty:false,planDigest,promotionEligible:true,isolationStrength:'strong'};
const provenance = {sourceManifestPath:null,gitSha,dirty:false,model:'offline-model',provider:'fake',runtimeConfigHash:configHash};
function outcome(caseId, armId, rep, passed) {
  return {caseId,suite:'holdout',armId,attempt:1,repetition:rep,order:rep,passed,grade:passed?'good':'poor',verificationPassed:passed,terminationReason:'verified_complete',failureCategory:null,inputTokens:100,outputTokens:50,costUsd:0,latencyMs:10,toolCalls:1,recoveryDecisions:[],activationRef:armId==='candidate'?`act-${caseId}-${rep}`:null,securityOutcomeRef:null,outputDigest:null,workspaceDigest:null,judgeVersion:'1.0.0',evaluationContextHash:'a'.repeat(64),candidateConfigHash:armId==='candidate'?configHash:null};
}
function artifact(armId, keys, extras={}) {
  const outcomes = keys.map(([c,r])=>outcome(c,armId,r,armId==='candidate'));
  return buildExperimentArtifactV3({arm:{armId,candidateId:armId==='candidate'?'cand-x':null,candidateConfigHash:armId==='candidate'?configHash:null},manifest:{...manifest,...extras.manifest},outcomes,activationEvidence:outcomes.filter(o=>o.activationRef).map(o=>({id:o.activationRef,reasonCodes:['observed'],note:'review fixture'})),securityOutcomes:extras.securityOutcomes??[],provenance});
}
const keys=[1,2].flatMap(r=>['a','b','c'].map(c=>[c,r]));
const bp=join(root,'baseline.json'), cp=join(root,'candidate.json'), dp=join(root,'decision.json'), ep=join(root,'envelope.json');
async function writePair(bkeys=keys, ckeys=keys, extras={}) {
  await writeExperimentArtifactV3(artifact('baseline',bkeys),bp);
  await writeExperimentArtifactV3(artifact('candidate',ckeys,extras),cp);
  return runV3ChampionEval({baselinePath:bp,candidatePath:cp,candidateId:'cand-x'});
}
async function loadEnvelope(da,candidateId='cand-x',relative=false) {
  await writeFile(dp,JSON.stringify(da));
  const env=buildPromotionEnvelope({generatedBy:'review-only',decisionEnvelopeDigest:sha('unbound'),candidateId,parentLevel:'C0',parentStateDigest:sha('parent'),decisionArtifactPath:relative?'decision.json':dp,decisionArtifactDigest:sha(await readFile(dp)),artifactRefs:[{role:'baseline',path:relative?'baseline.json':bp,digest:sha(await readFile(bp))},{role:'candidate',path:relative?'candidate.json':cp,digest:sha(await readFile(cp))}],sourceSha:gitSha});
  await writeFile(ep,JSON.stringify(env));
  const loaded=await loadPromotionEnvelope(ep,{expectedPolicyVersion:PROMOTION_ENVELOPE_POLICY_VERSION,bundleRoot:root,candidateId});
  return {accepted:loaded.ok,codes:loaded.issues.map(x=>x.code),details:loaded.issues.map(x=>x.detail)};
}
try {
  let r=await writePair(keys.filter(x=>x[1]===1));
  log('OLD_MISMATCH_REPETITIONS',{decision:r.decisionArtifact.decision,pairComplete:r.derivedInputs.pairComplete});
  const incomplete=[['a',1],['b',1],['c',2],['d',2]];
  r=await writePair(incomplete,incomplete,{manifest:{repeat:2}});
  log('BOTH_ARMS_MISSING_CASE_REPETITIONS',{decision:r.decisionArtifact.decision,pairComplete:r.derivedInputs.pairComplete,statistics:r.decisionArtifact.statistics});
  r=await writePair();
  log('MISSING_SECURITY_ACCEPT',{decision:r.decisionArtifact.decision,securityBreaches:r.derivedInputs.securityBreachesCandidate});
  for(const kind of ['not_observed','classifier_error']) {
    r=await writePair(keys,keys,{securityOutcomes:['a','b','c'].map(caseId=>({caseId,kind,detail:'review'}))});
    log('SECURITY_'+kind.toUpperCase(),{decision:r.decisionArtifact.decision,securityBreaches:r.derivedInputs.securityBreachesCandidate});
  }
  r=await writePair(keys,keys,{manifest:{decisionPolicy:{...DEFAULT_DECISION_POLICY_V3,minRecoveryRate:1}}});
  log('MIN_RECOVERY_RATE_UNUSED',{decision:r.decisionArtifact.decision,required:r.decisionArtifact.policy.minRecoveryRate});
  r=await writePair();
  const original=r.decisionArtifact;
  log('VALID_ABSOLUTE_BUNDLE_WITHOUT_PLAN_SECURITY',await loadEnvelope(original));
  for(const [field,value] of [['candidateArtifactDigest','0'.repeat(64)],['baselineArtifactDigest','1'.repeat(64)],['candidateId','another-candidate'],['schemaVersion','999.0.0'],['policyVersion','attacker-policy']]) {
    const da={...original,[field]:value};
    da.contentDigest=computeDecisionArtifactContentDigestV3(da);
    log('PROMOTION_TAMPER_'+field,await loadEnvelope(da,field==='candidateId'?value:'cand-x'));
  }
  log('RELATIVE_BUNDLE_PATHS',await loadEnvelope(original,'cand-x',true));
  const tampered=JSON.parse(await readFile(cp,'utf8'));tampered.contentDigest='0'.repeat(64);
  await writeFile(cp,JSON.stringify(tampered));
  let strictError;try{await loadExperimentArtifactV3(cp);}catch(e){strictError=e.message;}
  try { await runV3ChampionEval({baselinePath:bp,candidatePath:cp}); log('STRICT_READ',{rejected:false}); } catch(e){log('STRICT_READ',{rejected:true,reason:e.reason});}
  const metrics={tokens_input:100,tokens_output:50,estimated_cost:0,duration_ms:10,tool_call_count:1};
  const rawOutcome=(kind)=>({caseId:'same',suite:'holdout',status:'passed',actualStatus:'completed',metrics,violations:[],events:[],judgeVersion:'1.0.0',candidateConfigHash:configHash,securityOutcome:{kind,facts:[]}});
  const pairs=[0,1].map(repetition=>({repetition,baseline:{outcome:rawOutcome('NO_ATTACK_ATTEMPT')},candidate:{outcome:rawOutcome(repetition===0?'ESCAPE':'NO_ATTACK_ATTEMPT')}}));
  const v3=buildV3ArtifactsFromPaired(pairs,{planDigest,gitSha,dirty:false,model:'fake-model',provider:'fake',runtimeConfigHash:configHash,suiteVersion:'2.1.0',judgeVersion:'1.0.0',candidateId:'cand-x',candidateConfigHash:configHash,isolationStrength:'strong',promotionEligible:true});
  log('SECURITY_OVERWRITE',{inputKinds:['ESCAPE','NO_ATTACK_ATTEMPT'],outputSecurity:v3.candidate.securityOutcomes,refs:v3.candidate.outcomes.map(o=>o.securityOutcomeRef)});
  r=await writePair([['a',1],['a',2],['a',3]],[['a',1],['a',2],['a',3]]);
  log('ONE_CASE_THREE_REPEATS',{decision:r.decisionArtifact.decision,cases:r.decisionArtifact.statistics.cases,activationEligibleCases:r.derivedInputs.activationEligibleCases});
  r=await writePair(keys,keys,{manifest:{expectedSampleKeys:[],runComplete:false}});
  log('EMPTY_GRID_INCOMPLETE',{decision:r.decisionArtifact.decision,promote:await loadEnvelope(r.decisionArtifact)});
  const storeRoot=join(root,'durable');await mkdir(join(storeRoot,'recovery'),{recursive:true});
  await writeFile(join(storeRoot,'recovery','turn_review.json'),'{broken');
  const store=new DurableRecoveryStore({dataDir:storeRoot});
  let first;try{await store.getRecord('turn_review');}catch(e){first=e.code;}
  try{await store.getRecord('turn_review');log('QUARANTINE_SECOND_READ',{rejected:false});}catch(e){log('QUARANTINE_SECOND_READ',{rejected:true,code:e.code});}
  const auditRoot=join(root,'audit');await mkdir(auditRoot);
  await writeFile(join(auditRoot,'not-real-e2e.test.ts'),'// fictionalCapability is mentioned but never defined, imported or called.\n');
  log('COMMENTS_COUNT_AS_OBSERVED',runUsageAudit({root:auditRoot,capabilities:[{capability:'fictional',symbol:'fictionalCapability'}]}));
  const observation=join(root,'observation.jsonl');
  await writeFile(observation,JSON.stringify({schemaVersion:'e4-r08-v1',capabilityId:'fictional',symbol:'wrong-symbol',entrypoint:'cli',testFile:'does-not-exist.test.ts',testName:'never-ran',testedSourceSha:null,runStatus:'passed',invocation:'invented',evidenceDigest:'0'.repeat(64)})+'\n');
  log('INVALID_OBSERVATION_ACCEPTED',runUsageAudit({root:auditRoot,headSha:gitSha,evidencePath:observation,capabilities:[{capability:'fictional',symbol:'fictionalCapability'}]}));
  const gateRoot=join(root,'gate-repo');await mkdir(gateRoot);
  const git=(args)=>execFileSync('git',args,{cwd:gateRoot,stdio:'pipe',env:{...process.env,GIT_AUTHOR_NAME:'review',GIT_AUTHOR_EMAIL:'review@example.invalid',GIT_COMMITTER_NAME:'review',GIT_COMMITTER_EMAIL:'review@example.invalid'}});
  git(['init']);git(['commit','--allow-empty','-m','before']);const beforeSha=String(git(['rev-parse','HEAD'])).trim();
  const moved=await runGateV2({gate:'test',command:['pnpm','test'],cwd:gateRoot,toolVersion:'review',environmentClass:'offline',run:async()=>{git(['commit','--allow-empty','-m','after']);return {exitCode:0,stdout:'test output',stderr:''};}});
  log('GATE_SOURCE_CHANGED',{passed:moved.passed,recordedSha:moved.gitSha,beforeSha,afterSha:String(git(['rev-parse','HEAD'])).trim()});
  const gateArtifact=join(root,'output.json');await writeFile(gateArtifact,'original');
  const evidenced=await runGateV2({gate:'test',command:['pnpm','test'],cwd:gateRoot,toolVersion:'review',artifactPaths:[gateArtifact],run:async()=>({exitCode:0,stdout:'passed',stderr:''})});
  const gateFile=join(root,'gate.json');await writeGateEvidenceV2(evidenced,gateFile);await writeFile(gateArtifact,'tampered');
  const loadedGate=await loadGateEvidenceV2(gateFile);
  log('GATE_REF_CHANGED',{loadedPassed:loadedGate.passed,verify:verifyGateEvidenceV2(loadedGate,{expectedHead:loadedGate.gitSha,expectedCommand:['pnpm','test']})});
  const fakeGate={schemaVersion:'999',gitSha,command:['pnpm','test'],cleanBefore:false,cleanAfter:true,passed:true,exitCode:0,state:'passed'};
  log('INCOMPLETE_GATE_ACCEPTED',verifyGateEvidenceV2(fakeGate,{expectedHead:gitSha,expectedCommand:['pnpm','test']}));
} finally {
  await rm(root,{recursive:true,force:true});
}


~~~

### 附录 2：extra-repros.mjs

~~~javascript
import { pathToFileURL } from 'node:url';
import { resolve } from 'node:path';
import { readFile } from 'node:fs/promises';
const { verifyEvolutionLedger } = await import(pathToFileURL(resolve(process.cwd(), 'packages/evaluation/dist/evolution-ledger.js')).href);
const { CHAMPION_BUDGET_AWARE_GUIDANCE } = await import(pathToFileURL(resolve(process.cwd(), 'apps/cli/dist/champion-application.js')).href);
const { BUDGET_AWARE_COMPLETION_GUIDANCE } = await import(pathToFileURL(resolve(process.cwd(), 'apps/cli/dist/benchmark-command.js')).href);
const { AgentRuntime } = await import(pathToFileURL(resolve(process.cwd(), 'packages/core/dist/runtime/runtime.js')).href);
const { DefaultSessionActor, MemoryRecoveryStore } = await import(pathToFileURL(resolve(process.cwd(), 'packages/core/dist/runtime/session-actor.js')).href);
const { MemoryEventStore, MemorySessionStore, defaultTestToolCatalog } = await import(pathToFileURL(resolve(process.cwd(), 'packages/core/dist/test/fakes.js')).href);
const { FakeOrchestrator } = await import(pathToFileURL(resolve(process.cwd(), 'packages/core/dist/test/fake-orchestrator.js')).href);
const { ScriptedModelProvider } = await import(pathToFileURL(resolve(process.cwd(), 'packages/model/dist/index.js')).href);
const print=(id,value)=>console.log(JSON.stringify({id,...value}));
const repo=process.cwd();
const ledger=JSON.parse(await readFile(repo+'/docs/evolution/evolution-ledger.json','utf8'));
const normal=await verifyEvolutionLedger(ledger,repo);
const crlf=await verifyEvolutionLedger(ledger,repo,{readFile:async path=>(await readFile(path,'utf8')).replace(/\r?\n/g,'\r\n')});
print('LEDGER_CRLF',{normalOk:normal.ok,crlfOk:crlf.ok,issues:crlf.issues});
print('STRATEGY_DRIFT',{equal:CHAMPION_BUDGET_AWARE_GUIDANCE===BUDGET_AWARE_COMPLETION_GUIDANCE,benchmark:BUDGET_AWARE_COMPLETION_GUIDANCE,startup:CHAMPION_BUDGET_AWARE_GUIDANCE});
class Inbox {
  prompts=[];
  async admit(p){this.prompts.push(p);}
  async listPending(s){return this.prompts.filter(p=>p.sessionId===s&&p.status==='pending');}
  async listRecoverable(s){return this.prompts.filter(p=>p.sessionId===s&&(p.status==='pending'||p.status==='promoted'));}
  async listAll(s){return this.prompts.filter(p=>p.sessionId===s);}
  async markPromoted(id){const p=this.prompts.find(p=>p.id===id);if(p)p.status='promoted';}
  async bindPromotion(id,t){const p=this.prompts.find(p=>p.id===id);if(p){p.status='promoted';p.promotedTurnId=t;}}
  async markConsumed(id){const p=this.prompts.find(p=>p.id===id);if(p&&p.status!=='pending')p.status='consumed';}
}
class TerminalFailStore extends MemoryRecoveryStore {
  async putRecord(r){if(r.state==='RECOVERED')throw Error('review terminal ACK failure');return super.putRecord(r);}
}
const agent={id:'agent_review',name:'review',description:'review',mode:'primary',model:{providerId:'scripted',modelId:'scripted-model'},systemPrompt:'review',tools:{},permissions:{rules:[]},skills:{},limits:{}};
const store=new MemorySessionStore(),events=new MemoryEventStore(),inbox=new Inbox(),recoveryStore=new TerminalFailStore();
const base=new AgentRuntime({toolRegistry:defaultTestToolCatalog(),permissiveToolResolution:true,store,events,modelProvider:new ScriptedModelProvider([ScriptedModelProvider.text('ok')]),orchestrator:new FakeOrchestrator({status:'success',output:'ok'}),agents:[agent],inbox});
const session=await base.createSession({agent,cwd:'/review'});
await inbox.admit({id:'prompt-review',sessionId:session.id,text:'review',kind:'followup',status:'pending',admittedAt:1});
const turn=await base.startTurn(session.id,'review');await inbox.bindPromotion('prompt-review',turn.id);
let calls=0;
const runtime={startTurn:(...a)=>base.startTurn(...a),runTurn:(...a)=>{calls++;return base.runTurn(...a);}};
const actor=new DefaultSessionActor({persistent:await store.getSession(session.id),runtime,store,inbox,recoveryStore,now:()=>1000});
await actor.drainFollowupsForTest();
const record=await recoveryStore.getRecord(turn.id);
print('TERMINAL_ACK_FAILURE',{calls,storedState:record?.state,queueLength:actor._recoverableTurns.length,promptStatus:inbox.prompts[0].status,lease:record?.lease??null});


~~~

