# harness-agent 下一轮执行计划：E4-R45～E4-R50

## 1. 审查基线与目标

仓库：ki11a-Conton/harness-agent

reviewedSourceSha：
d201da5e8071e2780a745ffb86ddb31d3cdf547d

比较基线：
67955e0652e5ce5127eb4e7e654f4590515a2571

本轮目标：
补齐失败取证可靠性、构建隔离验收的判别力，以及当前状态文档的一致性。

本轮不要求新增产品功能，不要求重写 Runtime，
不要求付费模型评测，不要求创建新 release。

审查依据：
- 本轮提交差异及相关完整源码；
- 当前 HEAD 的 GitHub Actions job 状态；
- 仓库中的实现报告与测试代码。

证据限制：
- 审查者本轮未独立运行本地测试；
- 仓库报告中的测试数量属于作者报告；
- CI job 成功不等于已独立复核其全部 artifact；
- 本计划中的缺陷复现必须由执行 Agent 实际完成并报告。

## 2. 已完成工作处理原则

保留 R40～R44 的有效成果：

- R41 引入了 Git 探测有效性、结构化失败和三态比较；
- promotion-grade 前置探测失败已有 CLI 路径测试；
- R42 已将相关真实 gate 放入临时 workspace；
- R43 已新增固定百万样本边界与变异副本验证；
- 当前提交四个 CI job 成功；
- v1.8.0 已存在公开 release。

不得因为本计划有新任务，就将上述成果全部退回未完成。
也不得以 CI 全绿代替本轮缺口的针对性验收。

## 3. 所有任务共同遵守的执行提示词

你正在维护 harness-agent。

开始前：
1. 阅读 AGENTS.md。
2. 阅读本任务涉及的 tasks/ 文件及架构约束。
3. 阅读相关源码、测试和最近一轮报告。
4. 记录实际 HEAD、工作树状态及工具版本。
5. 如果 HEAD 已超出 reviewedSourceSha，先核对本任务缺口是否已修复。
   已修复时提供具体代码和验收证据，不重复实现。

实现约束：
- 遵守 Runtime Freeze。
- 不修改无关模块。
- 不绕过 ToolOrchestrator、PermissionEngine、
  SandboxManager 或 Verification。
- 不通过削弱断言、扩大排除范围或无限重试取得绿色结果。
- 不在共享源码树中制造用于失败测试的临时业务文件。
- 子进程使用独立 workspace 和明确 cwd。
- 不把历史未知原因改写成已确定原因。
- 不自动发布版本或启动付费评测。

测试原则：
- 先建立有判别力的复现或反例，再修复。
- 正常 CI 中的验证程序应能以 exit 0 表示验证成功。
- 如果内部子进程故意失败，父验证程序必须检查其真实退出码、
  预期失败信息和产物，再给出自身结果。
- 不接受“出现任意非零退出码就算验证成功”。

提交报告必须包括：
- 做了什么；
- 为什么需要改；
- 修改文件；
- 复现方法与修复前结果；
- 修复后命令、退出码和关键断言；
- testedSourceSha，以及是否存在未提交改动；
- 未执行项及原因；
- 残余限制。

禁止：
- 用旧版本测试结果标记新代码已通过；
- 把 NOT_RUN 写成 PASS；
- 将人为构造的 JSON 描述为生产链真实生成的 artifact；
- 为了提交报告而无限追踪下一次文档提交的 CI。

推荐顺序：
R45 → R46 → R47 → R48 → R49 → R50。

---

## 4. E4-R45：修复实际失败位置之前的诊断注册与保存

优先级：P1
依赖：无
状态：TODO

### 给 Agent 的提示词

你负责修复 E4-09 生产 E2E 在失败发生时丢失关键证据的问题。

重点阅读：
- apps/cli/src/e4-09-production-e2e.test.ts
- apps/cli/src/e4-09-diagnostics.ts
- apps/cli/src/e4-r40-forensics.test.ts
- docs/E4-R40-report.md

### 做什么

修复以下顺序问题：

1. buildRealChain() 先检查 benchmark exitCode。
2. 随后执行 evaluator 并断言 decision === ACCEPT。
3. 只有断言通过后，才注册 paired、V3 和 decision 文件。

因此 benchmark 失败或 decision 为 INVALID 时，
afterEach 即使执行，也无法采集尚未注册的文件。

主链同样需要检查：
实际 decision 在 ACCEPT 断言之后才落盘并注册，
因此 evaluator 返回非 ACCEPT 时可能丢失最关键的决策内容。

### 怎么做

1. 在任何可能失败的生产调用和断言之前，
   计算并注册已知输出路径。
   路径尚不存在时允许注册；采集时如实记录 missing。

2. evaluator 返回后，在 ACCEPT 断言之前保存实际返回结果。
   可以沿用现有 decision 文件，也可以采用有明确角色的诊断产物。
   必须保存真实结果，不得重建一个预期 ACCEPT 对象。

3. 在调用阶段开始前更新 stage。
   区分 benchmark、evaluate、promotion 等阶段。
   不得在调用成功后才标记该阶段。

4. 将诊断中的 testName 绑定真实失败测试身份。
   多个 adversarial 测试不得只留下无法区分的公共名称。

5. 保持原有业务断言和决策语义。
   取证失败不得覆盖原始异常。

6. 建立自动化回归验证：
   使用隔离子进程触发真实 E2E 接线中的确定性失败，
   父测试检查失败证据。
   优先利用现有测试接口；如需注入 seam，应限制在测试层。

7. 区分两类证据：
   - recorder 单元测试可以使用人工小夹具；
   - E2E 接线测试必须证明真实生产调用产生的文件被采集。
   不得将两者混称。

### 怎么验收

至少覆盖：

A. benchmark 阶段非零退出：
- 原始退出信息被保留；
- 已存在的输出文件被采集；
- 未产生文件明确标记 missing；
- stage 指向 benchmark。

B. evaluator 返回非 ACCEPT：
- 原 ACCEPT 断言仍然失败；
- 诊断保存实际 decision 和 reasonCodes；
- 已生成的 paired/V3 被采集；
- 清理原临时目录后仍可读取诊断副本。

C. evaluator 抛异常：
- 保留原异常；
- stage 指向 evaluate；
- 不伪造 decision；
- 之前已有的文件仍可采集。

D. 成功链：
- 原有断言通过；
- 不产生失败诊断包。

E. 禁用提前注册或提前保存的改动时，
   新回归测试必须能发现证据缺失。

交付：
- 最小代码修复；
- 可纳入自动化的回归测试；
- docs/E4-R45-report.md。

---

## 5. E4-R46：保证诊断包跨进程与重复采集不覆盖

优先级：P2
依赖：R45
状态：TODO

### 给 Agent 的提示词

你负责保证同一 CI run 中不同失败的诊断证据不会互相覆盖。

重点阅读：
- apps/cli/src/e4-09-diagnostics.ts
- apps/cli/src/e4-r40-forensics.test.ts
- .github/workflows/ci.yml

### 做什么

当前 ATTEMPT_SEQ 是模块内变量。
当不同进程使用相同 E2E_OBSERVATION_RUN_ID 和 label 时，
它们都可能生成 attempt-1，最终写入同一目录。

当前 recursive mkdir 和 writeFile 允许复用目录及覆盖文件。
同一个 recorder 多次 captureFailure 也需要明确语义。

### 怎么做

1. 先建立两个独立进程的复现：
   使用同一个诊断根目录、run ID 和 label，
   分别写入内容不同的诊断证据。

2. 使用原子创建的唯一目录，例如 mkdtemp，
   不仅依赖进程内计数或时间戳。

3. 保留可读身份字段：
   - logical run ID；
   - CI run attempt（存在时）；
   - recorder attempt；
   - 测试文件及名称；
   - 唯一 capture identity。

4. 明确重复调用 captureFailure 的行为：
   推荐每次创建独立 bundle；
   如果采用幂等返回，必须证明不会覆盖第一次证据。

5. 诊断目录名与 artifact role 采用受控名称。
   对重复 role 或名称冲突明确处理，不静默覆盖。

6. 检查 CI 上传命名是否能区分 run attempt。
   如需修改，只调整诊断 artifact 的身份和路径，
   不扩展到无关 workflow 重构。

### 怎么验收

- 两个独立进程同时采集，相同 run ID 和 label，
  产生两个不同目录。
- 两份 diagnostic.json 分别包含自己的内容。
- 两份 artifact 的原始摘要保持不同且正确。
- 同一 recorder 重复采集符合明确约定。
- 第二次采集之后，第一次证据逐字节不变。
- 默认成功路径不产生诊断。
- 验证不依赖 sleep 保证时间戳不同。
- CI run attempt 的处理有明确断言或配置验证。

交付：
- 目录分配修复；
- 跨进程及重复采集回归测试；
- 必要的 CI 命名调整；
- docs/E4-R46-report.md。

---

## 6. E4-R47：让诊断文件限制真正按字节生效

优先级：P2
依赖：R46
状态：TODO

### 给 Agent 的提示词

你负责修复诊断采集的资源限制与摘要语义。

重点阅读：
- apps/cli/src/e4-09-diagnostics.ts

### 做什么

当前实现先 readFile 整个文件，再转成 UTF-8 字符串，
之后才判断是否超过 MAX_COPY_BYTES。

随后：
- slice 使用字符数量，而不是字节数量；
- 超限文件仍然整体 JSON.parse；
- digest 基于解码后的文本，而不是源文件原始字节。

需要让限制、摘要和报告字段与实际行为一致。

### 怎么做

1. 明确以下概念：
   - sourceBytes；
   - capturedBytes；
   - sourceDigest；
   - capturedDigest；
   - truncated；
   - summary 是否执行以及未执行原因。

2. 小文件保留原始字节。
   仅在大小限制内进行 UTF-8 解码与 JSON 解析。

3. 超限文件使用流式或有限读取：
   - 内存使用不随整个源文件线性增长；
   - 输出大小按字节限制；
   - 如保留完整 sourceDigest，流式计算；
   - 超限时不整体 JSON.parse；
   - 明确实现头部截取还是头尾截取，并同步注释。

4. 摘要必须说明针对源文件还是截断副本。
   不允许一个 digest 字段被两种含义共用。

5. 缺失文件、读取错误、非法 JSON：
   分别记录真实原因；
   不覆盖最初的测试失败。

6. 保持实现范围有限。
   无须为这项修复建立通用日志平台。

### 怎么验收

覆盖：
- 限额以下的合法 JSON；
- 恰好达到限额；
- 超过限额；
- 多字节 UTF-8 内容；
- 非法 JSON；
- 缺失文件；
- 可确定触发的读取错误。

必须证明：
- 小文件副本与源字节一致；
- 超限输出不突破规定字节预算；
- 源摘要与独立计算一致；
- 截断副本摘要与副本一致；
- 超限文件不调用整文件 JSON 解析；
- 原始失败仍然可见。

资源验收：
使用有界读取路径的断言或隔离进程验证，
不要以机器敏感的毫秒阈值或 RSS 精确数值作为唯一依据。

交付：
- 有界采集实现；
- 边界测试；
- 字段或 schema 的兼容性说明；
- docs/E4-R47-report.md。

---

## 7. E4-R48：提高共享构建资源隔离测试的判别力

优先级：P2
依赖：无；建议在 R47 后执行
状态：TODO

### 给 Agent 的提示词

你负责补足 R42 测试对“共享构建产物未被改写”的验收能力。

重点阅读：
- apps/cli/src/e4-r42-gate-isolation.test.ts
- apps/cli/src/release-command.test.ts
- apps/cli/src/e4-r24-final-result-protocol.test.ts
- apps/cli/tsconfig.json
- docs/E4-R42-report.md

### 做什么

当前 sharedBuildSnapshot() 只包含：
- SHARED_DIST 顶层文件名；
- 一个 cli.tsbuildinfo 的内容摘要。

因此同名 JS 文件被覆盖、嵌套文件变化时，
快照可能保持不变，无法支持注释中的 byte-for-byte 声明。

这是验收覆盖缺口。
不要据此未经复现就声称真实 gate 已经污染主仓。

### 怎么做

1. 明确测试声称保护的资源范围：
   哪些 dist 目录、哪些缓存文件。
   范围必须与报告一致。

2. 对声明保护的文件生成确定性快照：
   - 相对路径；
   - 文件类型；
   - 文件内容摘要；
   - 确定性排序。

3. 明确缺失、读取失败和空目录的区别。
   不把任意读错误转换为空快照。

4. 在独立临时目录中测试快照判别力：
   同名覆盖、嵌套修改、增加和删除文件都应被发现。
   不向主仓共享 dist 注入破坏性变异。

5. 保留真实 gate 的隔离 workspace：
   真实 tsc、真实 git 身份、真实非零子进程。

6. 如果扩展快照造成与其他测试的并发冲突，
   优先调整测试资源所有权；
   不直接把整个测试仓库改为串行运行。

### 怎么验收

A. 判别力：
- 同名文件内容变化，快照不同；
- 嵌套文件变化，快照不同；
- 新增或删除文件，快照不同；
- 内容相同但目录枚举顺序不同，快照相同；
- 读取失败有明确结果，不伪装为空目录。

B. 真实 gate：
- tsc 成功；
- 输出位于临时 workspace；
- evidence gitSha 对应临时仓库；
- 非零子进程的退出码和 stderr 被保存；
- 声明保护的共享资源快照保持一致。

C. 组合运行：
运行 gate isolation、release-command 和
final-result-protocol 相关测试，报告真实结果。

交付：
- 快照和测试的最小改动；
- docs/E4-R48-report.md；
- 修正超出实际验证范围的历史表述时使用补记。

---

## 8. E4-R49：统一 release 与计划入口的当前状态

优先级：P2
依赖：R45～R48
状态：TODO

### 给 Agent 的提示词

你负责使当前计划入口和状态文档表达同一组事实。

重点阅读：
- plan.md
- README.md
- README.zh-CN.md
- HANDOVER.md
- docs/E4-STATUS.md
- docs/E4-R44-report.md
- GitHub v1.8.0 release 元数据

### 做什么

审查时 v1.8.0 已发布，
但 plan.md 仍在“已知未完成”中写 release 发布动作本身未执行。

同时 R44 报告前面的矩阵仍写 CI 上传未接入，
后面的补记已写接入完成。

需要区分历史执行事实和当前仓库状态，
并让本轮计划成为唯一明确的当前入口。

### 怎么做

1. 建立事实表：
   - 当前审查 SHA；
   - 本轮实现 SHA；
   - 实际测试 SHA；
   - 对应 CI run；
   - release tag 和发布时间；
   - release 与哪个提交对应；
   - 真实模型质量评测状态；
   - 未复核事项。

2. 发布状态拆开表达：
   - 历史计划是否执行发布；
   - GitHub release 当前是否存在；
   - 本轮是否做过发布完整性复核。

3. 不将“release 存在”解释为
   “本轮验证了发布二进制、签名或全部供应链证据”。

4. 保留历史报告的原始版本语境。
   当前已过时的限制可添加带提交引用的补记，
   不伪造历史时点的完成状态。

5. 将 R45～R50 的状态与报告入口写入当前 plan.md。
   旧计划保留明确的历史标记。

6. 控制 README 长度：
   当前状态只写摘要和入口，
   详细证据留在对应报告。

### 怎么验收

- 当前状态文档对 v1.8.0 是否发布没有冲突。
- 历史“本任务未发布”不会被理解为“仓库没有 release”。
- CI 上传接入状态一致。
- 每个 PASS 都能追溯到版本与证据。
- NOT_RUN 项仍然如实保留。
- docs:verify 通过。
- 没有执行新的发布动作。

交付：
- 状态文档更新；
- docs/E4-R49-report.md。

---

## 9. E4-R50：最终门禁与有限收口

优先级：P1，作为交付门槛
依赖：R45～R49
状态：TODO

### 给 Agent 的提示词

你负责验证本轮修改并给出可追溯的最终结论。

### 做什么

确认本轮缺口有针对性验收，
在固定版本上完成仓库要求的门禁，
然后停止本轮维护。

### 怎么做

1. 建立关闭矩阵：
   缺口 → 复现 → 修改 → 回归测试 → 被测版本 → 结果。

2. 先运行定向测试。
   失败时保存首次失败证据，分析后再决定是否重跑。
   不使用无限重试。

3. 在干净、已提交、运行期不被其他进程修改的工作区，
   按顺序运行：
   - pnpm typecheck
   - pnpm test
   - pnpm docs:verify

4. 按 AGENTS.md 和改动范围执行相关安全、集成测试。
   若修改触及原有 protocol、race、chaos 合同，
   运行对应套件并说明关联。
   不为增加测试数量重复运行无关组合。

5. 故意失败的 forensics：
   原始子测试非零不能直接算验证通过；
   必须由父验证程序确认预期失败及证据，
   或在报告中逐项记录独立验收结果。

6. 核实实现提交对应的远端 CI：
   Ubuntu、Windows、coverage、release attestation。
   记录 run ID、head SHA、attempt 和各 job 结果。
   未运行、取消、失败、跳过分别记录。

7. 明确报告边界：
   job 状态核实与 artifact 内容复核分开写。

8. 最终报告可在被测实现提交后增加纯文档提交。
   如果确实只有文档变化，注明代码等价依据；
   不把旧 run 冒充新 SHA 的 run，
   也不无限追踪报告自己的下一次提交。

### 怎么验收

- R45：非 ACCEPT 和异常路径保留真实决策或已有产物。
- R46：跨进程与重复采集不覆盖。
- R47：字节限制和摘要语义通过边界验收。
- R48：同名文件改写能被测试发现。
- R49：当前状态一致。
- 必需门禁通过，且证据对应正确版本。
- 未执行项单独列出。
- 不把历史间歇性失败自动宣布为已根治。

交付：
- docs/E4-R50-report.md；
- 更新后的 plan.md 状态；
- 简洁的变更说明与验收结果。

## 10. 完成与停止条件

本轮完成条件：
- 所有本轮必需验收通过；
- 证据能追溯到实际被测版本；
- 失败取证与隔离测试的声明不超过实际验证范围；
- 当前状态文档无冲突。

满足后停止本轮维护。

只有新发现的确定性正确性问题、安全问题、
发布完整性问题或已测量回归，
才能追加新的 Runtime 维护任务。

真实模型质量提升应另立目标、预算和评测计划。
不得把它自动追加为本轮完成条件。