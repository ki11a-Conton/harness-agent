# harness-agent 下一轮执行计划：E4-R40～E4-R44

> 本文件的每个任务都是可直接交给 Agent 的提示词，包含做什么、怎么做、怎么验收。重点是完成 R39 尚未关闭的稳定性工作与本次确认的探测错误处理，不重做已经完成的 R36～R38。

## 1. 审查基线与真实结论

- 仓库：https://github.com/ki11a-Conton/harness-agent
- reviewedSourceSha：`67955e0652e5ce5127eb4e7e654f4590515a2571`。
- 比较基线：`a7950fa1f386b2a1adc9ba35ba84aed0ad9ad50c`。
- 本轮增量：8 个提交，17 个文件，1929 行新增、426 行删除。
- 审查包含：通过 GitHub 插件核实提交/CI，读取增量源码和报告，重建 dist，实际运行全量测试及旧/新复现。
- 本次审查没有修改业务代码，没有 commit 或 push，没有付费模型调用。

### 1.1 已完成的上轮工作

| 上轮任务 | 代码/提交 | 本次判断 |
|---|---|---|
| R36 首次恢复发现 | `2341f6ee`；完成扫描后才提交标志，候选局部收集后统一入队 | 原复现已修复：读失败时 checked=false、scheduled=1；恢复后原 turn 执行并 consume。新测试覆盖 callback 自愈等场景，不重复实现 |
| R37 旧夹具测试收集 | `07c40cfe`；根 Vitest exclude 保留 defaults 并排除旧命名模式 | 正式测试收集不再被旧夹具污染；但 TypeScript 编译边界不同，见 K03 |
| R38 有效正例和 loader | `01c4ec74`；真实 digest、真实 ACCEPT、loader.ok，以及容量拒绝详情 | 原 J03 主要验收缺口已补齐；独立固定数值边界仍需补充，见 K04 |
| R39 状态与最终门禁 | `a0a99ce0` 至 `67955e0` 的文档提交 | 状态已诚实写 PARTIAL；历史失败不能被后续同代码绿灯抹除 |

### 1.2 本次实测结果

| 项目 | 结果 | 说明 |
|---|---|---|
| `pnpm typecheck` | PASS，退出 0 | `tsc -b`，新复现使用此后 dist |
| `pnpm test` | PASS，退出 0；318 files / 5740 tests | 本次只跑一轮全量，111.75s；没有靠反复重跑挑最后一次绿色 |
| `pnpm docs:verify` | PASS，ALL CHECKS PASS | 实际运行 |
| 当前 HEAD 的 CI | run `34689695442`，四 job success | Ubuntu、Windows、coverage、release attestation 均核实 |
| 原首次发现故障复现 | 已阻断 | 读失败不再标记完成，已有有限 timer |
| Git 探测失败注入 | 发现 K02 | 详见下节；只对子进程注入 fake git，真实仓库未改动 |
| 编译残留检查 | 发现当前测试夹具的 .js/.d.ts/.map 输出 | 证明夹具进入了构建输入；不证明它导致了历史 INVALID |

最新 CI：https://github.com/ki11a-Conton/harness-agent/actions/runs/34689695442

CI 核实范围是 workflow/job 状态；未下载并重放本 run 全部 artifact，不声称逐字节验过 release 证据。本次全量通过只代表当前环境这一轮通过，不代表旧 `01c4ec74` 失败已定位或被修复。

### 1.3 必须保留的历史事实

`docs/E4-R39-report.md` 记录：Windows 本地同一实现版本曾出现三次 `buildRealChain` 预期 ACCEPT 实得 INVALID；隔离运行曾通过。实现提交 `01c4ec74` 的 run `34687657690` Windows 主门禁失败，两次 attempt 的失败点不同；后续相同代码的文档提交又有完整绿运行。

这些是已有报告记录，本轮未在原 Windows 环境重新复现全部历史失败。不要把“这次 Linux 全量绿”或“最新 CI 绿”写成旧失败根因已解决。也不要把一个 CPU 负载实验未复现写成排除了所有资源/调度竞争。

## 2. 本轮问题分级

| ID | 优先级 | 问题 | 证据等级 | 任务 |
|---|---|---|---|---|
| K01 | P1 | 历史 E4-09 INVALID 缺少完整违规详情与失败产物；难以对失败作机制归因 | 现有测试只断言决策；afterEach 删除临时目录；历史报告根因未定 | R40 |
| K02 | P1 | Git 探测错误被转成空字符串/null，未知被当成变化或不变；sentinel 两次探测失败仍返回 hostMutated=false、details=[] | 当前实现独立故障注入确认 | R41 |
| K03 | P2 | 协议测试在共享 src 写旧夹具，release 测试同时真实执行 typecheck；临时测试进入共享 dist/构建状态 | 源码路径确认；本轮实际见到该夹具编译输出 | R42 |
| K04 | P2 | R38-b 用被测容量常量同时生成输入和预期，改大常量仍然生成更大的超限输入，不能锁住公开上限 | 源码和算式可直接确认；不是数值溢出的必然结果 | R43 |
| K05 | 收口 | 需要基于新证据区分修复完成、历史未复现、CI 状态与真实质量 | R39 尚未关闭项 | R44 |

K02 当前复现：

```json
{"healthyHeadKnown":true,"failed":{"schemaVersion":"1.0.0","headSha":null,"statusPorcelain":"","treeDigest":null},"healthyToFailedCalledMutation":true,"bothFailuresCalledMutation":false}
{"value":"ran","report":{"schemaVersion":"1.0.0","hostMutated":false,"details":[]}}
```

第二行来自真实 `withHostMutationSentinel`，两次 Git 探测都失败，但 callback 仍运行且 report 没有错误细节。它证明观测失败与观测结果混淆；不声称已实现真实沙箱逃逸，也不声称已证明 K02 是历史 K01 的根因。

## 3. 所有任务共用的 Agent 提示词

先读 AGENTS.md、当前 plan 入口、本计划、相关 tasks 与最新报告。记录 HEAD、工作树、运行环境；HEAD 已变化则先判断任务是否已实现，用实际验收标 NOT_NEEDED，禁止重复开发。

Runtime 架构继续 FROZEN。本轮聚焦测试诊断、evaluation/CLI 探测和测试隔离，不授权重写会话 Runtime。只有诊断得到可复现且符合冻结例外的 Runtime 缺陷时，才报告证据并限定最小修复；不得因模型效果或猜测而扩展。

默认离线，不付费、不发布、不自动晋升 champion。保留 ToolOrchestrator、PermissionEngine、SandboxManager 和 Verification，不跳过宿主机哨兵、不放宽资格、不删失败测试、不通过增加无限重试取绿。环境安全控制不能通过清空变量或卸载注入来绕过。

保护用户工作树。临时夹具、故障注入、构建对照放在自己拥有的隔离空间；不要让诊断脚本改被测仓库、影响其他运行或造成虚假的 dirty 状态。日志中不打印凭据或整份环境变量。必要测试必须有判别力，不新增镜像实现的无效断言。

每项产出最小补丁和 `docs/E4-Rxx-report.md`：包含复现、根因或未知、修改点、验收矩阵、实际命令/退出码/数量、被测 SHA/dirty 身份、未运行项。commit/push 按当前会话已有授权处理，不能虚构提交，也不能把历史权限表述当作当前新限制。

推荐顺序：R40 → R41 → R42 → R43 → R44。R40 先提供取证能力；R41/K02 有独立复现，不依赖必须复现历史波动才修；R42/K03 与 K01 因果关系分别记录。最后集中全仓验证，不边改源码边跑要求干净树的全量门禁。

## 4. E4-R40：失败当次保留可归因证据，不再只留下 INVALID

### 可直接发送给 Agent 的任务提示词

你负责 E4-R40。把 R39 未关闭的间歇失败变成可审查的完整事件记录。先补最小取证能力，再做有界复现；本项不预设生产代码有错，也不以本次重跑绿色作为修复结论。

**做什么**

1. E4-09 的有效链路预期 ACCEPT 但失败时，保存决策、违规原因、paired/V3 产物以及执行阶段事实。
2. release gate 子进程或 Windows 相关测试失败时，保存真实 stderr、退出码、信号/超时与必要路径事实。
3. 建立有限的复现与归因规则，避免重复跑大量完整套件却仍没有原因数据。

**先读哪里**

- `apps/cli/src/e4-09-production-e2e.test.ts`：`buildRealChain`、主链路测试、afterEach。
- `packages/evaluation/src/champion-eval-v3.ts` 返回的 derivedInputs/violations、decision artifact。
- `apps/cli/src/release-command.test.ts`、gate runner 实际返回的日志/证据路径。
- `docs/E4-R39-report.md` 第 4～9 节和 `.github/workflows/ci.yml` 的失败日志/artifact 上传逻辑。

**怎么做**

1. 在原断言之前增加失败条件下的证据保存，正常成功路径不复制大量数据。保存完成后仍执行原断言、保持非零结果，绝不自动改成 PASS。
2. 最小诊断包记录：test identity、runId、SHA/工作树身份、OS/Node/pnpm、CLI exit/lines、完整 decision/derived violation、baseline/candidate V3、paired partial/finalized pairs、失败 arm 的 status/reason/security outcome、关键阶段时间。
3. 若宿主机状态变化是可能来源，应保存实际参与判断的 before/after 探测记录；仅在之后重新调用 git 看到干净，不能证明当时也干净。优先复用数据流，避免另造一套改变时序的高频轮询。
4. 将诊断包写到独立、被测试框架明确忽略的目录，避免自我污染。afterEach 可清理工作目录，但失败诊断必须先保存；保存失败要输出错误和已知原因，不能覆盖原测试失败。
5. 对 CI 使用失败时也执行的上传步骤；每 OS/run/attempt 独立命名，避免不同重跑覆盖证据。不为每次成功保存所有大型 artifact。
6. 对历史问题最多进行一次定向运行和两次完整/相关组合运行作为本任务的取证窗口；如果没有复现，记录 NOT_REPRODUCED，交付可用诊断机制后停止。需要更多运行时，必须说明新的证据问题，不能循环直到绿色。
7. 如复现 INVALID，按实际 reason/violation 区分：计划身份、网格缺失、verifier/arm error、探测失败、真实 host mutation、其他原因。先还原失败机制，再设计一个可控注入；不能只从“并发时发生”推断唯一原因。

**怎么验收**

- [ ] 有效离线链继续 ACCEPT；诊断功能不改变决策、资格或成功路径。
- [ ] 注入一个明确的失败使原断言失败；退出非零，诊断包保留对应当次真实原因与匹配的 V3/paired/decision 数据。
- [ ] afterEach 运行后失败包仍可读；两次 run/attempt 不覆盖；读取保存的数据可解释失败阶段。
- [ ] gate 子进程非零/超时证据保留原 stderr、exit/signal，不能只留下 `expected false to be true`。
- [ ] 不记录 API key、完整环境或无关用户目录；诊断文件不让源树变脏、不进入父测试收集。
- [ ] 报告区分“诊断交付 PASS”“历史波动 NOT_REPRODUCED/已归因”；不能把两者合并成历史缺陷已修复。

**交付**

最小诊断补丁、必要的确定性失败取证测试、`docs/E4-R40-report.md`。没有历史失败的原因证据时停止归因猜测，后续任务仍可处理自己的独立复现。

## 5. E4-R41：宿主机探测失败显式为 unknown，不能伪装成修改或未修改

### 可直接发送给 Agent 的任务提示词

你负责 E4-R41，修复 K02。区分“探测成功且状态未变”“探测成功且状态变化”“探测失败无法确认”。缺失证明不能被当作安全证明，也不能无依据归因为子进程越界写入。

**做什么**

1. 修复 `gitOutput` 将非零/超时等错误变成空字符串的语义丢失。
2. `captureHostState` / sentinel 的结果必须携带可判定的有效性和失败原因。
3. promotion-grade 执行在必要宿主机状态不可验证时 fail-closed，并保持错误归类准确。

**先读哪里**

- `packages/evaluation/src/benchmark-isolation.ts`：`HostState`、`gitOutput`、`captureHostState`、`hostMutated`、`withHostMutationSentinel`。
- `apps/cli/src/benchmark-command.ts`：hostStateBefore/hostAfter、hostMutationPossible、最终 error/security outcome 分支。
- `packages/evaluation/src/benchmark-isolation.test.ts`、production E2E、相关 security outcome 构造。
- 查询全部调用者，确定 HostState/sentinel 是否落入已持久化协议；按实际兼容需求处理，不任意改所有 schema。

**怎么复现与怎么做**

1. 使用临时 Git 仓库和可注入的命令执行 seam，或仅对子进程 PATH 设置私有 fake git。正常阶段委托真实 git；故障阶段固定非零退出并写 stderr。不得改全局 Git、真实项目 PATH 或共享工作区。
2. 当前：正常→失败被 hostMutated 判 true；失败→失败判 false；真实 sentinel 在前后都失败时仍运行 callback，返回 hostMutated=false、details=[]。先固化这一复现。
3. 用结构化命令结果保留成功/失败、exit/signal/超时和受控错误细节。合法的空 porcelain 只能来自成功 status 调用；rev-parse 失败不能与合法源状态混同。
4. 采用清晰的三态比较或显式 error 结果。unknown 不表示“已确认无修改”，也不能沿用“child processes wrote outside workspace”的断言式原因。使用可识别的基础设施探测错误码/原因。
5. 若执行前必要探测失败，promotion-grade 动作不得启动；若执行后探测失败，结果不得被认证为可晋升的 clean 成功。不得靠禁用 sentinel、忽略错误或补默认 clean=true 取绿。
6. 健康探测真正发现 HEAD/status 变化仍拒绝并保留原始事实。所谓“其他测试可能修改”不能成为生产侧忽略真实变化的理由，应由测试隔离处理。
7. 将错误信息接入 R40 的诊断；避免输出无关命令环境和机密。不要对探测失败无限重试，也不要把它计作模型能力失败。

**怎么验收**

- [ ] 成功探测 + 无变化为 unchanged；成功探测 + 已知变化为 changed，原安全负例仍生效。
- [ ] before 失败、after 失败、两次失败、status 非零、rev-parse 非零、超时分别为可观察 unknown/error，不返回无错误的“未修改证明”。
- [ ] before 必要探测失败时真实受保护调用路径不执行动作；after 失败时不可产生 promotion-eligible 成功。
- [ ] 不把未知状态错误写成确定的子进程逃逸或真实仓库修改。
- [ ] helper 与真实 CLI/artifact 路径都覆盖；通过真实 sentinel 复验，不只测试新结果类型。
- [ ] 原 R27/R28/R33/R38 的有效 promotion fixture 仍通过；不放宽隔离/源码资格。
- [ ] 相关 benchmark-isolation、安全、CLI 测试与 typecheck 通过；报告明确 K02 与历史 K01 的因果仍需单独证明。

**交付**

最小 production 修复、确定性命令故障测试、`docs/E4-R41-report.md`。本项无需重写 Runtime；不能把它自动写成已经修复所有历史 INVALID。

## 6. E4-R42：隔离测试夹具与真实构建，避免共享 src/dist 相互干扰

### 可直接发送给 Agent 的任务提示词

你负责 E4-R42。修复 K03 的测试/构建隔离问题，保留真实 gate command 的端到端证据。目标不是把全套测试全局串行化，而是让会修改或编译同一资源的测试有明确归属。

**做什么**

1. R37 兼容测试生成的旧位置临时源码不能进入生产编译输入和共享 dist。
2. release-command 测试的真实 typecheck/build 不应与其他测试对共享源码、dist、tsbuildinfo 的读写无约束重叠。
3. 明确隔离变更解决的资源冲突；历史 INVALID 是否同源，仍需 R40 证据。

**先读哪里**

- `apps/cli/src/e4-r24-final-result-protocol.test.ts` 的 E4-R37：会写 `apps/cli/src/e4-r24-fixture-…-legacy-assert-fail.test.ts`。
- `apps/cli/tsconfig.json`：`include: ["src"]`、outDir=dist、共享 tsBuildInfoFile。
- `apps/cli/src/release-command.test.ts`：`runGate("typecheck", { root: process.cwd()… })` 以及 releaseGateCmd 的真实命令。
- `vitest.config.ts`、gate runner、测试工具与 package scripts、CI 工作流。

**已知证据**

本轮全量后发现 `apps/cli/dist/e4-r24-fixture-103-1789223680385-legacy-assert-fail.test.js` 及 .d.ts/.map 等 4 个文件，与当前测试生成物对应。旧源码不被 Vitest 执行不等于不被 tsc 编译。此现象不是历史 INVALID 根因证明，但共享构建输入确实被测试生成物污染。

**怎么做**

1. 先用可控夹具证明当前编译输入会包含该旧命名模式；不依赖压力测试碰巧重叠。
2. 对生成物采用窄范围 TypeScript 排除或在拥有相同配置的独立 workspace 中做旧位置兼容测试。排除必须只针对明确临时模式，不能把所有正式测试或业务源码排除掉。
3. 真实 gate runner 集成测试在自己的受控 workspace/checkout 执行，或建立仅覆盖必要共享资源的互斥边界。选择能证明真实命令和证据一致的最小方案；不要用固定 exitCode mock 替换全部真实验证。
4. 隔离环境要有真实 Git 身份、可解释 package/script 配置和自己的输出/构建缓存。证据 SHA 必须属于被测 workspace，不能复制主仓 SHA 冒充。
5. 对 green gate 与实际 nonzero gate 都跑真实 child command，验证 exitCode、passed、cleanBefore/After 和 CLI 结果一致。旧测试标题说 FAILING 但执行健康 chaos 后断言 0，不能将这种测试当作非零路径证明。
6. 临时数据清理只限自己创建的路径；不要运行全仓 dist 清空或删除其他进程文件。旧残留可报告并在自己测试环境清理，生产启动不加扫盘逻辑。
7. 定向同时执行相关协议/gate 测试，确认不产生对方的编译产物、不写对方 build info。无需对整个仓库反复压力测试。

**怎么验收**

- [ ] 旧夹具真实存在时：父测试不收集，TypeScript 也不把该临时源码加入生产输出；正式测试/业务源码仍在预期范围。
- [ ] 新目录 fixture 子进程真实执行成功/故意失败协议，不发生 0 tests 假通过。
- [ ] 真实 green/nonzero gate 的 evidence、stdout/stderr 和退出码保持一致；不依赖主仓当时是否有人编辑。
- [ ] 相关测试重叠运行时拥有独立资源或明确互斥，输出不会落入其他测试共享 dist/cache。
- [ ] dirty-source/真实 host mutation 的拒绝行为不回退。
- [ ] 报告分别写“构建污染修复”和“历史间歇失败是否重新复现”；无直接证据不合并归因。

**交付**

最小测试配置/隔离补丁、共享资源回归测试、`docs/E4-R42-report.md`。不以全局降低并发、增大所有超时或删除真实 gate 测试替代隔离。

## 7. E4-R43：用独立固定输入锁住公开容量上限

### 可直接发送给 Agent 的任务提示词

你负责 E4-R43。本项补 K04 的测试判别力，不预设 parser 的现有 1,000,000 上限实现有错，也不改变产品容量合同。

**做什么与根因**

R38-b 设 `repeat = EXECUTION_PLAN_MAX_PLANNED_SAMPLES + 5`，预期也用同一常量计算。令常量为 C、caseCount=3，输入规模为 `3×(C+5)`，改大 C 后仍超限。因此调大实现常量测试仍通过是输入随实现变化的自然结果，不需要用“可能超过安全整数”解释。

保留这种相对超限测试用于机制检查，同时增加独立的固定合同测试，分别验证允许范围和拒绝范围。

**怎么做**

1. 阅读 `execution-plan.ts`、R28/R33/R38 测试和 `docs/E4-R38-report.md`、R39 残余限制。
2. 以公开合同 1,000,000 planned samples 为独立依据写固定输入：单 case、limit=null，repeat 分别为 999999、1000000、1000001。期望不从被测常量派生。
3. 只调用 parser 的边界验证，不在这些测试中展开百万样本网格，避免新增巨量内存/时间成本。
4. 同时保留乘积不安全、非法预算字段、repeat 非整数以及有效完整 evaluator/loader 正例。不要把合同断言写成“永远只允许一个样本”等无关限制。
5. 在独立临时副本中将生产常量改为 2,000,000，固定 1000001 的拒绝断言必须失败；再改为 999999，固定 1000000 的接受断言必须失败。每次使用该副本真实加载的源码，记录实际变化和结果，结束后销毁自己的副本，不改主工作树。
6. 更新报告对旧 mutation 结果的解释。仅修改错误文案使测试失败不能代替容量边界行为的判别证据。

**怎么验收**

- [ ] 固定合同的 max−1/max/max+1 输入分别接受/接受/拒绝，max+1 给出容量 issue。
- [ ] 放宽/收紧生产常量的两种临时 mutation 都被固定输入测试捕获，不产生巨型 grid。
- [ ] 原有效 130000 case、真实 ACCEPT/loader.ok 与相对超限测试继续通过。
- [ ] 不修改正式容量上限，不新增机器敏感毫秒门限，不把测试改动包装成生产漏洞修复。
- [ ] `pnpm typecheck` 与相关 evaluation 测试通过，实际数量和退出码入报告。

**交付**

少量有判别力的边界测试、`docs/E4-R43-report.md`、历史报告的补充说明。已知合同锁定后停止扩充无关输入组合。

## 8. E4-R44：合并证据、完成 R39 的后续收口

### 可直接发送给 Agent 的任务提示词

你负责 E4-R44，依赖 R40～R43 的结果。最终结论必须区分“当前门禁通过”“独立缺陷关闭”“历史间歇失败已归因”三个不同事实。

**做什么、怎么做**

1. 阅读本轮补丁/报告及 R39 历史记录。建立 K01～K04 的证据矩阵：复现或缺口、修改、测试、实际被测版本、结论、残余限制。
2. R40 诊断可用但历史失败未出现时，写“诊断完成，历史问题 NOT_REPRODUCED/未归因”；不要写全部历史缺陷已关闭。K02/K03 可因独立复现和验收而单独关闭。
3. 在静止且可证明的最终工作区集中跑 `pnpm typecheck`、`pnpm test`、`pnpm docs:verify`；按修改范围运行 security/protocol/race/chaos 等必要门禁。同一最终版本已有完整记录可复用，不刷重复数量。
4. 本轮全量窗口内若失败，保存 R40 证据并根据具体新事实处理；不使用直到成功为止的 retry 策略。若必须追加验证，写明它要解决的具体不确定性。
5. 在 Windows 环境执行涉及 child process、路径、构建隔离的定向验收，或取得本轮新 SHA 的 Windows CI 证据。Linux 成功不替代 Windows。
6. 核实新提交的 Ubuntu/Windows、coverage、named usage audit 和 release job。不要把基线 67955e0 的 run 34689695442 归给新实现；失败、cancelled、skipped 保留真实状态。
7. 更新 plan.md 唯一入口、HANDOVER、中英文 README、E4-STATUS。采用一个清楚的当前状态摘要，历史 run 表注明范围，避免再逐次文档提交无限追踪自己的下一次 CI。
8. reviewedSourceSha/testedSourceSha/documentationCommitSha 分开。只查 job 状态就按状态报告；真正检查了 artifact 内容才能描述内容验证。
9. 真实模型效果与发布动作保持本轮范围外/NOT_RUN。未请求的付费 benchmark、架构重写或新功能不作为收口门槛。

**怎么验收**

- [ ] K02 的 unknown 不再伪装为 clean/确定 mutation，真实安全负例不回退。
- [ ] K03 有构建隔离的直接验证，不再生成对方工作区的临时编译输出。
- [ ] K04 固定边界与 mutation 证据有效，公开容量合同未被修改。
- [ ] K01 有失败证据保留机制，历史问题的复现/归因状态如实记录。
- [ ] 最终本地和可用 CI 的结果准确绑定被测版本；任何失败不被最后一次绿灯覆盖。
- [ ] 当前状态文档彼此一致，明确哪些完成、哪些 PARTIAL/NOT_REPRODUCED、哪些 NOT_RUN。
- [ ] 对每项剩余未知给出最小触发条件，不再无依据添加下一轮开发任务。

**交付与停止条件**

`docs/E4-R44-report.md`、必要当前状态修订、一页用户结论。若当前门禁通过但历史波动未归因，可同时报告这两个事实；不为了得到“完美完成”措辞而编造根因，也不为追求零未知无限开发。

## 9. 总控提示词

请依次执行本计划 E4-R40～R44。先核对最新 HEAD，已有修复通过验收则标 NOT_NEEDED。先补失败证据，再分别处理 Git 探测错误语义、共享构建隔离与独立容量边界测试。全部默认离线，保留安全与晋升门禁，不重做 R36～R38 已关闭内容，不覆盖用户工作树，不放宽断言、不循环重跑取绿。逐项报告真实被测版本、命令、退出码、限制；最后区分当前门禁、独立修复和历史未归因问题，收口后停止扩展。
