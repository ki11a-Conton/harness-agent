# harness-agent 下一轮执行计划：E4-R32～E4-R35

> 本文每个任务都是可直接交给编码 Agent 的提示词。目标是关闭本次审查得到的有限问题，并完成可追溯验收。不要为了持续开发而扩大范围。

## 1. 审查基线与结论

- 审查日期：2026-09-12。
- 仓库：https://github.com/ki11a-Conton/harness-agent
- reviewedSourceSha：`bcf3f42ca31fcf91c714c46745a90e7c91245f83`。
- 上轮基线：`493866f03d942e85870cc658eb721cfbf2389ec2`。
- 本轮增量：6 个提交，28 个文件，3077 行新增、632 行删除。
- 已通过 GitHub 插件核实提交、工作流、job 及 step 状态，并在本地读取实际源码、重建 dist、复跑测试和补充边界复现。
- 本计划不代表业务代码已修复；审查时没有修改业务代码、提交或推送。

### 1.1 已提交的工作，不能再写成“尚未提交”

| 提交 | 内容 | 本轮判断 |
|---|---|---|
| `f2f1b0b` | E4-R27 晋升资格语义 | 原 insecure-local 反例现为 INVALID，promotion loader 拒绝；原问题关闭 |
| `fb33ba9` | E4-R28 数字与规模约束 | 原四个字段反例全部拒绝；新增大数组边界仍需 R33 |
| `964ecc9` | README / HANDOVER / plan 索引更新 | 当时状态可作为历史保留，当前状态需要更新 |
| `8cfe6c1` | E4-R29 原始字节指纹 | 原 0x80/0x81 反例指纹已不同；原问题关闭 |
| `b09e220` | E4-R30 恢复故障重检 | 原 lease 写失败反例已有一个 timer；组合读写失败及 intent 退避仍需 R32 |
| `bcf3f42` | E4-R31 收口 | 报告主要记录提交前工作树状态；需要追加提交后的准确证据 |

原始复现本轮实测：

```json
{"field":"repeat","value":2.5,"accepted":false}
{"field":"maxModelCalls","value":-1,"accepted":false}
{"field":"maxLogicalRuns","operation":"delete key","accepted":false}
{"field":"maxEstimatedCostUsd","value":"invalid","accepted":false}
{"id":"INSECURE_PROMOTION","decision":"INVALID","promotionAccepted":false}
{"id":"BINARY_SOURCE_COLLISION","equal":false}
{"id":"LEASE_FAILURE_WAKE","scheduled":1,"calls":0}
```

### 1.2 真实验证记录

| 检查 | 本轮结果 | 解释 |
|---|---|---|
| `pnpm typecheck` | PASS，退出 0 | 实际执行 `tsc -b`，后续复现使用此次重建后的 dist |
| `pnpm docs:verify` | PASS，ALL CHECKS PASS | 不代表 HANDOVER 的所有自然语言状态都准确 |
| 全量 `pnpm test` 首跑 | FAIL：317 files；5719 passed / 1 failed | 收集了旧 hook-fail 临时夹具；另有配对 finalizedPairs 预期 4 实得 3 |
| `benchmark-command.test.ts` 定向重跑 | PASS：66/66 | 配对断言本次未再次出现，不能据此宣称根因已定位 |
| 全量第二跑 | FAIL：318 files；5721 passed / 1 failed | 仍有残留夹具；另有 E4-09 promotionEligible 断言波动 |
| 将确认未被 git 跟踪且被忽略的夹具移出收集目录后，全量第三跑 | PASS：316 files / 5718 tests，退出 0 | 未修改业务代码；污染文件保留用于诊断，没有借删除正式失败用例取绿 |
| 最新 SHA 的 GitHub Actions | PASS，run `34666585483` | 四个 job 均成功；两平台 strict usage audit step 均成功 |

CI：https://github.com/ki11a-Conton/harness-agent/actions/runs/34666585483

四个 job：Ubuntu 主门禁、Windows 主门禁、Ubuntu coverage、release attestation。本次核实了 API 状态与 step 结果，没有下载并独立重放所有 CI artifact，不能把“job success”描述成已逐字节复核所有证明文件。没有独立运行覆盖率、付费模型或真实 champion 质量实验。

### 1.3 下一轮问题清单

| ID | 优先级 | 证据与影响 | 任务 |
|---|---|---|---|
| H01 | P1 | intent 写入失败后，releaseLease 的 getRecord 再失败，会跳过 scheduleStoreRecheck；队头保留、action=0、timer=0，drain Promise 拒绝 | R32 |
| H02 | P2 | 连续 intent 失败时，成功 acquireLease 每次提前清零退避计数，6 次延迟均为 1000ms；与递增退避的声明不符 | R32 |
| H03 | P2 | 130000 个唯一 case、repeat=1，低于公布的 1000000 sample 上限，parser 在数组 spread 处抛 RangeError，尚未到达规模校验 | R33 |
| H04 | P2 | 故意失败的子进程夹具位于根测试 include 范围，残留后会被下一次全量测试收集；本轮已实际发生 | R34 |
| H05 | P2 | 最新代码已经推送且 CI 成功，但当前 HANDOVER/plan 仍写 R29～R31 未提交、CI NOT_RUN；R31 混用了审查基线与被测版本 | R35 |

范围说明：H01 证明的是异常逃逸和恢复停滞，不声称已发生外部动作重复。H02 有 1 秒下限，并非零延迟死循环。H03 证明公开 parser 抛异常，不声称已经造成生产服务远程拒绝服务。两次额外 E2E 断言波动是待归因观察，不单独宣布为确定产品缺陷。

## 2. 所有任务共同遵守的 Agent 提示词

你在 harness-agent 仓库执行下面指定的一项任务。先读取 AGENTS.md、plan.md 当前入口、本计划以及相关 tasks/ 文件；记录实际 HEAD 和工作树状态。若 HEAD 已变化，先检查本项是否已修复：满足验收则记录 NOT_NEEDED 和具体证据，不重复实现。

遵守 Runtime FROZEN。R32 仅因 H01/H02 的确定性复现获得最小恢复调度修改范围；其他任务不授权重写 Runtime。保留 ToolOrchestrator、PermissionEngine、SandboxManager 和 Verification。禁止降低资格、绕过校验或把未持久化动作当成已成功。

先建立能在当前代码上失败的针对性复现，再修改。负例应从有效正例派生，除组合故障任务明确要求外只改变一个因素。不能靠别的字段无效而提前拒绝来冒充目标校验通过。状态、调用次数和输出应来自真实生产入口，不能只测试新 helper。

默认全部离线，不调用真实付费 provider，不发布 release、不自动晋升 champion。保存用户未提交内容；有必要时使用隔离工作区。不要清空用户环境变量或移除安全控制来让测试通过；环境阻碍要记录具体边界并使用支持的隔离方式。

每项提交可审查补丁和 `docs/E4-Rxx-report.md`。报告必须包含：问题与范围、复现、根因、修改点、验收矩阵、实际命令/退出码/测试数量、实际被测 SHA 或明确的 dirty worktree 标记、尚未执行项。是否 commit/push 遵从当前会话已有授权；不得虚构提交号，也不得引用历史报告中的“未授权”限制覆盖当前用户授权。

执行顺序：R32 → R33 → R34 → R35。完成一项就停止扩大范围；R35 依赖前三项。每项至少运行对应测试与类型检查；同一最终代码版本的全仓门禁集中在 R35 完成。

## 3. E4-R32：修复恢复存储组合故障与 intent 退避

### 可直接发送给 Agent 的任务提示词

你负责 E4-R32。目标是使恢复操作在存储暂时故障期间保持 fail-closed，并在存储恢复后只通过已安排的 callback 自行继续；不得依赖新用户消息或手工再次 drain。

**做什么**

1. 修复 H01：intent 写失败后的 lease 清理读取错误，不能阻断后续重检安排，也不能形成未处理的后台 Promise 拒绝。
2. 修复 H02：连续 intent 写失败必须真正递增退避。一次成功的 lease 写不代表整条恢复写入路径已经恢复健康。
3. 限定检查相邻重检入口的 RecoveryStore.getRecord 读取失败。先复现，再修复同根因分支；不得扩大为整个存储层重构。

**先读哪里**

- `packages/core/src/runtime/session-actor.ts`：loadOrCreateRecoveryRecord、releaseLease、scheduleStoreRecheck、recoverHead、drainFollowups。
- 基线行号：765 附近直接读取 recovery record；816 附近 releaseLease 读取；1059～1061 提前清零；1088 在排 timer 前 await releaseLease。
- `packages/core/src/runtime/recovery-durable.test.ts`：R30 的 store fault injection、recordingScheduler、loadActor、buildBoundNonterminalTurn。
- `tasks/P2/SESSION-001.md`、`docs/E4-R30-report.md`；其他相关恢复任务按源码引用读取。

**怎么复现与怎么做**

1. 沿用现有 bound nonterminal turn 的真实 actor fixture。RecoveryStore 正常允许初始 PENDING/lease 写；当第一次写 `RECOVERY_IN_PROGRESS` intent 时抛出异常，并使下一次 getRecord 抛异常。不要通过直接调用 scheduleStoreRecheck 来制造“修复”。
2. 当前实测为：`rejected="recovery read outage", scheduled=0, calls=0, queue=1`。这个最小复现必须先被测试固定。
3. 将“安排重检”与“尽力清理 lease”解耦。清理错误要可观察；无论清理成功与否都保留一条有限唤醒路径。重进时读取最新 durable record，并尊重其他 owner 的有效 lease/CAS，不能用旧缓存强制释放他人 lease。
4. 为后台 callback 的异步错误设计局部处理；不能用空 catch 吞掉错误后永久停住，也不能递归立即 drain。
5. 对持续 intent 故障记录延迟序列。基线对同一 actor 连续 6 次重检得到 `[1000,1000,1000,1000,1000,1000]`。将退避计数重置放在代表本轮必要持久化步骤已成功的位置，明确重置语义。
6. 保留一个有效 timer、正延迟与最大延迟上限。故障重检不能额外消耗动作 attempt。不要把存储 I/O 故障记为模型失败或 EXHAUSTED。
7. 测试使用可触发 scheduler 和手动 clock；第一次允许显式 drain 启动，此后靠触发 actor 自己注册的 callback 验证恢复与连续退避。

**怎么验收**

- [ ] intent 失败 + release 读取失败：action=0，prompt 不 consumed，队头不 shift，安排恰好一个有效 timer，无未处理拒绝。
- [ ] 存储恢复后触发该 callback：重新读取 durable 状态，动作恰好一次，最终 RECOVERED，prompt consumed，队列继续。
- [ ] 重检回调期间 getRecord 再失败：继续安排有限重检，不挂死、不把 unknown 当成不存在、不执行动作。
- [ ] 长期 intent 失败：延迟确实增长并封顶，而非只断言 `next >= previous` 让全等序列蒙混过关。采用原策略时可验证 1s、2s、4s、8s、16s、30s、30s。
- [ ] 故障期间 durable attempt 不增长；健康恢复后按正常状态机计数。
- [ ] 恢复后其他 owner 仍持有 live lease：不抢跑；close 取消 timer，旧 callback 不复活 actor。
- [ ] R30 原 7 个测试和 marker-loss / reconcile 回归继续通过。
- [ ] 运行 `pnpm exec vitest run packages/core/src/runtime/recovery-durable.test.ts`、`pnpm test:race`、`pnpm typecheck`，记录实际结果。

**交付物与完成条件**

最小生产补丁、针对性故障测试、`docs/E4-R32-report.md`。报告分别列 H01/H02，不把“lease 写失败已会唤醒”当成 intent 组合故障证据。所有上述标准通过才 PASS；缺少自动回调恢复证据则 PARTIAL。

## 4. E4-R33：让 execution-plan 规模校验先于危险操作

### 可直接发送给 Agent 的任务提示词

你负责 E4-R33。修复共享 parseExecutionPlan 在大 case 数组上的确定性异常，并让已公布的容量合同与实际解析能力一致。保留 R28 已完成的预算字段校验，不重新设计计划协议。

**做什么**

1. 让合法容量范围内的计划不会因为数组 spread 触发调用栈错误。
2. 让超限计划以结构化 issue 拒绝，且拒绝发生在不必要的大规模拷贝、网格展开之前。
3. 消除本函数逐个 fingerprint key 调用 caseIds.includes 的明显二次方成员查询，使容量上限有可实现的校验路径。

**先读哪里**

- `packages/evaluation/src/execution-plan.ts`：parseExecutionPlan、EXECUTION_PLAN_MAX_PLANNED_SAMPLES、expectedSampleKeysFromExecutionPlan。
- 基线第 238 行 `caseIds.push(...caseIdsRaw)`、第 254 行 `caseIds.includes(k)`、第 274 行之后才执行的规模检查。
- `packages/evaluation/src/e4-r28-execution-plan-fields.test.ts`、`fixtures.ts`、`champion-eval-v3.ts`、`promotion-envelope.ts`。

**怎么复现**

从仓库根目录完成 `pnpm typecheck` 后运行下面的离线片段：

```js
// 保存为仓库外 .mjs 时请改成实际绝对 import；下面用于根目录 stdin module。
import { fixtureExecutionPlan } from './packages/evaluation/dist/fixtures.js';
import { parseExecutionPlan } from './packages/evaluation/dist/execution-plan.js';
const base = fixtureExecutionPlan({ suite: 'holdout', caseIds: ['a'], repeat: 1 });
const caseIds = Array.from({ length: 130000 }, (_, i) => `case-${i}`);
const plan = {
  ...base,
  caseIds,
  caseFingerprints: Object.fromEntries(caseIds.map(id => [id, 'a'.repeat(64)])),
};
try {
  const result = parseExecutionPlan(plan);
  console.log({ accepted: result.plan !== null, issues: result.issues });
}
catch (e) { console.log(e.name, e.message); }
```

基线实测：`RangeError Maximum call stack size exceeded`。将 case 数改为 3 可正常解析。repeat=1 时 130000 低于当前 1000000 上限；本复现不调用 provider。

**怎么做**

1. 在有限数字校验、数组类型/长度检查之后，尽早验证乘积安全性和容量上限。不要等遍历、拷贝所有内容后才拒绝。
2. 不把输入规模当作函数实参数量。复用合法数组、显式循环、slice 等均可；选择与当前只读约定相符的最小方案。
3. 复用已有 Set 做 case membership 校验，保持 duplicate、missing fingerprint、unplanned fingerprint 的原语义；不要为修复性能跳过完整绑定。
4. 保持 digest 协议和输入顺序语义，不排序或归一化后悄悄改变确认计划。
5. 默认保留当前公开上限；如果确有实测证据需要调整，报告内解释容量选择与兼容影响，并明确修改产品合同，不能只把上限调到复现值以下逃避修复。
6. 审查 grid helper 的实际调用者是否都先验证；如果存在直接展开未验证输入的入口，先复现再增加边界保护。无证据不要任意改变所有 helper API。

**怎么验收**

- [ ] 正常 3 case × 2 repeat：6 个唯一 sample key，原 canonical digest 保持不变。
- [ ] 130000 case × 1 repeat：按现合同成功解析，无 RangeError；测试不要把整份计划打印进日志。
- [ ] caseCount 本身超限、repeat × caseCount 超限、乘积不安全：均结构化拒绝，不展开网格、不抛运行时异常。
- [ ] 已确认预算四字段、null/0 语义、负数/小数/缺字段反例仍按 R28 拒绝。
- [ ] 大规模重复 case、缺 fingerprint、多余 fingerprint 均仍正确拒绝。
- [ ] 真实 evaluator / promotion-loader 对超限计划给出可解释拒绝，不泄漏原始 RangeError；正例继续通过。
- [ ] 大规模测试放入必要的 parser 单测；不要让每个集成组合都重复构造百万数据，也不要设置机器敏感的极小毫秒门限。
- [ ] 运行 R28 及新增 R33 测试、R27 eligibility 测试、`pnpm typecheck`，记录退出码与实际数量。

**交付物与完成条件**

共享 parser 的最小补丁、对应测试、`docs/E4-R33-report.md`。明确区分“范围内支持”和“范围外拒绝”；只捕获 RangeError 后返回 invalid，却仍拒绝公开范围内合法计划，不满足完成条件。

## 5. E4-R34：隔离故意失败的测试夹具，收敛验收噪声

### 可直接发送给 Agent 的任务提示词

你负责 E4-R34。让 final-result observation 的真实子进程测试可以保留故意失败的负例，同时不会因残留文件污染下一次全仓测试。顺带对本次两个非稳定 E2E 断言进行一次有限归因，证据不足时如实标记。

**做什么**

1. 消除 H04：生成在 `apps/cli/src/e4-r24-fixture-*.test.ts` 的子进程文件落入根 include 范围；`.gitignore` 只影响 Git，不阻止 Vitest 收集。
2. 让父测试收集与子进程专用夹具收集使用明确隔离的范围。
3. 保留真实 Vitest reporter、真实 final state、真实独立 usage-audit 端到端证明，不改成 mock reporter 的 helper 测试。

**先读哪里**

- `apps/cli/src/e4-r24-final-result-protocol.test.ts`：fixtureSource、runVitestSubprocess、cleanupFixture。
- `vitest.config.ts`：当前 include 与 reporter 配置。
- `apps/cli/test-infra/observation-vitest-reporter.ts`、`apps/cli/src/observation-evidence.ts`、`usage-audit.ts`。
- `.gitignore` 中夹具规则；`docs/E4-R31-report.md` 的历史环境说明。

**怎么做**

1. 先建立一个仍保留故意失败夹具的场景，证明当前父 suite 会错误收集它；这里模拟中断/清理失败，不实际破坏存储或关闭安全机制。
2. 选择父配置排除的专用临时夹具目录、或明确的子进程测试配置。父配置不能扫描子进程生成物；子配置必须精确选择该次文件并保留生产 reporter 接线。不要只全局 exclude 后使子进程找不到测试。
3. 对子进程结果同时断言退出码、实际测试数量及 committed/dropped evidence；防止“0 tests”或未启用 reporter 的假通过。
4. 每次测试使用自己的路径、runId 和 evidence directory，清理只处理本次生成物；不得批量删除其他 Agent 或用户文件。清理失败记录实际错误，残留即使存在也不应污染下一次父 suite。
5. 本次另见两项波动：benchmark-command.test.ts 的 finalizedPairs 4→3；E4-09 第一条链路的 promotionEligible true→false。前者定向 66/66，隔离残留后全量 5718/5718，根因尚未定位。不要预先归因于 R29 或操作系统。
6. 在有效隔离环境下保留一次针对性重跑的相关 paired artifact：partialPairs、invalid reason、source probe、isolation facts。若复现，定位后只修复有证据的基础设施或测试问题；若不复现，写明 NOT_REPRODUCED，保留诊断入口并停止循环重跑。不要扩大 timeout、放宽断言或减少用例来掩盖。

**怎么验收**

- [ ] 子进程 green 用例实际执行并提交观察证据，独立命名 audit 可读取。
- [ ] assertion-fail 和 afterAll-fail 子进程实际执行且失败，本次 run 不产生伪 PASS 观察，不借用旧 run。
- [ ] 故意保留一个失败夹具，再启动父 suite：该夹具不被收集；清理中断不再让下一轮多出测试文件。
- [ ] 两个不同 run 的夹具路径与证据相互独立；旧 run 不可冒充当前 run。
- [ ] 正常清理仅处理自己创建的文件；父配置排除规则不误排正式测试。
- [ ] 定向运行 observation 协议测试及相关 E2E；两个波动断言分别记录复现/未复现及证据，不能仅以最后一次绿色断言“已修复”。
- [ ] `pnpm typecheck` 通过；在 R35 再跑最终全量，不为同一风险无限重跑。

**交付物与完成条件**

夹具隔离补丁、必要的 collection 回归、`docs/E4-R34-report.md`。完成标准是残留存在时父测试仍正确收集，而不是每次运行前提醒用户手工删文件。本任务不需要为无法复现的波动编造修复。

## 6. E4-R35：更新提交后状态，完成最终验收并停止扩展

### 可直接发送给 Agent 的任务提示词

你负责 E4-R35，依赖 R32/R33/R34 的结果。你的目标是形成准确的最终状态，不预设每项一定 PASS。实际审核补丁与测试，不复制实施者的结论作为独立证据。

**做什么**

1. 将上一轮真实关闭与本轮边界补充明确分开，保持 R27/R29 原缺陷关闭，不把它们重新排进开发。
2. 修正当前入口/HANDOVER/README 的过期提交状态，保留历史报告当时的日期与证据。
3. 完成最终代码版本的本地门禁；有可用远端提交时，核实该 SHA 的 CI，而不是借用旧绿灯。
4. 给用户一页能直接判断“完成什么、还差什么”的结果，然后停止新增任务。

**先读哪里**

AGENTS.md、`plan.md`、`HANDOVER.md`、`README.md`、`README.zh-CN.md`、`docs/E4-STATUS.md`、`docs/E4-R31-report.md`、R32～R34 报告，以及 `.github/workflows/ci.yml`。

**怎么做**

1. 核查 R32 的组合故障、自行恢复与实际递增退避；核查 R33 的范围内成功和范围外结构化拒绝；核查 R34 的残留隔离与真实负例。缺少关键证据应标 PARTIAL，不能用总测试数补齐。
2. 为每项建立：问题 ID → 生产符号 → 测试名 → 实施 ref → 实际被测版本 → 结果 → 限制。
3. `reviewedSourceSha` 是计划依据；`testedSourceSha` 是测试所用提交；`documentationCommitSha` 是文档提交。若工作树带实现改动，写明 dirty worktree 并保留 diff 身份，不能将旧 HEAD 说成包含未提交修复。
4. 当前已有远端事实是 bcf3f42、run 34666585483、四 job success。R31 记录的“当时未提交/CI NOT_RUN”保留为历史，在顶部追加 superseded/current-state 指引。当前 HANDOVER/plan 不再继续描述 R29～R31 尚未提交。
5. 更新 plan.md 的唯一当前入口；可以把本文件保存为新的带日期 spec 并由入口指向它，避免多个文件同时自称“当前”。遵守 docs:verify 的真实约束，保持包数量等机器事实。
6. 审查 README 中的状态文字，同步过时的 local-pending-push/unfinished 描述。不是再写一次完整 README；只修改必要状态和证据链接。
7. 运行 `pnpm typecheck`、`pnpm test`、`pnpm docs:verify`、`pnpm test:race`、`pnpm test:security`、`pnpm test:protocol`、`pnpm test:chaos`、`pnpm e3:repro-current-defects`。专项门禁已在同一最终版本真实执行且有完整证据时可以复用，不需要为了报告重复运行。
8. promotion E2E 如要求干净源码，使用受控干净 checkout 完成验收，不能把未执行的干净树测试推断为 PASS。保留原始失败日志，注明清理/隔离了何种生成物以及之后结果。
9. 只读核查最终已推送实现 SHA 的 Actions、Ubuntu/Windows job、coverage、named usage audit、release attestation。若实现尚未推送，本地结果与远端 NOT_RUN 分开写；不能用本计划基线 bcf3f42 的 CI 证明 R32～R34 的新代码。
10. 若要声称 release attestation 内容或完整性已复核，应实际取得并验证产物；只查了 workflow/job 状态就仅报告状态。真实模型质量与发布动作未执行时保持 NOT_RUN。

**怎么验收**

- [ ] R32～R34 各项均有可追溯结果，缺口没有被“全套测试通过”覆盖掉。
- [ ] 最终全量运行的正式测试数量来自日志，无临时负例污染；失败不能只解释为“预期”后标全 PASS。
- [ ] 旧 G01～G04 原始复现继续阻断；报告准确写明 R28/R30 此轮补充边界。
- [ ] plan/HANDOVER/README 当前状态与实际 commit、push、CI 一致；历史证据保留但明确标日期/历史。
- [ ] 远端证据指向确切被测 SHA；没有将审查基线写入已修复测试的 testedSourceSha。
- [ ] docs:verify 通过；真实模型调用次数、未运行项、发布状态如实记录。
- [ ] 给出“已完成 / PARTIAL 或 BLOCKED / NOT_RUN”清单及下一步触发条件，没有新增猜测性架构任务。

**交付物与完成条件**

`docs/E4-R35-report.md`、必要的入口/交接/README 状态修订，以及一页最终结论。只有确切版本上的验收通过才能关闭；未完成事项说明具体原因和最小补足动作。没有新复现、生产问题或明确用户需求时，本轮到此结束。

## 7. 可直接复制的总控提示词

请读取本 plan.md，并在 harness-agent 当前代码上依次执行 E4-R32、R33、R34、R35。先核实 HEAD 与本计划基线差异，已修复项用真实验收标 NOT_NEEDED。每项先建立针对性失败复现，再做最小修复，最后按“怎么验收”逐条提供证据。默认离线，保留 Runtime FROZEN 与所有安全门禁，不擅自扩展功能，不用 helper 假结果代替真实生产链路。不要覆盖用户工作树，不借用旧 SHA 的 CI 作为新代码证明。将阶段性结果与实际未完成内容写入报告；最终全量验证集中收口，完成四项后停止。
