# harness-agent 下一步计划：完成 R98–R101 的剩余验收

> 审查日期：2026-09-20  
> 审查提交：`0075f723cfb6f6e38cf55829974b9d7634cf612b`  
> 上轮基线：`8dc0b2d4ac911b010ad5773ba801bab7d71b63ec`  
> 最新实现提交：`1ed8b92b971acf8368a7f627f60fc01d8e32fc49`；`0075f72` 只修改报告。  
> 使用环境：开发者本机 Windows；Linux 验收在 GitHub Actions 完成，无需安装 Linux、WSL 或 Docker。  
> 用法：先把“通用提示词”交给 Agent，再按顺序给它一个任务的完整内容。

## 0. 审查结论与本轮目标

这次提交有实质进展。上一轮的负数额度漏洞、同一 ledger 对象发现文件丢失后刷新额度、普通完成案例恢复时再次执行等问题已有修复。最新提交对应的 [CI 35488426033](https://github.com/ki11a-Conton/harness-agent/actions/runs/35488426033) 已完成，结论为 success。

但 R98–R101 的原验收条件仍未全部满足。主要问题已经从“没有状态与 worker”转移到“这些组件连接起来后，实际执行、预算、结果与恢复是否一致”。本轮只做六个收尾任务，不新增通用框架，不安排模型调优或扩大实验规模。

### 0.1 已完成的部分：保留，不重复实现

| 原任务 | 本轮确认的进展 | 仍不能据此宣称的结果 |
| --- | --- | --- |
| R98 账本 | 负数 consumed 被拒绝；同一已打开 handle 遇到 ledger 消失会报错；增加 campaign identity、显式 open mode 和 owner token | 生产调用方已正确使用这些能力；跨目录、重启和每次真实模型调用都已受控 |
| R98 恢复 | 有 case×arm 状态；正常完成后第二次运行新增调用为 0 | 状态丢失、篡改、并发、失败恢复和人工 reconciliation 全部正确 |
| R99 worker | worker 确实调用 arm 自己的构建；补上单例 dry-run digest；有报告分类与错误脱敏 | 工具成功路径已离线验收；每个案例的多轮调用被正确记账；结果可独立复验 |
| R100 身份 | driver 调用了执行观测检查；加入 driver 文件摘要；fingerprintCaseInCheckout 的 fallback 已删除 | 正式入口重新读取了实际输入；worker 的模型、endpoint、运行字节均与批准一致 |
| R101 CI | 历史 checkout 构建和 D6 dry-run 在 Ubuntu CI 实际执行；不再因缺少 arm 静默 skip | 两个历史构建都执行了完整成功案例；Windows 的专用双版本路径已验证 |

### 0.2 本轮确认的剩余问题

优先级中的 P0 表示“阻止把当前链路作为真实付费 A/B 的合格执行器”，不表示已发生线上攻击或真实超支。

| 编号 | 优先级 | 代码位置 / 事实 | 影响与任务 |
| --- | --- | --- | --- |
| N1 | P0 | `r97-arm-worker.mjs:731` 起，`reservationCount=1`、默认 `maxModelCalls=10`、dispatch 后固定 `consumed=1`；未读取实际 model_calls/retries | 账本统计的是案例启动次数，不能证明模型调用上限。交给 T1 |
| N2 | P1 | driver 打开 ledger 未传 mode；worker 显式 `mode:"auto"`；driver 不消费 `duplicateCampaignDirs` | 同授权换目录仍可获得新预算，重启后缺失账本也可能按首次创建处理。交给 T1 |
| N3 | P1 | execution-state 的 read 在文件缺失时返回空状态；begin 允许覆盖 running；recoverInFlight 不判断 owner；retry reconciliation 后 isDone 仍返回 true | 重跑、抢占活跃执行和授权重试被跳过。交给 T2 |
| N4 | P1 | isDone 只判断状态；不验证 resultHash/inputDigest；driver 汇总只看本次新增结果 | 篡改结果仍被跳过；上次 PARTIAL 的 provider 失败在第二次变成 COMPLETE。交给 T2/T3 |
| N5 | P1 | classifyReport 找不到 task_id 时使用 results[0]，并把 success=true 直接报告为 verifierPassed；finally 删除原报告；resultHash 仅摘要化描述文本 | 其他案例或不完整报告可被接纳；没有保存可复验的真实结果。交给 T3 |
| N6 | P0 | driver 调 worker 时没传批准的 provider/model/endpoint/input digest；worker 默认 openai/gpt-4o-mini，dispatchArgs 无 endpoint；main 的 fake 模式仍读 plan.observation | 批准内容与实际请求没有完整绑定。交给 T4 |
| N7 | P1 | observeArms 规划 driver repo 的 staged 输入，却 fingerprint arm 原目录；worker stageCase 又允许从 driver repo 查找；armBuildIdentity 漏掉实际导入的执行模块 | 观测、运行输入和构建字节可能不一致。交给 T4 |
| N8 | P1 | runChild 只发 SIGTERM；SIGKILL_GRACE_MS 未使用；stdoutChunks.length 被当成字节上限 | 忽略 SIGTERM 的子进程使 worker 不返回；输出上限单位错误。交给 T5 |
| N9 | P1 | driver main 没有进入 arm-worker 模式的正式 CLI；D6 仅做双 arm dry-run；W8 worker 测试用当前 REPO；专用 job 仅 ubuntu | CI 成功尚未覆盖“正式入口→两个版本→实际工具→验收→恢复”的同一条路径。交给 T4/T6 |

### 0.3 独立复现结果与边界

本轮从固定 SHA 下载源码，在隔离目录用 Node v24.19.0 执行了两个诊断脚本，均正常结束，无外部 HTTP/model 请求。没有运行最新仓库全套测试，没有在 Windows 本地复测，也没有执行真实付费实验。

driver 探针使用“已经授权”的测试 gate，以及执行观测检查的测试替身，隔离 gate 之后的行为；driver、ledger、execution-state 使用原始模块。它不证明授权可绕过。worker 探针用一个合成子 CLI 返回报告，检验父 worker 的预算、分类和清理行为；这不是实际模型消耗或真实案例通过的证据。

| 探针 | 实测结果 |
| --- | --- |
| 旧 ledger 漏洞复验 | `negativeRejected=true`；已打开 handle 的文件消失后 `missingAfterOpenRejected=true` |
| 正常恢复 | 一例×两臂第一次 2 次，第二次新增 0 次 |
| 修改结果与输入摘要 | 把 terminal record 的 resultHash 改成 64 个 0、inputDigest 改掉，恢复仍 COMPLETE，跳过 2 个单位 |
| 仅删除 execution-state.json | 同计划再运行新增 2 次，累计 committed 从 2 变 4 |
| provider 失败后恢复 | 首次 PARTIAL / 2 failures；第二次 COMPLETE / 0 failures / 0 measuredUnits / 0 新调用 |
| 同授权换 ledgerDir | 新目录又执行 2 次；结果 COMPLETE、code=null，结果中没有 duplicateCampaignDirs |
| 状态所有权 | 同单位第二次 begin 被接受；当前活进程的 running 被 recover 改成 unknown；reconcile(retry) 后 isDone=true |
| 不匹配报告 | 只有 different-case，success=true、verification_passed=false 的报告被判 passed=true |
| worker 参数捕获 | 批准模型为 approved-model；收到的 modelId/providerId/endpoint/inputDigest 均为空 |
| worker 合成子进程 | campaign grant=1；子 CLI 收到 maxModelCalls=10；报告声明 3 calls；worker 和 ledger 均只记 1，原报告被删除 |
| 构建摘要 | 改变 main.js 实际导入的 benchmark-command.js，子进程加载到新内容，但 buildDigest 不变 |
| 超时 | timeout=300ms；1500ms 时子进程已收到 SIGTERM，却仍存活，worker 未返回；诊断 watchdog 清理了进程组 |

### 0.4 一个可直接利用的现有接口

“离线不可能得到通过，必须付费”不是仓库能力的限制。

本轮检查了 baseline `e9776ba66190ea63b1bacb685c91aa900b6935e7` 和 candidate `a20373743b56de6a3a110fecdd254737ece71afa`：两者的 `apps/cli/src/benchmark-command.ts` 都导出：

```ts
export async function runBenchmarkCommand(
  argv: string[],
  providerOverride?: ModelProvider,
): Promise<{ exitCode: number; lines: string[] }>;
```

因此可以让子进程加载各自构建的该入口，注入 ScriptedModelProvider，执行真正的 request、工具和 TaskVerifier。无需把私有 runOneCase 导出，也无需付费才能验证工具写文件的成功与失败。

但该旧接口把 providerOverride 标为 offline-test。**不能给它塞真实付费 provider 后仍保留 offline-test 身份**。T1/T4 必须明确正式 transport 的预算接入与身份合同；若需要兼容适配，适配代码也属于被批准的执行材料。

## 1. 通用 Agent 提示词

你负责 `ki11a-Conton/harness-agent` 中下面指定的一项任务。目标是关闭本计划明确列出的缺陷，保留已经工作的代码。

1. 先读根 AGENTS.md、适用 tasks 文件、package.json，以及该任务列出的源码和测试。运行前记录 HEAD。若 HEAD 已变化，先复验问题；有实现和有效反例测试证明已修复时，报告 ALREADY_FIXED。
2. 遵守 Runtime Freeze。此次依据是可确定复现的预算、恢复、身份和执行证据缺陷；优先修 CLI/evaluation/provider adapter。工具副作用仍经 ToolOrchestrator、PermissionEngine、SandboxManager、Verification。
3. 不因为某个内部函数未导出就复制整个执行器。先查已导出的 runBenchmarkCommand、现有 createBudgetedProvider、paired executor/journal 和 campaign validator，复用其合同。
4. 不把新增模块、测试数量或报告行数当作完成标准。每个任务先形成最小 RED，再让同一个反例 GREEN；保留证据。
5. 本轮默认零外部模型请求。允许 ScriptedModelProvider、fake transport 或明确隔离的本地回环服务。不得读取或打印真实 key，不通过真实付费试跑证明基础正确性。
6. 实现和离线验收继续执行，不在任务中途反复请求用户确认。付费执行只有在已有授权覆盖最终计划、digest、预算和有效期时才能做；本次审查请求不提供该授权。
7. 不 reset、stash 或删除用户未提交内容。需要干净环境时使用隔离 checkout；日志和试验产物放到忽略目录或 checkout 外。
8. 不改旧实验结果，不扩充 holdout，不提高 budget 换取通过，不把所有验证都替换成 fake。新的离线 fixture 保持独立，不改变原 regression 的冻结计数。
9. 每个任务交付可审查的实现提交与简短证据。报告写实现 SHA 和对应 CI URL；不要为了让报告包含自己的提交 SHA 无限追加文档 commit。
10. 当前 `pnpm test`/`test:coverage` 排除了 r97-driver-closed-loop.test.ts。涉及 driver 的改动必须额外运行专用测试，不能只报告 pnpm test 成功。

### 实施顺序

| 顺序 | 任务 | 前置 | 完成后得到什么 |
| --- | --- | --- | --- |
| 1 | T1 / R98-A：真正共享的调用预算 | 无 | 每次模型调用前都有预算，目录和重启不会重授额度 |
| 2 | T2 / R98-B：可靠的案例状态机 | T1 的 campaign 生命周期合同 | 状态丢失不重跑、活 owner 不被夺取、reconcile 真正生效 |
| 3 | T3 / R99-A：结果证据与恢复汇总 | T2 | 保存原始报告，严格验收，恢复前后结论一致 |
| 4 | T4 / R100-A：正式入口与实际执行身份 | T1–T3 | 同一入口运行两份真实构建，输入/模型/endpoint 可验证 |
| 5 | T5 / R99-B：有界停止和取消 | T4 执行入口 | 超时、取消和超量输出都能结束执行且留下正确状态 |
| 6 | T6 / R101-A：跨平台离线闭环 | T1–T5 | Windows/Ubuntu CI 验收整条路径，再结束本轮 |

---

## T1 / R98-A — 把预算接到每次模型调用，并统一 campaign 生命周期

### 可直接发送给 Agent 的提示词

你负责 N1/N2。当前账本本身加强了，但 worker 仍用一例=一次的假设，调用方仍用 auto。请修实际执行边界，不再增加独立的 Rxx 预算框架。

**主要文件：**

- scripts/e4/r97-arm-worker.mjs
- scripts/e4/r97-campaign-driver.mjs
- packages/evaluation/src/r97-budget-ledger.ts
- packages/evaluation/src/paired-executor.ts 中 createBudgetedProvider 的现有合同
- 两个历史 arm 的 benchmark/provider 入口，以及对应契约测试

### 做什么

1. 让 campaignModelCalls 限制真实逻辑 generate 尝试，覆盖两臂、root、delegated worker 和恢复。
2. 把逻辑调用、transport retries、已发送但结果未知分开记录。
3. 区分首次创建、恢复、迁移，禁止同一授权通过目录切换或丢失账本获得新额度。

### 怎么做

1. 写 RED：额度 1 时子执行器试图连续 generate 3 次；旧 worker 允许按一个单位结账。新增测试必须计数真正进入 fake ModelClient.generate 的次数，不只看伪造的 model_calls 字段。
2. 选择最小 provider decorator/预算通道，遵循现有 createBudgetedProvider 的调用位置。在每次 generate 离开执行边界前，原子 reserve 一次；没有可靠预算通道就拒绝发送。
3. 先用各 arm 已导出的 runBenchmarkCommand(argv, scriptedProvider) 验证多轮离线路径。正式 provider 不能借 providerOverride 被标成 offline-test：必要的兼容 adapter 应明确记录 ABI、源 SHA 与 adapter/build digest，不能悄悄修改历史 arm 后仍宣称是原构建。
4. 取消 reservationCount=1 和 dispatch 后 consumed=1 的案例级假统计。case→多个 reservation 的关系应记录在同一个 campaign 状态中。事后解析 model_calls 只用于核对，不用于补救已经超额的请求。
5. runtime、工具循环和子代理创建的 client 必须使用同一预算包装；不要仅包顶层一次调用。transport retry 按已有协议单独计数，不把每次 HTTP retry 伪称新的逻辑 generate。
6. 在正式 driver 的初始化阶段选择明确模式。已有 campaign header 或执行结果时缺 ledger，应 BUDGET_STATE_MISSING；不可因为新进程丢失 established 内存标记而重建。
7. 固定 campaign 根目录及 header 合同。若允许迁移，移动并校验完整预算、journal、结果及身份；单纯换 --out/--ledger 必须拒绝或要求新授权。临时目录里的 advisory claim 不能是唯一权威状态。
8. 保留当前 schema 严格校验和 owner lock 修复。遇到 duplicateCampaignDirs 必须影响执行结果，不能仅挂在未被使用的 handle 属性里。
9. 不承诺能防御操作者删除整个机器所有证据。保证正常启动/恢复/迁移流程不重复授予同一授权；在报告中写清此边界。

### 怎么验收

- campaign grant=3；baseline 某案例调用两轮，candidate 只剩一轮；任何第四次 generate 在进入 provider 前被拒绝。
- 单案例需要多轮和子代理调用时，每一轮都有对应 reservation；跨进程争最后一个额度最多一个成功。
- 同计划恢复新增 0 调用；已发送后进程终止的 reservation 保留 unknown 占用，不自动退款或重发。
- 同一目录重启前删除 ledger、保留 header/状态：拒绝且 0 调用。换新目录继续同一授权：拒绝且 0 调用。
- 缺少预算 IPC/ledger、故障写盘、超过授权预算时，真实与离线模式都不会绕过检查。
- report 的 model_calls/retries 与 ledger/实际 fake client 计数一致；不把 case 数或进程启动次数当模型调用数。

针对性命令：

```powershell
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-budget-ledger.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts
```

新增预算集成测试应放入上述契约文件或同等专用测试，并在 T6 必跑。若历史 arm 的正式 provider 无法可靠接入，明确 BLOCKED；不得以离线统计正确宣称真实预算已生效。

---

## T2 / R98-B — 修复案例状态的丢失、所有权、并发与 reconciliation

### 可直接发送给 Agent 的提示词

你负责 N3 和 N4 中“终态记录能否被信任”的部分。不要改掉已经正确的正常 resume=0 行为。T1 的 campaign 生命周期是前置。

**主要文件：** r97-execution-state.ts、r97-campaign-driver.mjs、r97-arm-worker.mjs 及 execution-state/driver 测试。

### 做什么

1. established campaign 的 execution-state 缺失不能变成空状态。
2. 一个单位同一时刻只有一个有效 owner/attempt；恢复不抢占活进程。
3. 让显式 retry reconciliation 真正允许一次受预算约束的新尝试。
4. 校验记录身份、输入摘要及与 ledger 的关联；把合法跳过与异常拒绝分开。

### 怎么做

1. RED 覆盖：删除 state 后重跑；同单位连续 begin 两次；活进程 running 被 recover 改 unknown；reconcile(retry) 后 isDone=true 导致 driver 永远跳过。
2. 使用与 T1 一致的初始化/恢复判断。已建立 campaign 发现 state 缺失、损坏或 foreign 时停止，不执行 read() ?? emptyState()。
3. 用同一已有 campaign lock/事务合同保护 read-modify-write。仅靠写临时文件再 rename 不能防两个 writer 互相覆盖；不要再造第三套锁实现。
4. begin 必须原子地检查可启动状态并持久化 owner token、pid/host 或等价租约证据。running 状态拒绝第二次 begin；旧 owner 不能完成新 attempt。
5. recover 先验证 owner。活 owner 是 BUSY；确认死亡或合法过期的 owner 才能转 outcome_unknown。跨主机无法判断时保守停止，不能把“不知道”视为死亡。
6. 给 reconciliation 使用明确结构字段和状态转换，不依赖 detail 文本。retry 决定应在 dispatcher 的下一次选择中生效，绑定原 input digest 与新 attempt；旧 UNKNOWN 的额度按 T1 合同保留。
7. 保留旧 attempt 的审计记录。不要用新 attempt 覆盖唯一一条记录后丢掉旧 reservation；可以扩展现有 journal，不要求新增数据库。
8. parser 严格检查每条记录的 experimentId/planDigest 与 header 一致，attempt/reservation 唯一关系，时间和状态组合合法。不能把缺 startedAt 静默变成 0。
9. 修复 worker 传 `now` 函数给 begin 的 `now?: number` 字段的问题，传实际时间值。用当前模块接口保证时钟类型一致。
10. skip 前验证当前单位的 input identity，关联的结果引用交给 T3 校验。完成状态不能仅靠“有一个非空 resultHash 字符串”成立。

### 怎么验收

- 删除当前 handle 或重启后所需 state 文件：命名拒绝、0 新调用，原 ledger 不变。
- 两个进程竞争同一 case×arm×repetition：只有一个 begin 成功，最多一次进入 provider；独立单位并发不丢记录。
- 第二次打开 campaign 不会将活进程的 running 改 unknown；死 owner 被隔离后不会自动重发。
- reconcile(retry) 后 dispatcher 真正执行一个新 attempt，且受剩余预算和原输入摘要约束；没有授权决策时 0 重试。
- accept-as-failed 保持失败终态；完成单位不可通过 reconciliation 随意重开。
- 同时修改输入摘要、record/header identity、时间字段和 reservation 引用的反例全部 fail-closed。
- 测试使用握手/屏障控制进程顺序，不用“多 sleep 一会儿”碰运气。

```powershell
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-execution-state.test.ts packages/evaluation/src/r97-budget-ledger.test.ts
```

---

## T3 / R99-A — 保存真实结果，严格分类，并从完整历史重算汇总

### 可直接发送给 Agent 的提示词

你负责 N4 的恢复汇总和 N5。目标是让离线 validator 只凭持久化产物重算结论；恢复不能清掉过去的失败或分数。

**主要文件：** r97-arm-worker.mjs 中 classifyReport/resultHashFor/清理路径；driver 的 skip 和汇总；现有 benchmark/campaign validator 与 paired journal。

### 做什么

1. 持久保存本次单例报告、必要事件和结果身份，不在 finally 中删除唯一证据。
2. 报告必须严格对应当前 case、arm、attempt、输入与构建。
3. 区分 benchmark success、verifier pass、基础设施失败和运行完整度。
4. resume 从所有已验证的持久结果汇总，保持同一 campaign 的结论稳定。

### 怎么做

1. 写 RED：仅有其他 task_id 的报告不能匹配；缺字段/重复结果不能通过；文件写入案例 success=true 但没有对应验证证据不能被称为 verified pass；provider 失败后 resume 不能变成功。
2. 删除 `find(...) ?? results[0]` 的默认接纳。完整 ID 与 CLI bare ID 用明确、无歧义映射；只有请求的那个案例才可被接纳。验证 schema/version、suite、计划和 build/attempt 的关联。
3. 复用实际 report/validator 合同。不同 suite 可能以预期拒绝或预期失败为成功，不能一刀切要求所有 benchmark success 的 verification_passed=true；但 verifiedPasses 必须确有 verifier 证据，名称与含义一致。
4. 原始报告落入 campaign 的不可混淆 attempt 目录，校验后计算字节 hash 和相对路径，再原子写 terminal journal。清理只能删除 staging/workspace 临时内容。
5. 并发 attempt 和 repetition 使用不同路径，避免旧报告冒充新执行产物。不能因为看到退出码 0 就接纳旧文件。
6. 保存足以重算的安全结果和必要事件；敏感文本先脱敏，不以泄露凭据换取完整证据。固定脱敏后产物的 hash 合同。
7. 恢复时重新计算 artifact hash，检查 input/build identity。缺报告、hash 变更、缺臂、重复例或 foreign attempt 均拒绝，不能自动重跑覆盖原证据。
8. 汇总读取“历史已验证结果 + 本次新结果”。将 newCalls/newUnits 与 cumulativeCalls/measuredUnits/failures 分开；不能用本次 unitResults=[] 得出历史没有失败。
9. 普通 case_failed 是有效负例；provider/harness/infrastructure/unknown 不是有效评分。选择清晰的 COMPLETE/PARTIAL/REFUSED 合同并贯穿恢复与 validator。
10. 错误 event 后再出现 completed 不能抹掉先前错误；流缺少合法终态不能产生 verified pass。

### 怎么验收

- 错案例、空报告、重复 task_id、旧 attempt 报告、不匹配 suite/build 均拒绝。
- 文件写入 fixture：“只说 done”验证失败，真正工具写入才通过；预期拒绝类案例按其自己的 judge 合同评分。
- 首次全是 provider_error → PARTIAL；恢复新增 0 调用，但历史 failures 与 measuredUnits 的含义和数量保持不变。
- 首次含真实通过/有效负例 → 恢复新增 0 调用，累计分母、分子和 budget 完全一致。
- 修改/删除任意已关联原始报告或 resultHash，恢复及独立 validator 都非零退出。
- worker 结束后原报告仍存在；validator 读的是本次 driver 的产物，没有另跑 R87 replay 来代替验收。

```powershell
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-arm-worker-contract.test.ts packages/evaluation/src/r97-execution-state.test.ts
```

交付实际可用的 campaign 验证命令，并写入 help/报告。优先扩展现有 validator；若当前 schema 不兼容，先写清最小适配，不复制整套验证系统。

---

## T4 / R100-A — 打通正式入口，把批准身份绑定到实际执行

### 可直接发送给 Agent 的提示词

你负责 N6/N7/N9 的入口部分。修复“摘要检查通过但实际 worker 使用另一套参数”的问题。前置为 T1–T3 的预算、状态与结果合同。

**主要文件：** driver 的 main/runDriver/observeArms；worker 的 dispatch/stage/build identity；r97-plan.ts；r97-observe-arms.mjs；必要的最小 benchmark adapter。

### 做什么

1. 提供版本化、help 可发现的 campaign CLI，实际进入 arm-worker 路径。
2. 离线与正式模式共享案例调度、工具、verifier、状态、预算和结果逻辑，只在 transport/provider 配置上区别。
3. 在执行入口重新观测真正要执行的构建和输入，不复用 plan.observation 作为当前事实。
4. 把模型、endpoint、输入与 source/build/adapter 身份传递到实际 worker，并验证它们被使用。

### 怎么做

1. 删除或隔离固定 `messages:[{role:"user",content:"r97"}]` 的 provider 冒烟路径。若保留开发工具，必须有独立类型/命令，不能生成正式 campaign COMPLETE。
2. 实现启动/恢复入口、参数校验和 help。默认没有执行意图或没有匹配授权仍为 NOT_RUN；正式 provider 只能在有效 gate 之后构造。
3. 离线 worker 优先调用每个 arm 自己导出的 runBenchmarkCommand(argv, scriptedProvider)。子进程应记录真实模块位置与 build identity；不能把两臂都导入当前 REPO。
4. 为两个 r98-fixtures 配置不同脚本响应：一组产生真实文件工具调用后结束，另一组只有完成文本。捕获实际模型输入及工具动作，不能用两个不同 caseId 产生不同 resultHash 代替上下文验证。
5. 构造一个规范化 ExecutionSpec，包含实际 case inventory、输入摘要、source SHA、build/adapter hash、provider、model、endpoint identity、有效期、cap、输出根与执行模式。driver、worker、ledger/header 共用同一合同。
6. 显式向 worker 传递批准的 providerId/modelId/endpoint 以及输入摘要。worker 必须使用它们构造请求；不能回落到 openai/gpt-4o-mini 或环境里的另一 endpoint。缺必需字段直接拒绝。
7. 明确冻结输入来源。采用同一 snapshot 时，两臂规划和执行都读取该 snapshot；采用各自输入时各自指纹与执行一致。staging 后再次验证实际字节，worker 不得悄悄找 driver repo 的替代案例。
8. 构建摘要覆盖实际加载的执行产物及必要本地依赖，使用字节 hash，不依赖 mtime/size。至少覆盖 benchmark-command、runtime、provider/verification 的实际执行依赖与 adapter；范围用构建清单定义，避免把整个工作目录随意打包。
9. 当前 driver digest 哈希了若干 src/*.ts，但运行导入的是 dist。修为构建产物身份或可验证的 source→artifact 映射；仅更改执行 dist 也必须导致旧批准失效。
10. 观测函数在正式入口由程序执行；planObservation 保留为历史材料。比较函数接收一个名为 observed 的对象，不等于它已经被重新观测。
11. 最终计划绑定整组 case；单例执行摘要应由该批准计划中的 unit 确定。不能只对当前任意单例 dry-run 生成 digest 后自行接纳，而不验证它属于被批准的输入集。
12. 正式执行时间来自实际时钟；下一次调用前继续检查有效期与取消。`--now` 之类测试时钟只允许隔离离线模式，不作为生产过期判断。
13. 需要改变历史构建的兼容层时，记录原 source SHA、适配内容和新 build digest，生成新计划。保留旧实验，绝不把改过的构建伪称未修改历史版本。

### 怎么验收

- 一个已提交的正式命令从 plan 到两臂执行，无需用户临时编写 .ci/*.mjs 或手工改 plan.json。
- 离线相同路径得到真实工具写入的通过和“只说 done”的失败；两臂模块位置及 build identity 各自正确。
- 批准非默认模型和本地测试 endpoint，实际捕获请求中的 model/目标地址与批准一致；污染环境默认值不能改变请求目的地。
- 保留旧 observation，分别修改 staged case、实际 benchmark-command.js、runtime/provider 产物、model/endpoint/adapter：每项均在首个外部请求之前拒绝。
- 缺 arm 输入不会从 driver repo 回退；调换两个 worker 构建必须被发现。
- 运行中授权过期，下一调用被拒绝；已经发出的尝试不虚假退款。
- 未授权入口不构造真实 provider；离线身份不可被用来启动真实外部 transport。

```powershell
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-plan.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts
```

准备好两臂后补跑 r97-driver-closed-loop.test.ts。明确接口和验收完整性优先，不通过增加通用配置层来扩大任务。

---

## T5 / R99-B — 让超时、取消和输出上限真正结束执行

### 可直接发送给 Agent 的提示词

你负责 N8，并把授权期限的停止机制接入 T4 的真实执行路径。原 runChild 在超时只发送 SIGTERM，合成子进程忽略该信号后 worker 一直不返回；修复这个已复现问题。

**主要文件：** r97-arm-worker.mjs 的 runChild、driver 的 campaign 控制和预算/状态收尾。优先复用仓库已有进程管理能力。

### 做什么

1. campaign deadline、unit deadline、用户取消均可终止实际执行。
2. 处理不响应终止信号的子进程及其后代；所有路径有限时间返回。
3. 正确限制 stdout/stderr 的字节量，并维护中断后的预算/状态证据。

### 怎么做

1. RED：启动只使用本地 CPU/定时器的子 fixture，安装忽略 SIGTERM 的 handler。短 deadline 后收到信号但不退出，旧 worker 不返回。
2. 实现有界停止流程：请求取消→有限宽限→平台适配的强制结束→等待 close/回收。用现有进程工具合同；不要只停止父进程而留下工具子孙进程继续写文件。
3. Windows 和 Linux 的终止机制分别实现并测试。用户本机不需要提供 Linux；Ubuntu CI 负责 Linux 反例。
4. 给外层调用提供 AbortSignal 或等价明确取消句柄。dry-run/staging/dispatch 共用剩余总期限，不能每个阶段重新获得一整份 campaign 时间。
5. 捕获 spawn 失败、close/error/abort 竞态，保证 promise 只完成一次，timer/监听器回收。区分确定未启动、已启动已发送、结果未知。
6. 用 Buffer 字节长度累计 stdout/stderr；超过限额时停止或写入有界日志，不能用数组 chunk 数量当字节数。
7. 取消后保留已发调用的预算；不明请求归 UNKNOWN。记录 timeout/cancel/output_limit，而非伪装为 case_failed 或 verified pass。
8. 保留现有错误脱敏；诊断中只使用合成 canary，不读取真实凭据。

### 怎么验收

- 忽略 SIGTERM 的子 fixture 在 deadline+grace 的规定上限内退出；worker 返回命名结果，没有遗留后代。
- provider 流 hang、工具子进程 hang、dry-run hang、用户取消均可结束；case 和 reservation 的最终状态符合合同。
- 测试独立 watchdog 清理只是防测试卡死；被测实现未自行停止时必须失败，不能用测试清理当成实现成功。
- 单个巨大 chunk 和许多小 chunk 都受相同字节限额约束。
- 超时后 resume 不重复发出 UNKNOWN 请求，不把中断结果改成 COMPLETE。

```powershell
pnpm typecheck
pnpm exec vitest run packages/evaluation/src/r97-arm-worker-contract.test.ts packages/evaluation/src/r97-execution-state.test.ts packages/evaluation/src/r97-redaction.test.ts
```

---

## T6 / R101-A — 在 Windows/Ubuntu CI 验收同一条完整离线路径

### 可直接发送给 Agent 的提示词

你负责 N9 的最终验收。T1–T5 完成后，把正式 campaign 入口作为验收对象。保留本轮已经跑通的真实 checkout/build 步骤，补齐缺失行为；不要把“测试名称出现了”当成双版本案例执行证据。

**主要文件：** .github/workflows/ci.yml、r97-observe-arms.mjs、driver/worker 集成测试、package.json、R98/R99–R101 报告。

### 做什么

1. 专用集成 job 在 Windows 和 Ubuntu 都运行；所需 arm checkout/build 自行准备。
2. 从正式入口到结果 validator 做实际两臂成功/失败案例、恢复、预算、身份和取消验收。
3. 纠正报告完成范围，交付具体可运行命令和最终状态。

### 怎么做

1. 将现有专用 job 改为跨平台矩阵，路径使用 runner.temp/Node 临时目录。Windows 本机运行说明用 PowerShell/Node，不要求 bash、WSL 或 Docker。
2. setup 命令必须是仓库中真实存在的脚本。明确 setup-only/observe/run 的行为；构建失败是失败，缺 arm 不是 skip。不要通过删掉整个 driver 文件的测试来满足专用 gate。
3. 使用两个已批准、不同 source/build identity 的 arm。验证真正的 worker→benchmark 执行发生在各自构建，不能停在 D6 dry-run，也不能用当前 REPO 的 W8 代替两臂。
4. 通过正式 CLI 启动 T4 的离线 provider。无真实 key；本地 provider 或回环服务记录实际 generate/HTTP 次数，外部 transport 禁止。不得 mock 掉授权检查来证明端到端授权成立。
5. 使用结构化测试结果/稳定断言验证关键场景执行。现有 verbose grep 可以保留为辅助；不要继续为终端符号、相对路径和阈值写大量修补逻辑。
6. 上传本次 campaign 的 plan、观察摘要、ledger、journal、脱敏单例报告、最终汇总和 validator 输出；摘要可追到原报告。
7. mutation/反例直接改变行为：让两臂都用一个构建、跳过 verifier、固定 request=r97、绕过预算、resume 丢历史失败，对应测试必须失败。
8. 更新报告中的“DONE”范围。保留旧事实与 SHA，追加勘误：“已有 keyless MODEL_ERROR 冒烟”不等于“成功工具链验收”；“没有离线成功能力必须付费”改为当前采用的真实离线接口与证据。
9. 先完成所有实现和离线验收，再整理最终小型非 holdout 实验材料。若正式 transport 的预算/身份仍未接通，输出 BLOCKED 和具体原因，不提前写 OFFLINE_ACCEPTED 或要求用户付费验证。

### 必须实际运行的验收矩阵

| 场景 | Windows CI | Ubuntu CI | 关键证据 |
| --- | --- | --- | --- |
| 两个不同 arm + 两种不同 request | 必跑 | 必跑 | 每臂模块/build identity、捕获输入、真实工具事件 |
| 写文件成功 / 只说完成但未写文件 | 必跑 | 必跑 | TaskVerifier 的正反结果和持久报告 |
| 多轮与子代理共享预算 3 次 | 必跑 | 必跑 | 第四次未进入 provider；实际计数与 ledger 一致 |
| 成功、有效负例、provider 失败后的恢复 | 必跑 | 必跑 | 新调用 0；累计结果和失败不消失 |
| 缺 state/ledger、换目录、hash 改动 | 必跑 | 必跑 | 命名拒绝；0 新调用 |
| 同单位竞争、活 owner、死 owner、reconcile retry | 必跑 | 必跑 | 所有权和预算证据，未重复执行 |
| 输入/构建/model/endpoint 漂移 | 必跑 | 必跑 | 实际执行边界拒绝，不仅纯函数比较 |
| 超时/取消/输出过量 | 必跑 | 必跑 | 有界返回，无遗留进程，状态正确 |
| 外部 provider 未获授权 | 必跑 | 必跑 | 真实 gate 拒绝、provider 未构造、外部请求 0 |

### 怎么验收

- 两平台专用 job 全通过，关键场景没有 skip；现有 Ubuntu offline cold-start 继续成功。
- 从干净 checkout 的命令能复现，用户无需手写临时脚本，也无需本机 Linux。
- 最终报告能够由独立命令从这次真实产物重算，summary 篡改会失败。
- required typecheck、build、test、coverage、相关 security 和 docs gates 通过；CI head 明确对应实施提交。
- 无外部付费执行时标为 `OFFLINE_ACCEPTED / PAID_NOT_RUN`，不得宣称模型能力提升、真实胜率改善或可晋升。

最终集成命令至少包含以下现有检查，以及实现后的正式 CLI/validator 命令：

```powershell
pnpm typecheck
pnpm build
pnpm test
pnpm test:coverage
pnpm test:security
pnpm docs:verify
pnpm exec vitest run --reporter=verbose packages/evaluation/src/r97-driver-closed-loop.test.ts packages/evaluation/src/r97-arm-worker-contract.test.ts packages/evaluation/src/r97-budget-ledger.test.ts packages/evaluation/src/r97-execution-state.test.ts packages/evaluation/src/r97-plan.test.ts packages/evaluation/src/r97-redaction.test.ts packages/evaluation/src/r98-fixture-cases.test.ts
```

完整 gates 在集成阶段执行一次并满足仓库要求；每个小改动先跑相关反例，不为凑次数重复全套。

## 2. 统一反馈模板与结束条件

每个任务完成时向用户报告：

```text
任务：Tn / 原任务编号
状态：DONE / BLOCKED / ALREADY_FIXED / NOT_REPRODUCED
起始 SHA / 实现 SHA：
对应发现：Nn
RED：命令、预期失败、实际结果
GREEN：命令、退出码、关键结果
provider generate / transport retry / 外部请求次数：
持久证据路径与 validator 命令：
本地环境 / Windows CI / Ubuntu CI：
CI URL 与 head SHA：
剩余限制：
```

本轮结束必须同时满足：

1. 每次实际调用受同一预算约束，恢复和换路径不授予新额度。
2. 状态所有权正确；结果 hash、输入身份和原报告都在恢复时被验证。
3. 失败不会在恢复中消失；累计汇总可以从原结果独立重算。
4. 正式入口确实执行两份构建、真实工具和 verifier；批准参数就是实际参数。
5. 超时与取消能结束真实进程树。
6. Windows/Ubuntu CI 覆盖同一闭环，没有作者机器私有前提。

达到以上条件后停止这轮基础设施修改。下一步最多整理一个范围明确、可原样执行的非 holdout 小实验。没有覆盖最终计划的付费授权则保持 PAID_NOT_RUN；不要自动增加 R102+ 框架任务或扩大实验。

## 3. 固定版本证据索引

- [本轮提交](https://github.com/ki11a-Conton/harness-agent/commit/0075f723cfb6f6e38cf55829974b9d7634cf612b)
- [相对上轮的 7 个 commit](https://github.com/ki11a-Conton/harness-agent/compare/8dc0b2d4ac911b010ad5773ba801bab7d71b63ec...0075f723cfb6f6e38cf55829974b9d7634cf612b)
- [本轮 CI](https://github.com/ki11a-Conton/harness-agent/actions/runs/35488426033)
- [driver](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/scripts/e4/r97-campaign-driver.mjs)
- [arm worker](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/scripts/e4/r97-arm-worker.mjs)
- [budget ledger](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/packages/evaluation/src/r97-budget-ledger.ts)
- [execution state](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/packages/evaluation/src/r97-execution-state.ts)
- [plan / execution observation](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/packages/evaluation/src/r97-plan.ts)
- [worker 契约测试](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/packages/evaluation/src/r97-arm-worker-contract.test.ts)
- [driver 测试](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/packages/evaluation/src/r97-driver-closed-loop.test.ts)
- [CI 定义](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/.github/workflows/ci.yml)
- [R99–R101 报告及其已承认限制](https://github.com/ki11a-Conton/harness-agent/blob/0075f723cfb6f6e38cf55829974b9d7634cf612b/docs/E4-R99-R101-report.md)
- [baseline 的公开 benchmark 入口](https://github.com/ki11a-Conton/harness-agent/blob/e9776ba66190ea63b1bacb685c91aa900b6935e7/apps/cli/src/benchmark-command.ts#L194)
- [candidate 的公开 benchmark 入口](https://github.com/ki11a-Conton/harness-agent/blob/a20373743b56de6a3a110fecdd254737ece71afa/apps/cli/src/benchmark-command.ts#L194)

