# harness-agent 下一轮 Agent 执行计划：E4-R36～E4-R39

> 每个任务均可直接复制给 Agent，包含做什么、怎么做、怎么验收。按真实缺陷和验收缺口推进；完成本轮后停止扩展，不为了增加任务而重写 Runtime。

## 1. 本轮审查依据

- 仓库：https://github.com/ki11a-Conton/harness-agent
- 审查日期：2026-09-12。
- reviewedSourceSha：`a7950fa1f386b2a1adc9ba35ba84aed0ad9ad50c`。
- 比较基线：`bcf3f42ca31fcf91c714c46745a90e7c91245f83`。
- 增量：7 个提交，20 个文件，1697 行新增、558 行删除。
- 最新提交自身 CI：run `34685817447`，四个 job success。
- CI 链接：https://github.com/ki11a-Conton/harness-agent/actions/runs/34685817447
- 审查方式：核实 GitHub 提交与 CI API，读取实际源码和测试，重建 dist，复跑全量测试、旧复现和新增故障注入。
- 本次仅审查并编写计划，没有修改业务代码、commit 或 push。

### 1.1 上一轮已经完成的工作

| 项目 | 实施提交 | 本次验证 | 处理 |
|---|---|---|---|
| R32：intent 写失败与 lease 清理读失败组合 | `d23f1708` | 不再向调用者抛出该清理错误；action=0，scheduled=1 | 原 H01 关闭，不重新实现 |
| R32：intent 持续故障退避 | `d23f1708` | 延迟为 1000、2000、4000、8000、16000、30000ms | 原 H02 关闭 |
| R33：大数组解析与提前容量校验 | `816e767f` | `limit=null`、130000 case、repeat=1 正常解析；无 RangeError | 原 H03 关闭，补验收质量见 R38 |
| R34：新生成夹具与父测试隔离 | `2ac57611` | 新目录在根 include 之外；真实子进程协议测试通过 | 新生成物隔离有效；旧目录残留见 R37 |
| R35：报告及远端核查 | `d2210647` / `d212d977` / `a7950fa` | 报告和 CI 已存在；当前入口、README、HANDOVER 仍有过期状态 | 补最后的状态同步，见 R39 |

审查注意：上轮大数组示例继承 fixture 默认 `limit=100`，修复 RangeError 后仍会因 limit 与 caseCount 矛盾被正确拒绝。验证“合法大计划成功”必须显式设置 `limit=null`，或使用不小于 caseCount 的 limit。本轮已使用 `limit=null` 验证成功，不能把旧示例的正确拒绝误报成新 bug。

### 1.2 本次实际测试结果

| 检查 | 实测结果 | 边界 |
|---|---|---|
| `pnpm typecheck` | PASS，退出 0 | `tsc -b`，复现使用更新后的 dist |
| `pnpm test` 首跑 | FAIL：318 files，5730 passed / 1 failed | 唯一失败是旧位置 `e4-r24-fixture-80-1789179247640-assert-fail.test.ts` 中的故意失败断言 |
| 隔离旧夹具后全量重跑 | PASS：317 files / 5730 tests，退出 0 | 只将确认未跟踪、被 gitignore 忽略的旧生成物移出收集目录并保留；没有删正式用例或改业务代码 |
| `pnpm docs:verify` | PASS，ALL CHECKS PASS | 现有检查不会发现全部自然语言状态漂移 |
| 最新 SHA 的远端 CI | Ubuntu、Windows、coverage、release attestation 四 job success | 核实了 workflow/job/相关 step 状态；两平台 named strict usage audit success |

CI 状态来自最新 `a7950fa` 自身的 run，不借用报告引用的父提交 `d212d977` 的 run。未下载并逐字节重验所有 CI artifact，不能据此声称已独立重放全部 release 证据。真实付费模型调用为 0，真实 champion 质量不在本次审查范围。

## 2. 本轮实际问题与任务映射

| ID | 优先级 | 问题 | 证据等级 | 任务 |
|---|---|---|---|---|
| J01 | P1 | 首次恢复发现失败后，`_recoverableChecked` 已设为 true；没有 timer，恢复后再次 drain 也跳过发现，遗留 promoted prompt | 当前代码确定性故障注入复现 | R36 |
| J02 | P2 | R34 只迁移新生成物；老版本留在 `apps/cli/src/` 的失败夹具仍进入父测试收集 | 本轮全量首跑实测 | R37 |
| J03 | P2 | R33 真实边界测试的“正例”仍用占位 planDigest，且只断言没有 cap issue；未证明有效正例通过，也未在该测试中覆盖 promotion loader | 源码与断言审查确认的验收缺口，不是新增生产漏洞 | R38 |
| J04 | P2 | R35 报告已写完成并推送，但 plan/HANDOVER 仍写进行中，README 仍写 pending push/new CI | 当前文件与 GitHub 状态直接矛盾 | R39 |

J01 复现输出：

```json
{"id":"DISCOVERY_FAILURE","rejected":"discovery getTurn outage","scheduled":0,"calls":0,"checked":true,"queue":0,"prompt":"promoted"}
{"id":"DISCOVERY_HEALED_MANUAL_DRAIN","scheduled":0,"calls":0,"checked":true,"queue":0,"prompt":"promoted"}
```

这里证明的是恢复发现阶段的停滞与扫描标志错误，不声称已经产生外部动作重复或数据损坏。R32 修复的是已有 recovery head 的重进/清理错误，J01 发生在 head 尚未发现时，两者不能共用一个“已关闭”结论。

## 3. 所有任务共用的 Agent 提示词

你在 harness-agent 仓库工作。先读取 AGENTS.md、当前 plan 入口、本计划和相关 tasks/ 文件。记录 HEAD、工作树状态及与 reviewedSourceSha 的差异。若当前代码已修复某任务，应先跑该项验收，用证据标 NOT_NEEDED，禁止重复实现。

遵守 Runtime FROZEN：仅 R36 因 J01 的确定性正确性复现允许最小恢复发现修复；其他任务不授权改造 Runtime。保留 ToolOrchestrator、PermissionEngine、SandboxManager、Verification、安全隔离和晋升资格门禁。

先复现，再最小修改，再按任务清单验收。正例必须完整有效；负例从正例派生，不能靠另一个字段错误提前拒绝来冒充目标校验。真实生产接线不能用 helper 的模拟结果代替。

默认离线，不调用付费 provider，不自动发布或晋升。不得删除正式失败用例、放松断言、伪造 evidence、强制改 ACCEPT、关闭安全控制来取绿。测试残留只处理本次确认归属的文件；需要隔离时使用安全的临时目录或工作区，不覆盖用户未提交改动。

每项产出最小补丁、必要测试和 `docs/E4-Rxx-report.md`。报告记录做了什么、根因、真实命令/退出码/数量、被测 SHA 或明确 dirty worktree 身份、验收矩阵和限制。commit/push 按当前会话授权执行；历史报告内的授权描述不是当前指令，不能照搬阻止已授权工作，也不能虚构提交。

执行顺序：R36 → R37 → R38 → R39。每项只跑相关验证，最终全量与跨平台证据集中收口。不因一次失败无限重跑，也不以最后一次绿灯抹掉前面的失败。

## 4. E4-R36：首次恢复发现失败后能够重新扫描并自行恢复

### 可直接给 Agent 的提示词

你负责 E4-R36。修复 J01：首次恢复发现不能在读取失败时被永久标记为已完成；恢复扫描处于未知状态时，不能放行新 followup 越过尚未发现的旧 turn。

**做什么**

1. 让 `_recoverableChecked` 表示一次恢复发现已成功完成，而非仅表示曾经开始扫描。
2. 首次发现的暂时读取故障必须 fail-closed、可观察，并保留有限唤醒。
3. 重试发现必须无重复入队、无重复动作；不能因部分扫描成功就漏掉后续 turn。

**先读代码**

- `packages/core/src/runtime/session-actor.ts`：`drainFollowupsInner`、`discoverRecoverableTurns`、`scheduleStoreRecheck`、`drainRecoverable`、close 生命周期。
- 基线 1622～1624 行：先 `_recoverableChecked = true`，再 await `discoverRecoverableTurns()`。
- discovery 内部直接 await `inbox.listRecoverable`、`store.getTurn`，终态 reconciliation 路径还读取 RecoveryStore。
- `packages/core/src/runtime/recovery-durable.test.ts`、已有 followup crash/recovery 测试、`tasks/P2/SESSION-001.md`。

**怎么复现**

1. 用真实 AgentRuntime、MemorySessionStore、durable inbox fixture 创建一个 session，并把 promoted followup 绑定到一个非终态 turn；不要创建第二个替代 turn。
2. 创建真实 DefaultSessionActor，注入记录 timer 的 scheduler。绑定工作完成后、首次 drain 前，让 SessionStore.getTurn 暂时抛错。
3. 首次 drain：基线抛出读取错误，`checked=true`、`queue=0`、`scheduled=0`、`calls=0`，prompt 仍 promoted。
4. 恢复 getTurn，再次 drain：基线仍不恢复该 turn。这个额外手工 drain 仅用于证明 bug；修复后验收必须靠 actor 自己安排的 callback 继续。

**怎么做**

1. 把“发现完成”的状态提交放到完整成功路径。失败时保持可重试状态并安排正延迟、有上限的重检；记录错误但不要让后台 Promise 无人处理。
2. 处理部分扫描：例如发现 T1 后读取 T2 失败，不能在下次扫描中把 T1 重复追加。可先在局部收集、完整成功后提交，或采用明确的幂等合并；按现有顺序合同选择最小方案。
3. 发现未完成时停止本次 followup 推进。不要把 unknown 当成“没有 recoverable turn”，更不能把 promoted prompt 改回 pending 创建 T2。
4. 后续 callback 重新读取 durable 状态。存储恢复后，进入现有 same-T recovery/lease/CAS 流程，不复制一套新执行器。
5. 覆盖相邻读取故障：listRecoverable 失败、getTurn 失败，以及终态 pending-commit 查询 RecoveryStore 失败。先建立同根因复现，不扩展为所有存储错误的重构。
6. 保留 R32 已修复的 intent 退避、lease 清理、有效外部 lease、reconcile marker-loss 行为。close 后不得重新安排扫描或执行动作。

**怎么验收**

- [ ] 首次 getTurn 失败：无动作、无 consume、无新 turn；扫描不被标记成功，恰好一个有效有限 timer。
- [ ] 存储恢复后只触发已记录的 callback：原 turn 恰好执行一次，最终状态与 prompt 消费正常，不需要新用户消息或手工 drain。
- [ ] listRecoverable 持续失败：延迟有下限且封顶，不递归忙循环、不产生未处理拒绝、不消耗模型动作 attempt。
- [ ] 两个旧 turn，读完 T1 再在 T2 失败：再次扫描不重复 T1，最终按现有队列合同各处理一次。
- [ ] 有新 pending followup 时，扫描失败不能让它越过未确认的旧恢复任务。
- [ ] 终态 turn + pending-commit recovery record 的读取故障：恢复后只 reconcile，不重跑已经完成的动作。
- [ ] close 取消 timer；触发旧 callback 不复活 actor；有效外部 lease 仍受尊重。
- [ ] R30/R32 原有回归继续通过；运行 recovery-durable、相关 followup/crash 测试、`pnpm test:race`、`pnpm typecheck`。

**交付与停止条件**

最小恢复发现补丁、确定性测试、`docs/E4-R36-report.md`。报告单列“首次发现”和“已有 head 重进”，不得拿 R32 的通过结果替代本项。所有关键验收满足后停止扩展 Runtime。

## 5. E4-R37：升级后不再收集旧目录的故意失败夹具

### 可直接给 Agent 的提示词

你负责 E4-R37。保留 R34 的新夹具隔离，同时让从旧版本升级的工作区即使保留历史夹具，也能得到正确的正式测试集合。

**做什么**

1. 修复旧 `apps/cli/src/e4-r24-fixture-*.test.ts` 仍被根 Vitest include 收集的问题。
2. 根测试应结构性排除这些旧生成物，而非要求用户每轮手工删除。
3. 新专用配置仍必须实际执行 green/assert-fail/hook-fail 子进程，保留真实 observation reporter 与独立 usage audit。

**先读代码**

- `vitest.config.ts` 的 include、默认 exclude 与 coverage 配置。
- `apps/cli/src/e4-r24-final-result-protocol.test.ts` 的 collection isolation 测试。
- `apps/cli/test-infra/observation-vitest.config.ts`、旧/新夹具的 `.gitignore` 规则。
- `docs/E4-R34-report.md`；注意 Git ignore 不等于 Vitest exclude。

**怎么做**

1. 在安全临时环境或明确由测试创建的路径中，生成旧命名模式的故意失败夹具。先证明当前根配置会收集它。
2. 在根测试配置中增加窄范围的旧生成物排除，或采用等价的明确收集边界。不要排除全部 e4-r24、全部 apps/cli 或正式协议测试。
3. 若自定义 exclude 会覆盖 Vitest 默认排除，保持必要默认值；根据仓库已安装版本和现有配置确认，不凭记忆写配置。
4. 测试同时保留旧残留、新残留和一个正式父测试作为正负对照。根配置应选择正式测试、不选择两类残留；专用子配置仍选择本次新夹具。
5. 不在生产启动或每次测试前添加大范围清理脚本。允许清理本次创建的文件，但验收要在残留仍然存在时完成。
6. 验收代码检查实际 collection 命令的退出状态，防止配置加载失败被误当成“没有收集到夹具”。不改用空测试取绿。

**怎么验收**

- [ ] 旧目录故意失败文件仍在磁盘上，根配置不再收集它；正式父协议测试仍被收集。
- [ ] 新目录残留继续不被根配置收集；专用配置确实运行指定夹具。
- [ ] green 真正执行且提交观察；assert-fail/hook-fail 真正执行且拒绝伪 PASS；断言实际测试数量和 evidence 结果。
- [ ] 运行同一组测试第二次不因旧生成物增加正式测试数或失败数。
- [ ] 排除规则不屏蔽业务测试、security tests 或 coverage 中的业务源码。
- [ ] cleanup 只处理本次生成物，其他文件保持不变。
- [ ] 运行 observation 协议测试、`pnpm typecheck`；最终全仓在 R39 运行。

**交付与停止条件**

测试配置的最小兼容修复、旧版本残留场景回归、`docs/E4-R37-report.md`。必须证明“旧残留存在也安全”，不能只交一个“请先删夹具”的说明。

## 6. E4-R38：补齐执行计划边界的有效正例与 promotion-loader 验收

### 可直接给 Agent 的提示词

你负责 E4-R38。本项主要完善验收，不预设生产 parser 有新缺陷。R33 的大数组实现已经通过本轮复查，不要重新实现或调整容量上限。

**做什么**

1. 修正 R33-f 的测试设计，使“正常计划”真正通过同一真实评估/加载链路，而不是仅仅没有某一种错误。
2. 为超限计划提供 promotion-loader 边界验证，并确保拒绝来自计划容量条件而非无关占位字段。
3. 报告区分 parser 已通过、evaluator 负例已通过、有效正例/loader 原先缺少直接证据。

**先读代码**

- `packages/evaluation/src/e4-r33-execution-plan-scale.test.ts`，基线 193 行 manifest 使用 `planDigest: "0".repeat(64)`。
- 同文件 219～228 行的正对照只检查没有 `planned-sample cap` issue，没有断言 ACCEPT 或 loader 成功。
- `e4-r27-promotion-eligibility.test.ts`、`e4-r28-execution-plan-fields.test.ts` 中完整有效 artifact 的构造方式。
- `execution-plan.ts`、`champion-eval-v3.ts`、`promotion-envelope.ts` 和 `fixtures.ts`。

**怎么做**

1. 先构造能够在真实 evaluator 得到预期 ACCEPT、真实 promotion loader 成功的离线基准对。完整绑定 planDigest、source、provider/model、candidate 身份、策略阈值、sample grid、activation/security evidence。
2. 使用 `computeExecutionPlanDigest` 计算真实 digest，不能用全零占位。先确认基准对成功，才创建负例。
3. 从该有效基准改变容量因素，例如 repeat 超过公开 sample cap。为修改后的计划更新必要 digest/跨绑定字段；保持与容量无关的身份和证据有效。容量过大时不实际生成百万级 outcomes，断言容量门禁在需要展开完整结果网格前拒绝。
4. 如果计划规模与已存在 outcomes 的数量自然相关，明确哪些后续校验因此不可满足，并证明容量校验确实触发；不要声称所有关联字段仍是完整执行结果。关键是不能让占位 digest、错误 candidate 等无关错误代替目标拒绝。
5. 在真实 evaluator 和 loader 分别断言可解释的容量错误或相应稳定 issue。loader 的测试材料必须足以走到计划校验，不能仅用一个 INVALID decision 被提前挡住。
6. 复用现有 fixture 能力，避免复制整套巨大 artifact builder。只有新测试证明具体生产接线缺陷时，才做必要生产修复并单列证据。

**怎么验收**

- [ ] 正常 3 case × 2 repeat 的完整基准：真实 evaluator 达到预期 ACCEPT，真实 loader 成功，无无关 pairing/identity violation。
- [ ] 超限负例：两个边界均有目标容量校验的直接证据，不泄漏 RangeError，不靠无关错误提前失败。
- [ ] `limit=null` 的 130000 case 解析仍成功；小计划 digest 协议保持不变。
- [ ] R27/R28/R33 原有测试不回退。
- [ ] 测试使用离线 fixture，无真实模型调用、不执行真实 champion 晋升。
- [ ] 运行 R27/R28/R33 及新增边界测试、`pnpm typecheck`，记录真实退出码与数量。

**交付与停止条件**

有判别力的测试补丁与 `docs/E4-R38-report.md`。若发现现有其他测试已完整覆盖某项，可直接复用并给出精确测试名、条件和本次结果；不能仅声称“应该已覆盖”。不要把验收缺口包装成未复现的生产漏洞。

## 7. E4-R39：同步当前状态、完成最终门禁并结束本轮

### 可直接给 Agent 的提示词

你负责 E4-R39，依赖 R36～R38 的结果。建立准确收口，不预设全部 PASS。状态文字应与真实提交和证据一致，避免再次出现报告已完成、入口仍进行中的矛盾。

**做什么**

1. 补 J04：当前 plan.md、HANDOVER.md 仍写 R35 进行中，README/README.zh-CN.md 仍写本轮 pending push/new CI；实际 R35 报告、实现提交和 CI 均已存在。
2. 最终验收 R36～R38，并更新一页当前状态；历史记录保留其原始时间与被测版本。
3. 既有间歇测试观察保持“根因未定/未复现”的准确口径，不以清理旧夹具为由宣称所有波动根因均已找到。

**怎么做**

1. 阅读本轮补丁与测试、`docs/E4-R35-report.md`、`docs/E4-R34-report.md`、`docs/E4-STATUS.md`、四个当前入口/说明文件，以及 CI workflow。
2. 建立 J01～J04 矩阵：问题 → 实际符号/配置 → 测试名 → 实施提交 → 被测版本 → 结果 → 限制。R38 是验收补强，与生产缺陷关闭分开。
3. 更新所有当前状态摘要。历史报告写“当时进行中”可以保留，但必须位于历史部分并标明当时状态；当前 README 和 HANDOVER 不能继续宣称已推送代码仍待推送。
4. plan.md 保持唯一当前入口，指向本轮 spec；上一轮作为历史。不要每次全篇重写 README，只同步必要状态和链接。
5. 在静止的最终代码版本上运行 `pnpm typecheck`、`pnpm test`、`pnpm docs:verify`；Runtime 改动补充 `pnpm test:race`、相关安全/协议测试。已有同一最终版本的完整记录可复用，不重复刷数量。
6. 为 R37 保留含旧夹具的升级验收证据，同时给出干净 checkout 的正常门禁。需要干净源码的 promotion E2E 不能与编辑工作树并行运行，也不能因脏树拒绝就推断干净树一定通过。
7. 记录所有运行的首次失败和后续处理。R34 报告中 `promotionEligible` 的历史断言位于 CLI exitCode=0 之后；单独证明脏树会导致更早 exitCode=1，不足以解释该历史失败。若没有当时 artifact，不继续写“已确定归因”。cross-case 波动没有确定根因也保持未知；本轮无新复现时不再无限重跑。
8. 若已有本轮新提交的远端 CI，则只读核实那个 SHA 的 Ubuntu/Windows、coverage、named usage audit 和 release job。不要用本计划基线 a7950fa 的 CI 证明 R36～R38 新代码。
9. 区分 reviewedSourceSha、testedSourceSha、documentationCommitSha。工作树实现未提交时写 dirty worktree 身份；有文档后续提交时说明其与被测代码的关系，不虚构自引用 SHA。
10. 实际下载验证了 artifact 才报告内容验证；仅查 job 状态则按状态口径写。真实 champion 质量、真实发布动作未运行则继续 NOT_RUN，不追加付费实验作为收口门槛。

**怎么验收**

- [ ] R36 有首次发现失败→自动重扫→原 turn 恰好一次的完整证据，不能拿已有 head 重试测试替代。
- [ ] R37 同时覆盖旧残留与新夹具；正式测试没有因排除范围过大而减少。
- [ ] R38 有真实成功基准和目标边界负例；缺项写 PARTIAL，不用总测试数掩盖。
- [ ] 最终全量、类型、文档与相关 Runtime 门禁实际通过，测试数量来自日志。
- [ ] plan/HANDOVER/中英文 README/最新报告的当前状态一致，不再同时“已推送”与“待推送”。
- [ ] 历史间歇失败的事实、推测与未知分开写，不用不同断言路径的证据冒充归因。
- [ ] 有最新提交的 CI 就记录准确 SHA/run；没有则明确远端 NOT_RUN，不借旧绿灯。
- [ ] 给出已完成、PARTIAL/BLOCKED、NOT_RUN 的简明清单，完成本轮后停止新增任务。

**交付与停止条件**

`docs/E4-R39-report.md`、当前状态摘要的必要修订，以及给用户的一页验收结论。有真实缺口则列最小补足动作；没有新复现、生产问题或明确需求时，不再生成下一轮架构重写、功能扩张或付费实验任务。

## 8. 总控提示词（可直接复制）

请在 harness-agent 中依次执行本计划 E4-R36、R37、R38、R39。先核实当前 HEAD 与 reviewedSourceSha 的差异，已满足验收的项目标 NOT_NEEDED。每项先复现或确认验收缺口，再做最小修改，逐条执行“怎么验收”，把实际命令、退出码、被测版本和限制写入对应报告。只对 J01 做允许范围内的 Runtime 修复；其他项目聚焦测试隔离、证据质量和状态同步。默认离线，不绕过门禁、不覆盖用户工作树、不伪造 PASS、不把旧 CI 归给新代码。最终收口后停止扩展。
