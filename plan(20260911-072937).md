# harness-agent 下一轮执行计划：E4-R27～R31

本文是给代码 Agent 的执行提示词。每项任务均包含做什么、怎么做、怎么验收；执行时需同时遵守第 3 节共同规则。

## 1. 审查基线与实际验证

- 仓库：https://github.com/ki11a-Conton/harness-agent
- reviewedSourceSha：`493866f03d942e85870cc658eb721cfbf2389ec2`。
- 比较基线：`bcf34b7179152ac2fc24931f268fada8a67f82d9`。
- 增量：47 个文件，4199 行增加、1181 行删除。
- 本轮检查时间：2026-09-11。
- 当前提交自己的 CI run：https://github.com/ki11a-Conton/harness-agent/actions/runs/34572489732 。GitHub API 返回 completed / success。
- 仓库 R26 报告引用的是前一个源码/文档提交 `2b2d3db` 的 run 34571905570；本轮额外确认了最终 `493866f` 的 run，二者不能混为同一个被测 SHA。
- 本地 `pnpm typecheck`：exit 0。
- 本地 `pnpm test`：314 files / **5675 tests passed**，exit 0。
- 本地 `pnpm docs:verify`：ALL CHECKS PASS。
- `build` 和 `typecheck` 在 package.json 都是 `tsc -b`，本轮不重复计作两份独立验证。
- 没有真实模型调用，没有付费 benchmark，没有发布，没有修改业务代码。测试中的 fake/scripted 调用不等于真实模型调用。

本轮结论：R21～R26 的实现确实已提交。生产 release 路径现在调用引用文件复核，空 executionPlan 被拒绝，观察记录由测试 reporter 在结果阶段提交，源码指纹也已改为内容计算。这些改动应保留。本计划处理增量复核发现的四个明确缺口，不再重复上一轮全部工程。

### 1.1 已关闭与仍有缺口的边界

| 上轮主题 | 本次确认 | 下一步 |
|---|---|---|
| release 产物复核未接入生产 | readGateEvidence 已调用 reverifyGateEvidenceRefs；相关测试在全仓通过 | 保留，不重写 |
| executionPlan:{} 被接受 | 旧复现现在 INVALID，promotion accepted=false | 继续修复计划内部语义和隔离资格 |
| observation runId / test-body commit | loader 检查 runId；CLI 严格运行有命名参数；新增 Vitest reporter 和子进程测试 | 保留；本次未做所有运行中断边界的穷尽审计 |
| status 文本指纹 | 已按内容计算 | 仍使用 UTF-8 解码，二进制存在碰撞 |
| ACK 与 marker 双写失败 | 新代码通过 durable turn 终态协调 | lease/intent 暂时故障后的自动唤醒仍未关闭 |

“全仓测试通过”是已执行事实；“无任何缺陷”不是本轮结论。最新 CI 成功也不等于真实模型质量已经验证。

## 2. 本轮发现：可复现证据与范围

### G01 / 高：insecure-local 仍能通过 evaluator 和 promotion loader

位置：`packages/evaluation/src/execution-plan.ts` 的 parseExecutionPlan/crossBindExecutionPlan；`champion-eval-v3.ts`；`promotion-envelope.ts`。

问题：当前校验主要要求 manifest 与计划的 isolationStrength/promotionEligible 相互一致，却没有强制执行“promotionEligible=true 必须有 strong 隔离”等必要语义。两处一起声明 insecure-local，同时保留 promotionEligible=true，可以保持字段一致并被接受。

复现方法：三个 case × 两次重复，完整 activation/security refs，baseline 不通过、candidate 通过。使用完整计划，计划和 manifest 均为 insecure-local，promotionEligible=true；按真实 writer 重新计算内容摘要，调用真实 evaluator 生成 decision，再调用真实 promotion loader。

```json
{"id":"INSECURE_PROMOTION","decision":"ACCEPT","promotion":{"accepted":true,"codes":[],"details":[]}}
```

这是协议自相矛盾未被拒绝的证据，不是声称能伪造受信 CI 签名，也不证明现有真实 benchmark 执行过越界操作。

### G02 / 中：执行计划 parser 漏验预算字段，重复数接受小数

位置：`packages/evaluation/src/execution-plan.ts`。

源码定义了 nullableNum，但没有调用它来校验四个预算字段。repeat/limit 只调用有限数范围检查，没有整数要求。完整 fixture 单字段变更结果：

```json
{"field":"repeat","value":2.5,"accepted":true}
{"field":"maxModelCalls","value":-1,"accepted":true}
{"field":"maxLogicalRuns","accepted":true}
{"field":"maxEstimatedCostUsd","value":"invalid","accepted":true}
```

第三项表示删除 maxLogicalRuns 字段后 parser 仍接受。这里证明的是共享 parser 缺口，不将它夸大为 CLI 实际执行了负预算付费请求。生成网格使用循环，因此还应给不可接受的规模配置设置明确资源边界。

### G03 / 中：不同二进制字节产生相同源码指纹

位置：`apps/cli/src/benchmark-command.ts` 的 probeSourceSnapshot。

当前对文件 readFileSync(path, 'utf8') 后再哈希。无效 UTF-8 字节会被解码为替换字符，不同原始字节可能变成同一字符串。

复现：临时 Git 仓库有一个初始提交，未跟踪 binary.bin 先写 Buffer([0x80])，计算指纹；再写 Buffer([0x81])，重新计算。

```json
{"id":"BINARY_SOURCE_COLLISION","equal":true}
```

该问题主要影响 dirty/offline 身份、确认和 journal 归属；强隔离晋升路径当前还要求干净源码，不能把这个碰撞直接描述为已绕过干净树限制。

### G04 / 中：恢复存储暂时故障没有安排自动重检

位置：`packages/core/src/runtime/session-actor.ts` 的 acquireLease / persistRecoveryIntent 失败分支；`docs/E4-R25-report.md` 验收矩阵。

复现：一个 bound nonterminal turn，RecoveryStore 暂时拒绝写入，注入记录调用的 scheduler，执行一次 drain。结果：

```json
{"id":"LEASE_FAILURE_WAKE","scheduled":0,"calls":0}
```

故障时 action=0 是正确的 fail-closed；但没有 scheduler callback，存储恢复后该 actor 不会因此自动重新执行 drain。需要外部消息/显式 drain 才能再试，不符合上一轮“无需新用户消息、有限唤醒”的验收要求。

R25 报告以外部持有有效 lease 的 R17 测试代替 lease/intent 写失败测试，二者不是同一故障路径。本项不否认 durable turn 协调已修复 marker 丢失。

## 3. 所有任务共同规则

1. 先读取 AGENTS.md、根 plan.md 指向的任务、相关 tasks 文件，记录当前 HEAD 与工作树状态。若执行基线已比 reviewedSourceSha 更新，先判断缺陷是否已修复。
2. 不覆盖用户未提交改动。需要隔离时建 worktree，不 reset 用户工作树。
3. Runtime architecture FROZEN。G04 是已复现的正确性/活性缺陷，允许最小恢复调度修复；G01～G03 不授权重写 Runtime。
4. Core 不依赖 UI/provider/business integration；工具副作用继续通过 ToolOrchestrator、PermissionEngine、SandboxManager、Verification。
5. 所有负例从已通过的正例派生，一次只改变目标条件。不能让 fixture 因另一项缺失而提前失败后声称目标门禁有效。
6. 使用真实生产入口验证 evaluator、promotion、release/CLI 路径。辅助 helper 的测试不替代生产接线。
7. 默认离线；不调用真实付费 provider，不开启正式发布。不得降低测试门槛、手工改 ACCEPT、删除失败用例或伪造证据。
8. 任务完成输出 docs/E4-Rxx-report.md，包括被测 SHA、复现前后、实现符号、测试命令/退出码和残余限制。
9. PASS 需要逐条验收证据；未运行写 NOT_RUN；部分完成写 PARTIAL；当前已有修复可写 NOT_NEEDED，但必须验证。
10. 一项任务完成后停止扩大范围。收口时再运行全仓测试与相关门禁，不以增加测试数量为目标。

## 4. 执行顺序

| 任务 | 优先级 | 前置 | 输出 |
|---|---|---|---|
| E4-R27 晋升隔离资格语义 | P1 | 无 | insecure/none 不可晋升 |
| E4-R28 执行计划字段与规模约束 | P1 | R27 共享协议修改协调 | 完整且有界的 parser |
| E4-R29 二进制源码指纹 | P2 | 无 | 原始字节身份 |
| E4-R30 恢复存储故障有限唤醒 | P2 | 无 | 无外部消息也能重检 |
| E4-R31 独立验收与计划收口 | P1 | R27～R30 | 逐条件关闭矩阵 |

按顺序执行即可，不要求并行 Agent。先完成本轮确定性缺陷，不新建无证据的功能路线图。

## 5. E4-R27：把隔离资格作为晋升硬条件

### 给 Agent 的提示词

你负责 E4-R27，修复 G01。请阅读共同规则、execution-plan.ts、champion-eval-v3.ts、promotion-envelope.ts、paired-v3-builder.ts、benchmark isolation 实现和相关测试。目标是拒绝内部字段一致但不满足晋升必要条件的实验产物。

### 做什么

- 为 promotion-grade evidence 建立明确语义条件。
- evaluator、promotion loader、writer 复用同一资格判断。
- 区分策略评估结果与正式晋升资格，防止 insecure 记录被重新赋予资格。

### 怎么做

1. 建立 strong、clean、complete 的完整正常 pair，调用 runV3ChampionEval 和 loadPromotionEnvelope 验证正例。
2. 将两臂计划/manifest 的隔离模式一致改为 insecure-local，保留 true 资格并重算所有必要摘要，复现 G01。再测试 none。
3. 定义 promotionEligible=true 的最低条件：strong 隔离、已知且一致的源码、满足干净树要求、完整样本/完成状态，以及当前协议确有的隔离证明字段。
4. 字段一致只是一项条件，不等于字段组合合法。加入独立 semantic validation，返回明确代码和原因。
5. 梳理实际 backend/self-test 证据从生成到 artifact 的流向；需要引用时使用实际产物，不创建假的 selfTestPassed 常量。无法证明 OS 隔离的测试标为接线验证。
6. 检查 candidate 及 baseline 的资格来源，缺失必要事实必须 fail closed。历史产物可诊断读取，但不能获得当前晋升资格。
7. 决策语义保持一致：如果 ACCEPT 表示可晋升，insecure 必须非 ACCEPT；若区分质量结果与晋升资格，输出应明确且 promotion 必须拒绝，不能静默更改现有 CLI 含义。
8. 不添加无必要的签名系统。本任务处理协议矛盾和必要证据，不声称哈希能证明拥有写权限者绝不造假。

### 怎么验收

- [ ] strong + clean + complete 的正常 production fixture 仍通过。
- [ ] insecure-local 和 none 即使两处一致且摘要全部重算，均不能晋升。
- [ ] promotionEligible=true 与隔离/clean/source 组合矛盾时返回稳定错误。
- [ ] 缺失必要资格事实不能通过省略字段绕过。
- [ ] 真实 runV3ChampionEval 与 loadPromotionEnvelope 的负例分别执行，不仅测 helper。
- [ ] 部分运行、错网格、错 policy、错 candidate 的既有拒绝仍成立。
- [ ] 所有测试离线；文档明确 fake 隔离仅证明接线。

交付 docs/E4-R27-report.md，附必要条件表、真实调用链和复现前后结果。

## 6. E4-R28：完整校验计划数字字段与网格规模

### 给 Agent 的提示词

你负责 E4-R28，修复 G02。请阅读共同规则、parseExecutionPlan、expectedSampleKeysFromExecutionPlan、CLI 参数解析和预算执行逻辑。R27 若已修改共享模块，请在其结果上继续，避免覆盖改动。

### 做什么

- 消除已定义验证器未被调用的字段遗漏。
- 统一 missing/null/0/正值语义和整数要求。
- 在展开网格前拒绝不可执行或资源规模异常的计划。

### 怎么做

1. 为四个预算字段建立参数化矩阵：缺失、null、0、合法正值、负数、字符串、NaN、Infinity。明确 JSON 路径与直接 JS 调用路径的不同。
2. maxLogicalRuns/maxModelCalls 使用安全整数；token 预算按真实计量单位确定整数合同；cost 允许合理有限小数。所有字段必须实际调用 validator。
3. repeat/limit 使用明确的安全整数规则；seed 与现有 PRNG 和 CLI 接受范围保持一致。避免 parser 放宽、执行器取整的静默差异。
4. 明确计划 caseIds 已是选择后集合，还是由 limit 再截取。当前 CLI 与网格生成器必须使用同一解释，矛盾计划应拒绝。
5. 对 repeat × caseCount × armCount 做溢出和规模检查，先检查再分配数组。使用已存在的预算/产品限制定义边界；如需新增上限，记录理由与兼容行为，不随意砍正常实验。
6. 检查 schemaVersion、billingClass、candidate、policy、effective parameters 的类型/枚举边界；只补与执行合同相关的校验，不另造一套重复 schema。
7. 保留正常计划的 canonical digest 兼容；若协议语义必须升级，明确版本迁移与旧证据的诊断地位。

### 怎么验收

- [ ] 本轮四个 parser 反例全部被拒绝。
- [ ] repeat=2.5、非整数 limit、非安全整数计数被拒绝。
- [ ] null/unlimited、0/forbid 与正值上限在 parser、dry-run、执行中一致。
- [ ] 成本小数合法正例不被误拒绝。
- [ ] 极端重复数在网格展开前快速返回结构化错误，不运行超大循环。
- [ ] 正常三 case × 两 repeat 得到六个唯一 pair key、十二个逻辑 arm run。
- [ ] 完整正常 artifact 经 evaluator/replay 仍通过；非法字段经真实文件 reader 也被阻断。
- [ ] typecheck 与 execution-plan/benchmark/paired identity 相关测试通过。

交付 docs/E4-R28-report.md，逐字段写明合同、使用的 validator 和测试名称。

## 7. E4-R29：以原始字节计算源码身份

### 给 Agent 的提示词

你负责 E4-R29，修复 G03。请阅读共同规则、probeSourceSnapshot、case fingerprint、execution identity 和 R23 测试。目标是修复二进制内容身份丢失，并明确文件类型/读取错误的处理。

### 做什么

- 原始文件内容按 Buffer 哈希，禁止 UTF-8 解码后再代表文件字节。
- 保留确定的路径排序、index/worktree 区分和模式信息。
- 说明源码指纹覆盖范围与未知状态。

### 怎么做

1. 在临时 Git 仓库复现 0x80→0x81 未跟踪文件的指纹碰撞；补 tracked 文件同类负例。
2. 对文件直接读取 Buffer，hash.update(buffer)。文本路径/元数据使用独立、无歧义的结构编码。
3. 检查换行、BOM、NUL、二进制、删除、模式、symlink 的合同。不要无依据归一化源码字节；摘要应代表实际输入。
4. 若 symlink 需要身份，区分链接本身与链接目标内容。对 submodule 是否覆盖工作树脏内容作明确说明，不声称覆盖了未读取内容。
5. 文件不可读与文件已删除分开处理；不可读输入应使 probe unknown/error，不能编码成正常 missing 然后宣称内容已验证。
6. 保持 clean checkout 的快速路径；避免每次请求重新遍历仓库。复核同一脏文件内容更改会导致 journal identity 变化。
7. 不对模型质量配置作无关调整；需要升级 fingerprint 协议时确保旧 journal 不会误复用。

### 怎么验收

- [ ] 0x80 与 0x81 得到不同指纹，tracked/untracked 都覆盖。
- [ ] 完全相同字节在重复 probe 中稳定。
- [ ] A→B 文本、删除、staged/unstaged 变化仍被识别。
- [ ] 读取错误明确返回未知/失败，不与删除混为正常验证。
- [ ] 无关 ignored 输出目录保持既有排除规则。
- [ ] 指纹变化通过实际 execution identity 影响 journal 归属。
- [ ] 报告明确 symlink/submodule/ignored 的覆盖边界，不夸大为全文件系统快照。

交付 docs/E4-R29-report.md，并给出二进制负例的修复前后值和复用行为测试。

## 8. E4-R30：恢复存储暂时故障后必须有限唤醒

### 给 Agent 的提示词

你负责 E4-R30，修复 G04。先阅读共同规则、Runtime Freeze、session-actor.ts、recovery-durable.test.ts、DurableRecoveryStore 和 R25 报告。此任务已有确定性活性缺陷复现，允许最小调度修复，不允许扩展成新的 Runtime 架构。

### 做什么

- lease/intent 写入暂时失败时，保持 action=0，并安排有限重检。
- 存储恢复后不依赖新用户消息或测试主动 drain。
- 保留单飞、队列顺序、关闭取消和恢复次数上限。

### 怎么做

1. 创建 bound nonterminal turn，使用可切换故障的 store 和可记录/触发的 scheduler。一次 drain 后先断言当前没有 schedule，以此复现 G04。
2. 分开覆盖初始记录写失败、lease acquisition 错误、intent 错误、CAS 冲突、其他 owner 的有效 lease。不能以有效 lease 到期用例代替写失败用例。
3. 对暂时错误安排有下限的退避/重检，保证每个 actor 同时至多一个有效重试 timer。回调执行前重新读 durable 状态，不能沿用过期记录。
4. 存储故障重检不应消耗模型 action 的 attempt budget；实际开始 action 前仍要求 lease/intent 已持久化。
5. 永久错误或损坏记录按现有合同 blocked/terminal，并有诊断。不能无界热循环，也不能自动清掉损坏记录来恢复。
6. 检查 durableTurnIsTerminal 当前把读取错误当 false 的行为。读取失败不是已知 nonterminal；用故障注入验证是否会错误进入 retry。若存在确定性问题，改为明确 unknown 并等待协调。
7. close/unload 取消 timer；过时 callback 不能复活已关闭 actor。保持 T2 不越过尚未协调完成的 T1。
8. 至少一项测试使用真正的 durable store 或可控持久化接口；不把相同 MemoryStore 上重建对象称为进程崩溃持久性证明。

### 怎么验收

- [ ] 故障时 provider/action 调用数 0，prompt 不 consume，T1 不 shift。
- [ ] 有且仅有有效的有限重检 timer；测试触发 callback，而不是主动再调用 drain。
- [ ] 切换 store 恢复并触发 callback 后无需新消息即可完成。
- [ ] lease、intent、CAS、有效外部 lease 分别有正负例。
- [ ] marker 丢失但 durable turn 已终态时仍只补 ACK，不重跑已知完成动作。
- [ ] durable turn 读取未知时不会被当作确认未完成而危险重试。
- [ ] close 后触发旧 callback 无 action、无新 timer。
- [ ] 长期故障有界退避，attempt/队列状态准确；相关 race/security 回归通过。

交付 docs/E4-R30-report.md。修正 R25 的“有限唤醒已验收”引用，保留其 marker-loss 修复的真实历史结论。

## 9. E4-R31：用原验收条件收口

### 给 Agent 的提示词

你负责 E4-R31。在 R27～R30 完成后执行独立验收。不要只汇总报告中的勾选；对照本计划每条原始验收条件检查源码和真实测试。

### 做什么

- 建立 G01～G04 的准确关闭矩阵。
- 在固定源码上运行最终工程门禁。
- 更新唯一当前计划入口和有错误的关闭声明。
- 给工程维护设置停止条件，不继续无依据扩大加固范围。

### 怎么做

1. 关闭矩阵列：问题→生产实现符号→修复 commit→正常正例→单因素负例→被测 SHA→结果→限制。
2. 重新执行本轮四类复现。禁止通过破坏 fixture 的其他字段获得拒绝。
3. 运行 pnpm typecheck、pnpm test、pnpm docs:verify，以及 Runtime 变更所需 race/security/integration 门禁。记录实际命令、退出码和日志，不手填测试数量。
4. 若现有授权包含 push/CI，则按精确 SHA 检查新 run；否则完成本地可审查结果，远端标 NOT_RUN，不能拿旧 run 代替。
5. 检查 release bundle 复核、named-run observation audit 和 Windows 平台门禁未回退。
6. 根 plan.md 保持唯一索引；将本长文按仓库现有日期命名规则归档并引用。不要通过删除 docs verifier 来让入口通过。
7. 修正 R22 数字校验和 R25 唤醒的过度关闭；旧报告保持被测日期/SHA，并加 superseded 指引，不能改写旧证据。
8. 区分 runtimeReleaseReady（该 SHA 工程门禁）、promotion evidence integrity（证据协议）、champion quality（真实模型效果）。本计划未做真实模型效果验证。
9. 报告提交产生新 SHA 时区分 testedSourceSha 与 documentationCommitSha；不制造自引用 SHA 循环。

### 怎么验收

- [ ] G01 insecure/none 不可晋升，strong 正例仍成功。
- [ ] G02 非法/缺失预算、小数重复数、规模异常均在正确边界拒绝。
- [ ] G03 二进制字节差异改变指纹和需要绑定的身份。
- [ ] G04 暂时 store 故障后 scheduler 实际触发自愈，关闭取消正确。
- [ ] 每个 PASS 可定位到真实测试与源码；未运行项保持 PARTIAL/NOT_RUN。
- [ ] 全仓测试及相关门禁通过；最新远端结论严格绑定 SHA。
- [ ] 当前计划入口与 docs:verify 一致。
- [ ] 没有付费调用、正式 release 或无关架构扩张。

交付 docs/E4-R31-report.md 和一页最终状态。通过后结束本轮维护。下一阶段只有真实 benchmark 失败、生产问题或明确用户需求才新建任务；模型质量改进继续使用 benchmark→failure cluster→hypothesis→challenger→paired eval。

## 10. 给执行 Agent 的总控提示词

```text
请在 harness-agent 执行本计划 E4-R27～R31。
审查基线为 493866f03d942e85870cc658eb721cfbf2389ec2。
先读取 AGENTS.md 和当前计划入口，核对 HEAD 与工作树，保护已有改动。
每项先正常正例，再单因素复现，再最小修复，最后验证真实生产入口。
逐条完成做什么、怎么做、怎么验收，并写对应报告。
重点：字段一致不等于资格合法；helper 存在不等于生产调用；
手动 drain 不等于 scheduler 自动唤醒；UTF-8 字符串不等于原始字节。
默认离线，不做付费模型运行和正式发布。Runtime 只改有复现的最小缺陷。
若新 HEAD 已修复某项，先验证再标 NOT_NEEDED，不重复造工程。
最终提供精确被测 SHA、关闭矩阵、测试结果和剩余限制。
全部验收通过后停止扩张，等待真实质量或生产证据决定下一阶段。
```
