{
  "expected": {
    "status": "completed"
  },
  "suite": "regression",
  "tags": [
    "testing",
    "javascript"
  ],
  "verification": [
    {
      "kind": "command",
      "command": "node",
      "args": [
        "--test",
        "test/parse.test.js"
      ],
      "description": "runs the requested test file explicitly (E4-R77 revision: was `node --test test/`, which this Node treats as a module path and fails for reasons unrelated to the agent; bare `node --test` was rejected too — it exits 0 when NO test file exists, so it cannot discriminate)"
    }
  ]
}
