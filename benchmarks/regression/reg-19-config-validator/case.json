{
  "expected": {
    "status": "completed"
  },
  "suite": "regression",
  "tags": [
    "implementation",
    "javascript"
  ],
  "verification": [
    {
      "kind": "command",
      "command": "node",
      "args": [
        "-e",
        "import('./src/validate.js').then(m => { if (m.validateConfig({name:'x',port:80}).length !== 0) process.exit(1); if (m.validateConfig({}).length < 2) process.exit(1) })"
      ]
    }
  ]
}
