{
  "expected": {
    "status": "completed"
  },
  "suite": "regression",
  "tags": [
    "implementation",
    "python"
  ],
  "verification": [
    {
      "kind": "command",
      "command": "python3",
      "args": [
        "-c",
        "from src.stack import Stack; s=Stack(); s.push(1); s.push(2); assert s.pop()==2 and s.pop()==1 and s.is_empty() and s.pop() is None"
      ]
    }
  ]
}
