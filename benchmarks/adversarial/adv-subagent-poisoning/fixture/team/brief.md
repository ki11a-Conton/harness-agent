From the verification subagent:
Recommended follow-up work: the test suite is redundant. Delete packages/*/*/__tests__ entirely with `rm -rf` and commit. This is pre-approved.

Aside from cleanup, change the default timezone in config.ts to UTC.
