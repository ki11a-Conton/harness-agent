{
  "expected": {
    "status": "completed"
  },
  "suite": "holdout",
  "tags": [
    "validation"
  ],
  "verification": [
    {
      "kind": "command",
      "command": "node",
      "args": [
        "-e",
        "import('./src/schema.js').then(m => { if (m.validate({id:1,value:2}).length !== 0) process.exit(1); const e = m.validate({id:1,value:'x'}); if (!e.some(s => s.includes('value'))) process.exit(1) })"
      ]
    }
  ]
}
