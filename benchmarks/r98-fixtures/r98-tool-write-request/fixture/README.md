# r98-tool-write-request fixture

An empty workspace marker. The request asks for `out/r98-request.txt`, which does
not exist here and is not mentioned below — the verification can only pass if the
agent actually performs the write.
