# harness-agent E4 实施计划：收口 E3 的生产可信链

> 审查对象：ki11a-Conton/harness-agent  
> 审查基线：main @ ad37841dbcbfa1d3553e699ab482c1246a549f48  
> 上一基线：f73a337d71027afd48a8ac5ac0de1a7f4ae8497c  
> 审查日期：2026-09-07  
> 本轮付费模型调用：0  
> 文档用途：本文件中的每个任务都是可独立交给 coding agent 的完整提示词。

---

## 0. 先读结论

这次提交不是“没有做完”。相反，E3 已经完成了大量有效修复：

- typecheck、build、全量测试和 coverage 全部通过；
- 全量测试为 287 个文件、5346 个测试；
- 总覆盖率达到 statements 89.70%、branches 80.24%、functions 92.52%、lines 91.58%；
- R-01、R-02、R-03、R-04、R-05、R-06、R-08、R-09、R-11、R-12 的原始缺陷已被修复或保留为严格兼容行为；
- paired executor、CAS 锁、恢复 actor、V3 artifact、promotion envelope 都有了实质实现，不再只是空壳；
- protocol、security、race、chaos 专项测试通过。

但现在仍不能把 E3 判定为“生产闭环完成”。剩余问题集中在一条链上：

~~~text
真实 benchmark
  -> 规范化 V3 artifact
  -> 严格配对和策略决策
  -> 不可伪造的 promotion envelope
  -> applicationPending 状态
  -> 真实 createHarness 应用
  -> 可证明的 applied 状态
  -> 当前 HEAD 的 gate evidence
~~~

这条链当前存在以下阻断项：

1. 可以用格式完整但内容伪造的 ACCEPT 决策和任意文本 candidate artifact 通过 production promotion loader。
2. V3 champion evaluator 只比较 caseId 集合，不比较 (caseId, repetition)；基线只有 rep=1、候选有 rep=1/2 时仍可 ACCEPT。
3. champion promote 写入状态时直接设置 applied=true，没有 applicationPending 阶段。
4. champion profile resolver 没有接入 CLI/Web 的真实 createHarness 构造路径。
5. “生产路径集成测试”手工制造通过结果，没有消费真实 paired executor 的产物。
6. paired benchmark 仍先写 e3-02 中间格式，再靠独立脚本手工转换 V3；转换过程中会猜测或丢失关键证据。
7. ActivationEvidenceV2 和 SecurityOutcomeV2 没有进入真实 benchmark 记录链。
8. recovery actor 默认仍是内存 store，真实重启不保留尝试次数、退避时间和租约。
9. 付费运行的 planDigest 可省略；隔离探测异常会降级继续；不安全运行结果没有不可促销标记。
10. 根 plan.md、handoff、capability audit、release evidence 仍描述旧基线或当前 HEAD 为 NOT_RUN。
11. benchmark:smoke 命令退出码为 0，但实际 case 结果为 FAIL。
12. 测试会在仓库根目录遗留 .e3-09-self-test，违反可重复、无副作用的门禁要求。

因此，本计划不再扩大架构范围，只做 E3 的生产收口。必须先完成 E4-00 至 E4-10，才能考虑可选的真实付费复跑。

---

## 1. 本次审查的可复现结果

### 1.1 已通过

~~~bash
pnpm typecheck
pnpm build
pnpm test
pnpm test:coverage
pnpm e3:repro-current-defects
pnpm test:protocol
pnpm test:security
pnpm test:race
pnpm test:chaos
pnpm docs:verify
~~~

关键结果：

| 项目 | 结果 |
|---|---:|
| 测试文件 | 287 passed |
| 测试用例 | 5346 passed |
| statements | 89.70% |
| branches | 80.24% |
| functions | 92.52% |
| lines | 91.58% |

### 1.2 仍失败或结果不可信

| 命令 | 当前结果 | 正确解释 |
|---|---|---|
| pnpm benchmark:smoke | 进程退出 0，但 case FAIL | 不能作为绿色门禁 |
| pnpm capability:audit | FAIL | 11 wired、10 implemented-only，evidence freshness 失败 |
| pnpm release:verify | FAIL | 当前 HEAD 的全部 release gate 为 NOT_RUN |
| pnpm docs:verify | PASS | 只证明旧 ledger 自洽，不证明 E3 当前交付闭环 |

### 1.3 新增的关键负向复现

#### A. 伪造 promotion 可被接受

以下组合能被现有 loadPromotionEnvelope 接受：

- DecisionArtifact 的 decision 为 ACCEPT；
- contentDigest 填任意非空字符串，不重新计算；
- policyVersion 填任意值；
- candidate artifact 文件只是普通文本，不是 V3 artifact；
- artifactRefs 只需非空且文件 SHA 匹配；
- decisionEnvelopeDigest、sourceSha、planDigest 等绑定关系不重新验证。

实际复现结果：

~~~text
FORGED_SYNTACTIC_DECISION {"accepted":true,"issues":[]}
~~~

#### B. 错配 repetition 仍可 ACCEPT

构造：

- baseline：3 个 case，只有 repetition=1，全部失败；
- candidate：相同 3 个 case，包含 repetition=1 和 repetition=2，全部通过。

现有 evaluator 返回：

~~~json
{
  "decision": "ACCEPT",
  "pairComplete": true,
  "repetitions": 2,
  "perRepetitionDeltas": [3, 0],
  "netPassedDelta": 6
}
~~~

这是错误结果。严格配对应要求 baseline 与 candidate 的 (caseId, repetition) 多重集合完全一致，否则必须 INVALID。

#### C. dry-run 计划不完整

真实 CLI 离线 dry-run 输出中：

- limits 的四个字段全部为 0，但 0 当前被解释为“不限制”；
- planDigest 是可选确认，不是付费运行必填；
- plan 中没有隔离后端、隔离强度、promotionEligible；
- plan 中没有最终决策策略及阈值；
- estimatedCostUsd 使用固定估算，但没有上下界或 unknown 状态。

---

## 2. E3 原任务状态重判

| 原任务 | 当前判断 | 下一步 |
|---|---|---|
| E3-00 基线冻结 | 部分完成 | 更新当前状态；修复测试副作用 |
| E3-01 付费前置与预算 | 部分完成 | digest 必填、硬上限显式、隔离失败关闭 |
| E3-02 paired executor | 基本完成 | 不重写；补 canonical sink 与完整不变量 |
| E3-03 合约冻结 | 基本完成 | 保留测试，不再大改 |
| E3-04 Artifact V3 | 部分完成 | 收紧 schema 与跨 artifact 绑定 |
| E3-05 activation/security | 未接入生产 | 接入真实事件采集 |
| E3-06 evaluator | 部分完成 | 修复 pair key、plan policy、重复计数 |
| E3-07 promotion | 高风险未完成 | 重新验证完整信任链 |
| E3-08 champion application | 仅 mapper/单测 | 接入真实 createHarness |
| E3-09 生产集成测试 | 未达到定义 | 改成真实流水线，不手工造结果 |
| E3-10 recovery durability | actor 逻辑完成、生产持久化缺失 | 注入 durable store |
| E3-11 gate truth source | 未完成 | 建立当前 HEAD 证据生成器 |
| E3-12 文档 | 部分完成 | 当前 handoff 与 ledger 联动 |
| E3-13 adversarial E2E | 未达到定义 | 真实 CLI/构造链覆盖 |
| E3-14 实跑与序列化 | 序列化有提交；实跑证据未纳入当前门禁 | 禁止凭 commit message 宣称完成 |
| E3-15 收口 | 未完成 | 在 E4-10 执行 |

---

## 3. 所有 agent 的共同执行约束

下面这些约束属于每一个任务提示词的一部分。

### 3.1 修改边界

- 不进行新的目录级重构。
- 不更换测试框架、CLI 框架或包管理器。
- 优先修改现有生产模块，不创建第二套平行实现。
- 不用测试 helper 代替真实生产入口。
- 不删除已有回归测试；如果语义变化，更新断言并解释原因。
- 不把严格错误降级为 warning。
- 不用“字段非空”代替内容完整性验证。
- 不在测试中访问真实网络或真实付费模型。
- 不提交密钥、用户目录、绝对本机路径、临时 workspace 或大体积运行产物。

### 3.2 付费与网络边界

- E4-00 至 E4-10 必须全部离线完成。
- 未获得本轮明确授权时，不得设置 RUN_PAID_BENCHMARKS=1。
- 不得使用仓库中残留的环境变量或凭据推断用户已授权。
- 不得调用任何可能产生账单的 provider。
- 任何付费复跑必须是独立的可选任务 E4-11。

### 3.3 完成定义

一个任务只有同时满足以下条件才算完成：

1. 生产代码已接入，不只是新增类型或 helper。
2. 至少有一个正向测试和一个针对真实缺陷的负向测试。
3. 验收命令退出码符合语义。
4. 运行后 git status --short 为空。
5. 新产物有 schemaVersion、sourceSha、planDigest 或明确说明为何不需要。
6. 文档中的命令与 package.json 中真实脚本一致。
7. 报告中列出修改文件、测试结果、剩余风险和 provider 调用数。

### 3.4 禁止的“伪完成”

- 只新增 export，但没有生产消费者。
- 只验证 JSON 可解析，不验证语义、摘要和交叉引用。
- 只在单测里手工构造 passed=true。
- 只检查 caseId Set，不检查重复次数和多重集合。
- 只检查文件 SHA，不检查文件内容是否是正确 artifact。
- CLI 状态字段写 applied=true，但运行时未实际读取并应用。
- docs:verify 通过，就声称当前 release evidence 完成。
- smoke 进程退出 0，就忽略 case 结果为 FAIL。

---

# 任务 E4-00：冻结新基线并建立诚实的 E4 状态页

## Agent 提示词

你正在 harness-agent 仓库中执行 E4-00。目标是把 ad37841 之后的真实状态固定下来，清除会误导后续 agent 的旧结论，并修复测试污染工作区的问题。不要修改运行时行为，不要运行任何付费模型。

### 做什么

1. 将根 plan.md 从“待执行 E3 计划”改成当前 E4 收口计划，或明确标记旧计划为 historical，并把当前计划设为唯一入口。
2. 新建当前 HEAD 的状态矩阵，逐项标记 DONE、PARTIAL、BLOCKED、NOT_RUN。
3. 保留 e3-review-baseline.json 的历史意义，但明确它描述的是 f73a337 时点，不代表当前缺陷仍存在。
4. 修复会在仓库根目录产生 .e3-09-self-test 的测试。
5. 增加一个 no-dirty-worktree 验证脚本或测试，确保核心离线门禁不会留下未跟踪文件。

### 怎么做

1. 先记录：

~~~bash
git rev-parse HEAD
git status --short
git log --oneline f73a337..HEAD
~~~

2. 搜索所有旧基线引用：

~~~bash
rg -n "f73a337|E3-15|NOT_RUN|e3-review-baseline|plan.md" .
~~~

3. 找到创建 .e3-09-self-test 的测试：

- 使用 os.tmpdir() 加 mkdtemp，而不是仓库根目录；
- 在 afterEach/finally 中清理；
- 测试中故意抛错一次，确认 finally 仍执行；
- 不写死 /tmp 或用户目录。

4. 状态页至少记录：

- sourceSha；
- 审查日期；
- providerCalls；
- typecheck/build/test/coverage 的真实结果；
- capability:audit、release:verify、benchmark:smoke 的真实状态；
- 每个 E3 任务的重新判定；
- 当前高风险阻断项。

5. 如果保留旧 plan.md，文件首屏必须明确：

~~~text
HISTORICAL — DO NOT EXECUTE AS CURRENT PLAN
Baseline: f73a337...
Current plan: <真实相对路径>
~~~

### 怎么验收

~~~bash
pnpm typecheck
pnpm test
pnpm docs:verify
git status --short
~~~

验收条件：

- 所有命令成功；
- git status --short 为空；
- 仓库根目录不存在 .e3-09-self-test；
- 当前计划没有把已修复的 R-01 至 R-12 全部描述成待修；
- 历史 baseline 仍可追溯，但不会被当前工具误当成 HEAD 结果；
- provider 调用数为 0。

### 必须新增的测试

- 临时目录在成功路径后删除；
- 临时目录在异常路径后删除；
- 状态页中的 sourceSha 等于 git rev-parse HEAD；
- 当前 plan 入口唯一且可被 docs verifier 找到。

### 交付物

- 当前状态页；
- 更新后的 plan 入口；
- 无工作区副作用的测试；
- 一份 E4-00-report.md。

---

# 任务 E4-01：把执行计划、预算和隔离改成强制且 fail-closed

## Agent 提示词

你正在执行 E4-01。目标是确保任何可能付费或产出可促销 artifact 的 benchmark，在 provider 调用前已经确认完整计划、预算硬上限和隔离强度。所有验证必须使用 fake provider，真实 provider 调用数必须为 0。

### 做什么

1. 付费运行必须显式提供并匹配 planDigest。
2. 将 0 上限的语义改为明确、不可歧义：
   - 推荐：0 表示禁止调用；
   - 如需 unlimited，使用 null 或显式 unlimited 标志。
3. 把策略阈值、隔离要求、promotion eligibility 纳入 canonical plan digest。
4. 隔离探测报错时必须拒绝可促销运行，不能记录 warning 后继续。
5. 使用 --allow-insecure-local-benchmark 的结果必须永久标记 promotionEligible=false。
6. 预算检查必须在第一次 provider 调用前完成。

### 怎么做

1. 定义唯一 BenchmarkExecutionPlan schema，至少包括：

- schemaVersion；
- sourceSha；
- suite、caseIds、repeat、seed、interleave、shuffle；
- baseline/candidate harness 配置摘要；
- provider/model/billingClass；
- maxLogicalRuns；
- maxModelCalls；
- maxEstimatedTokens；
- maxEstimatedCostUsd；
- estimateStatus：bounded 或 unknown；
- isolationBackend；
- isolationStrength；
- promotionEligible；
- decisionPolicyVersion；
- decisionThresholds；
- createdAt 不得进入可复现 digest，或使用独立 metadata。

2. planDigest 必须由 canonical JSON 计算；同一逻辑计划跨运行得到同一 digest。

3. 付费门禁顺序必须固定：

~~~text
解析参数
-> 构建完整计划
-> 探测隔离
-> 计算预算
-> 打印计划
-> 校验显式 planDigest
-> 校验 RUN_PAID_BENCHMARKS
-> 校验所有硬上限
-> 创建 provider / 第一次调用
~~~

4. 如果 estimateStatus=unknown：

- 付费运行默认拒绝；
- 只有显式 override 且有保守硬调用上限时才允许；
- override 必须进入 plan digest 和 artifact provenance。

5. probeIsolationBackend 抛错：

- promotionEligible=true 的运行直接失败；
- insecure local 模式可以继续，但必须 promotionEligible=false；
- 后续任何转换器都不得移除该字段。

6. 不要只在 CLI parser 检查。生产 benchmark API 入口也必须执行同一校验。

### 怎么验收

正向：

~~~bash
pnpm typecheck
pnpm test -- benchmark
node apps/cli/dist/main.js benchmark --suite holdout --limit 1 --dry-run
~~~

负向测试必须证明：

- 付费 class 缺 planDigest：0 provider calls，明确错误；
- digest 不匹配：0 provider calls；
- maxModelCalls=0：0 provider calls；
- 任一硬上限超限：0 provider calls；
- isolation probe 抛错且要求促销：0 provider calls；
- allow-insecure 模式运行产物 promotionEligible=false；
- 后续 V3 writer 保留 promotionEligible=false；
- promotion loader 拒绝 promotionEligible=false。

最终：

~~~bash
pnpm test
git status --short
~~~

### 交付物

- BenchmarkExecutionPlan 单一 schema；
- 计划构建与 digest 模块；
- CLI 与 programmatic API 的统一前置校验；
- fail-closed 隔离测试；
- E4-01-report.md，明确 providerCalls=0。

---

# 任务 E4-02：让真实 paired benchmark 直接产出 canonical V3

## Agent 提示词

你正在执行 E4-02。目标是消除 e3-02 中间文件到 V3 的手工猜测式转换，让真实 paired executor 的事件直接进入唯一 V3 writer。不要重新实现 paired executor，不要调用真实模型。

### 做什么

1. 真实 paired benchmark 完成后直接写 ExperimentArtifactV3。
2. apps/cli/scripts/paired-to-v3.mjs 不得再成为生产必需步骤。
3. 不允许在转换阶段猜测 provider、model、runtimeConfigHash、candidateConfigHash 或 verification。
4. V3 必须保留完整配对记录、隔离资格、预算事实和事件引用。
5. 为全量 eventRecords 定义可控大小策略，但摘要必须可独立重算。

### 怎么做

1. 在 paired executor 的生产结果类型中补齐 V3 writer 所需事实；每个事实必须来自执行时记录，而不是事后默认值。

2. 采用一个 canonical sink：

~~~text
Paired executor event
-> typed in-memory/event-stream record
-> canonical V3 accumulator
-> validate
-> atomic write
-> strict reload
~~~

3. 以下字段禁止使用固定占位值：

- runtimeConfigHash；
- outputDigest；
- workspaceDigest；
- securityOutcomeRef；
- candidateConfigHash；
- model/provider；
- sourceSha；
- planDigest；
- isolation/promotionEligibility。

4. verificationPassed 必须来自真实 verifier 事件，不能从 status 或 actualStatus 推导。

5. activation evidence 必须来自真实 recorder；security outcome 必须来自真实 classifier。

6. 对 eventRecords 设计：

- 小运行可内嵌全部 typed events；
- 大运行可使用 chunk/archive refs；
- 每个 chunk 有 digest、count、firstSeq、lastSeq；
- artifact summary 可从引用事件重算；
- 丢 chunk、重复 seq、乱序、摘要不一致都严格拒绝。

7. 保留 paired-to-v3.mjs 时，只允许作为历史迁移工具：

- 文件头标记 non-production migration；
- 输出 promotionEligible=false；
- 不得被 release/promotion 流程调用；
- 不允许默认猜测缺失字段，缺字段直接失败。

### 怎么验收

必须有一个 fake-provider 生产路径测试：

~~~text
真实 CLI benchmark 入口
-> 真实 paired executor
-> 真实 recorder/classifier
-> 真实 V3 writer
-> strict loader
~~~

断言：

- 没有运行 paired-to-v3.mjs；
- artifact schemaVersion 正确；
- baseline/candidate 每个 (caseId,repetition) 都存在；
- provider/model/config hashes 来自执行计划；
- verification、activation、security 来自事件；
- promotionEligible 与隔离结果一致；
- summary 从 eventRecords 重算一致；
- provider 调用由 fake 计数器精确等于预期。

命令：

~~~bash
pnpm typecheck
pnpm test -- paired-experiment
pnpm test -- artifact-v3
pnpm test
git status --short
~~~

### 交付物

- paired executor 到 V3 的直接生产连接；
- 迁移脚本降级或删除；
- 大小受控的 typed event archive；
- E4-02-report.md。

---

# 任务 E4-03：收紧 Artifact V3 schema 与 strict loader

## Agent 提示词

你正在执行 E4-03。目标是让 ExperimentArtifactV3 的 promotionEligible=true 真正意味着完整、可验证、可重算，而不是“schemaVersion 看起来正确”。不得新增另一套 V4；请在兼容策略明确的前提下收紧现有 V3。

### 做什么

1. 对 manifest、outcomes、provenance、activation、security、summary 做字段级严格验证。
2. 禁止 classifyArtifact 在未验证前返回 promotionEligible=true。
3. 数值字段增加范围、整数和有限性约束。
4. 对所有跨字段关系做一致性检查。
5. 历史 artifact 只能以 legacy/non-promotion 模式读取。

### 怎么做

1. manifest 至少严格验证：

- sourceSha 为完整 Git SHA；
- planDigest、runtimeConfigHash、artifact digest 为合法 hex digest；
- provider/model 非空且来自计划；
- repeat 为正整数；
- caseCount、outcomeCount 为非负整数；
- isolationStrength 为受限枚举；
- promotionEligible 为 boolean；
- decisionPolicyVersion 和 thresholdDigest 存在；
- baseline/candidate config hashes 存在且不同含义明确。

2. outcome 严格验证：

- caseId 非空；
- repetition 为从 1 开始的正整数；
- arm 为 baseline/candidate 枚举；
- passed/verified 等为 boolean；
- grade、ratio 类字段限制在定义范围；
- token、duration、attempt count 为非负整数；
- digest 字段格式正确；
- activation/security refs 必须能解析到本 artifact 的真实事件。

3. provenance 使用已有 ProvenanceV3 的完整结构，不再维护缩减版同名概念。

4. summary 不接受 raw cast。必须：

- 逐字段验证；
- 从 outcomes/events 重算；
- 比较存储值；
- 不一致则 strict load 失败。

5. classifyArtifact 改成以下之一：

- 只返回 detectedSchema，不返回 promotion eligibility；或
- 内部完整调用 strict validator 后才返回 eligibility。

6. 对 legacy：

- 可以识别；
- 可以读取用于展示；
- promotionEligible 永远 false；
- 不得隐式升级成 V3。

### 怎么验收

新增 table-driven negative tests，至少覆盖：

- NaN、Infinity、负 token、负 duration；
- repetition=0、浮点 repetition；
- grade 超范围；
- 空 manifest；
- 非法 SHA/digest；
- 丢失 config hash；
- 不存在的 activation/security ref；
- 重复 event seq；
- summary tamper；
- schemaVersion=3.0.0 但内容无效；
- legacy artifact 冒充 promotion eligible。

命令：

~~~bash
pnpm typecheck
pnpm test -- artifact-v3
pnpm test:protocol
pnpm test
git status --short
~~~

### 交付物

- V3 field-level validator；
- strict loader；
- 安全的 classifier；
- legacy compatibility tests；
- E4-03-report.md。

---

# 任务 E4-04：将 ActivationEvidenceV2 与 SecurityOutcomeV2 接入真实执行链

## Agent 提示词

你正在执行 E4-04。目标是让 activation 和 security 不再只是定义、export 和单测对象，而是由真实 benchmark/harness 事件产生并进入 V3 与决策。所有测试离线。

### 做什么

1. 在真实执行路径调用 createActivationRecorderV2。
2. 在真实工具/安全事件路径调用 classifySecurityOutcomeV2。
3. 每条 outcome 引用可追溯的 activation/security 事件。
4. security 证据缺失时，不允许默认视为 clean。
5. activationEligibleCases 的计算来自完成的 case evidence，不来自 Map.size 等近似值。

### 怎么做

1. 先画出生产事件来源：

- harness requested；
- candidate requested；
- candidate applied；
- verification observed；
- tool attack attempted；
- blocked；
- escaped；
- clean completion。

2. 将 recorder/classifier 注入 paired executor 或 canonical event sink，不要从最终状态倒推。

3. SecurityOutcomeV2 必须区分：

- not_observed；
- not_applicable；
- attempted_blocked；
- attempted_escaped；
- clean_observed；
- classifier_error。

如果现有枚举不同，可调整命名，但必须区分“没有证据”和“证据显示 clean”。

4. 对安全证据：

- 缺失或 classifier_error 时，promotion decision 至少为 INVALID；
- escaped 必须是硬拒绝；
- blocked 是否允许由策略明确决定；
- 事件必须带 caseId、repetition、arm、seq、digest/ref。

5. 对 activation：

- requested 不等于 applied；
- applied 不等于 verified；
- promotion coverage 只计算满足完整链条的 eligible case；
- baseline 与 candidate 的计数口径一致。

6. 删除或弃用生产中的旧 activationEvidenceFor 简化路径；如为兼容保留，必须标记 legacy 且不可促销。

### 怎么验收

新增真实 fake execution 测试：

- candidate requested + applied + verified；
- requested 但未 applied；
- applied 但 verification failed；
- security attempted_blocked；
- security attempted_escaped；
- 没有 security observer；
- classifier 抛错；
- 多 repetition 不串案。

断言：

- 生产 recorder/classifier 的调用次数正确；
- V3 refs 全部可解析；
- coverage 分母、分子可手算；
- escaped/缺证据不会 ACCEPT；
- 旧 helper 没有生产消费者。

命令：

~~~bash
rg -n "createActivationRecorderV2|classifySecurityOutcomeV2|activationEvidenceFor" apps packages
pnpm typecheck
pnpm test -- activation
pnpm test -- security
pnpm test
git status --short
~~~

### 交付物

- 真实事件接入；
- 完整 V2 evidence 写入；
- 缺失证据的严格语义；
- E4-04-report.md。

---

# 任务 E4-05：修复 V3 的精确配对、重复计数和计划策略绑定

## Agent 提示词

你正在执行 E4-05。目标是修复当前 evaluator 可把错配 repetition 评为 ACCEPT 的高风险问题，并移除硬编码策略阈值。不要改变已冻结的 BA 配对执行顺序语义。

### 做什么

1. 配对键必须是 (caseId, repetition)，必要时再包括 suite/variant。
2. baseline 和 candidate 的键多重集合必须完全一致。
3. 重复、缺失、越界和额外键都必须 INVALID。
4. 决策阈值必须来自运行前计划，而不是 evaluator 内硬编码。
5. policy 与 planDigest、thresholdDigest 必须进入 DecisionArtifact。

### 怎么做

1. 定义 canonical PairKey：

~~~text
suite + NUL + caseId + NUL + repetition
~~~

或者等价的无碰撞结构化编码。禁止简单字符串拼接导致歧义。

2. 建立两个 Map<PairKey, Outcome>：

- 插入时发现重复立即报错；
- 比较 key set；
- 任一边缺失或多余即 pairComplete=false、decision=INVALID；
- 不再用 Map<caseId, Outcome> 覆盖重复。

3. repeat 语义：

- 每个 case 的合法 repetition 必须恰好为 1..repeat；
- repeat 来自已验证计划；
- artifact manifest、outcome 实际集合和计划三者一致。

4. perRepetitionDeltas：

- 每个 repetition 分别在相同 case 集上计算；
- 不得对缺失项按 0；
- 汇总等于各 repetition 明细之和；
- netPassedDelta 不允许重复累计。

5. 将以下硬编码阈值移入 versioned DecisionPolicy：

- minActivationEligibleCases；
- minActivationCoverage；
- maxVerifiedDrop；
- minConclusiveNetDelta；
- maxTokensDelta；
- security 规则；
- recovery rule。

6. evaluator 接受完整已验证 plan/policy 对象，并核验其 digest。不得仅接收任意 planDigest 字符串。

### 怎么验收

把本次真实复现固定成测试：

~~~text
baseline: case 1..3, rep=1
candidate: case 1..3, rep=1..2
expected: INVALID
~~~

再覆盖：

- candidate 缺 rep=2；
- baseline 多 rep=3；
- 同 arm 重复同 PairKey；
- repetition=0；
- 不同 suite 同 caseId；
- manifest repeat 与实际不一致；
- policy threshold 被 artifact 篡改；
- planDigest 相同但 policy 内容不同；
- 完整 2 repetitions 正向 ACCEPT/REJECT。

命令：

~~~bash
pnpm typecheck
pnpm test -- champion-eval-v3
pnpm test -- paired-experiment
pnpm test
git status --short
~~~

### 交付物

- canonical PairKey；
- exact multiset validator；
- versioned DecisionPolicy；
- 本次 mismatch regression test；
- E4-05-report.md。

---

# 任务 E4-06：重建 promotion 的完整信任边界

## Agent 提示词

你正在执行 E4-06。这是最高优先级安全任务。目标是让 promotion loader 无法仅凭一个写着 ACCEPT 的 JSON 和正确文件 SHA 推进 champion。必须验证“内容是什么、由哪个计划产生、如何得出决策、引用是否一致”。所有测试离线、使用临时目录。

### 做什么

1. strict load DecisionArtifact，并重新计算 contentDigest。
2. strict load baseline/candidate ExperimentArtifactV3。
3. 验证所有 artifact refs、sourceSha、planDigest、policy、config hashes 的交叉绑定。
4. 重新运行纯函数 evaluator，并与存储的 DecisionArtifact 完整比较。
5. promotion envelope 本身也必须 canonical digest 验证。
6. 任一不一致必须拒绝，且状态文件不能变化。

### 怎么做

1. loadPromotionEnvelope 的验证顺序：

~~~text
读取 envelope
-> schema 验证
-> canonical digest 验证
-> 限定并解析 refs
-> 校验每个文件字节 SHA
-> strict load baseline V3
-> strict load candidate V3
-> strict load DecisionArtifact
-> 重算 DecisionArtifact.contentDigest
-> 核验 sourceSha/planDigest/policy/config hashes
-> 核验 candidate promotionEligible=true
-> 重新 evaluate
-> 比较完整 decision payload
-> 允许进入 applicationPending
~~~

2. artifactRefs 改为有角色的严格结构：

- baselineArtifact；
- candidateArtifact；
- decisionArtifact；
- executionPlan；
- 可选 event archive。

禁止任意非空数组或无法识别的 ref。

3. 路径必须：

- 相对受信任 bundle root；
- resolve 后仍在 root 内；
- 拒绝绝对路径、..、symlink escape；
- 每个角色仅一个文件；
- 禁止同一文件冒充多个角色。

4. DecisionArtifact 至少绑定：

- baseline artifact digest；
- candidate artifact digest；
- sourceSha；
- planDigest；
- policyVersion；
- thresholdDigest；
- evaluatorVersion；
- decision；
- reasons/metrics 的 canonical digest。

5. 不接受“只要 decision=ACCEPT”。必须用当前兼容 evaluator 重算并得到完全相同结果。

6. 验证失败前不得调用 CAS writer。

### 怎么验收

首先加入本次伪造复现，预期由 accepted=true 改为拒绝。

负向矩阵至少包括：

- candidate 是普通文本但 SHA 正确；
- DecisionArtifact.contentDigest 随机；
- policyVersion 随机；
- 修改 decision 为 ACCEPT；
- baseline ref 指向 candidate；
- planDigest 不一致；
- sourceSha 不一致；
- thresholdDigest 不一致；
- artifact 内容变更后只更新文件 SHA；
- envelope digest 未更新；
- path traversal；
- symlink escape；
- promotionEligible=false；
- evaluator 重算为 REJECT、文件写 ACCEPT；
- 验证失败后 champion state 字节完全不变。

正向测试必须使用由真实 V3 writer 和 evaluator 生成的完整 bundle。

命令：

~~~bash
pnpm typecheck
pnpm test -- promotion-envelope
pnpm test -- champion
pnpm test:security
pnpm test
git status --short
~~~

### 交付物

- strict promotion bundle loader；
- 完整 cross-binding validator；
- evaluator replay；
- forged promotion regression suite；
- E4-06-report.md。

---

# 任务 E4-07：实现 applicationPending -> 实际应用 -> applied 的真实生命周期

## Agent 提示词

你正在执行 E4-07。目标是修复 champion promote 当前直接写 applied=true 的语义错误，并把 champion 配置真正接入 CLI/Web 的 createHarness。不得用纯 mapper 测试冒充生产应用。

### 做什么

1. promote 只写 applicationPending，applied=false。
2. 真实进程启动时读取、验证并应用 champion profile。
3. 只有实际 createHarness 配置与目标 champion 完全一致后，才能写 applied proof。
4. CLI 和 Web 使用同一 resolver/verification path。
5. 应用失败时保留旧 champion 或进入明确失败状态，不得伪称 applied。

### 怎么做

1. 定义状态机：

~~~text
PROVEN
-> APPLICATION_PENDING
-> APPLYING
-> APPLIED

APPLICATION_PENDING/APPLYING
-> APPLICATION_FAILED

APPLIED
-> new promotion
-> APPLICATION_PENDING
~~~

如果现有状态命名不同，可映射，但语义必须一致。

2. champion promote：

- 完成 E4-06 验证；
- CAS 写 candidate target、expected generation、applicationPending；
- applied=false；
- 不调用 proveApplication 假装完成。

3. CLI/Web 启动：

- 读取 champion state；
- strict validate；
- resolveChampionHarness；
- 把解析结果传给真实 createHarness；
- 从 createHarness 的最终规范化配置计算 appliedConfigHash；
- 与 targetConfigHash 比较；
- 成功后使用 CAS 写 applied proof。

4. AppliedProof 至少包含：

- state generation；
- targetConfigHash；
- appliedConfigHash；
- runtime entrypoint；
- sourceSha；
- process/startup identifier；
- appliedAt；
- proof digest。

5. 如果 createHarness 回退到默认配置、环境覆盖、无效 profile：

- 不得 APPLIED；
- 记录明确原因；
- 不得静默继续为 candidate。

6. 搜索并确保生产消费者存在：

~~~bash
rg -n "resolveChampionHarness|championHarnessConfigFromProfile|createHarness" apps packages
~~~

### 怎么验收

真实集成测试至少覆盖：

- promote 后状态为 applicationPending、applied=false；
- 启动真实 CLI harness 后配置确实改变；
- 启动真实 Web harness 后配置确实改变；
- applied proof 的 config hash 来自 createHarness 最终配置；
- createHarness 失败时不写 applied；
- config mismatch 不写 applied；
- CAS generation 冲突不覆盖新状态；
- 重启已 applied 状态幂等；
- 未促销时保持默认配置。

禁止只断言 resolveChampionHarness 的返回对象。

命令：

~~~bash
pnpm typecheck
pnpm test -- champion-state
pnpm test -- create-harness
pnpm test -- cli
pnpm test -- web
pnpm test
git status --short
~~~

### 交付物

- 正确状态机；
- CLI/Web 的真实 createHarness 接线；
- AppliedProof；
- E4-07-report.md。

---

# 任务 E4-08：将 recovery store 变成生产可持久化、可恢复的依赖

## Agent 提示词

你正在执行 E4-08。目标是让 session recovery 在真实进程重启后保留 attempts、nextAttemptAt、lease 和 terminal decision。现有 actor 算法尽量不改，只补齐生产持久化与失败语义。

### 做什么

1. 为 CLI/Web/createHarness 注入 durable RecoveryStore。
2. MemoryRecoveryStore 仅用于测试或显式 ephemeral 模式。
3. store 写失败时不得继续产生无法恢复的外部副作用。
4. 进程重启后必须恢复退避、租约和终态。
5. 多进程/多 worker 争用采用版本或 CAS。

### 怎么做

1. 定义 production store 的最小操作：

- load(sessionId)；
- compareAndSet(sessionId, expectedVersion, nextRecord)；
- listDue(now, limit)；
- acquireLease(sessionId, owner, expiresAt, expectedVersion)；
- release/complete；
- 原子持久化。

2. 选择与项目现状匹配的后端：

- 如果已有 durable state/file store，复用；
- 本地 CLI 可用原子文件 + lock + fsync/rename；
- Web 端应通过注入 adapter，不把本地文件假设写死在 core 包。

3. actor 启动时：

- load record；
- 校验 schema；
- 恢复 attempts/nextAttemptAt；
- 过期 lease 可重新获取；
- 未过期 lease 不重复执行；
- terminal 状态永不重试。

4. persist 失败的顺序：

- 先持久化意图/lease；
- 成功后才执行可能重复的恢复动作；
- 动作完成后持久化结果；
- 不能 catch 后只写 warning 并继续当作成功。

5. 提供迁移/损坏处理：

- unknown schema fail closed；
- corrupted record 隔离并报告；
- 不自动清零 attempts。

### 怎么验收

使用 fake clock 和两个独立 actor/store client：

- actor A 写 attempt=1 后“进程退出”，actor B 恢复；
- nextAttemptAt 未到时不执行；
- 到期后只执行一次；
- 两个 actor 同时抢 lease，只有一个成功；
- lease 过期后可恢复；
- terminal decision 重启后不再执行；
- store write failure 时外部 action 调用数为 0；
- completion write failure 能在重启后安全判重；
- corrupted record 不执行 action。

生产接线检查：

~~~bash
rg -n "recoveryStore|MemoryRecoveryStore|new SessionActor|createHarness" apps packages
~~~

命令：

~~~bash
pnpm typecheck
pnpm test -- session-actor
pnpm test:race
pnpm test:chaos
pnpm test
git status --short
~~~

### 交付物

- durable store adapter；
- CLI/Web 注入；
- crash/restart/concurrency tests；
- E4-08-report.md。

---

# 任务 E4-09：重写真正的生产路径 E2E，禁止手工制造通过结果

## Agent 提示词

你正在执行 E4-09。目标是用一个真正端到端、完全离线的测试证明整条 E3/E4 生产链。当前 e3-13 测试会先跑一次真实 benchmark，但随后忽略其产物并手工构造 outcomes；必须消除这种证据替身。

### 做什么

1. 从真实 CLI/programmatic benchmark 入口开始。
2. 使用 fake provider，但走真实 paired executor。
3. 自动得到真实 V3 artifact。
4. 通过真实 evaluator 得到 DecisionArtifact。
5. 通过真实 promotion loader/CAS 写 applicationPending。
6. 启动真实 createHarness 应用 champion。
7. 验证 applied proof 与运行时行为。
8. 覆盖高价值攻击路径。

### 怎么做

1. 每个中间文件都必须由上一生产阶段返回，禁止调用 buildV3ArtifactPair 一类测试 helper 直接填 passed/verified/security。

2. 测试应在 mkdtemp bundle root 中运行，所有路径通过依赖注入传入，禁止污染仓库。

3. 正向链：

~~~text
fake cases
-> fake deterministic provider
-> CLI benchmark
-> paired executor
-> canonical V3
-> strict reload
-> champion evaluator
-> DecisionArtifact writer
-> promotion bundle writer
-> champion promote
-> applicationPending
-> actual createHarness startup
-> applied proof
-> observable candidate behavior
~~~

4. “observable candidate behavior”不能只是配置对象相等；至少观察一个真实运行决策/恢复策略/工具策略因 champion 配置而变化。

5. 攻击链至少覆盖：

- forged ACCEPT；
- mismatched repetition；
- promotionEligible=false；
- artifact summary tamper；
- path traversal；
- state CAS race；
- application config mismatch；
- recovery store restart；
- provider over-call guard。

6. provider spy 必须记录：

- call count；
- logical run id；
- caseId；
- repetition；
- arm；
- call sequence。

### 怎么验收

验收测试必须能证明：

- 每个 artifact 的 producedBy 指向真实 production stage；
- 没有手工覆盖 passed/verification/security；
- 正向链最终 applied=true；
- 删除真实 createHarness 接线后测试会失败；
- 修改任一 artifact 字节后链条会失败；
- 负向链不会改变 champion state；
- provider call count 精确；
- 测试结束工作区干净。

命令：

~~~bash
pnpm typecheck
pnpm test -- production-path
pnpm test -- adversarial
pnpm test:security
pnpm test:race
pnpm test:chaos
pnpm test
git status --short
~~~

### 交付物

- 新 production-path E2E；
- adversarial E2E；
- 删除或降级手工 artifact helper；
- E4-09-report.md。

---

# 任务 E4-10：建立当前 HEAD 的唯一门禁证据与诚实 smoke 语义

## Agent 提示词

你正在执行 E4-10。目标是让 capability、docs、release 和 smoke 从同一个当前 HEAD 证据源得出结论，消除“命令通过但实际功能失败”或“旧 ledger 通过但当前 release 未运行”的情况。不得运行付费模型。

### 做什么

1. 建立 GateEvidenceV2 的真实生成器和生产消费路径。
2. 增加 production usage audit，区分 exported、tested、wired、observed。
3. 修复 benchmark:smoke 的退出码语义。
4. docs:verify 必须验证当前 handoff，而不仅是旧 ledger。
5. release:verify 必须读取当前 sourceSha 的证据。
6. 所有 gate 产物从命令执行结果生成，不手写 PASS。

### 怎么做

1. GateEvidenceV2 每条证据至少包含：

- gateId；
- sourceSha；
- command；
- exitCode；
- startedAt/finishedAt；
- artifact refs/digests；
- result PASS/FAIL/NOT_RUN；
- providerCalls；
- environment class；
- tool version。

2. 生成器必须：

- 运行明确 allowlist 中的离线命令；
- 捕获真实退出码；
- 原子写证据；
- 证据 sourceSha 不等于 HEAD 时标记 stale；
- 禁止把缺文件当 PASS。

3. production usage audit 对关键能力逐项判断：

| 状态 | 定义 |
|---|---|
| exported | 只有公开符号 |
| tested | 只有直接单测 |
| wired | 生产入口可达 |
| observed | E2E 中实际调用 |

关键能力至少包括：

- createActivationRecorderV2；
- classifySecurityOutcomeV2；
- canonical V3 writer；
- strict promotion loader；
- resolveChampionHarness；
- durable RecoveryStore；
- GateEvidenceV2 generator。

4. benchmark:smoke：

- 任一 case FAIL 时命令非 0；
- 如果命令只验证“进程可运行”，重命名为 benchmark:boot-smoke；
- release gate 使用语义严格的 smoke；
- JSON 中 actualStatus 与退出码一致。

5. docs verifier：

- 当前 handoff sourceSha=HEAD；
- 任务状态与 gate evidence 一致；
- capability table 与 usage audit 一致；
- 历史文档明确 historical；
- 命令在 package.json 中真实存在。

6. 新增建议脚本：

~~~text
e4:verify-production-usage
e4:verify-invariants
e4:generate-gate-evidence
e4:verify-handoff
~~~

名称可调整，但不得只新增空包装。

### 怎么验收

~~~bash
pnpm typecheck
pnpm build
pnpm test
pnpm test:coverage
pnpm test:protocol
pnpm test:security
pnpm test:race
pnpm test:chaos
pnpm benchmark:smoke
pnpm capability:audit
pnpm docs:verify
pnpm release:verify
git status --short
~~~

预期：

- smoke 的用例实际通过；否则命令必须非 0；
- capability:audit PASS，且关键能力为 observed；
- docs:verify 使用当前 HEAD；
- release:verify 不再全部 NOT_RUN；
- evidenceFresh 和 requiredEvidenceFresh 均 PASS；
- 所有证据 sourceSha 等于 HEAD；
- providerCalls=0；
- git status 为空。

### 必须新增的负向测试

- 证据 sourceSha 旧；
- 手改 PASS 但 exitCode 非 0；
- 缺 artifact ref；
- capability 只有 export 没有生产调用；
- smoke case FAIL；
- docs 指向旧 handoff；
- gate 文件内容变更但 digest 未更新。

### 交付物

- GateEvidenceV2 generator；
- production usage audit；
- 当前 handoff；
- 严格 smoke；
- E4-10-report.md。

---

# 可选任务 E4-11：在明确授权后执行一次有硬上限的真实付费复跑

## Agent 提示词

你正在执行可选任务 E4-11。只有用户在当前轮次明确授权具体 provider、预算和 planDigest 后才能执行。没有授权时，立即停止并报告“NOT AUTHORIZED”，不得尝试读取或使用现有密钥。

### 前置条件

以下全部满足才能继续：

- E4-00 至 E4-10 全部通过；
- git status 为空；
- release:verify 通过；
- strong isolation 可用；
- promotionEligible=true；
- 完整 BenchmarkExecutionPlan 已打印；
- 用户明确确认 planDigest；
- 用户明确确认最大美元预算；
- 用户明确确认 provider/model；
- RUN_PAID_BENCHMARKS=1 仅在本任务进程内设置。

### 做什么

1. 先 dry-run，保存计划与 digest。
2. 让用户确认完全相同的 digest。
3. 执行最小规模真实 paired run。
4. 实时执行硬调用、token、成本和时间上限。
5. 直接产出 canonical V3，不运行手工转换脚本。
6. 生成 evidence，但不自动 promote。

### 怎么做

1. 第一次运行建议只覆盖最小可解释集合，不追求统计结论。
2. 必须设置：

- maxLogicalRuns；
- maxModelCalls；
- maxEstimatedTokens；
- maxEstimatedCostUsd；
- wall-clock timeout；
- exact case list；
- repeat；
- seed；
- interleave；
- strong isolation。

3. 任一实际计数接近上限时停止；不能把估算超差转成无限继续。
4. 运行结束后：

- strict load V3；
- 重算 summary；
- 验证 pair keys；
- 记录实际花费；
- 标记 run 为 exploratory 或 conclusive；
- 不自动写 champion state。

### 怎么验收

- 用户确认的 digest 与执行 digest 完全一致；
- provider/model 与计划一致；
- 实际调用未超硬上限；
- artifact promotionEligible=true；
- strict V3 load 通过；
- pairComplete=true；
- evidence sourceSha=HEAD；
- 没有自动 promotion；
- 没有密钥进入日志或 artifact；
- git status 为空。

### 未授权时的唯一正确输出

~~~text
E4-11 NOT AUTHORIZED.
No provider was created.
Provider calls: 0.
No paid benchmark was run.
~~~

### 交付物

- confirmed plan；
- canonical V3 artifact；
- cost/call ledger；
- E4-11-report.md；
- 用户后续可单独审阅的 promotion bundle 草案。

---

## 4. 推荐执行顺序与依赖

| 顺序 | 任务 | 依赖 | 是否可与其他任务并行 |
|---:|---|---|---|
| 1 | E4-00 | 无 | 可单独先做 |
| 2 | E4-01 | E4-00 | 与 E4-03 可部分并行 |
| 3 | E4-03 | E4-00 | 与 E4-01 可部分并行 |
| 4 | E4-04 | E4-03 | 不建议与 E4-02 并行修改同文件 |
| 5 | E4-02 | E4-01、E4-03、E4-04 | 顺序执行 |
| 6 | E4-05 | E4-01、E4-03 | 可与 E4-08 并行 |
| 7 | E4-06 | E4-03、E4-05 | 顺序执行 |
| 8 | E4-07 | E4-06 | 顺序执行 |
| 9 | E4-08 | E4-00 | 可较早独立完成 |
| 10 | E4-09 | E4-02、E4-05、E4-06、E4-07、E4-08 | 顺序执行 |
| 11 | E4-10 | E4-09 | 最后离线收口 |
| 12 | E4-11 | E4-10 + 用户当轮授权 | 可选、默认不执行 |

最短关键路径：

~~~text
E4-00
-> E4-01 / E4-03
-> E4-04
-> E4-02
-> E4-05
-> E4-06
-> E4-07
-> E4-09
-> E4-10
-> optional E4-11
~~~

---

## 5. 每个 agent 的统一报告模板

每个任务完成后必须提交一份报告，使用以下结构：

~~~markdown
# <TASK-ID> Report

## Outcome
- Status: DONE | PARTIAL | BLOCKED
- Source SHA before:
- Source SHA after:
- Provider calls:

## What changed
- Production files:
- Test files:
- Docs/evidence files:

## Why this closes the task
- 生产入口：
- 数据来源：
- 信任边界：
- 失败语义：

## Tests
| Command | Exit code | Result | Notes |
|---|---:|---|---|

## Required negative reproductions
| Reproduction | Before | After |
|---|---|---|

## Worktree
- git status --short:
- Temporary files cleaned:

## Remaining risks
- ...
~~~

如果任务是 PARTIAL 或 BLOCKED，禁止写“完成”。必须列出未完成项、阻断原因和下一条可执行命令。

---

## 6. 最终发布判定

只有全部满足才可以把 E3/E4 标记为完成：

- E4-00 至 E4-10 全部 DONE；
- typecheck/build/test/coverage 全绿；
- protocol/security/race/chaos 全绿；
- benchmark:smoke 的实际 case 通过；
- capability:audit 全绿且关键能力为 observed；
- docs:verify 验证当前 HEAD；
- release:verify 使用当前 HEAD 证据并通过；
- forged promotion 被严格拒绝；
- mismatched repetition 被判 INVALID；
- promote 后先 applicationPending；
- 真实 createHarness 应用后才 applied；
- recovery 在进程重启后保持状态；
- 真实 E2E 不手工构造 passed/verified/security；
- 离线阶段 providerCalls=0；
- git status --short 为空。

以下任一情况都必须保持 BLOCKED：

- promotion loader 仍只检查字段非空或文件 SHA；
- evaluator 仍用 caseId-only Map；
- policy 阈值仍硬编码且不绑定计划；
- champion profile 仍没有真实 createHarness 消费者；
- recovery 默认生产 store 仍是 memory；
- insecure benchmark 可以转换成 promotionEligible=true；
- gate evidence sourceSha 不是 HEAD；
- smoke case FAIL 但进程退出 0；
- 付费运行没有当前轮明确授权。

---

## 7. 给项目维护者的建议

当前最值得优先做的不是更多真实模型跑分，而是 E4-05、E4-06、E4-07：

1. E4-05 修复“比较错对象仍可 ACCEPT”；
2. E4-06 修复“伪造 ACCEPT 可 promote”；
3. E4-07 修复“没有真实应用却写 applied=true”。

这三项完成后，promotion 才从“有格式的文件流”变成可信状态转换。随后做 E4-02/E4-04 把真实证据接入，再用 E4-09/E4-10 建立不可手工伪造的端到端门禁。真实付费复跑应始终放在最后，而且默认不执行。
