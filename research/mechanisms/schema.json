{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Mechanism Manifest",
  "type": "object",
  "required": [
    "id",
    "source_agent",
    "source_report",
    "provenance",
    "category",
    "problem",
    "preconditions",
    "expected_benefit",
    "risks",
    "implementation_scope",
    "evaluation_cases",
    "status"
  ],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z][a-z0-9_-]+$" },
    "source_agent": { "type": "string", "enum": ["codex", "claude-code", "hermes", "opencode", "pi"] },
    "source_report": { "type": "string", "pattern": "^reports/[a-z-]+/.+" },
    "provenance": {
      "type": "string",
      "enum": ["original", "inspired", "reimplemented", "derived"]
    },
    "attribution": { "type": "string" },
    "category": {
      "type": "string",
      "enum": ["prompting", "memory", "planning", "tool_use", "learning", "scheduling", "error_recovery", "context_management", "evaluation", "security", "other"]
    },
    "problem": { "type": "string" },
    "preconditions": { "type": "string" },
    "expected_benefit": { "type": "string" },
    "risks": { "type": "string" },
    "implementation_scope": { "type": "string" },
    "evaluation_cases": {
      "type": "array",
      "items": { "type": "string" }
    },
    "status": {
      "type": "string",
      "enum": ["candidate", "proposed", "evaluating", "accepted", "rejected", "shipped"]
    }
  }
}