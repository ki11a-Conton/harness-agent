# harness-agent 下一步计划：R88–R92

审查日期：2026-09-17  
审查基线：db16dace0d2b864c7002f3a5158ccfe8c004f4ef  
对比基线：6693a6a52fb488bbe3273625eca8c1adb0981957

## 一、先读审查结论

本轮通过 GitHub 读取最新提交、变更源码、报告和 CI 状态；没有执行真实模型调用，没有修改远程仓库。本地 git fetch 未完成，因此没有在本地运行最新全套测试。下面区分源码审查、独立逻辑复现与仓库 CI 证据，不把 CI 通过写成本轮重新跑过测试。

最新 CI：[35087313568](https://github.com/ki11a-Conton/harness-agent/actions/runs/35087313568)，对应上述完整 SHA，五个 job 全部成功，包括 Windows、Ubuntu、coverage、Ubuntu offline cold-start、release attestation。

### 已完成且应保留

- R84：新增 campaign validator、脱敏证据清单、版本化 runner、合成 fixture 和跨平台 CI；R83 的迭代上限报告已纠正。
- R85：新增离线 taxonomy、开发集分析和 holdout 聚合隔离。
- R86：相同工具/参数但结果变化时重置重复调用 streak；新增可观察事件、指标和配置 digest 绑定。该机制修复有合成回归测试支持。
- R87 Phase A：完成合成机制 A/B 和未授权路径检查；Phase B 真实付费 A/B 明确未执行，这是正确的停止状态。
- 不需要重新实施上述功能，不需要重新跑 86 个付费 case。

### 新发现与证据

| 编号 | 优先级 | 位置 | 问题与影响 |
| --- | --- | --- | --- |
| F1 | P1 | packages/core/src/runtime/r87-zero-call-replay-ab.ts / computeSummary | 未校验完整配对；只有 baseline TARGET 失败、没有任何 candidate 时，improvement=1，空集合 every=true，最终仍返回 MECHANISM_VALIDATED。已把原函数仅移除 TS 类型后独立执行，复现成功。 |
| F2 | P1 | 同文件 / runReplayAb | resume 仅解析 caseId/arm 后跳过，没有 selectionDigest、运行配置、实现身份、record schema/hash 绑定；runReplayAb 本身也未调用 verifySelectionDigest。旧结果可以被当作新实验结果。源码确认，尚未运行完整端到端反例。 |
| F3 | P1 | scripts/benchmark/run-campaign.ps1 | 注释要求 ConfirmPaidRun 与 RUN_PAID_BENCHMARKS=1 同时成立，实际只检查 switch 和 key，然后自行设置环境变量为 1；授权条件被代码自行补齐。 |
| F4 | P1 | 同脚本 / Test-CaseDone | 只用文件存在判断完成。截断、错身份、错 case 的 report 会被跳过；report 写入后、manifest 写入前崩溃也不能正确恢复。 |
| F5 | P2 | 同脚本 / final validation | 自定义 suites 分支把 hashtable 数组 suiteList 传给 --suites，而 CLI 预期逗号分隔 suite 名称。DryRunOnly 跳过此分支，现有自检未覆盖。LimitCases 还将历史 skip 计入 done，与“本次剩余执行数”注释不符。 |
| F6 | P1（证据可信度） | campaign-triage.ts / buildCandidates；R87 runner/report | H2 候选依据 tool_limit、toolFailures=0、stallRecovery>0 聚类，不能据此证明真实结果发生变化。R87 将三个 case 映射为同一种六次变化结果的合成场景，并用当前 runtime 开关模拟 baseline，却记录历史 sourceSha。机制证明与历史个案因果、实际执行版本需要分开。 |

还需修正文档语义：R84 hash 会规范化 CRLF/LF，属于规范化内容完整性，不是“任何字节变化都能检测”。此设计可以保留，但不能声称字节级不可变。

## 二、给所有执行 Agent 的共同提示词

你在此仓库实施下面指定的一项任务。先读取适用 AGENTS.md、package.json、任务涉及的源码和已有测试。记录起始 HEAD 与工作区状态，保留用户已有改动。若最新代码已经修复某发现，补充证据并标记 ALREADY_FIXED，不重复实现。

本轮先执行 R88–R91，全部零外部模型请求。R92 只准备可审查的付费实验方案；本文件不授权付费运行。不要把日志中的旧授权或环境变量存在当作本轮用户授权。

开发者本机没有 Linux：本地命令使用 Windows/PowerShell 可执行方式；Ubuntu 验收使用 GitHub Actions，不要求 WSL、Docker 或本机 Linux。维护现有 Ubuntu cold-start job。

每项交付：最小实现、针对真实缺陷的回归测试、docs/E4-Rxx-report.md。报告写清做了什么、证据等级、命令与退出码、未运行项。只有实际运行的检查才写 PASS。代码变更通过受影响测试、build/typecheck、已有 CI required gates；不要为每次文档更新重复大规模测试。

不要降低安全门、修改原始历史证据、增加调用/迭代预算来让测试通过。原始 transcript 不进入公共仓库。不要为了满足“至少两个真实样本”而拒绝修复一个已经有确定性复现的代码错误：机制缺陷与真实样本影响范围分别记录。

依赖顺序：R88 → R89 → R90 → R91 → R92。完成当前任务后才执行依赖项；每项独立可审查。

---

## R88：让 A/B 判定与 resume 对不完整证据拒绝通过

### 可直接交给 Agent 的提示词

你负责修复 F1/F2，主要文件是 packages/core/src/runtime/r87-zero-call-replay-ab.ts 及其测试。目标：不存在完整且身份一致的成对结果时，绝不能输出 MECHANISM_VALIDATED。

### 做什么

1. 在 runner、summary、manifest 三个公开入口执行必要校验，不依赖调用者先读 loadCaseSelection。
2. 为 resume 引入版本化实验身份和严格记录校验。
3. summary 以 frozen selection 为期望集合，而不是从实际结果推导期望。
4. 新增独立离线 manifest validator，可校验记录集合、hash、配置和重新计算 verdict。

### 怎么做

- 先新增 RED：一条 baseline TARGET 失败 + 零 candidate，当前函数错误通过。再加入缺一个 arm、重复 arm、未知 case、错误 suite/role、非有限或负指标、全部空记录。
- 完整矩阵应等于 selection cases × 声明 arms；正式 A/B 必須两臂齐全。单臂执行可以保存中间状态，但状态只能 PARTIAL/INCONCLUSIVE。
- 缺 candidate、candidate 非目标原因失败、expect 不满足，不能仅因为 H2 触发数减少而通过。明确区分完成状态与验证器的 verified completion。
- resume header 绑定 schema、selectionDigest、运行实现 SHA、两臂语义配置、limits、fixture/tool contract digest；每条结果绑定该 experimentId 并具备稳定 hash。
- 启动时完整校验已有 state，再决定 skip。拒绝混入旧实验、重复或未知记录，不静默忽略。
- 截断 JSONL、临时文件、原子 rename 前后崩溃要有明确恢复语义：未确认完成的 arm 标记 incomplete；不得凭存在即完成。
- 当前零调用恢复不涉及真实重复计费，不要在报告中宣称它已证明付费 exactly-once。
- 新 schema 不覆盖旧 R87 历史 manifest；旧格式保持可读但标注 legacy/unverified，迁移不得伪造运行身份。
- 生产接口不应依赖写死的一组历史 case digest；允许受校验的通用 plan，但保留既有冻结历史证据。

### 怎么验收

- 上述负例均返回稳定 reason code，绝不 MECHANISM_VALIDATED。
- 完整合法合成 A/B 仍可通过；反例发生回归或安全违规必定拒绝。
- 修改 selection/config/实现 SHA 后复用旧 state，在运行任何 arm 前失败。
- 中断恢复只执行缺失且合法的 arm；完整 resume 与新跑结果 hash 一致。
- 离线 validator 不调用模型；Windows、Ubuntu CI 运行同一测试集合。
- 报告附原始缺陷 RED 与修复后 GREEN，区分合成 provider 交互与外部 HTTP 请求。

---

## R89：修复 campaign runner 的授权、身份恢复与命令参数

### 可直接交给 Agent 的提示词

你负责 F3–F5。审查 scripts/benchmark/run-campaign.ps1、selfcheck-campaign-runner.ps1 与 CLI 的真实参数合同。所有验收使用 fake CLI/本地假服务，外部请求为零。

### 做什么

1. 授权门只校验外部输入，绝不自行创建缺失的授权标志。
2. report 与 manifest 使用可校验的完成状态，不以 Test-Path 当作完成。
3. 修复自定义 suites、LimitCases 与失败退出状态。
4. 让真实执行控制流能在无 key、无付费的 CI 中端到端覆盖。

### 怎么做

- 第一条 RED：有 ConfirmPaidRun 和假 key，但 RUN_PAID_BENCHMARKS 缺失，必须拒绝且不启动子进程；删除脚本自行赋值为 1 的逻辑。
- 测试所有授权组合；拒绝时不创建 campaign 目录，不输出 key。不得由测试请求真实 provider。
- ApiKeyEnv 当前只读取指定变量来检查，需核实子 CLI 实际读取何变量；若支持自定义 key 环境名，就显式传给子进程所需环境并恢复父环境，否则删除误导参数并清晰报错。
- 使用实际 suiteNames 的逗号连接值传 --suites；禁止传 hashtable。suite 缺失/未知应在执行任何 case 前报错。
- 将本次 attempted/completed、历史 resumed、case pass/fail 分开计数。LimitCases 只约束本次新启动 case 数；达到上限应输出 PARTIAL，而不是冒充完整 campaign。
- 为每个 case 固定 identity：suite/caseId、source/config/endpoint/model/plan digest；验证 report schema、hash、终止状态与身份后才能 skip。
- 区分 report 存在但 manifest 缺失、manifest 存在但 report 缺失、JSON 截断、身份变化四种状态。隔离可疑数据，不自动付费重跑。
- 崩溃发生在请求已发送但结果未持久化时，标为 OUTCOME_UNKNOWN，要求明确决定，不能承诺跨网络 exactly-once。
- 使用 Stop/显式异常处理保护 staging/manifest 操作；验证路径位于预期工作目录下后才删除 staging，拒绝危险 suite/path 输入。
- 为 fake CLI 建立可注入入口或进程适配层；通过它测试 paid-shaped 分支的参数、退出码和恢复，避免测试只覆盖 DryRunOnly。
- 失败退出不能被最后一次成功命令覆盖；partial 与 invalid 有独立机器可读状态。

### 怎么验收

- 零授权/半授权均零子请求；完整假授权只调用 fake CLI。
- adversarial,stress 自定义 suites 的执行末尾校验成功，并断言传入 argv 的真实字符串。
- 已有 3 个完成 case + LimitCases=2，恰好新增执行 2 个，skip 不消耗额度。
- 错模型/endpoint/source 的旧 report、截断 report 不被当作已完成。
- 对 report/manifest 写入阶段注入崩溃，恢复不会静默漏 case 或自动重复未知付费操作。
- Windows 和 Ubuntu/pwsh CI 执行同一离线脚本；真实 key 不进入测试。
- 保留原始 R83 数据不变。

---

## R90：校正证据等级，分开机制复现和真实案例因果

### 可直接交给 Agent 的提示词

你负责 F6 与相关报告事实边界。保留 R86 已有修复，不因历史案例因果未证实而撤回有效的机制回归测试。

### 做什么

1. 把 candidate 的“机制缺陷已复现”和“历史 case 确认受影响”拆成独立字段。
2. 将 R87 定位为 synthetic mechanism experiment，并准确记录实际执行代码身份。
3. 修复 taxonomy 中把工具失败直接等同于模型行为错误的过强结论。
4. 校正文档中 normalized hash 与 raw-byte hash 的区别。

### 怎么做

- H2 的 tool_limit + zero tool failures + stallRecovery 只能作为候选特征：重复读取不变内容也满足它。只有原始事件证明结果变化且对应 gate 触发，才能确认该 case 受此 bug 影响。
- 新 schema 建议分别记录 mechanismStatus、caseAttributionStatus、evidenceRefs、candidateCases、confirmedAffectedCases；缺少事件时用 UNKNOWN，不把候选数量写成确诊数量。
- 新增反例：连续成功返回相同内容、连续返回变化内容，二者聚合指标相同，但归因应不同或前者未知。
- 工具出错可能来自 harness/环境/schema；不能仅 toolFailures>0 就断言模型选择错误。需要相应反馈和调用证据，否则降为候选/证据不足。
- 现有 R87 三个 TARGET 是同一种合成 trace 的三次标记，不是三个独立历史 trace 的精确回放。明确声明独立机制场景数量和映射关系。
- manifest 的 executedSourceSha 记录真正执行的版本；historicalReferenceSha 只是参考；baselineMode=emulated_semantics，不能用 sourceSha 暗示检出了历史代码。
- 同一 runtime 开关对照可保留为机制实验；如果要声称真实版本 A/B，必须在独立 checkout/build 中运行两个真实 SHA。
- REPLAY_LIMITS 的 20 与 benchmark 的 30 明确标为实验配置差异，不改历史 evidence。必要时新增 30 的合成边界测试，但不能事后说历史就是 30。
- R84 normalizedDigest 明确说明 CRLF/LF 等价；若确实需要字节审计，新增 rawSha256，旧 digest 保留兼容。
- 不读取 holdout 逐案例内容用于调参；不制造历史丢失 trace；提交带 schema/version 的勘误，而非删除历史记录。

### 怎么验收

- 相同聚合计数但不同轨迹的反例，不被强行赋予相同因果结论。
- report 与 manifest 显示真实 executedSourceSha，明确 emulated baseline 和 synthetic trace。
- 任何真实改善结论必须有真实事件证据；缺失时明确 UNKNOWN。
- CRLF/LF 规范化测试与 raw-byte 测试分别匹配声明。
- 仍然保留 R86 changing-result 正例、constant-result 反例和安全测试通过。
- provider HTTP 调用为零。

---

## R91：修复 Windows verifier 的确定性兼容问题

### 可直接交给 Agent 的提示词

你负责调查 R85 报告中的 H1：Windows .cmd/.ps1 shim 与 shell:false 的兼容。此前因真实样本少未推进，但确定性代码缺陷不需要等待第二个付费案例。先在 Windows CI 复现，复现失败则报告 NOT_REPRODUCED，不能凭报告直接改执行器。

### 做什么

1. 构造最小 command verifier fixture，覆盖原生可执行文件、Node 脚本、Windows cmd shim、PowerShell 脚本。
2. 选择明确、安全的执行合同，消除跨平台验证器假失败。
3. 保持 R79 的结构化 argv 与不隐式开启 shell 的安全边界。

### 怎么做

- 查找 runArgv 实际实现及 TaskVerifier 调用链；用仓库生成的无害临时脚本建立 RED。
- 优先将可控 benchmark verifier 声明为 node 可执行文件 + 脚本参数，避开平台 shim；需要 cmd/ps1 时显式声明 interpreter，不把所有命令切换为 shell:true。
- 解释器参数编码必须使用平台规则；不能简单 join(args, ' ')。对不支持的脚本类型 fail-closed 并给出可操作原因。
- 路径含空格、Unicode、引号、&、|、百分号、美元符号、括号作为字面参数时，不能意外触发第二条命令。
- 验证 cwd、timeout、AbortSignal、exit code、stdout/stderr，以及 turn error/cleanup error 的优先级。
- 如只修改 benchmark 定义，记录 case content digest 变化；新结果不能与旧定义当作完全相同配置比较。

### 怎么验收

- windows-latest 真正启动各个无害 fixture 并通过；不是仅 mock spawn 参数。
- Ubuntu 对支持的平台无关路径通过；Windows 专属测试在 Ubuntu 明确 skip。
- 注入字符不会创建额外 sentinel 文件；超时/取消能终止子进程并留下正确失败原因。
- 原有 R79/R80 verifier 测试不回退；不新增外部模型调用。
- 报告区分已确认的 shim 问题、ENOENT 的其他可能原因及尚未证实的历史影响范围。

---

## R92：准备真实小型 A/B，运行前才请求具体授权

### 可直接交给 Agent 的提示词

你负责在 R88–R91 验收后准备真实 A/B 的可审查方案。本任务默认完成规划、离线检查和假服务演练，不发送真实 provider 请求。

### 做什么

1. 选定 6–10 个非 holdout case，明确来自开发集、用于机制验证，不能宣称代表总体表现。
2. 使用两个实际 commit 的隔离 checkout/build，或者明确只进行同版本受控开关实验；两类实验不可混写。
3. 生成完整 execution plan 和 campaign-wide 授权 digest。
4. 全流程 fake-provider 演练通过后，才呈现给用户决定是否执行。

### 怎么做

- 优先复用现有 benchmark execution-plan 合同；selection digest 只覆盖案例选择，不足以充当付费授权 digest。
- digest 必须绑定实际 SHA/build、case 内容、provider/model/endpoint identity、配置、limits、arms、重复次数、串行度1、过期时间和全局预算。
- 不把计划估算上限写成真实运行成本硬上限；分别说明 model-call/token/time 的运行时可执行上限，以及价格未知时无法证明的美元费用上限。
- 如果调用层不能执行所声明的硬上限，先把该项标 BLOCKED；不能通过文字宣称已限制。
- 预先冻结 case 顺序和比较指标，禁止看到候选结果后改样本；新修复导致配置变化时重新生成计划。
- 真实基线选择要解释修复范围：H2 单项验证与多项修复联合验证分别陈述，避免把收益全归给 H2。
- 注入 identity 漂移、过期授权、预算用尽、429/5xx、断线、持久化失败、中途停止，证明 runner 能按约定停下。
- 输出待审批材料：SHA、case 清单、endpoint identity、调用/时间/token 上限、成本未知项、计划 digest、过期时间、输出位置。
- 只有用户批准这份具体计划后才能运行；当前审查请求不构成付费授权。未授权时报告 READY_FOR_AUTHORIZATION / NOT_RUN，交付仍然完整。

### 怎么验收

- fake-provider 完整矩阵、resume、预算停止和异常测试通过；外部请求0。
- 未授权或授权过期/错 digest，首个 provider 请求前拒绝。
- 真实两臂必须有独立构建身份；不能把模拟开关记录为历史 checkout。
- Windows 实验流程可用，Ubuntu 的冷启动和离线门禁继续在 CI 通过。
- 用户未授权时没有真实成绩，不填写通过率改善。
- 即使后续小型 A/B 成功，也不自动全量86例重跑，不自动再次消耗 holdout。

## 三、最终交付和停点

R88–R91 完成后，提交一张证据表：发现、修复 commit、RED/GREEN、CI URL、仍未知的历史影响。R92 提供具体待批准的执行计划即可。

本阶段的目标是保证“验收通过”真的需要完整、正确且来源清楚的证据；修复已有工具执行兼容问题。下一阶段再讨论真实模型效果。不得把 CI 全绿、合成机制改善和真实任务通过率改善当成同一件事。

## 四、源码入口

- [本轮差异](https://github.com/ki11a-Conton/harness-agent/compare/6693a6a52fb488bbe3273625eca8c1adb0981957...db16dace0d2b864c7002f3a5158ccfe8c004f4ef)
- [R87 runner](https://github.com/ki11a-Conton/harness-agent/blob/db16dace0d2b864c7002f3a5158ccfe8c004f4ef/packages/core/src/runtime/r87-zero-call-replay-ab.ts)
- [campaign runner](https://github.com/ki11a-Conton/harness-agent/blob/db16dace0d2b864c7002f3a5158ccfe8c004f4ef/scripts/benchmark/run-campaign.ps1)
- [triage](https://github.com/ki11a-Conton/harness-agent/blob/db16dace0d2b864c7002f3a5158ccfe8c004f4ef/packages/evaluation/src/campaign-triage.ts)
- [R87 报告](https://github.com/ki11a-Conton/harness-agent/blob/db16dace0d2b864c7002f3a5158ccfe8c004f4ef/docs/E4-R87-report.md)

